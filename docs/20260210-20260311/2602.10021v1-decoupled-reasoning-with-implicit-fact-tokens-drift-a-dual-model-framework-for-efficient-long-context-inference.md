---
title: "Decoupled Reasoning with Implicit Fact Tokens (DRIFT): A Dual-Model Framework for Efficient Long-Context Inference"
title_zh: DRIFT：基于隐式事实标记的解耦推理——一种用于高效长上下文推理的双模型框架
authors: "Wenxuan Xie, Yujia Wang, Xin Tan, Chaochao Lu, Xia Hu, Xuhong Wang"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.10021v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 高效长上下文推理的双模型框架
tldr: 针对大语言模型在处理长上下文和动态知识时面临的冗余及推理效率问题，本文提出DRIFT框架。该框架采用双模型架构，将知识提取与推理过程解耦。通过轻量级知识模型将文档块动态压缩为与查询相关的隐式事实标记，并映射至推理模型空间。实验证明，DRIFT在长上下文任务中表现优异，显著提升了推理效率和准确性，为扩展LLM上下文能力提供了新范式。
selection_source: fresh_fetch
motivation: 现有的RAG和知识编辑方法受限于有限的上下文窗口、检索噪声及灾难性遗忘，难以高效处理海量动态知识。
method: 提出DRIFT双模型架构，利用轻量级模型将文档动态压缩为隐式事实标记，并将其投影到推理模型的嵌入空间中以替代原始文本。
result: 实验结果显示，DRIFT在长上下文任务上显著优于同类规模的基准模型，在保持推理准确性的同时提升了处理效率。
conclusion: DRIFT通过解耦知识与推理，为扩展大语言模型的有效上下文窗口和推理能力提供了一种可扩展且高效的解决方案。
---

## 摘要
将广泛且动态的知识集成到大语言模型（LLMs）中仍然是一个重大挑战，这是由于事实数据与推理模式之间固有的纠缠。现有的解决方案，从非参数化的检索增强生成（RAG）到参数化的知识编辑，在实践中往往受到有限上下文窗口、检索器噪声或灾难性遗忘风险的限制。在本文中，我们提出了 DRIFT，这是一种新颖的双模型架构，旨在显式地将知识提取与推理过程解耦。与静态提示词压缩不同，DRIFT 利用轻量级知识模型，根据查询动态地将文档块压缩为隐式事实标记（implicit fact tokens）。这些稠密表示被投影到推理模型的嵌入空间中，在保持推理准确性的同时取代了原始且冗余的文本。大量实验表明，DRIFT 显著提升了长上下文任务的性能，在同等规模的模型中优于强基准模型。我们的方法为扩展 LLMs 的有效上下文窗口和推理能力提供了一种可扩展且高效的范式。我们的代码可在 https://github.com/Lancelot-Xie/DRIFT 获取。

## Abstract
The integration of extensive, dynamic knowledge into Large Language Models (LLMs) remains a significant challenge due to the inherent entanglement of factual data and reasoning patterns. Existing solutions, ranging from non-parametric Retrieval-Augmented Generation (RAG) to parametric knowledge editing, are often constrained in practice by finite context windows, retriever noise, or the risk of catastrophic forgetting. In this paper, we propose DRIFT, a novel dual-model architecture designed to explicitly decouple knowledge extraction from the reasoning process. Unlike static prompt compression, DRIFT employs a lightweight knowledge model to dynamically compress document chunks into implicit fact tokens conditioned on the query. These dense representations are projected into the reasoning model's embedding space, replacing raw, redundant text while maintaining inference accuracy. Extensive experiments show that DRIFT significantly improves performance on long-context tasks, outperforming strong baselines among comparably sized models. Our approach provides a scalable and efficient paradigm for extending the effective context window and reasoning capabilities of LLMs. Our code is available at https://github.com/Lancelot-Xie/DRIFT.

---

## 论文详细总结（自动生成）

这是一份关于论文《Decoupled Reasoning with Implicit Fact Tokens (DRIFT): A Dual-Model Framework for Efficient Long-Context Inference》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLMs）在处理**长上下文**和**动态知识集成**时面临的挑战提出了解决方案。
*   **研究动机**：现有的方法存在明显局限。**RAG（检索增强生成）**受限于检索器的准确率天花板和长序列的计算开销；**参数化知识编辑**容易导致模型遗忘固有推理逻辑；**静态提示词压缩**则往往在压缩过程中丢失与特定查询（Query）相关的关键信息。
*   **核心问题**：如何显式地将“知识提取”与“核心推理”解耦，在大幅压缩输入长度的同时，保留甚至增强模型对长文本的理解与推理能力？

### 2. 方法论：核心思想与技术细节
DRIFT 提出了一种**双模型架构**，将推理过程分为知识处理和逻辑推理两个模块：
*   **核心思想**：使用一个轻量级的“知识模型”将长文档动态压缩为“隐式事实标记（Implicit Fact Tokens）”，这些标记作为稠密的语义表示，通过投影层进入大型“推理模型”的嵌入空间。
*   **关键技术细节**：
    1.  **分桶压缩（Bucketed Compression）**：放弃传统的固定比例压缩，改为基于长度范围的映射。这避免了模型在处理信息稀疏或稠密文本时的偏见，增强了语义抽象能力。
    2.  **三阶段训练流程**：
        *   **阶段1：隐式事实重建预训练（LFRP）**：训练知识模型生成能让推理模型重建原始文档的隐式标记（静态压缩）。
        *   **阶段2：查询感知动态压缩微调（QAFT-DC）**：引入 Query，训练知识模型仅提取与问题相关的证据信息。
        *   **阶段3：问答微调（QAFT-QA）**：端到端训练推理模型利用隐式标记生成最终答案。
    3.  **并行处理**：长文档被递归切分为语义连贯的块（Chunks），知识模型并行处理这些块，显著降低推理延迟。

### 3. 实验设计
*   **数据集与场景**：
    *   **BAMBOO**：测试长文本 QA 和代码补全。
    *   **L-Eval**：评估单步和多步推理。
    *   **LongBench-v2**：极具挑战性的多文档理解和结构化数据推理。
    *   **LoCoMo**：长短期对话记忆与时间推理。
    *   **BABILong**：测试超长上下文（最高达 1M tokens）下的“大海捞针”能力。
*   **对比基准（Baselines）**：
    *   **硬压缩**：LLMLingua-2（词元剪枝）。
    *   **软压缩**：ICAE、COCOM、xRAG（隐空间压缩）。
    *   **传统方法**：NaiveRAG（基于 BGE-M3 检索）、Vanilla LLM（原始模型直接输入）。

### 4. 资源与算力
*   **模型配置**：默认使用 **Qwen2.5-Instruct-3B** 作为知识模型，**Mistral-7B-Instruct-v0.2** 作为推理模型。此外还测试了 Qwen2.5-7B 和 14B 版本。
*   **算力说明**：论文提到使用了 **LoRA（低秩适配）** 进行参数高效微调（r=16, α=32）。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）、具体的 GPU 数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：构建了包含 **300K 实例**的大规模“文档-问答-证据”数据集。
*   **充分性**：
    *   **多维度评估**：涵盖了从 16k 到 1M tokens 的不同长度区间。
    *   **消融实验**：详细验证了 LFRP、QAFT-DC 和 QAFT-QA 三个阶段对最终性能的贡献，证明了每个阶段的必要性。
    *   **鲁棒性测试**：测试了 32x、64x 甚至 128x 的极端压缩比，结果显示 DRIFT 在高压缩比下依然稳健。
    *   **通用性测试**：验证了微调是否损害模型的通用能力（如数学、代码、逻辑推理），结果显示性能下降极小。
*   **客观性**：对比了多种不同类型的压缩算法，并使用了 LLM-Judge（Qwen-2.5-72B）进行自动化评分，减少了主观偏差。

### 6. 主要结论与发现
*   **性能卓越**：在 LongBench-v2 上，DRIFT 在 32x 压缩下的准确率（29.22%）显著超过了原始 Mistral-7B 模型（20.87%）。
*   **效率提升**：在处理 256k tokens 的文档时，DRIFT 实现了平均 **7 倍的推理加速**。
*   **长程能力**：在 BABILong 测试中，DRIFT 在 64k 长度下的表现优于直接输入方式近 4 倍，且能扩展至 1M tokens。
*   **学习模式**：通过诊断指标发现，推理模型在训练中经历了从“对齐原始文本”到“开发专用隐式推理策略”的演进过程。

### 7. 优点
*   **架构创新**：解耦了知识获取与逻辑推理，让大模型专注于深度推理而非被冗余背景信息淹没。
*   **动态性**：相比静态压缩，其查询感知（Query-aware）特性确保了压缩过程不丢失任务关键信息。
*   **高压缩比**：在保持高精度的前提下实现了极高的压缩比（32x-128x），极大地扩展了有效上下文窗口。
*   **即插即用潜力**：通过投影层连接不同规模的模型，具有良好的扩展性。

### 8. 不足与局限
*   **可解释性挑战**：隐式事实标记（Latent Tokens）是连续向量，人类无法直观阅读，这使得调试和验证知识提取过程变得困难。
*   **模型规模限制**：实验主要集中在 14B 以下的模型，尚未在大规模模型（如 70B+）上验证其扩展定律（Scaling Laws）。
*   **训练依赖**：需要高质量的“证据（Evidence）”标注数据进行微调，数据构建成本较高。
*   **优化手段单一**：目前主要依赖监督微调（SFT），尚未引入强化学习（RL）来进一步优化知识选择策略。

（完）
