---
title: "StatePlane: A Cognitive State Plane for Long-Horizon AI Systems Under Bounded Context"
title_zh: StatePlane：受限上下文下长程 AI 系统的认知状态平面
authors: "Sasank Annapureddy, John Mulcahy, Anjaneya Prasad Thamatani"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13644v1"
tags: ["query:lh-llm"]
score: 10.0
evidence: 有限上下文下长程推理的认知状态平面
tldr: 针对大语言模型在长周期任务中受限于上下文窗口和KV缓存的问题，本文提出了StatePlane，一种模型无关的认知状态平面。该系统借鉴认知心理学，通过管理情景、语义和程序状态的演化与衰减，实现了高效的状态合成与自适应遗忘。StatePlane无需扩展窗口或重新训练，即可在有限上下文中维持长程推理能力，并提供了安全治理机制和多领域基准测试。
selection_source: fresh_fetch
motivation: 现有的长上下文处理方法将记忆视为静态存储，难以在长周期、多会话任务中有效保留与决策相关的动态状态。
method: 提出一种基于认知心理学的状态模型，通过情景分割、选择性编码、意图路由检索及自适应遗忘等机制管理AI系统的认知状态。
result: 在六个领域特定基准测试中，StatePlane证明了在不增加上下文窗口或重新训练模型的情况下，依然能实现卓越的长程智能表现。
conclusion: StatePlane为受限上下文下的长周期AI系统提供了一种高效、安全且可扩展的认知状态管理框架。
---

## 摘要
大语言模型 (LLM) 和小语言模型 (SLM) 在严格的上下文窗口和键值 (KV) 缓存限制下运行，这从根本上限制了它们在长交互周期内进行连贯推理的能力。现有方法——如扩展上下文窗口、检索增强生成、摘要或静态文档——将记忆视为静态存储，无法在长期运行的多会话任务中保留与决策相关的状态。我们引入了 StatePlane，这是一个与模型无关的认知状态平面，用于管理在受限上下文下运行的 AI 系统中情景、语义和程序状态的形成、演化、检索和衰减。基于认知心理学和系统设计，StatePlane 将情景分割、通过信息论约束进行的选型编码、带意图路由的目标条件检索、重构式状态合成以及自适应遗忘进行了形式化。我们提出了一个形式化的状态模型、KV 感知算法、包括写入路径防投毒在内的安全与治理机制、企业级集成路径，以及一个包含六个特定领域基准测试的评估框架。StatePlane 表明，无需扩展上下文窗口或重新训练模型，即可实现长程智能。

## Abstract
Large language models (LLMs) and small language models (SLMs) operate under strict context window and key-value (KV) cache constraints, fundamentally limiting their ability to reason coherently over long interaction horizons. Existing approaches -- extended context windows, retrieval-augmented generation, summarization, or static documentation -- treat memory as static storage and fail to preserve decision-relevant state under long-running, multi-session tasks. We introduce StatePlane, a model-agnostic cognitive state plane that governs the formation, evolution, retrieval, and decay of episodic, semantic, and procedural state for AI systems operating under bounded context. Grounded in cognitive psychology and systems design, StatePlane formalizes episodic segmentation, selective encoding via information-theoretic constraints, goal-conditioned retrieval with intent routing, reconstructive state synthesis, and adaptive forgetting. We present a formal state model, KV-aware algorithms, security and governance mechanisms including write-path anti-poisoning, enterprise integration pathways, and an evaluation framework with six domain-specific benchmarks. StatePlane demonstrates that long-horizon intelligence can be achieved without expanding context windows or retraining models.

---

## 论文详细总结（自动生成）

### 论文总结：StatePlane —— 受限上下文下长程 AI 系统的认知状态平面

#### 1. 核心问题与整体含义（研究动机和背景）
当前大语言模型（LLM）和小型语言模型（SLM）在处理长周期、多会话任务时面临根本性限制：**有限的上下文窗口和 KV 缓存约束**。现有的解决方案（如扩展窗口、RAG、摘要）通常将记忆视为“静态存储”，仅保存文本片段，而忽略了决策背后的逻辑、约束和承诺。
论文指出，长程智能本质上是一个**状态管理问题**而非上下文长度问题。受人类认知心理学启发，作者提出了 **StatePlane**，旨在通过外部化的认知状态平面，在不增加模型参数或窗口大小的情况下，实现跨越长交互周期的连贯推理。

#### 2. 方法论：核心思想与关键技术
StatePlane 是一个模型无关（Model-agnostic）的控制平面，其核心思想是将“历史记录”转化为“结构化状态”。

*   **三位一体记忆模型**：借鉴认知心理学，将状态分为**情景记忆**（事件、目标、结果）、**语义记忆**（事实、规则）和**程序记忆**（工作流、技能）。
*   **事件边界检测（Event Boundary Detection）**：利用 KL 散度衡量状态分布的变化，识别目标转移或决策等关键时刻，从而触发状态编码。
*   **选择性编码与信息瓶颈**：通过信息论约束（Information Bottleneck）优化状态表示，仅保留对未来决策最具显著性（Salience）的信息，实现高效压缩。
*   **目标条件检索与意图路由**：根据查询意图（事实、程序或经历）将请求路由至相应的存储库，并结合当前目标进行排名。
*   **重构式召回（Reconstructive Recall）**：在每次调用模型前，根据 $L_{max}$（硬性 Token 预算）合成最小且充分的上下文，确保不超出 KV 缓存限制。
*   **自适应遗忘机制**：引入指数级衰减函数，未被重复利用的状态会逐渐淡化，作为一种正则化手段减少干扰和幻觉。
*   **双调用契约（Two-Call Contract）**：通过 `PrepareContext`（读取路径）和 `CommitOutcome`（写入路径）两个标准接口与任何 Agent 框架集成。

#### 3. 实验设计
作者提出了一个包含六个维度的基准测试套件（StatePlane Benchmark Suite），用于评估长程状态管理：
*   **LH-PCT (长程政策合规)**：早期绑定的规则在后期是否仍被遵守。
*   **ELR (异常分类账)**：处理特定案例的例外情况。
*   **RCDS (理由稳定性)**：评估决策逻辑是否前后一致，避免“翻来覆去”。
*   **TH-CBC (重度工具案例)**：在大量工具输出下的成本稳定性。
*   **PRSE (隐私执行)**：基于角色的对抗性提取防御。
*   **MP-RI (记忆投毒)**：针对写入路径投毒和注入的防御。

**对比方法（Baselines）**：包括原生无状态 LLM、滑动窗口法、摘要追加法、标准 RAG 以及“摘要+检索”混合法。

#### 4. 资源与算力
论文**未明确说明**具体的算力消耗（如 GPU 型号、数量或训练时长）。由于 StatePlane 主要是作为一种系统架构和推理侧的控制平面，其核心逻辑在于算法调度和状态存储（使用 PostgreSQL, Redis 等），而非模型本身的重新训练，因此对训练算力的依赖较低。

#### 5. 实验数量与充分性
*   **实验覆盖**：论文设计了 6 组针对性强的基准测试，涵盖了从逻辑一致性到安全防御的多个维度。
*   **消融实验**：文中提到进行了消融研究以评估各组件（如遗忘机制、编码器）的贡献。
*   **客观性**：通过在相同的 Token 预算（$L_{max}$）和 1x 到 8x 上下文溢出长度下对比多种基准方法，实验设计较为公平。但由于是新提出的框架，缺乏大规模第三方数据集的验证。

#### 6. 主要结论与发现
*   **状态优于存储**：证明了通过管理“状态”（约束和承诺）而非简单的“历史文本”，可以显著提升长程推理能力。
*   **无需扩展窗口**：在受限的上下文预算内，StatePlane 能够保持决策的稳定性和政策合规性。
*   **安全性提升**：通过“写入门控”和“类型化证据”机制，有效抵御了针对记忆系统的提示词注入和投毒攻击。
*   **解耦进化**：实现了在不重新训练模型的情况下，通过外部状态的演化来改变 Agent 的行为。

#### 7. 优点
*   **认知启发式设计**：将心理学中的记忆分类和遗忘曲线引入 AI 系统，逻辑严密且符合直觉。
*   **企业级就绪**：详细考虑了安全、审计、多租户隔离和 PII（个人隐私信息）脱敏，具有很强的工程落地价值。
*   **模型无关性**：可以无缝集成到现有的 LLM 或 SLM 架构中，无需修改模型权重。

#### 8. 不足与局限
*   **显著性估计依赖**：系统的有效性高度依赖于“显著性（Salience）”评估的准确性，若评估失准可能导致关键信息丢失。
*   **延迟开销**：写入路径的门控检查和状态编码可能会引入额外的推理延迟。
*   **协作假设**：治理机制假设 Agent 会配合平面的调度，对于极具破坏性的恶意 Agent 效果可能受限。
*   **基准测试局限**：提出的六个基准测试多为领域特定，可能需要更广泛的通用任务验证。

（完）
