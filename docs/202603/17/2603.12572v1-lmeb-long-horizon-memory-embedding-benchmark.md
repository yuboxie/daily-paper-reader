---
title: "LMEB: Long-horizon Memory Embedding Benchmark"
title_zh: LMEB：长跨度记忆嵌入基准
authors: "Xinping Zhao, Xinshuo Hu, Jiaxin Xu, Danyu Tang, Xin Zhang, Mengjia Zhou, Yan Zhong, Yao Zhou, Zifei Shan, Meishan Zhang, Baotian Hu, Min Zhang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12572v1"
tags: ["query:lh-llm"]
score: 10.0
evidence: 嵌入模型长程记忆检索的基准测试
tldr: 针对现有文本嵌入基准在长程记忆检索评估方面的不足，本文提出了LMEB基准。该基准涵盖22个数据集和193个零样本任务，分为情景、对话、语义和程序四类记忆类型。通过对15个主流模型的评估，发现现有模型在处理碎片化、上下文相关及长跨度记忆任务时仍面临挑战，且性能与传统检索任务不完全相关，为记忆增强系统提供了重要的评估框架。
selection_source: fresh_fetch
motivation: 现有基准侧重于传统段落检索，忽视了模型在处理碎片化、上下文依赖及长跨度记忆检索任务中的关键能力。
method: 构建了包含22个数据集和193个零样本任务的LMEB基准，涵盖四种记忆类型，并对15个从亿级到百亿级参数的嵌入模型进行了评估。
result: 实验发现LMEB难度适中，模型规模并不直接决定性能，且其评估结果与MTEB具有正交性，表明传统检索能力难以泛化至长程记忆任务。
conclusion: LMEB填补了记忆嵌入评估的空白，揭示了当前模型在长程记忆处理上的局限，为推动更通用的文本嵌入技术发展提供了基础。
---

## 摘要
记忆嵌入对于 OpenClaw 等记忆增强系统至关重要，但在当前的文本嵌入基准测试中，对其评估的研究尚不充分。这些基准测试狭隘地关注传统的段落检索，未能评估模型处理涉及碎片化、上下文相关且时间跨度较长的信息的长跨度记忆检索任务的能力。为了解决这一问题，我们推出了长跨度记忆嵌入基准（LMEB），这是一个评估嵌入模型处理复杂长跨度记忆检索任务能力的综合框架。LMEB 涵盖了 22 个数据集和 193 个零样本检索任务，横跨四种记忆类型：情景记忆、对话记忆、语义记忆和程序记忆，包含 AI 生成和人工标注的数据。这些记忆类型在抽象程度和时间依赖性方面有所不同，捕捉了记忆检索的不同方面，反映了现实世界中的多样化挑战。我们评估了 15 个广泛使用的嵌入模型，参数量从亿级到百亿级不等。结果显示：(1) LMEB 提供了合理的难度水平；(2) 更大的模型并不总是表现更好；(3) LMEB 与 MTEB 表现出正交性。这表明该领域尚未收敛出一个能够在所有记忆检索任务中表现出色的通用模型，且传统段落检索的性能可能无法泛化到长跨度记忆检索。总之，通过提供标准化且可复现的评估框架，LMEB 填补了记忆嵌入评估中的关键空白，推动了文本嵌入在处理长期、上下文相关的记忆检索方面的进一步发展。LMEB 可在 https://github.com/KaLM-Embedding/LMEB 获取。

## Abstract
Memory embeddings are crucial for memory-augmented systems, such as OpenClaw, but their evaluation is underexplored in current text embedding benchmarks, which narrowly focus on traditional passage retrieval and fail to assess models' ability to handle long-horizon memory retrieval tasks involving fragmented, context-dependent, and temporally distant information. To address this, we introduce the Long-horizon Memory Embedding Benchmark (LMEB), a comprehensive framework that evaluates embedding models' capabilities in handling complex, long-horizon memory retrieval tasks. LMEB spans 22 datasets and 193 zero-shot retrieval tasks across 4 memory types: episodic, dialogue, semantic, and procedural, with both AI-generated and human-annotated data. These memory types differ in terms of level of abstraction and temporal dependency, capturing distinct aspects of memory retrieval that reflect the diverse challenges of the real world. We evaluate 15 widely used embedding models, ranging from hundreds of millions to ten billion parameters. The results reveal that (1) LMEB provides a reasonable level of difficulty; (2) Larger models do not always perform better; (3) LMEB and MTEB exhibit orthogonality. This suggests that the field has yet to converge on a universal model capable of excelling across all memory retrieval tasks, and that performance in traditional passage retrieval may not generalize to long-horizon memory retrieval. In summary, by providing a standardized and reproducible evaluation framework, LMEB fills a crucial gap in memory embedding evaluation, driving further advancements in text embedding for handling long-term, context-dependent memory retrieval. LMEB is available at https://github.com/KaLM-Embedding/LMEB.

---

## 论文详细总结（自动生成）

### LMEB: 长跨度记忆嵌入基准分析总结

#### 1. 核心问题与整体含义
*   **研究动机**：当前的文本嵌入基准（如 MTEB）主要侧重于传统的段落检索（Passage Retrieval），即从组织良好的文档中寻找信息。然而，在现实的记忆增强系统（如 AI Agent）中，模型需要处理的是**长跨度记忆检索**。
*   **核心挑战**：这类任务涉及的信息通常是碎片化的、高度依赖上下文的，且在时间上分布极广。现有基准无法有效评估模型在处理情景、对话、程序等复杂记忆类型时的表现。
*   **整体含义**：论文提出了 **LMEB (Long-horizon Memory Embedding Benchmark)**，旨在填补这一空白，为开发能够处理长期、上下文相关记忆的嵌入模型提供标准化的评估框架。

#### 2. 方法论：核心思想与技术细节
*   **记忆分类法（Taxonomy）**：基于“抽象程度”和“时间依赖性”两个维度，将记忆分为四类：
    1.  **情景记忆 (Episodic)**：低抽象、高时间依赖（如：过去发生的具体事件）。
    2.  **对话记忆 (Dialogue)**：高抽象、高时间依赖（如：多轮对话中的用户偏好）。
    3.  **语义记忆 (Semantic)**：低抽象、低时间依赖（如：稳定的事实知识）。
    4.  **程序记忆 (Procedural)**：高抽象、低时间依赖（如：操作工具的步骤、技能）。
*   **统一数据格式**：采用标准化的 IR（信息检索）风格，包含 `queries`（查询）、`corpus`（语料库）、`qrels`（相关性标注）以及可选的 `candidates`（受限候选池，用于模拟真实的对话历史约束）。
*   **评估协议**：支持零样本（Zero-shot）评估，主要指标为 **NDCG@10** 和 **Recall@10**。

#### 3. 实验设计
*   **数据集规模**：涵盖 22 个数据集，共计 193 个检索任务。数据来源包括 AI 生成（如 EPBench）和人工标注（如 QASPER, NovelQA）。
*   **对比模型**：评估了 15 个主流嵌入模型，参数量跨度从 239M 到 12B，包括：
    *   **大型模型 (>1B)**：KaLM-Embedding-Gemma3 (12B), bge-multilingual-gemma2 (9B), Qwen3-Embedding (8B/4B), NV-Embed-v2 (7B), e5-mistral-7b (7B)。
    *   **中小型模型 (<1B)**：jina-v5 (small/nano), bge-m3, EmbeddingGemma-300M, E5-large 等。
*   **实验场景**：分为“无指令 (w/o inst.)”和“带指令 (w/ inst.)”两种设置，观察任务指令对模型性能的影响。

#### 4. 资源与算力
*   **算力说明**：论文主要侧重于基准测试的构建和模型推理评估，**并未明确说明**执行这些评估所使用的具体 GPU 型号、数量或总训练/推理时长。
*   **工具链**：基于 MTEB v2 框架构建，支持 Transformers、Sentence-Transformers 和 vLLM 推理后端。

#### 5. 实验数量与充分性
*   **实验规模**：论文对 15 个模型在 22 个数据集上进行了详尽的测试，并提供了细化到每个数据集的 NDCG@10 和 Recall@10 结果。
*   **充分性评价**：实验设计非常充分。除了基础性能测试，还进行了：
    1.  **多样性分析**：通过加权 Jaccard 相似度分析了数据集间的差异。
    2.  **相关性分析**：计算了 LMEB 与 MTEB 的 Pearson 和 Spearman 相关系数。
    3.  **指令敏感度分析**：探讨了不同模型对指令的偏好差异。
*   **公平性**：所有模型均在零样本设置下评估，确保了比较的客观性。

#### 6. 主要结论与发现
*   **难度适中**：顶尖模型的平均分在 61.41 (NDCG@10)，说明 LMEB 既有挑战性，又不是不可逾越。
*   **规模法则失效**：**更大的模型并不总是表现更好**。例如，300M 的 EmbeddingGemma 在某些任务上优于 12B 的模型，说明架构和训练数据质量比参数量更关键。
*   **正交性发现**：LMEB 与 MTEB 的相关性接近于 0。这意味着在传统段落检索上表现优异的模型，在长程记忆检索任务中可能表现平平。
*   **指令影响不一**：不同模型对指令的反应迥异，部分模型（如 BGE-M3）在无指令下表现更好，而另一些则高度依赖指令。

#### 7. 优点
*   **分类科学**：通过心理学维度的记忆分类，使评估更具诊断性。
*   **填补空白**：首次系统性地针对碎片化、时间敏感的记忆检索进行大规模基准测试。
*   **易于扩展**：提供了开源工具包和标准格式，方便社区集成新模型和新数据集。
*   **真实性强**：引入了 `candidates` 约束机制，更贴近 Agent 实际调用记忆的场景。

#### 8. 不足与局限
*   **语言局限**：目前主要集中在**英文**数据集，缺乏对多语言记忆检索的评估。
*   **数据偏差**：部分数据集（如情景记忆）高度依赖 AI 生成，可能与真实人类记忆分布存在偏差。
*   **模态单一**：目前仅限于文本嵌入，未涉及多模态（图像、视频）记忆嵌入的评估。
*   **动态性不足**：基准测试仍是静态的，未能模拟记忆在长期运行中的动态更新和遗忘机制。

（完）
