---
title: Enhancing Mental Health Classification with Layer-Attentive Residuals and Contrastive Feature Learning
title_zh: 利用层注意力残差与对比特征学习增强心理健康分类
authors: "Menna Elgabry, Ali Hamdi, Khaled Shaban"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14075v1"
tags: ["query:llm-mh"]
score: 9.0
evidence: 使用Transformer表示进行心理健康分类
tldr: "针对心理健康分类中类别重叠和特征空间纠缠的挑战，本文提出了一种增强表示学习的新框架。该框架结合了层注意力残差聚合（融合所有Transformer层信息）和有监督对比特征学习（通过拉大类别间几何距离减少重叠）。在SWMH基准测试中，该方法以74.36%的得分超越了MentalBERT等领域专用模型，证明了优化表示几何结构比单纯的领域预训练更有效。"
selection_source: fresh_fetch
motivation: 传统的Transformer微调在心理健康分类中容易产生纠缠的特征空间，且未能充分利用模型各层的信息。
method: 提出层注意力残差聚合以融合多层语义，并结合渐进加权的有监督对比学习来优化特征空间的类别分布。
result: "在SWMH数据集上达到74.36%的准确率，显著优于MentalBERT和MentalRoBERTa等领域预训练模型。"
conclusion: 精心设计的表示几何和层感知残差集成能够超越领域自适应预训练，并提供更好的模型可解释性。
---

## 摘要
心理健康分类由于多种原因而具有挑战性。首先，不同心理健康问题之间存在重叠。此外，心理健康问题的征兆取决于情境语境，这增加了分类的难度。尽管微调 Transformer 模型提升了心理健康分类的性能，但标准的交叉熵训练往往会产生纠缠的特征空间，且未能充分利用 Transformer 包含的所有信息。我们提出了一种专注于表示学习的新框架，以改进心理健康分类。该框架通过两种方法实现：第一，层注意力残差聚合（layer-attentive residual aggregation），它作用于残差连接，在保持高层语义的同时，对所有 Transformer 层的表示进行加权和融合；第二，有监督对比特征学习（supervised contrastive feature learning），利用带有渐进加权的温度缩放有监督对比学习，通过重构特征空间来增加易混淆心理健康问题之间的几何间隔，并减少类别重叠。该方法在 SWMH 基准测试中取得了 74.36% 的评分，表现最佳，且优于 MentalBERT 和 MentalRoBERTa 等领域专用模型，相比表现最好的模型，其领先幅度达 3.25% - 2.2%，召回率高出 2.41 个百分点。这些研究结果表明，通过精心设计的表示几何结构和层感知残差集成，可以超越心理健康文本分类中的领域自适应预训练，同时通过学习到的层重要性提供了增强的可解释性。

## Abstract
The classification of mental health is challenging for a variety of reasons. For one, there is overlap between the mental health issues. In addition, the signs of mental health issues depend on the context of the situation, making classification difficult. Although fine-tuning transformers has improved the performance for mental health classification, standard cross-entropy training tends to create entangled feature spaces and fails to utilize all the information the transformers contain. We present a new framework that focuses on representations to improve mental health classification. This is done using two methods. First, \textbf{layer-attentive residual aggregation} which works on residual connections to to weigh and fuse representations from all transformer layers while maintaining high-level semantics. Second, \textbf{supervised contrastive feature learning} uses temperature-scaled supervised contrastive learning with progressive weighting to increase the geometric margin between confusable mental health problems and decrease class overlap by restructuring the feature space. With a score of \textbf{74.36\%}, the proposed method is the best performing on the SWMH benchmark and outperforms models that are domain-specialized, such as \textit{MentalBERT} and \textit{MentalRoBERTa} by margins of (3.25\% - 2.2\%) and 2.41 recall points over the highest achieving model. These findings show that domain-adaptive pretraining for mental health text classification can be surpassed by carefully designed representation geometry and layer-aware residual integration, which also provide enhanced interpretability through learnt layer importance.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **LARC-RoBERTa** 的新框架，旨在通过优化特征表示的几何结构和多层信息融合，提升心理健康文本分类的准确性与可解释性。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：心理健康分类面临两大挑战：一是**类别重叠（Entanglement）**，如焦虑和抑郁的症状描述高度相似，导致标准交叉熵损失下的特征空间界限模糊；二是**层利用率低**，传统微调仅使用 Transformer 的最后一层，忽略了中间层蕴含的丰富语法和语义分层信息。
*   **研究背景**：虽然已有 MentalBERT 等领域预训练模型，但它们仍遵循传统的微调范式，未能从根本上解决特征空间纠缠和多层特征整合的问题。

### 2. 论文提出的方法论
该框架基于 **RoBERTa-Large**（24层），核心包含两个创新组件：
*   **层注意力残差聚合（Layer-Attentive Residual Aggregation）**：
    *   **核心思想**：为 Transformer 的每一层（共24层）及最终池化输出分配一个可学习的权重 $\alpha_l$。
    *   **技术细节**：通过 Softmax 归一化权重，对各层的 `[CLS]` 向量进行加权求和，得到融合表示 $z_{fused}$。
    *   **残差增强**：将融合表示与最后一层的输出进行残差连接并层归一化，以保留高层语义并稳定梯度。
*   **温度缩放的有监督对比学习（Temperature-Scaled SupCon）**：
    *   **核心思想**：在特征空间中拉近同类样本，推开异类样本。
    *   **自适应温度 $\tau_i$**：引入一个小型神经网络根据输入样本的“难度”动态调整对比损失的温度参数。对于模糊样本使用较高温度以保持优化稳定，对于清晰样本使用较低温度以强化分离。
    *   **渐进式加权**：对比损失的权重 $\lambda(t)$ 随训练轮数线性增加，允许模型先通过交叉熵建立基础分类能力，再通过对比学习精细化特征分布。

### 3. 实验设计
*   **数据集**：使用 **SWMH 基准数据集**，包含来自 Reddit 心理健康相关板块的 54,412 条帖子，涵盖 4 种心理健康问题及 1 个对照组（共 5 类）。
*   **对比方法（Baselines）**：
    *   通用模型：BERT, RoBERTa-Large。
    *   医疗/临床领域模型：BioBERT, ClinicalBERT。
    *   心理健康领域专用模型：**MentalBERT**, **MentalRoBERTa**。
*   **评估指标**：加权 F1 分数和召回率（Recall）。

### 4. 资源与算力
*   **算力说明**：文中提到由于 **GPU 显存限制**，采用了 Batch Size 为 16、最大序列长度 256 的配置。
*   **具体型号**：论文**未明确说明**具体的 GPU 型号（如 A100 或 V100）及数量。
*   **训练细节**：训练 15 个 Epoch，使用 AdamW 优化器，对预训练权重和新层采用不同的学习率（分别为 $2\times10^{-6}$ 和 $1\times10^{-5}$）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主实验对比（与 6 种主流模型对比）。
    *   进行了**消融实验**，分别验证了“仅层注意力”和“仅对比损失”的效果。
    *   所有实验均重复运行 **5 次**（不同随机种子），并报告了均值和标准差（$74.36\% \pm 0.50$）。
*   **充分性评价**：实验设计较为充分，通过标准差展示了模型的稳定性。消融实验清晰地证明了两个核心组件的协同效应（1+1>2）。

### 6. 主要结论与发现
*   **超越领域预训练**：该模型（F1 74.36%）显著优于经过大规模领域语料预训练的 MentalRoBERTa（72.16%）和 MentalBERT（71.11%）。
*   **打破预训练迷思**：研究证明，通过精心设计的**表示几何结构**和**层感知集成**，即使不进行昂贵的领域自适应预训练，也能在特定任务上取得更优性能。
*   **召回率提升**：召回率比最强基准提升了 2.41%，这在心理健康筛查中至关重要（减少漏诊）。

### 7. 优点与亮点
*   **可解释性**：学习到的层权重 $\alpha_l$ 可以揭示哪些层（语法层或语义层）对特定心理疾病的识别更重要，为临床分析提供参考。
*   **特征解耦**：通过自适应温度的对比学习，成功拉开了焦虑与抑郁等易混淆类别的几何距离。
*   **即插即用**：该架构改进具有通用性，可推广至其他高风险、细粒度的 NLP 分类任务。

### 8. 不足与局限
*   **语言与平台偏差**：实验仅限于英语 Reddit 帖子，在其他语言、社交平台或临床病历上的泛化能力有待验证。
*   **缺乏时序上下文**：模型将每条帖子视为独立样本，忽略了心理健康诊断中至关重要的**纵向时间序列信息**（如症状随时间的演变）。
*   **静态分类**：目前采用的是类别分类，而非更符合临床实际的谱系化（Spectrum-based）或共病（Comorbidity）分析。

（完）
