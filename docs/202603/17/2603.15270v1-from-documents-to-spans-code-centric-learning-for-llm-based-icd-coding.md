---
title: "From Documents to Spans: Code-Centric Learning for LLM-based ICD Coding"
title_zh: 从文档到片段：基于大语言模型的以代码为中心的 ICD 编码学习
authors: "Xu Zhang, Wenxin Ma, Chenxu Wu, Rongsheng Wang, Kun Zhang, S. Kevin Zhou"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15270v1"
tags: ["query:ehr-tr"]
score: 9.0
evidence: 基于大语言模型的长临床文档 ICD 编码
tldr: 针对ICD编码中数据集覆盖不足、解释性差及长文本微调成本高的问题，本文提出了一种以代码为中心的学习框架（Code-Centric Learning）。该框架将监督重点从完整临床文档转向可扩展的短证据片段，通过混合训练策略和数据扩展，显著降低了训练成本，提升了模型对未见代码的泛化能力并保留了可解释性。实验表明，该方法使小型LLM达到了与大型闭源模型相当的性能。
selection_source: fresh_fetch
motivation: 现有的ICD编码微调面临代码覆盖有限、缺乏可解释证据以及长文档处理成本高昂的挑战。
method: 提出以代码为中心的学习框架，通过短证据片段进行监督，并结合混合训练策略与数据扩展。
result: 该方法在显著降低计算成本的同时，大幅提升了对未见代码的预测准确率，并使小模型性能比肩大型闭源模型。
conclusion: 将学习重心从文档转向片段能有效提升LLM在ICD编码任务中的泛化能力、效率和可解释性。
---

## 摘要
ICD 编码是医疗保健领域一项关键且具有挑战性的任务。最近，基于大语言模型（LLM）的方法在 ICD 编码中表现出比判别式方法更强的泛化能力。然而，针对 ICD 编码微调 LLM 面临三个主要挑战：首先，现有的公开 ICD 编码数据集对 ICD 代码空间的覆盖有限，限制了模型泛化到未见代码的能力；其次，朴素的微调会降低 LLM 的可解释性，因为很少有公开数据集包含所分配代码的明确支持证据；第三，ICD 编码通常涉及冗长的临床文档，使得微调 LLM 的计算成本非常高。为了解决这些问题，我们提出了“以代码为中心的学习”（Code-Centric Learning），这是一种将监督从完整临床文档转向可扩展的短证据片段的训练框架。该框架的核心思想是，片段级学习能够提高 LLM 执行文档级 ICD 编码的能力。我们提出的框架由混合训练策略和以代码为中心的数据扩展组成，这显著降低了训练成本，提高了对未见 ICD 代码的准确性，并保留了可解释性。在相同的 LLM 主干网络下，我们的方法显著优于强基准模型。值得注意的是，我们的方法使小规模 LLM 能够达到与大得多的专有模型相当的性能，证明了其在全自动 ICD 编码方面的有效性和潜力。

## Abstract
ICD coding is a critical yet challenging task in healthcare. Recently, LLM-based methods demonstrate stronger generalization than discriminative methods in ICD coding. However, fine-tuning LLMs for ICD coding faces three major challenges. First, existing public ICD coding datasets provide limited coverage of the ICD code space, restricting a model's ability to generalize to unseen codes. Second, naive fine-tuning diminishes the interpretability of LLMs, as few public datasets contain explicit supporting evidence for assigned codes. Third, ICD coding typically involves long clinical documents, making fine-tuning LLMs computationally expensive. To address these issues, we propose Code-Centric Learning, a training framework that shifts supervision from full clinical documents to scalable, short evidence spans. The key idea of this framework is that span-level learning improves LLMs' ability to perform document-level ICD coding. Our proposed framework consists of a mixed training strategy and code-centric data expansion, which substantially reduces training cost, improves accuracy on unseen ICD codes and preserves interpretability. Under the same LLM backbone, our method substantially outperforms strong baselines. Notably, our method enables small-scale LLMs to achieve performance comparable to much larger proprietary models, demonstrating its effectiveness and potential for fully automated ICD coding.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Documents to Spans: Code-Centric Learning for LLM-based ICD Coding》的结构化深入总结：

### 1. 论文的核心问题与整体含义
ICD 编码（国际疾病分类编码）是将临床文档转化为标准化诊断代码的过程，对医疗计费和数据分析至关重要。尽管大语言模型（LLM）在这一任务上展现了潜力，但现有的微调方法面临三大挑战：
*   **代码覆盖有限**：公开数据集（如 MIMIC）仅覆盖约 10% 的 ICD 代码，导致模型对罕见或未见代码的泛化能力差。
*   **缺乏可解释性**：大多数数据集仅提供代码标签，缺乏支持证据（Evidence Spans），导致模型预测过程不透明。
*   **训练效率低下**：临床文档通常极长，直接对长文档进行 LLM 微调会带来极高的计算成本（二次方复杂度）。

**核心动机**：提出一种从“文档级学习”转向“片段级学习”的框架，通过学习“证据片段-代码”的对应关系来提升模型性能。

### 2. 方法论：以代码为中心的学习（CCL）
该框架的核心思想是：如果 LLM 学会了为短证据片段分配代码，它就能更好地在长文档中定位和聚合证据。

*   **混合训练策略（Mixed Training）**：
    *   **文档级监督**：使用极少量（约 200 篇）带有专家标注证据片段的完整文档，训练模型提取证据并分配代码的能力。
    *   **片段级监督**：使用大量的“短证据片段 + 单个代码”对进行训练，提高训练效率并注入广泛的代码知识。
*   **以代码为中心的数据扩展（Data Expansion）**：
    *   **Gold Pairs（金牌对）**：从官方 ICD 索引和列表提取权威的术语-代码对。
    *   **Silver Pairs（银牌对）**：利用强模型（如 Llama-3.1-70B）从公开数据集中挖掘并总结代表性的证据片段。
    *   **Synthetic Pairs（合成对）**：针对数据集中完全未出现的代码，利用 LLM 根据 ICD 层级结构和邻近代码知识合成合理的证据片段，实现 100% 的代码空间覆盖。

### 3. 实验设计
*   **数据集**：
    *   **MIMIC-IV**：作为大规模噪声训练源。
    *   **MDACE**：专家标注证据的子集，用于评估可解释性和准确性。
    *   **ACI-Bench**：合成临床笔记，作为分布外（OOD）测试集评估泛化性。
*   **Benchmark 与对比方法**：
    *   **判别式模型**：PLM-ICD, PLM-CA, GKI-ICD。
    *   **生成式/LLM 方法**：CoT（思维链）、MAC（多智能体协作）、CLH（Code Like Humans）、以及传统的全文档 SFT（监督微调）。
    *   **主干网络**：Qwen3-4B, Llama-3.1-8B。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA H20 GPU (96GB VRAM)**。
*   **训练时长对比**：
    *   **传统 SFT**：在 10 万篇文档上训练，每轮需 70 小时，总计约 **210 小时**。
    *   **CCL（本文方法）**：在 200 篇文档和 15 万个片段上训练，每轮仅需 4 小时，总计约 **40 小时**。
*   **效率提升**：总训练时间减少了约 **80%**，训练效率提升了 **5.2 倍**。

### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的说服力：
*   **主实验**：在两个不同性质的测试集上验证了不同参数量（1B 到 8B）模型的表现。
*   **消融实验**：逐步验证了证据注入、金牌/银牌/合成数据对性能的贡献。
*   **专项分析**：专门针对“未见代码（Unseen Codes）”的准确率进行了对比（从 8.8% 提升至 50.0%）。
*   **可解释性评估**：通过计算预测证据与人类标注证据的召回率（Recall）和 F1 值来量化评估。
*   **人类评估**：邀请两名医生对合成数据的质量（支持度、临床真实性等）进行了盲审。

### 6. 主要结论与发现
*   **片段学习可迁移**：在短片段上学习的代码知识可以有效迁移到长文档的编码任务中。
*   **小模型逆袭**：经过 CCL 微调的 8B 模型在多个指标上达到了与 GPT-4 或 250B 级超大模型相当甚至更好的水平。
*   **未见代码突破**：通过合成数据扩展，模型处理从未在训练文档中出现的代码的能力大幅增强。
*   **人机协作潜力**：由于模型先输出证据再输出代码，人类医生可以通过修正证据来干预结果，实验显示引入人类证据可将 Micro-F1 从 59.3 提升至 78.0。

### 7. 优点
*   **高效性**：巧妙避开了长文本微调的计算瓶颈，使普通科研机构也能训练高性能医疗模型。
*   **泛化性**：解决了 ICD 编码中长尾代码（Rare Codes）覆盖不足的顽疾。
*   **透明度**：强制模型输出证据片段，符合医疗场景对可解释性的刚需。

### 8. 不足与局限
*   **合成数据真实性**：人类评估显示，合成证据在“临床真实性”和“冗余度”上仍有提升空间（约 42% 的合成数据被认为真实性一般）。
*   **复杂逻辑缺失**：目前的片段学习主要针对“描述-代码”的直接映射，尚未完全整合 ICD 官方指南中复杂的编码规则（如“排除规则”、“合并编码指令”等）。
*   **依赖强模型生成**：数据扩展阶段高度依赖 Llama-3.1-70B 或 GPT-4 等强模型来生成高质量片段。

（完）
