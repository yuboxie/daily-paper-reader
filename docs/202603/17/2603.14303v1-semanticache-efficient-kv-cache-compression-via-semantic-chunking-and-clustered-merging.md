---
title: "SemantiCache: Efficient KV Cache Compression via Semantic Chunking and Clustered Merging"
title_zh: SemantiCache：通过语义分块与聚类合并实现的高效 KV 缓存压缩
authors: "Shunlong Wu, Hai Lin, Shaoshen Chen, Tingwei Lu, Yongqin Zeng, Shaoxiong Zhan, Hai-Tao Zheng, Hong-Gee Kim"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14303v1"
tags: ["query:lh-llm"]
score: 10.0
evidence: 通过语义分块进行高效 KV 缓存压缩
tldr: 针对现有KV缓存压缩方法因破坏语义完整性导致性能下降的问题，本文提出SemantiCache。该框架通过分隔符进行语义分块，并利用贪婪种子聚类（GSC）算法将块内Token合并为语义核心，辅以比例注意力机制平衡权重。实验表明，该方法在保持模型性能的同时，显著降低了内存占用，并将推理速度提升了2.61倍。
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩方法通常基于离散Token或非语义分块，容易导致语义碎片化和不可逆的信息损失。
method: 采用基于分隔符的语义分块，结合贪婪种子聚类算法提取语义核心，并利用比例注意力机制重新平衡合并后的注意力贡献。
result: 在多个基准测试中，SemantiCache实现了高达2.61倍的解码加速，并大幅减少了内存占用，且性能与原始模型相当。
conclusion: SemantiCache通过对齐语言的语义层次结构，证明了在保持语义完整性的前提下实现高效KV缓存压缩的可行性。
---

## 摘要
现有的 KV 缓存压缩方法通常作用于离散的 token 或非语义分块。然而，这些方法往往会导致语义碎片化，破坏了语言上的连贯单元，从而造成不可逆的信息损失和模型性能下降。为解决这一问题，我们提出了 SemantiCache，这是一种新型压缩框架，通过使压缩过程与语言的语义层次结构保持一致来维护语义完整性。具体而言，我们首先利用分隔符（即自然的语义边界）将缓存划分为语义连贯的分块。在每个分块内部，我们引入了一种计算高效的基于种子的贪婪聚类（Greedy Seed-Based Clustering, GSC）算法，将 token 归类为语义簇。这些簇被进一步合并为语义核心，并辅以比例注意力（Proportional Attention）机制，以重新平衡合并 token 后减少的注意力贡献。在多种基准测试和模型上的广泛实验表明，SemantiCache 将推理的解码阶段加速了高达 2.61 倍，并显著减少了内存占用，同时保持了与原始模型相当的性能。

## Abstract
Existing KV cache compression methods generally operate on discrete tokens or non-semantic chunks. However, such approaches often lead to semantic fragmentation, where linguistically coherent units are disrupted, causing irreversible information loss and degradation in model performance. To address this, we introduce SemantiCache, a novel compression framework that preserves semantic integrity by aligning the compression process with the semantic hierarchical nature of language. Specifically, we first partition the cache into semantically coherent chunks by delimiters, which are natural semantic boundaries. Within each chunk, we introduce a computationally efficient Greedy Seed-Based Clustering (GSC) algorithm to group tokens into semantic clusters. These clusters are further merged into semantic cores, enhanced by a Proportional Attention mechanism that rebalances the reduced attention contributions of the merged tokens. Extensive experiments across diverse benchmarks and models demonstrate that SemantiCache accelerates the decoding stage of inference by up to 2.61 times and substantially reduces memory footprint, while maintaining performance comparable to the original model.

---

## 论文详细总结（自动生成）

### SemantiCache: 通过语义分块与聚类合并实现的高效 KV 缓存压缩

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）处理长文本的需求增加，Key-Value (KV) 缓存的线性增长成为了推理瓶颈，导致巨大的内存开销和推理延迟。现有的压缩方法（如剔除法或简单的合并法）通常作用于离散的 Token 或固定大小的非语义分块。这种做法忽略了语言的自然层次结构，容易导致**语义碎片化（Semantic Fragmentation）**，即破坏了短语、从句或句子的完整性，造成不可逆的信息损失。本论文旨在通过模拟人类记忆长文本的认知策略，提出一种保持语义完整性的压缩框架。

#### 2. 方法论：核心思想与关键技术
SemantiCache 采用了一个三阶段的层次化压缩流水线：
*   **语义分块 (Semantic Chunking)：** 利用自然语言的分隔符（如标点符号 `.,?!;:`、空格、换行符等）作为边界，将 KV 缓存划分为语义连贯的分块。分隔符本身的 KV 状态被原样保留，作为结构锚点。
*   **相似性聚类 (Similarity Clustering)：** 在每个分块内部，引入**贪婪种子聚类 (GSC) 算法**。该算法通过计算 Key 向量之间的余弦相似度，将相似度超过预设阈值 $\tau$ 的 Token 归为同一个“语义簇”。这是一种单次扫描的高效算法。
*   **聚类合并 (Clustered Merging)：** 
    *   **语义核心生成：** 对每个簇内的 Key 和 Value 进行均值池化（Mean-pooling），生成代表性的“语义核心”。
    *   **比例注意力 (Proportional Attention)：** 为了防止合并导致的注意力稀释，在计算注意力分数时引入补偿项 $\log s$（$s$ 为簇的大小）。这确保了合并后的核心在注意力机制中的贡献与其原始包含的 Token 数量成正比。

#### 3. 实验设计
*   **数据集/场景：** 
    *   **LongBench：** 包含 21 个子任务的多任务基准测试，涵盖单/多文档问答、摘要、少样本学习、代码补全等。
    *   **Needle-in-a-Haystack (NIAH)：** 测试模型在长文档中检索特定信息的能力。
*   **基准模型：** Llama-3-8B-Instruct 和 Mistral-7B-Instruct-v0.2。
*   **对比方法：** 
    *   **剔除类 (Eviction-based)：** StreamingLLM, H2O, SnapKV。
    *   **合并类 (Merging-based)：** CaM, D2O。

#### 4. 资源与算力
*   **硬件：** 实验在 **NVIDIA A100 80GB GPU** 上进行。
*   **训练/推理：** 该方法属于 **Training-free（无需训练）** 的推理优化技术。论文未明确提及具体的 GPU 数量和总运行耗时，但强调了其 GSC 算法的计算低开销。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流模型上测试了三种不同的 KV 缓存预算（20%、35%、50%），并在 LongBench 的 21 个任务上取平均分。
*   **消融实验：** 验证了语义分块相对于固定大小分块和全局聚类的优越性，并探讨了相似度阈值 $\tau$ 对性能与压缩率平衡的影响。
*   **客观性：** 实验涵盖了准确度（Accuracy）和效率（Latency, Memory）两个维度，对比了当前最先进的多种基线方法，实验设计较为全面且客观。

#### 6. 主要结论与发现
*   **性能保持：** 在高压缩率下，SemantiCache 的表现优于所有对比基线，能够更好地维持模型在长文本任务中的理解能力。
*   **效率提升：** 在 32k 上下文长度下，相比全量 KV 缓存，实现了高达 **2.61倍** 的解码速度（TPOT）提升。
*   **资源优化：** 显著降低了内存占用（例如从 24.27GB 降至 15.94GB），且预fill阶段的延迟（TTFT）增加极小，优于其他合并类方法。

#### 7. 优点（亮点）
*   **语义对齐：** 首次明确提出并解决了 KV 压缩中的“语义碎片化”问题，使压缩过程符合语言学原理。
*   **算法高效：** GSC 聚类算法简单且确定，避免了复杂的迭代优化，适合实时推理。
*   **补偿机制：** 比例注意力机制有效地解决了合并操作带来的信息稀释问题。

#### 8. 不足与局限
*   **超参数敏感性：** 阈值 $\tau$ 的选择对压缩效果影响较大，可能需要针对不同模型或任务进行微调。
*   **分隔符依赖：** 语义分块高度依赖预定义的分隔符集合，对于缺乏明显分隔符的特殊文本（如某些编程语言或特定语种）可能效果受限。
*   **极端长度测试：** 实验主要集中在 32k 以内的长度，对于更极端（如 100k+）的上下文表现尚待进一步验证。

（完）
