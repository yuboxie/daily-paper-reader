---
title: "Anchoring Emotions in Text: Robust Multimodal Fusion for Mimicry Intensity Estimation"
title_zh: 将情感锚定在文本中：用于模仿强度估计的鲁棒多模态融合
authors: "Lingsi Zhu, Yuefeng Zou, Yunxiang Zhang, Naixiang Zheng, Guoyuan Wang, Jun Yu, Jiaen Liang, Wei Huang, Shengping Liu, Ximin Zheng"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14976v1"
tags: ["query:llm-ac"]
score: 9.0
evidence: 情感计算中用于情感模仿强度估计的多模态融合
tldr: 针对自然环境下情绪模仿强度（EMI）估计中物理信号易受噪声干扰和缺失的问题，本文提出TAEMI框架。该方法打破传统对称融合范式，以稳定的文本语义为核心锚点，通过双交叉注意力机制对齐并过滤视觉和音频冗余。同时，引入可学习缺失模态Token和模态丢弃策略增强鲁棒性。在Hume-Vidmimic2数据集上，该方法在六个情绪维度上均达到SOTA性能。
selection_source: fresh_fetch
motivation: 传统的视觉和音频信号在自然环境中极易受噪声干扰或发生数据缺失，导致多模态融合效果不稳定。
method: 提出以文本为锚点的双交叉注意力机制，并结合可学习缺失模态Token与模态丢弃策略来处理不完整数据。
result: 在Hume-Vidmimic2数据集上，该框架在六个连续情绪维度上的平均皮尔逊相关系数显著优于现有基准方法。
conclusion: 通过将稳定的文本语义作为多模态融合的中心锚点，可以有效提升情绪模仿强度估计在复杂现实场景下的鲁棒性与准确性。
---

## 摘要
在自然环境中估计情感模仿强度 (EMI) 是情感计算中一项关键且具有挑战性的任务。主要困难在于如何有效地对高度异构模态之间复杂的非线性时间动态进行建模，特别是在物理信号受损或缺失的情况下。为解决这一问题，我们提出了 TAEMI（以文本为锚点的情感模仿强度估计），这是一个为第 10 届 ABAW 竞赛设计的新型多模态框架。基于连续的视觉和听觉信号极易受瞬时环境噪声影响的观察，我们打破了传统的对称融合范式。相反，我们利用文本转录作为核心锚点，文本转录本质上编码了稳定的、与时间无关的语义先验。具体而言，我们引入了一种以文本为锚点的双重交叉注意力机制，利用这些鲁棒的文本查询来主动过滤帧级冗余并对齐噪声物理流。此外，为了防止在不受限的现实场景中因不可避免的数据缺失而导致的性能灾难性下降，我们在训练过程中集成了可学习的缺失模态 Token 和模态 Dropout 策略。在 Hume-Vidmimic2 数据集上的广泛实验表明，TAEMI 能够有效捕捉细粒度的情感变化，并在不完美条件下保持鲁棒的预测韧性。我们的框架在六个连续情感维度上实现了最先进的平均皮尔逊相关系数，显著优于现有的基准方法。

## Abstract
Estimating Emotional Mimicry Intensity (EMI) in naturalistic environments is a critical yet challenging task in affective computing. The primary difficulty lies in effectively modeling the complex, nonlinear temporal dynamics across highly heterogeneous modalities, especially when physical signals are corrupted or missing. To tackle this, we propose TAEMI (Text-Anchored Emotional Mimicry Intensity estimation), a novel multimodal framework designed for the 10th ABAW Competition. Motivated by the observation that continuous visual and acoustic signals are highly susceptible to transient environmental noise, we break the traditional symmetric fusion paradigm. Instead, we leverage textual transcript--which inherently encode a stable, time-independent semantic prior--as central anchors. Specifically, we introduce a Text-Anchored Dual Cross-Attention mechanism that utilizes these robust textual queries to actively filter out frame-level redundancies and align the noisy physical streams. Furthermore, to prevent catastrophic performance degradation caused by inevitably missing data in unconstrained real-world scenarios, we integrate Learnable Missing-Modality Tokens and a Modality Dropout strategy during training. Extensive experiments on the Hume-Vidmimic2 dataset demonstrate that TAEMI effectively captures fine-grained emotional variations and maintains robust predictive resilience under imperfect conditions. Our framework achieves a state-of-the-art mean Pearson correlation coefficient across six continuous emotional dimensions, significantly outperforming existing baseline methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TAEMI**（Text-Anchored Emotional Mimicry Intensity estimation）的多模态框架，旨在解决自然环境下情感模仿强度（EMI）估计的鲁棒性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实（In-the-wild）场景下，如何准确且鲁棒地估计情感模仿强度。
*   **研究背景**：情感模仿是人类社交互动的基本机制。现有的自动估计方法面临两大挑战：
    1.  **模态异构性与噪声**：视觉（面部表情）和听觉（语音语调）信号是连续且高帧率的，极易受到环境噪声（如遮挡、背景杂音）的干扰。
    2.  **融合范式缺陷**：传统的对称融合（如简单特征拼接）未能充分利用模态间的互补性，且在数据缺失（如麦克风故障、人脸出画）时性能会剧烈下降。
*   **核心动机**：作者观察到，尽管音视频信号不稳定，但**文本转录（语义）**提供了稳定的、与时间无关的上下文先验，可以作为对齐其他噪声模态的“锚点”。

### 2. 方法论
TAEMI 框架打破了传统的对称融合范式，核心思想是**以文本为核心锚点引导多模态对齐**。
*   **特征提取**：
    *   **音频**：使用 Wav2Vec 2.0 提取 12 秒片段的特征。
    *   **视觉**：使用预训练的 ViT 提取最多 400 帧的视觉特征。
    *   **文本**：利用 Whisper 进行语音转文字，并使用 GTE 编码器提取全局语义特征（CLS Token）。
*   **以文本为锚点的双交叉注意力（Text-Anchored Dual Cross-Attention）**：
    *   将文本特征作为 **Query (查询)**，将音频和视觉特征分别作为 **Key (键)** 和 **Value (值)**。
    *   通过交叉注意力机制，利用稳定的语义信息主动过滤音视频中的帧级冗余，实现细粒度的时空对齐。
*   **鲁棒性增强机制**：
    *   **可学习缺失模态 Token**：当视觉或文本缺失时，使用特定的可训练占位符代替，维持网络输入稳定性。
    *   **模态丢弃（Modality Dropout）**：训练时以 $p=0.1$ 的概率随机丢弃某一模态，强制模型不依赖单一模态，增强容错性。
*   **优化目标**：采用多标签回归任务，最小化预测值与 6 维情感强度（仰慕、娱乐、决心、同理心痛苦、兴奋、喜悦）之间的均方误差（MSE）。

### 3. 实验设计
*   **数据集**：使用 **Hume-Vidmimic2** 数据集，包含约 15,000 个视频（超过 25 小时），涵盖复杂的模仿互动。
*   **Benchmark（基准）**：对比了第 10 届 ABAW 竞赛的官方基准（ViT, Wav2Vec2）以及近年来的顶尖方案（如 Savchenko 等人、Zhang 等人、Yu 等人的方法）。
*   **评估指标**：6 个情感维度的平均皮尔逊相关系数（Mean Pearson Correlation Coefficient, $\rho$）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A800-SXM4-80GB GPU** 上运行。
*   **训练细节**：采用 AdamW 优化器，学习率为 $1 \times 10^{-4}$，Batch Size 为 32，使用了混合精度训练和早停策略（Patience=5）。
*   **预训练模型**：加载了 Wav2Vec 2.0 和 GTE 的预训练权重。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了主实验对比、消融实验以及锚点模态对比实验。
*   **消融实验**：
    1.  验证了交叉注意力相对于简单拼接（$\rho$ 从 0.45 提升至 0.55）的优越性。
    2.  验证了缺失模态 Token 和模态丢弃对性能的贡献（分别贡献了 0.03 和 0.04 的提升）。
*   **锚点对比**：对比了以视觉、音频、文本分别作为 Query 的效果，结果证明**以文本为锚点**效果最好（0.55 vs 0.39/0.51）。
*   **评价**：实验设计逻辑严密，通过控制变量法证明了核心创新的有效性，对比了多种 SOTA 方法，结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **文本的锚点作用**：在情感计算中，文本语义比原始物理信号更具抗噪性，能有效引导异构模态的对齐。
*   **SOTA 性能**：TAEMI 在 Hume-Vidmimic2 验证集上达到了 **0.55** 的平均皮尔逊相关系数，刷新了该任务的纪录。
*   **鲁棒性至关重要**：通过简单的丢弃策略和占位符 Token，可以显著提升模型在不完整数据下的生存能力。

### 7. 优点（亮点）
*   **范式创新**：打破了“模态平等”的传统观念，提出“非对称融合”，为多模态对齐提供了新思路。
*   **实用性强**：针对现实场景中常见的“数据缺失”问题设计了专门的鲁棒性模块。
*   **性能卓越**：在竞争激烈的 ABAW 竞赛任务中取得了显著领先。

### 8. 不足与局限
*   **依赖转录质量**：该方法高度依赖文本锚点，如果语音识别（ASR）在极端噪声下失效或产生严重误导，可能会影响整体性能。
*   **计算延迟**：使用了 Whisper、Wav2Vec 2.0 和 ViT 等多个大型预训练模型，推理时的计算开销较大，可能难以在低功耗移动端实时运行。
*   **情感覆盖面**：虽然在 6 个维度上表现良好，但尚未验证其在更广泛、更复杂的人类情感类别上的泛化能力。

（完）
