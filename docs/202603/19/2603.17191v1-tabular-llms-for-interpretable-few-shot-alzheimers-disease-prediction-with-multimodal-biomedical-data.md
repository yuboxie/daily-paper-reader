---
title: "Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data"
title_zh: 用于多模态生物医学数据可解释少样本阿尔茨海默病预测的表格大语言模型
authors: "Sophie Kearney, Shu Yang, Zixuan Wen, Weimin Lyu, Bojian Hou, Duy Duong-Tran, Tianlong Chen, Jason H. Moore, Marylyn D. Ritchie, Chao Chen, Li Shen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17191v1"
tags: ["query:llm-mh"]
score: 9.0
evidence: 用于阿尔茨海默病预测的表格大语言模型
tldr: 本研究针对阿尔茨海默病（AD）临床数据规模小且不完整的挑战，提出了TAP-GPT框架。该框架基于TableGPT2，利用表格提示词进行微调，实现了多模态生物标志物数据的少样本分类。实验证明，TAP-GPT在处理缺失数据和高维输入方面表现优异，并能提供符合生物学逻辑的可解释推理，为临床决策支持系统奠定了基础。
selection_source: fresh_fetch
motivation: 传统的深度学习模型在处理小规模且不完整的阿尔茨海默病表格数据时效果不佳，而大语言模型在少样本泛化和可解释性方面具有潜力。
method: 提出了基于TableGPT2微调的TAP-GPT框架，通过表格化提示词处理多模态生物标志物和影像数据，实现少样本AD分类。
result: TAP-GPT在少样本场景下优于传统机器学习基准，且在无需插值的情况下对缺失数据表现出极强的鲁棒性。
conclusion: 该研究展示了表格专用大模型在多模态临床预测任务中的有效性，为构建基于智能体的临床决策支持系统提供了新思路。
---

## 摘要
阿尔茨海默病 (AD) 的准确诊断需要处理表格形式的生物标志物数据，然而这类数据通常规模较小且不完整，深度学习模型在这些数据上的表现往往难以超越经典方法。预训练大语言模型 (LLMs) 提供了少样本泛化、结构化推理和可解释的输出，为临床预测提供了强有力的范式转变。我们提出了 TAP-GPT (Tabular Alzheimer's Prediction GPT)，这是一个基于 TableGPT2 构建的领域自适应表格 LLM 框架，并针对使用表格提示（而非纯文本）的少样本 AD 分类进行了微调。我们在四个源自 ADNI 的数据集上评估了 TAP-GPT，包括用于二元 AD 分类的 QT-PAD 生物标志物以及区域级结构化 MRI、淀粉样蛋白 PET 和 tau 蛋白 PET。在多模态和单模态设置下，TAP-GPT 相比其骨干模型有所改进，在少样本设置中优于传统的机器学习基准，同时与最先进的通用 LLMs 相比保持了竞争力。我们展示了特征选择可以减轻高维输入带来的性能下降，并且 TAP-GPT 在模拟和现实世界的缺失情况下无需插补即可保持稳定的性能。此外，TAP-GPT 产生了与已确立的 AD 生物学相一致的结构化、模态感知的推理，并在自我反思下表现出更高的稳定性，支持其在迭代多智能体系统中的应用。据我们所知，这是表格专用 LLM 在基于多模态生物标志物的 AD 预测中的首次系统应用，证明了此类预训练模型可以有效处理结构化临床预测任务，并为表格 LLM 驱动的多智能体临床决策支持系统奠定了基础。源代码可在 GitHub 上公开获取：https://github.com/sophie-kearney/TAP-GPT。

## Abstract
Accurate diagnosis of Alzheimer's disease (AD) requires handling tabular biomarker data, yet such data are often small and incomplete, where deep learning models frequently fail to outperform classical methods. Pretrained large language models (LLMs) offer few-shot generalization, structured reasoning, and interpretable outputs, providing a powerful paradigm shift for clinical prediction. We propose TAP-GPT Tabular Alzheimer's Prediction GPT, a domain-adapted tabular LLM framework built on TableGPT2 and fine-tuned for few-shot AD classification using tabular prompts rather than plain texts. We evaluate TAP-GPT across four ADNI-derived datasets, including QT-PAD biomarkers and region-level structural MRI, amyloid PET, and tau PET for binary AD classification. Across multimodal and unimodal settings, TAP-GPT improves upon its backbone models and outperforms traditional machine learning baselines in the few-shot setting while remaining competitive with state-of-the-art general-purpose LLMs. We show that feature selection mitigates degradation in high-dimensional inputs and that TAP-GPT maintains stable performance under simulated and real-world missingness without imputation. Additionally, TAP-GPT produces structured, modality-aware reasoning aligned with established AD biology and shows greater stability under self-reflection, supporting its use in iterative multi-agent systems. To our knowledge, this is the first systematic application of a tabular-specialized LLM to multimodal biomarker-based AD prediction, demonstrating that such pretrained models can effectively address structured clinical prediction tasks and laying the foundation for tabular LLM-driven multi-agent clinical decision-support systems. The source code is publicly available on GitHub: https://github.com/sophie-kearney/TAP-GPT.

---

## 论文详细总结（自动生成）

以下是对论文《Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：阿尔茨海默病（AD）的诊断依赖于多模态表格数据（如AT[N]生物标志物、MRI、PET影像特征）。
*   **核心挑战**：临床表格数据通常具有**样本量小（Few-shot）**、**特征维度高**、**数据缺失严重**的特点。传统的深度学习模型在小规模表格数据上往往表现不如随机森林或XGBoost等树模型，且缺乏临床所需的可解释性。
*   **研究目的**：探索预训练表格大语言模型（Tabular LLMs）在极少样本情况下，利用其内置的生物医学先验知识和结构化推理能力，提升AD预测的准确性与可解释性。

### 2. 方法论
*   **核心思想**：提出 **TAP-GPT** 框架，基于 **TableGPT2-7B**（一种结合了表格编码器和Qwen2.5解码器的架构）进行领域自适应微调。
*   **关键技术细节**：
    *   **架构适配**：保留TableGPT2的语义表格编码器，使用 **QLoRA**（4-bit量化）对解码器进行参数高效微调，使其学习AD诊断的特定逻辑。
    *   **提示词工程（Prompt Engineering）**：设计了四种提示格式，对比“表格化（Tabular）”与“序列化（Serialized）”输入，以及“零样本（Zero-shot）”与“少样本（Few-shot）”的表现。
    *   **特征选择（Feature Selection）**：针对高维影像数据（如74个脑区特征），引入 **LASSO** 或 LLM 引导的排名方法，将特征降维至最优区间（如 $p=16$），以适配 LLM 的上下文窗口。
    *   **可解释推理**：通过思维链（CoT）引导模型输出 JSON 格式的结果，包含预测标签、推理理由和置信度评分。
    *   **少样本 ICL**：在提示词中嵌入 $k$ 个带标签的示例（In-Context Learning），使模型在推理时参考相似病例。

### 3. 实验设计
*   **数据集**：
    1.  **QT-PAD**：包含15个临床标志物（CSF、PET、MRI、遗传学及人口统计学数据）。
    2.  **区域级影像数据**：包括结构化MRI（体积）、淀粉样蛋白PET（SUVR）和Tau蛋白PET（SUVR），涵盖74个脑区。
*   **Benchmark（基准）**：
    *   **传统机器学习**：逻辑回归（LR）、随机森林（RF）、SVM、XGBoost。
    *   **表格基础模型**：TabPFN。
    *   **通用LLM**：Qwen2.5-Instruct、Qwen3-8B、GPT-4.1-mini。
    *   **消融实验**：针对示例数量 $k$ 和特征数量 $p$ 进行网格搜索。

### 4. 资源与算力
*   **硬件环境**：使用 NVIDIA **A100 GPU (80GB HBM)** 集群，配备 160GB 系统内存。
*   **实验规模**：进行了大规模计算，涉及 **335个实验变体**（涵盖不同模态、种子、提示格式和超参数组合），累计产生约 **0.25 TB** 的模型检查点和适配器。

### 5. 实验数量与充分性
*   **实验量**：研究在 4 个独立数据集上运行，每个实验重复 **10 个随机种子** 以确保统计显著性。
*   **充分性评价**：
    *   **全面性**：涵盖了从单模态到多模态、从零样本到少样本的完整对比。
    *   **鲁棒性测试**：专门设计了**模拟缺失值**（10%-50%）和**真实世界缺失值**（ADNI原始不完整数据）的鲁棒性实验。
    *   **客观性**：严格划分了训练集、验证集、测试集以及独立的 ICL 示例池，避免了数据泄露。

### 6. 主要结论与发现
*   **性能优越**：在少样本（如 $k=8$）设置下，TAP-GPT 在 F1 分数上显著优于传统机器学习模型和 TabPFN。
*   **格式敏感性**：在少样本场景下，**表格化提示词**（保持行列结构）的效果优于纯文本序列化描述。
*   **特征选择的必要性**：对于高维影像数据，适当的特征选择能显著缓解 LLM 在长上下文下的性能衰减。
*   **缺失值鲁棒性**：TAP-GPT 在**无需数据插补（Imputation）**的情况下，能直接处理缺失值，且在缺失率高达 40% 时仍保持稳定性能。
*   **稳定性**：在自我反思（Self-reflection）实验中，TAP-GPT 比 GPT-4.1-mini 表现出更高的预测一致性。

### 7. 优点
*   **领域适配性强**：首次系统性地将表格专用 LLM 应用于多模态 AD 预测，填补了该领域的空白。
*   **临床实用性**：模型不仅给出分类，还能提供符合生物学逻辑（如引用海马体萎缩、APOE4状态）的推理，增强了临床信任度。
*   **端到端处理**：能够直接处理非结构化缺失数据，简化了传统医学数据预处理的复杂流程。

### 8. 不足与局限
*   **架构依赖**：受限于 TableGPT2 的固定架构，难以直接升级到更新、更强大的 LLM 解码器（如 DeepSeek 或 Llama 3）。
*   **推理幻觉风险**：定性分析发现，模型偶尔会误解特征的方向性（例如误将高 FDG 摄取视为 AD 迹象，而实际应为低摄取）。
*   **计算开销**：尽管使用了 QLoRA，但 LLM 的推理成本和延迟仍远高于传统的随机森林等轻量级模型。
*   **泛化验证**：目前仅在 ADNI 单一来源的数据集上验证，尚需在更多样化的外部临床队列中进行测试。

（完）
