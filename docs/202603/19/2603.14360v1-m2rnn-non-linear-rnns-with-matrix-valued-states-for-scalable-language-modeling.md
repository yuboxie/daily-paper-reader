---
title: "M$^2$RNN: Non-Linear RNNs with Matrix-Valued States for Scalable Language Modeling"
title_zh: M$^2$RNN：具有矩阵值状态的非线性 RNN，用于可扩展语言建模
authors: "Mayank Mishra, Shawn Tan, Ion Stoica, Joseph Gonzalez, Tri Dao"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14360v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 用于可扩展语言建模的非线性 RNN 结构
tldr: 针对 Transformer 在实体追踪等复杂任务上的表达能力限制，本文提出 M$^2$RNN。该架构采用矩阵值隐藏状态和非线性状态转移，通过扩展状态规模并利用张量核心提升效率。实验表明，M$^2$RNN 在大规模语言建模和长文本泛化中表现优异，尤其在混合架构中以更小的状态规模超越了 Gated DeltaNet 等现有 SOTA 模型，证明了非线性 RNN 在可扩展语言模型中的潜力。
selection_source: fresh_fetch
motivation: Transformer 受限于 TC$^0$ 复杂度类，难以处理实体追踪和代码执行等需要更强表达能力的计算任务。
method: 引入具有矩阵值隐藏状态和非线性状态转移机制的 M$^2$RNN 架构，并利用张量核心优化计算效率。
result: 在 7B MoE 混合模型中，M$^2$RNN 以更小的状态规模实现了更低的困惑度，并在 LongBench 长文本测试中领先 SOTA 模型达 8 分。
conclusion: 非线性 RNN 层是构建高效、可扩展且具备卓越长文本泛化能力的语言模型的重要组件。
---

## 摘要
Transformer 具有高度并行性，但受限于 TC$^0$ 复杂度类的计算，排除了诸如实体追踪和代码执行等经证明需要更强表达能力的任务。受此限制启发，我们重新审视了用于语言建模的非线性循环神经网络 (RNN)，并引入了矩阵到矩阵 RNN (M$^2$RNN)：一种具有矩阵值隐藏状态和强表达能力非线性状态转移的架构。我们证明了非线性 RNN 的语言建模性能受其状态大小的限制。我们还展示了状态大小扩展机制如何实现对张量核心 (tensor cores) 的高效利用。实验表明，M$^2$RNN 在训练中未见过的序列长度上实现了完美的隐状态追踪泛化。这些优势也转化到了大规模语言建模中。在循环层与注意力层交替的混合设置中，混合 M$^2$RNN 在 7B MoE 模型上比等效的 Gated DeltaNet 混合模型在困惑度上领先 0.4-0.5 个点，同时循环层使用的状态大小缩小了 3 倍。值得注意的是，在现有混合架构中即使仅将一个循环层替换为 M$^2$RNN，也能获得与混合 M$^2$RNN 相当的准确率提升，且对训练吞吐量的影响极小。此外，带有一个 M$^2$RNN 层的混合 Gated DeltaNet 模型还实现了卓越的长上下文泛化，在 LongBench 上比最先进的混合线性注意力架构高出多达 8 个点。总之，这些结果确立了非线性 RNN 层作为构建高效且可扩展语言模型的一种极具吸引力的基础组件。

## Abstract
Transformers are highly parallel but are limited to computations in the TC$^0$ complexity class, excluding tasks such as entity tracking and code execution that provably require greater expressive power. Motivated by this limitation, we revisit non-linear Recurrent Neural Networks (RNNs) for language modeling and introduce Matrix-to-Matrix RNN (M$^2$RNN): an architecture with matrix-valued hidden states and expressive non-linear state transitions. We demonstrate that the language modeling performance of non-linear RNNs is limited by their state size. We also demonstrate how the state size expansion mechanism enables efficient use of tensor cores. Empirically, M$^2$RNN achieves perfect state tracking generalization at sequence lengths not seen during training. These benefits also translate to large-scale language modeling. In hybrid settings that interleave recurrent layers with attention, Hybrid M$^2$RNN outperforms equivalent Gated DeltaNet hybrids by $0.4$-$0.5$ perplexity points on a 7B MoE model, while using $3\times$ smaller state sizes for the recurrent layers. Notably, replacing even a single recurrent layer with M$^2$RNN in an existing hybrid architecture yields accuracy gains comparable to Hybrid M$^2$RNN with minimal impact on training throughput. Further, the Hybrid Gated DeltaNet models with a single M$^2$RNN layer also achieve superior long-context generalization, outperforming state-of-the-art hybrid linear attention architectures by up to $8$ points on LongBench. Together, these results establish non-linear RNN layers as a compelling building block for efficient and scalable language models.

---

## 论文详细总结（自动生成）

这篇论文由 UC Berkeley、Princeton 和 Together AI 等机构的研究者（包括 Tri Dao 等知名学者）合作完成，旨在解决 Transformer 和线性 RNN 在复杂状态追踪任务上的表达能力瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **Transformer 的局限性**：Transformer 的计算复杂度属于 $TC^0$ 级别，难以处理需要更强表达能力的逻辑任务，如实体追踪、代码执行和复杂的置换组合。
*   **线性 RNN 的不足**：虽然 Mamba-2 等线性 RNN（SSM）推理效率高，但其状态转移是线性的，在处理如 $S_5$ 置换群等硬状态追踪任务时表现较差。
*   **传统非线性 RNN 的瓶颈**：LSTM/GRU 等非线性 RNN 虽表达能力强，但通常采用向量值状态（Vector-valued states），状态容量小且难以利用现代 GPU 的张量核心（Tensor Cores）进行高效并行计算。
*   **研究目标**：设计一种既具备非线性表达能力，又拥有大规模矩阵状态，且能高效利用硬件的循环神经网络架构。

### 2. 方法论：M$^2$RNN 架构
*   **核心思想**：引入**矩阵值隐藏状态**（Matrix-Valued States）和**非线性状态转移**。
*   **关键技术细节**：
    *   **矩阵状态更新**：状态 $H_t$ 是一个 $K \times V$ 的矩阵。更新公式为 $Z_t = \tanh(H_{t-1}W + k_t v_t^\top)$，其中 $k_t v_t^\top$ 是输入的外积扩展，用于增加状态容量。
    *   **遗忘门机制**：采用与输入相关但与前一状态独立的遗忘门 $f_t$，公式为 $H_t = f_t H_{t-1} + (1-f_t)Z_t$。这种独立性允许预计算门控值。
    *   **多值注意力（Multi-value）表述**：借鉴 Mamba-2，通过共享查询（Query）和键（Key）来进一步扩展状态规模，而不显著增加参数量。
    *   **硬件优化**：开发了定制的 Triton 内核。由于状态是矩阵，其递归过程中的矩阵乘法（GEMM）形状固定，不依赖于 Batch Size，从而避免了传统 RNN 在小 Batch 训练时的填充（Padding）浪费，充分利用张量核心。

### 3. 实验设计
*   **模型规模**：
    *   410M 参数的稠密模型（24层）。
    *   7B 参数的混合专家模型（MoE，1.1B 激活参数，40层）。
*   **数据集**：使用 Nemotron-CC-v2 的高质量子集，训练 100B Token。
*   **对比方法（Baselines）**：
    *   **同质架构**：Mamba-2、Gated DeltaNet、传统 RNN、GRU、Transformer++。
    *   **混合架构**：将循环层与注意力层交替（如 1:7 比例），对比 Hybrid Mamba-2 和 Hybrid Gated DeltaNet。
*   **评测任务**：
    *   **基础语言建模**：Wikitext、LAMBADA 及 10 余项常识推理任务。
    *   **长文本与检索**：RULER 评测集（大海捞针及其变体）、LongBench（长文本摘要、代码等）。
    *   **状态追踪**：$S_3$ 置换群任务。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   410M 模型：使用 **32 张 NVIDIA H100** GPU。
    *   7B MoE 模型：使用 **64 张 NVIDIA H100** GPU。
*   **训练细节**：上下文长度为 4096，使用 AdamW 优化器，BF16 混合精度训练。

### 5. 实验数量与充分性
*   **实验覆盖面**：论文涵盖了从 410M 到 7B 的规模扩展实验、同质与混合架构对比、长文本泛化实验、状态追踪消融实验。
*   **公平性**：所有对比模型在相同的硬件、相同的数据顺序和相同的超参数（如学习率、Batch Size）下训练，确保了结果的客观性。
*   **消融实验**：专门验证了状态大小（矩阵 vs 向量）和遗忘门对性能的影响，证明了矩阵状态是性能提升的核心驱动力。

### 6. 主要结论与发现
*   **状态追踪完美泛化**：M$^2$RNN 在 $S_3$ 任务上实现了 100% 的长度泛化，远超线性 RNN。
*   **混合架构优势显著**：在 7B MoE 规模下，Hybrid M$^2$RNN 的困惑度（PPL）优于 Hybrid Gated DeltaNet 0.4-0.5 个点，且状态规模仅为后者的 1/3。
*   **“画龙点睛”效应**：在现有的混合线性 RNN 架构中，**仅需替换一层**为 M$^2$RNN，即可获得显著的准确率提升（LongBench 提升高达 8 分），而训练吞吐量仅下降约 6%。
*   **状态规模至关重要**：实验证明非线性 RNN 过去表现不佳主要是因为状态太小，而非非线性本身的问题。

### 7. 优点
*   **理论与实践结合**：从计算复杂度理论出发，解决了 Transformer 无法处理的特定任务。
*   **硬件友好**：通过矩阵化递归设计，解决了传统非线性 RNN 在现代 GPU 上利用率低的问题。
*   **极高的实用性**：提出的“单层替换”方案为现有大模型架构优化提供了一条低成本、高收益的路径。

### 8. 不足与局限
*   **计算开销**：尽管有硬件优化，非线性递归本质上仍无法像线性 RNN 那样使用并行扫描（Parallel Scan），在同质架构下其训练速度慢于 Mamba-2。
*   **内核优化空间**：目前的 Triton 实现仍受限于内存带宽，未来需要更底层的 CUDA/CUTLASS 优化。
*   **超长文本挑战**：虽然在 16k 长度表现良好，但在极长序列（如 100k+）下的计算延迟可能会因其顺序递归特性而变得显著。

（完）
