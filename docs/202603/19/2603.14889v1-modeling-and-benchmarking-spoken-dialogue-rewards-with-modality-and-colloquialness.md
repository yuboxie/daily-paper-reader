---
title: Modeling and Benchmarking Spoken Dialogue Rewards with Modality and Colloquialness
title_zh: 结合模态与口语化特征的语音对话奖励建模与基准测试
authors: "Jingyu Lu, Yuhan Wang, Fan Zhuo, Xize Cheng, Changhao Pan, Xueyi Pu, Yifu Chen, Chenyuhao Wen, Tianle Liang, Zhou Zhao"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14889v1"
tags: ["query:llm-ac"]
score: 9.0
evidence: 基准测试包含情感和副语言特征的口语对话奖励
tldr: 针对端到端语音对话系统在模态（韵律、情感）和口语化（书面语与自然语音差异）方面的不足，本文提出了SDiaReward奖励模型。该模型基于新构建的SDiaReward数据集，通过多轮对话语音的偏好对进行训练，能够联合评估语音表达力与口语化程度。同时，研究者建立了ESDR-Bench基准。实验证明，SDiaReward在偏好准确率上显著优于通用音频大模型，展现了强大的泛化能力。
selection_source: fresh_fetch
motivation: 现有的语音对话系统难以捕捉副语言特征及自然对话中的口语化表达，缺乏有效的端到端评估奖励模型。
method: 提出了一种直接处理多轮语音片段的端到端奖励模型SDiaReward，并构建了包含偏好对的专用数据集和分层评估基准ESDR-Bench。
result: SDiaReward在成对偏好准确率上达到了SOTA水平，显著超越了现有的通用音频大语言模型。
conclusion: 该研究通过整合模态与口语化特征，为语音对话系统的优化提供了更精准的评估工具，提升了跨领域的泛化性能。
---

## 摘要
端到端语音对话系统的快速演进要求超越单纯的文本语义，以纳入副语言细微差别和人类对话的自发性。然而，当前的方法面临两个关键差距：涉及韵律和情感的模态差距（modality gap），以及区分书面脚本与自然语音的口语化差距（colloquialness gap）。为应对这些挑战，我们推出了 SDiaReward，这是一种在 SDiaReward-Dataset 上训练的端到端多轮奖励模型。该数据集是一个新颖的情节级（episode-level）偏好对集合，专门针对上述差距。该模型直接作用于完整的多轮语音情节，并通过成对偏好监督进行优化，从而能够在单个评估器中实现对模态和口语化程度的联合评估。我们进一步建立了 ESDR-Bench，这是一个用于鲁棒情节级评估的分层基准。实验表明，SDiaReward 实现了最先进的成对偏好准确率，显著优于通用音频大语言模型（LLMs）。进一步的分析表明，SDiaReward 捕捉到了超越表面合成线索的相对对话表现力，提高了在不同领域和录制条件下的泛化能力。代码、数据和演示可在 https://sdiareward.github.io/ 获取。

## Abstract
The rapid evolution of end-to-end spoken dialogue systems demands transcending mere textual semantics to incorporate paralinguistic nuances and the spontaneous nature of human conversation. However, current methods struggle with two critical gaps: the modality gap, involving prosody and emotion, and the colloquialness gap, distinguishing written scripts from natural speech. To address these challenges, we introduce SDiaReward, an end-to-end multi-turn reward model trained on SDiaReward-Dataset, a novel collection of episode-level preference pairs explicitly targeting these gaps. It operates directly on full multi-turn speech episodes and is optimized with pairwise preference supervision, enabling joint assessment of modality and colloquialness in a single evaluator. We further establish ESDR-Bench, a stratified benchmark for robust episode-level evaluation. Experiments demonstrate that SDiaReward achieves state-of-the-art pairwise preference accuracy, significantly outperforming general-purpose audio LLMs. Further analysis suggests that SDiaReward captures relative conversational expressiveness beyond superficial synthesis cues, improving generalization across domains and recording conditions. Code, data, and demos are available at https://sdiareward.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《Modeling and Benchmarking Spoken Dialogue Rewards with Modality and Colloquialness》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
随着端到端语音对话系统的发展，评估标准已不再局限于文本语义，而需涵盖副语言特征（如韵律、情感）和对话的自发性。然而，现有系统面临两大挑战：
*   **模态差距（Modality Gap）**：文本评估器无法感知语音中的韵律和情感，且难以区分真实人类语音与合成语音。
*   **口语化差距（Colloquialness Gap）**：文本优化的系统往往生成过于书面、僵硬的回复，缺乏自然对话中的填充词、断句和互动线索。
论文旨在通过数据驱动的奖励建模（Reward Modeling），为语音对话系统提供一个能够同时评估模态真实性和口语化程度的端到端评估器。

### 2. 方法论：核心思想与技术细节
论文提出了 **SDiaReward** 框架，其核心包含数据集构建和模型训练两部分：
*   **数据集构建 (SDiaReward-Dataset)**：
    *   **模态感知对**：对比真实人类语音（来自 YouTube、MELD、DailyTalk）与高质量 TTS 合成的语音，训练模型识别韵律自然度。
    *   **口语化对**：利用 LLM 将书面风格对话改写为包含填充词、碎片化表达的口语风格，并保持音频合成条件一致，训练模型识别风格偏好。
*   **模型架构**：
    *   基于多模态大模型（如 Qwen2.5-Omni）作为骨干网络，直接处理交错的语音-文本序列。
    *   **特征聚合**：对比了多种池化策略，发现 **Mean Pooling（平均池化）** 在处理多轮对话的情节级信息时最稳定。
    *   **损失函数**：采用 Bradley-Terry 偏好框架，并引入了 **Center Loss（中心化正则项）**。该项将奖励分数锚定在零附近，防止分数无限漂移，并确保模型学习的是相对偏好而非录音条件的绝对差异。

### 3. 实验设计
*   **数据集与场景**：涵盖了“野外”录音（Wild）、半野外（Semi-wild，如 MELD 情感对话）和脚本录音（Scripted，如 DailyTalk）。
*   **基准测试 (ESDR-Bench)**：从验证集中提取并进行分层抽样，确保情感、意图和领域分布均衡，避免高频数据主导评估结果。
*   **对比方法**：
    *   **闭源模型**：GPT-4o-audio、Gemini 2.5 Pro/Flash。
    *   **开源模型**：Kimi-Audio、Qwen2.5/3-Omni、VITA-Audio。
    *   **消融实验**：针对不同参数规模（3B vs 7B）、池化策略（Last, Attention, Mean）以及是否有 Center Loss 进行了对比。

### 4. 资源与算力
*   **算力配置**：使用了 **4 张 GPU**（具体型号未明确说明，推测为 A100 或 H100 级别）。
*   **训练细节**：采用 DeepSpeed ZeRO-2 优化器、FlashAttention-2 和 bf16 精度。
*   **时长与参数**：训练 1 个 epoch，学习率为 $2 \times 10^{-5}$，总 Batch Size 为 32。

### 5. 实验数量与充分性
*   **数据规模**：包含约 1.3 万个偏好对（约 200 小时配对语音），规模较大。
*   **实验维度**：涵盖了不同领域（10 个领域、250 个场景）、不同情感标签和不同录音质量。
*   **验证充分性**：除了自动指标，还进行了 **人工验证（75 组样本）**，证明模型预测与人类偏好的一致性（加权一致性达 83.5%）。消融实验覆盖了架构选择和损失函数，逻辑严密且公正。

### 6. 主要结论与发现
*   **专用模型优于通用模型**：SDiaReward-7B 在模态评估上的准确率（96.61%）远超 Gemini 2.5 Pro（72.63%）和 GPT-4o（51.12%）。
*   **模态差距更难跨越**：通用模型在口语化（文本风格）判断上表现尚可，但在区分韵律自然度（模态感知）上几乎失效，表现接近随机。
*   **相对表达力**：模型不仅在检测合成痕迹，还学会了捕捉“相对对话表现力”。即使在不同录音条件下，模型也能准确排列质量高低。

### 7. 优点与亮点
*   **端到端处理**：直接在多轮语音情节上操作，无需中间文本转换，保留了完整的副语言信息。
*   **解决分数漂移**：引入 Center Loss 有效解决了奖励模型在不同音频域（如嘈杂 vs 清晰）下的分数偏移问题。
*   **分层基准**：ESDR-Bench 的分层抽样策略为语音对话评估提供了更科学、更具泛化性的参考。

### 8. 不足与局限
*   **领域偏差**：尽管引入了正则化，模型在不同领域（如脚本 vs 野外）的绝对分数仍存在偏移，可能影响下游强化学习的稳定性。
*   **数据覆盖**：目前主要侧重于英语，且“野外”数据比例较高，未来需增加更多高质量的表演语音和多语种支持。
*   **应用限制**：作为奖励模型，仍存在被“奖励作弊（Reward Hacking）”的风险，需谨慎用于下游策略优化。

（完）
