---
title: "Open Biomedical Knowledge Graphs at Scale: Construction, Federation, and AI Agent Access with Samyama Graph Database"
title_zh: 大规模开放生物医学知识图谱：基于 Samyama 图数据库的构建、联邦化与 AI 智能体访问
authors: "Madhulatha Mandarapu, Sandeep Kunkunuru"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15080v3"
tags: ["query:llm-mr"]
score: 9.0
evidence: 包含药物相互作用和临床试验的生物医学知识图谱
tldr: 针对生物医学知识碎片化问题，本文基于高性能 Rust 图数据库 Samyama 构建了三个大规模开源知识图谱（Pathways、Clinical Trials、Drug Interactions）。研究提出了一套可复用的 ETL 流程，实现了跨图谱联邦查询，并引入了模式驱动的 MCP 服务，使 AI 智能体能高效访问图数据。在 BiomedQA 基准测试中，该方法显著提升了 LLM 的回答准确率，展示了在大规模生物医学数据集成与智能问答方面的卓越性能。
selection_source: fresh_fetch
motivation: 解决生物医学知识分散在不同孤立数据库中、难以统一集成和高效利用的问题。
method: 利用 Rust 编写的 Samyama 数据库构建大规模知识图谱，通过 ETL 模式进行去重集成，并生成模式驱动的 MCP 服务供 LLM 智能体调用。
result: "成功构建了包含 790 万节点和 2800 万条边的联邦图谱，且 AI 智能体在药理学问答中的准确率达到 98%，远超传统方法。"
conclusion: 本研究证明了高性能图数据库结合模式驱动的智能体接口，是实现大规模生物医学知识集成与精准检索的有效方案。
---

## 摘要
生物医学知识分散在孤立的数据库中——Reactome 用于通路，STRING 用于蛋白质相互作用，ClinicalTrials.gov 用于研究注册，DrugBank 用于药物词汇，DGIdb 用于药物-基因相互作用，SIDER 用于副作用。我们介绍了三个开源生物医学知识图谱——Pathways KG（来自 5 个数据源的 118,686 个节点和 834,785 条边）、Clinical Trials KG（来自 5 个数据源的 7,774,446 个节点和 26,973,997 条边）以及 Drug Interactions KG（来自 3 个数据源的 32,726 个节点和 191,970 条边）——这些图谱均构建于用 Rust 编写的高性能图数据库 Samyama 之上。我们的贡献主要体现在三个方面：首先，我们描述了一种可重复的 ETL 模式，用于从异构公共数据源构建大规模知识图谱，支持跨源去重、批量加载（Python Cypher 和 Rust 原生加载器）以及便携式快照导出。其次，我们展示了跨知识图谱的联邦化：将所有三个快照加载到单个图租户中，实现了跨数据集的基于属性的连接。第三，我们引入了用于大语言模型（LLM）智能体访问的模式驱动型 MCP 服务器生成，并在新的 BiomedQA 基准测试（40 个药理学问题）上进行了评估：领域特定的 MCP 工具实现了 98% 的准确率，而模式感知的 text-to-Cypher 为 85%，独立 GPT-4o 为 75%，且模式错误率为零。所有数据源均采用开放许可。合并后的联邦图（790 万个节点，2800 万条边）在商用云硬件上加载约需 3 分钟，单知识图谱查询在 80-100 毫秒内完成，跨知识图谱联邦连接在 1-4 秒内完成。

## Abstract
Biomedical knowledge is fragmented across siloed databases -- Reactome for pathways, STRING for protein interactions, ClinicalTrials.gov for study registries, DrugBank for drug vocabularies, DGIdb for drug-gene interactions, SIDER for side effects. We present three open-source biomedical knowledge graphs -- Pathways KG (118,686 nodes, 834,785 edges from 5 sources), Clinical Trials KG (7,774,446 nodes, 26,973,997 edges from 5 sources), and Drug Interactions KG (32,726 nodes, 191,970 edges from 3 sources) -- built on Samyama, a high-performance graph database written in Rust.   Our contributions are threefold. First, we describe a reproducible ETL pattern for constructing large-scale KGs from heterogeneous public data sources, with cross-source deduplication, batch loading (Python Cypher and Rust native loaders), and portable snapshot export. Second, we demonstrate cross-KG federation: loading all three snapshots into a single graph tenant enables property-based joins across datasets. Third, we introduce schema-driven MCP server generation for LLM agent access, evaluated on a new BiomedQA benchmark (40 pharmacology questions): domain-specific MCP tools achieve 98% accuracy vs. 85% for schema-aware text-to-Cypher and 75% for standalone GPT-4o, with zero schema errors.   All data sources are open-license. The combined federated graph (7.9M nodes, 28M edges) loads in approximately 3 minutes on commodity cloud hardware, with single-KG queries completing in 80-100ms and cross-KG federation joins in 1-4s

---

## 论文详细总结（自动生成）

### 论文总结：大规模开放生物医学知识图谱的构建、联邦化与 AI 访问

#### 1. 论文的核心问题与整体含义
生物医学领域的研究高度依赖于分布在不同孤立数据库（如 Reactome、STRING、ClinicalTrials.gov 等）中的知识。目前，研究人员通常需要手动下载、编写脚本进行交叉引用，这一过程不仅缓慢、易出错，且难以重复。论文的核心目标是解决**生物医学知识的碎片化问题**，通过构建大规模、可复用的知识图谱（KG），并利用高性能图数据库和 AI 智能体接口（MCP），实现跨领域知识的高效集成与自然语言访问。

#### 2. 论文提出的方法论
论文提出了一套基于 **Samyama 图数据库**（使用 Rust 编写）的完整解决方案：
*   **可重复的 ETL 模式**：分为五个阶段：下载（支持断点续传）、解析与过滤（针对人类数据）、去重（通过共享注册表跟踪实体）、批量加载（支持 Python Cypher 和 Rust 原生加载器）以及快照导出（生成便携式 `.sgsnap` 文件）。
*   **跨图谱联邦化（Cross-KG Federation）**：不采用预先合并数据的方式，而是将多个 KG 快照加载到同一个数据库租户中，利用共享标识符（如 UniProt ID、DrugBank ID、基因符号）进行**基于属性的连接（Property-based Joins）**。
*   **模式驱动的 MCP 服务器生成**：利用 Model Context Protocol (MCP) 标准，根据图数据库的 Schema 自动生成 LLM 智能体可调用的工具。这些工具将复杂的 Cypher 查询封装为带参数的函数，使 LLM 能够通过自然语言精准检索图数据。

#### 3. 实验设计
*   **数据集**：整合了 13 个开放许可的公共数据库，构建了三个 KG：
    *   **Pathways KG**：11.9 万节点，83.5 万边。
    *   **Clinical Trials KG**：777 万节点，2697 万边。
    *   **Drug Interactions KG**：3.3 万节点，19.2 万边。
*   **基准测试（Benchmark）**：引入了 **BiomedQA**，包含 40 个涵盖药理学、临床试验和生物通路的专业问题。
*   **对比方法**：
    1.  **MCP 工具**：使用预定义的参数化 Cypher 模板。
    2.  **Schema-aware Text-to-Cypher**：向 GPT-4o 提供完整的图模式，由其生成 Cypher 语句。
    3.  **Standalone GPT-4o**：不访问数据库，仅凭模型内置知识回答。

#### 4. 资源与算力
*   **硬件环境**：实验在 **AWS g4dn.4xlarge** 实例上运行。
*   **配置细节**：16 vCPU (AMD EPYC), 62 GB RAM, **NVIDIA A10G GPU** (24GB VRAM)。
*   **性能表现**：合并后的联邦图（7.9M 节点，28M 边）占用约 33 GB 内存。快照加载总时长约 3 分钟。Rust 原生加载器在 1 秒内即可完成 3.2 万节点图谱的构建。

#### 5. 实验数量与充分性
*   **实验规模**：构建了总计近 800 万节点的大规模图谱，这在生物医学 KG 领域属于较高水平。
*   **测试维度**：实验涵盖了构建性能（加载时间）、查询延迟（单图 vs 跨图）以及 AI 问答准确率。
*   **充分性评价**：实验设计较为充分，通过 BiomedQA 的 7 个类别问题验证了系统的实用性。对比实验展示了 MCP 工具在消除“模式幻觉”（Schema Hallucination）方面的显著优势。不过，40 个问题的基准测试样本量相对较小，未来可进一步扩大。

#### 6. 论文的主要结论与发现
*   **AI 访问精度**：MCP 工具在 BiomedQA 上达到了 **98% 的准确率**，显著优于 Text-to-Cypher (85%) 和独立 GPT-4o (75%)。
*   **零模式错误**：通过 MCP 工具访问，LLM 完全避免了生成错误的图路径或不存在的属性标签。
*   **联邦查询效率**：在近 3000 万条边的规模下，跨图联邦查询（涉及 3 个 KG 的连接）可在 **1-4 秒**内完成，单图查询仅需 80-100 毫秒。
*   **构建速度**：Rust 原生加载器比 Python HTTP 加载器快几个数量级。

#### 7. 优点
*   **高性能与可扩展性**：利用 Rust 语言特性和 Samyama 数据库，实现了极速的数据加载和查询响应。
*   **工程化程度高**：提供了便携式快照和开源的 ETL 流程，极大降低了研究人员复现和部署大规模 KG 的门槛。
*   **AI 集成创新**：通过 MCP 协议将 KG 转化为 LLM 的“外部技能”，解决了 LLM 在专业领域知识匮乏和生成错误查询语句的问题。

#### 8. 不足与局限
*   **数据时效性**：快照是静态的，而临床试验等数据更新频繁，需要定期重新运行 ETL 流程。
*   **标识符对齐风险**：联邦查询依赖于属性匹配（如基因名），若不同源数据的命名规范不统一，可能导致连接遗漏。
*   **内存依赖**：大规模联邦图谱需要较大的内存（33GB+），在低配机器上运行受限。
*   **联邦机制限制**：目前要求所有快照加载到同一租户，尚不支持真正的分布式跨服务器联邦查询。

（完）
