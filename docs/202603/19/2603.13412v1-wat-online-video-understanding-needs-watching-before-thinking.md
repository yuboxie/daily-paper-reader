---
title: "WAT: Online Video Understanding Needs Watching Before Thinking"
title_zh: WAT：在线视频理解需要先看后想
authors: "Zifan Han, Hongbo Sun, Jinglin Xu, Canhui Tang, Yulong Lei, Xuchong Zhang, Hongbin Sun, Zhongjiang He, Hao Sun"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.13412v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 视频LLM中的长时序上下文保留
tldr: 针对现有视频大模型在在线流式场景下难以平衡长时上下文与内存限制的问题，本文提出WAT框架。该框架将视频理解分为“观察”和“思考”两个阶段：观察阶段通过分层记忆系统高效维护历史摘要；思考阶段利用上下文感知检索从记忆中提取关键信息进行推理。此外，本文还推出了WAT-85K数据集。实验证明，WAT在多个在线视频基准测试中达到SOTA性能，并支持实时处理。
selection_source: fresh_fetch
motivation: 现有的视频大模型在处理在线流式视频时，难以在严格的内存限制下保留长时上下文信息。
method: 提出一种两阶段框架，通过分层记忆系统进行实时观察，并结合上下文感知检索机制进行针对性思考。
result: 在StreamingBench和OVO-Bench等基准测试中取得领先成绩，并能以实时帧率运行。
conclusion: WAT框架通过分离观察与思考过程，有效解决了在线视频理解中的长时记忆管理与实时推理难题。
---

## 摘要
多模态大语言模型 (MLLMs) 在图像理解方面展现出强大的能力，这促使了近期将其扩展到视频推理的努力。然而，现有的视频大语言模型 (Video LLMs) 在在线流媒体场景中面临挑战，因为在严格的内存限制下必须保留长期的时序上下文。我们提出了 WAT (Watching Before Thinking)，这是一个用于在线视频推理的两阶段框架。WAT 将处理过程分为与查询无关的“观察”阶段和由查询触发的“思考”阶段。观察阶段构建了一个分层记忆系统，包含一个缓冲近期帧的短期记忆 (STM) 和一个固定容量的长期记忆 (LTM)，后者利用冗余感知的剔除策略维护历史内容的多元化摘要。在思考阶段，一种上下文感知检索机制将查询与当前的 STM 上下文相结合，从 LTM 中检索相关的历史帧以进行跨时序推理。为了支持在线视频任务的训练，我们推出了 WAT-85K，这是一个包含流媒体风格标注的数据集，强调实时感知、回溯追踪和预测。实验表明，WAT 在在线视频基准测试中达到了最先进的性能，包括在 StreamingBench 上达到 77.7% 的准确率，在 OVO-Bench 上达到 55.2%，在以实时帧率运行的同时，超越了现有的开源在线视频大语言模型。

## Abstract
Multimodal Large Language Models (MLLMs) have shown strong capabilities in image understanding, motivating recent efforts to extend them to video reasoning. However, existing Video LLMs struggle in online streaming scenarios, where long temporal context must be preserved under strict memory constraints. We propose WAT (Watching Before Thinking), a two-stage framework for online video reasoning. WAT separates processing into a query-independent watching stage and a query-triggered thinking stage. The watching stage builds a hierarchical memory system with a Short-Term Memory (STM) that buffers recent frames and a fixed-capacity Long-Term Memory (LTM) that maintains a diverse summary of historical content using a redundancy-aware eviction policy. In the thinking stage, a context-aware retrieval mechanism combines the query with the current STM context to retrieve relevant historical frames from the LTM for cross-temporal reasoning. To support training for online video tasks, we introduce WAT-85K, a dataset containing streaming-style annotations emphasizing real-time perception, backward tracing, and forecasting. Experiments show that WAT achieves state-of-the-art performance on online video benchmarks, including 77.7% accuracy on StreamingBench and 55.2% on OVO-Bench, outperforming existing open-source online Video LLMs while operating at real-time frame rates.

---

## 论文详细总结（自动生成）

以下是对论文《WAT: Online Video Understanding Needs Watching Before Thinking》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频大语言模型（Video LLMs）在**在线流媒体（Online Streaming）**场景下表现不佳。主要矛盾在于：如何在有限的内存和计算资源下，既能保留长期的历史上下文，又能维持对近期动态的高保真感知。
*   **研究动机**：
    *   **离线模型的局限**：通常需要一次性输入所有帧，无法处理持续到达的视频流。
    *   **现有在线模型的缺陷**：采样压缩（丢弃过多细节）或存储压缩（简单合并相邻帧导致时序混乱），难以平衡“细粒度细节”与“长时语义多样性”。
    *   **核心理念**：提出“先看后想”（Watching Before Thinking），将视频处理解耦为持续的感知（观察）和按需的推理（思考）。

### 2. 方法论
WAT 框架分为两个核心阶段：

#### **A. 观察阶段（Watching）：分层记忆系统**
为了高效管理历史信息，WAT 构建了双层记忆架构：
*   **短期记忆 (STM)**：采用 FIFO（先进先出）队列，作为一个高保真的滑动窗口缓冲区，保留最近的视频帧特征，用于捕捉即时的细粒度动作和时序动态。
*   **长期记忆 (LTM)**：具有固定容量（如 768 帧），存储历史显著帧。
    *   **冗余感知剔除策略（Redundancy-Aware Eviction）**：当 LTM 满员时，系统计算所有存储帧之间的余弦相似度，识别并剔除语义最冗余（相似度最高）的帧，同时设有保护机制防止最近存入的帧被过早删除。

#### **B. 思考阶段（Thinking）：上下文感知检索与推理**
当用户提出查询（Query）时触发：
*   **上下文感知检索**：将 Query 与当前 STM 中的视觉特征融合，生成一个增强的查询向量，从 LTM 中检索出 Top-K 个最相关的历史帧。
*   **检索对齐对比学习 (RACL)**：在训练中引入辅助的对比损失函数，强制模型学习 Query 与检索到的视觉证据之间的语义对齐，提高检索的准确性。
*   **推理生成**：将 STM 特征、检索到的 LTM 特征以及 Query 拼接，输入 MLLM 生成最终回答。

### 3. 实验设计
*   **数据集**：
    *   **WAT-85K**：作者构建的专用训练集，整合了 LLaVA-Video 和 TimeChat-Online 的数据，并针对实时感知、回溯追踪和主动预测任务进行了人工增强。
*   **Benchmark（基准测试）**：
    *   **在线场景**：StreamingBench（涵盖 10 类实时理解任务）、OVO-Bench（评估感知、回溯和前瞻能力）。
    *   **离线场景**：MLVU、VideoMME（验证模型在长视频任务上的泛化性）。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini 1.5 Pro, Claude 3.5 Sonnet。
    *   **开源离线模型**：Video-LLaMA2, InternVL-V2, LLaVA-OneVision 等。
    *   **开源在线模型**：Flash-VStream, VideoLLM-online, TimeChat-Online, Dispider 等。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA A800 (80G) GPU**。
*   **训练细节**：
    *   底座模型：Qwen2.5-VL-7B。
    *   训练策略：冻结视觉编码器，全参数微调 Projector 和语言模型（LLM）。
    *   训练时长：在 WAT-85K 上训练 1 个 epoch。
    *   输入规格：分辨率 448x448，采样率 1 FPS。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流视频理解榜单上进行了全面评估，涵盖了从短视频到长达 1 小时的长视频。
*   **消融实验**：
    *   验证了 STM 和 LTM 的互补性（STM 提升了约 2-5% 的准确率）。
    *   测试了不同 LTM 长度（32 到 768）的影响，证明了更大容量的 LTM 对长时序任务的显著增益。
    *   验证了 RACL 损失函数的有效性（提升了约 1-4% 的性能）。
*   **充分性评价**：实验设计严谨，对比对象涵盖了当前最顶尖的闭源和开源模型，消融实验针对核心组件进行了逐一拆解，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：WAT 在 StreamingBench（77.7%）和 OVO-Bench（55.2%）上均取得了开源在线模型中的 **SOTA（最先进）** 性能。
*   **超越闭源**：在 StreamingBench 上，WAT 甚至超过了 Gemini 1.5 Pro 和 GPT-4o 的在线表现。
*   **离线竞争力**：尽管是为在线设计的，WAT 在离线长视频榜单（VideoMME）上也表现出色，证明其记忆检索机制能有效提取长视频的关键信息。
*   **实时性**：模型能够以实时帧率运行，满足实际应用需求。

### 7. 优点
*   **架构直观**：解耦“观察”与“思考”符合人类认知逻辑，有效解决了流式视频处理中的内存溢出问题。
*   **记忆管理科学**：冗余感知剔除策略比传统的均匀采样或随机丢弃更能保留视频的语义多样性。
*   **数据贡献**：WAT-85K 数据集针对在线视频理解的痛点（如预测未来、回溯历史）进行了优化，对社区有重要价值。

### 8. 不足与局限
*   **采样率限制**：实验主要基于 1 FPS，对于某些需要极高时间分辨率（如高速运动分析）的特殊场景，可能会丢失瞬时信息。
*   **检索依赖性**：模型的性能高度依赖于检索模块的准确性，如果 Query 无法有效激活相关的历史记忆，推理阶段可能会出现偏差。
*   **闭源差距**：虽然在部分榜单领先，但在 OVO-Bench 的某些复杂逻辑推理任务上，距离 Gemini 1.5 Pro 仍有一定绝对分值差距。

（完）
