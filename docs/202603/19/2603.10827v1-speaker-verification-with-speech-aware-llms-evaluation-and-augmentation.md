---
title: "Speaker Verification with Speech-Aware LLMs: Evaluation and Augmentation"
title_zh: 基于语音感知大语言模型的说话人验证：评估与增强
authors: "Thomas Thebaud, Yuzhe Wang, Laureano Moro-Velazquez, Jesus Villalba-Lopez, Najim Dehak"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10827v1"
tags: ["query:llm-ac"]
score: 6.0
evidence: 语音感知大语言模型与说话人身份编码
tldr: "本研究探讨了语音感知大语言模型（LLM）在说话人识别任务中的表现。作者首先提出一种基于Token概率的通用评分协议，发现现有模型识别能力较弱。随后，提出一种轻量级增强方案，通过注入冻结的ECAPA-TDNN说话人嵌入并利用LoRA进行微调。实验表明，该方法使TinyLLaMA-1.1B在VoxCeleb1-E上达到1.03%的等错误率，在保持自然语言交互能力的同时，实现了接近专用系统的识别精度。"
selection_source: fresh_fetch
motivation: 旨在解决现有语音感知大语言模型在说话人身份编码方面能力不足且缺乏统一评估标准的问题。
method: 提出了一种通用的评分协议，并设计了通过注入冻结说话人嵌入和LoRA适配器来增强LLM识别能力的ECAPA-LLM架构。
result: "实验显示基准模型识别性能较差，而增强后的ECAPA-LLM在VoxCeleb1-E上取得了1.03%的等错误率。"
conclusion: 通过轻量级的模块注入和微调，可以显著提升大语言模型的说话人识别能力，使其兼具高性能识别与自然语言交互优势。
---

## 摘要
语音感知大语言模型（LLMs）可以接受语音输入，但其训练目标主要强调语言内容或特定领域（如情感或说话人性别），目前尚不清楚它们是否编码了说话人身份。首先，我们提出了一种与模型无关的评分协议，利用“是/否”标记概率的置信度分数或对数似然比，为仅限 API 的模型和开源权重模型生成连续的验证分数。利用该协议，我们对近期的语音感知 LLM 进行了基准测试，并观察到较弱的说话人辨别能力（在 VoxCeleb1 上的等错误率 EER 超过 20%）。其次，我们引入了一种轻量级增强方法，通过学习到的投影注入冻结的 ECAPA-TDNN 说话人嵌入，并仅训练 LoRA 适配器，从而使 LLM 具备自动说话人验证（ASV）能力。在 TinyLLaMA-1.1B 上，由此产生的 ECAPA-LLM 在 VoxCeleb1-E 上实现了 1.03% 的 EER，在保留自然语言接口的同时，性能接近专用说话人验证系统。

## Abstract
Speech-aware large language models (LLMs) can accept speech inputs, yet their training objectives largely emphasize linguistic content or specific fields such as emotions or the speaker's gender, leaving it unclear whether they encode speaker identity. First, we propose a model-agnostic scoring protocol that produces continuous verification scores for both API-only and open-weight models, using confidence scores or log-likelihood ratios from the Yes/No token probabilities. Using this protocol, we benchmark recent speech-aware LLMs and observe weak speaker discrimination (EERs above 20% on VoxCeleb1). Second, we introduce a lightweight augmentation that equips an LLM with ASV capability by injecting frozen ECAPA-TDNN speaker embeddings through a learned projection and training only LoRA adapters. On TinyLLaMA-1.1B, the resulting ECAPA-LLM achieves 1.03% EER on VoxCeleb1-E, approaching a dedicated speaker verification system while preserving a natural-language interface.