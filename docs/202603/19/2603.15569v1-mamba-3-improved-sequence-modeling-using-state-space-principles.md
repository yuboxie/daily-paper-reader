---
title: "Mamba-3: Improved Sequence Modeling using State Space Principles"
title_zh: Mamba-3：基于状态空间原理的改进序列建模
authors: "Aakash Lahoti, Kevin Y. Li, Berlin Chen, Caitlin Wang, Aviv Bick, J. Zico Kolter, Tri Dao, Albert Gu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15569v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 针对极长序列的高效注意力机制
tldr: Mamba-3 针对 Transformer 推理效率低和现有线性模型在状态跟踪等任务上表现不佳的问题，基于状态空间模型（SSM）原理提出了三项核心改进：更具表达力的递归、复数值状态更新规则以及多输入多输出（MIMO）架构。该模型在保持推理高效的同时，显著提升了检索、状态跟踪和语言建模性能，在 1.5B 规模下超越了 Gated DeltaNet 等模型，并优化了性能与效率的平衡。
selection_source: fresh_fetch
motivation: 旨在解决现有线性模型在追求推理效率时牺牲模型质量（如状态跟踪能力）的问题。
method: 结合了 SSM 离散化衍生的强表达力递归、复数值状态更新以及不增加解码延迟的 MIMO 架构。
result: 在 1.5B 规模下，Mamba-3 的下游任务准确率比 Gated DeltaNet 高出 1.8 个百分点，且仅需 Mamba-2 一半的状态大小即可达到同等困惑度。
conclusion: Mamba-3 成功推进了序列建模在性能与推理效率之间的帕累托前沿。
---

## 摘要
扩展推理时计算已成为大语言模型（LLM）性能的重要驱动力，使得推理效率与模型质量共同成为模型设计的核心焦点。虽然目前的 Transformer 模型具有很强的模型质量，但其二次方计算复杂度和线性内存需求使得推理成本高昂。这促使了具有线性计算复杂度和常数内存需求的亚二次方模型的发展。然而，许多近期的线性模型为了算法效率而牺牲了模型质量和能力，在状态追踪等任务上表现不佳。此外，它们在理论上的线性推理在实践中仍然存在硬件效率低下的问题。在推理优先视角的指导下，我们受线性模型的状态空间模型（SSM）观点启发，提出了三项核心方法论改进。我们结合了：（1）源自 SSM 离散化的更具表达力的递归；（2）能够实现更丰富状态追踪的复数值状态更新规则；以及（3）在不增加解码延迟的情况下提升模型性能的多输入多输出（MIMO）公式。结合架构改进，我们的 Mamba-3 模型在检索、状态追踪和下游语言建模任务中取得了显著提升。在 1.5B 参数规模下，Mamba-3 相比次优模型（Gated DeltaNet）将平均下游准确率提高了 0.6 个百分点，而 Mamba-3 的 MIMO 变体进一步将准确率提高了 1.2 个百分点，总增益达到 1.8 个百分点。在状态大小实验中，尽管 Mamba-3 使用的状态大小仅为前作的一半，但其困惑度与 Mamba-2 相当。我们的评估表明，Mamba-3 能够推进性能-效率的帕累托前沿。

## Abstract
Scaling inference-time compute has emerged as an important driver of LLM performance, making inference efficiency a central focus of model design alongside model quality. While the current Transformer-based models deliver strong model quality, their quadratic compute and linear memory make inference expensive. This has spurred the development of sub-quadratic models with reduced linear compute and constant memory requirements. However, many recent linear models trade off model quality and capability for algorithmic efficiency, failing on tasks such as state tracking. Moreover, their theoretically linear inference remains hardware-inefficient in practice. Guided by an inference-first perspective, we introduce three core methodological improvements inspired by the state space model (SSM) viewpoint of linear models. We combine: (1) a more expressive recurrence derived from SSM discretization, (2) a complex-valued state update rule that enables richer state tracking, and (3) a multi-input, multi-output (MIMO) formulation for better model performance without increasing decode latency. Together with architectural refinements, our Mamba-3 model achieves significant gains across retrieval, state-tracking, and downstream language modeling tasks. At the 1.5B scale, Mamba-3 improves average downstream accuracy by 0.6 percentage points compared to the next best model (Gated DeltaNet), with Mamba-3's MIMO variant further improving accuracy by another 1.2 points for a total 1.8 point gain. Across state-size experiments, Mamba-3 achieves comparable perplexity to Mamba-2 despite using half of its predecessor's state size. Our evaluations demonstrate Mamba-3's ability to advance the performance-efficiency Pareto frontier.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Mamba-3**，这是对状态空间模型（SSM）的一次重大升级，旨在解决现有线性模型在推理效率与模型质量之间的权衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：虽然 Transformer 在质量上领先，但其二次方计算和线性内存需求导致推理成本极高。现有的亚二次方模型（如 Mamba-2、线性注意力）虽然推理快，但存在两个痛点：
    1.  **表达力不足**：在状态追踪（如奇偶校验、复杂逻辑）和长程检索任务上表现弱于 Transformer。
    2.  **硬件效率低**：理论上的线性复杂度在实际硬件（GPU）上由于算术强度低，导致计算单元闲置，推理速度未达预期。
*   **研究动机**：基于“推理优先”的视角，通过回归 SSM 的基本数学原理，提升模型的表达能力并优化硬件利用率，从而推向性能-效率的帕累托（Pareto）前沿。

### 2. 方法论：核心思想与关键技术
Mamba-3 提出了三项核心改进：
*   **指数-梯形离散化（Exponential-Trapezoidal Discretization）**：
    *   **核心思想**：改进了从连续系统到离散递归的转换方法。
    *   **技术细节**：相比 Mamba-1/2 使用的一阶“指数-欧拉”法，Mamba-3 引入了二阶精度的梯形法则。这在数学上等同于在状态输入上应用了一个隐式的、数据依赖的卷积。
    *   **效果**：这种更复杂的递归形式使得模型可以省去之前版本中必不可少的显式短因果卷积层。
*   **复数值状态空间（Complex-valued SSM）**：
    *   **核心思想**：引入复数特征值以捕捉“旋转”动力学。
    *   **技术细节**：通过“RoPE Trick”实现，将复数更新等效为在输入 $B$ 和输出 $C$ 投影上应用数据依赖的旋转位置嵌入（RoPE）。
    *   **效果**：解决了线性模型无法处理奇偶校验（Parity）等状态追踪任务的理论缺陷。
*   **多输入多输出（MIMO）公式**：
    *   **核心思想**：将状态更新从向量外积提升为矩阵乘法。
    *   **技术细节**：通过增加秩 $R$，提高解码时的算术强度（FLOPs/Byte）。
    *   **效果**：在不增加解码延迟的前提下，显著增加了模型的计算量和表达力，更好地利用了 GPU 的 Tensor Core。
*   **架构微调**：引入了 BC/QK 归一化和可学习的偏置项，进一步稳定训练并增强表达力。

### 3. 实验设计
*   **数据集**：使用 **FineWeb-Edu**（100B tokens）进行预训练。
*   **模型规模**：涵盖 180M、440M、880M 到 **1.5B** 四个量级。
*   **Benchmark**：
    *   **语言建模**：LAMBADA、HellaSwag、PIQA、Arc、WinoGrande、OpenBookQA。
    *   **检索能力**：SWDE、SQuAD、FDA、TriviaQA、NQ、DROP 以及大海捞针（NIAH）测试。
    *   **合成任务**：奇偶校验（Parity）、模运算（Modular Arithmetic）等 Chomsky 层级任务。
*   **对比方法**：Transformer、Mamba-2、Gated DeltaNet (GDN)。

### 4. 资源与算力
*   **硬件**：论文提到在 **NVIDIA H100-SXM5** GPU 上进行了内核基准测试和推理实验。
*   **算力细节**：虽然未明确给出总的 GPU 小时数，但明确了所有模型均在 100B tokens 上训练，且为 Mamba-3 开发了专门的 Triton（前向/预填充）和 CuTe（解码）高性能内核。

### 5. 实验数量与充分性
*   **实验规模**：做了大量实验，包括四个参数规模的横向对比、不同状态大小（State Size）的消融实验、以及针对混合架构（Hybrid）的探索。
*   **充分性**：实验设计非常全面。不仅对比了下游任务准确率，还深入分析了困惑度（Perplexity）与推理延迟的关系。通过合成任务验证了复数状态的理论优势，通过 NIAH 验证了长文本检索能力，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：在 1.5B 规模下，Mamba-3 SISO 优于 GDN 0.6 个百分点；Mamba-3 MIMO 进一步提升 1.2 个百分点，总计领先 1.8 个百分点。
*   **效率飞跃**：Mamba-3 仅需 Mamba-2 **一半的状态大小**即可达到相同的困惑度，这意味着在相同质量下推理速度更快。
*   **能力突破**：成功解决了 Mamba-2 无法处理的奇偶校验等状态追踪任务。
*   **硬件友好**：MIMO 变体在增加 4 倍解码计算量的情况下，实际墙钟时间（Wall-clock time）几乎没有增加。

### 7. 优点
*   **理论与实践结合**：改进不仅有深厚的 SSM 数学背景（离散化、复数域），还紧贴硬件特性（MIMO 提升算术强度）。
*   **架构简化**：通过改进离散化方法，成功去除了传统线性模型中冗余的短卷积层。
*   **开源贡献**：提供了高性能的训练和推理内核，具有很强的实用价值。

### 8. 不足与局限
*   **训练开销**：MIMO 变体虽然推理快，但训练速度比 SISO 慢（约 2 倍），因为其计算复杂度更高。
*   **检索瓶颈**：尽管有所提升，但在处理半结构化或非结构化数据的复杂检索任务时，纯线性模型仍略逊于拥有完整 KV Cache 的 Transformer，通常仍需结合少量注意力层（混合架构）。
*   **超参数敏感性**：新的离散化参数 $\lambda$ 和 MIMO 秩 $R$ 的选择可能需要针对不同任务进行精细调优。

（完）
