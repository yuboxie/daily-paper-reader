---
title: "MemMA: Coordinating the Memory Cycle through Multi-Agent Reasoning and In-Situ Self-Evolution"
title_zh: MemMA：通过多智能体推理与原位自我演化协调记忆周期
authors: "Minhua Lin, Zhiwei Zhang, Hanqing Lu, Hui Liu, Xianfeng Tang, Qi He, Xiang Zhang, Suhang Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18718v1"
tags: ["query:lh-llm"]
score: 10.0
evidence: 用于长程交互的记忆增强LLM智能体
tldr: 针对长程交互中记忆管理碎片化的问题，本文提出 MemMA 框架。该框架通过多智能体协同优化记忆循环：由 Meta-Thinker 提供全局策略指导构建与检索，并引入原位自进化机制，通过合成探针问答对实时验证并修复记忆。实验证明 MemMA 在多种模型和存储后端上均表现优异，显著提升了智能体的长程任务处理能力。
selection_source: fresh_fetch
motivation: 现有记忆系统将构建与检索视为孤立过程，缺乏全局策略引导且难以针对下游失败进行有效的记忆修复。
method: 提出一种多智能体框架，利用 Meta-Thinker 协调记忆周期，并结合原位自进化机制在记忆持久化前进行验证与修复。
result: 在 LoCoMo 基准上，MemMA 显著超越了现有基线，并证明了其在不同大模型和存储后端上的通用性与有效性。
conclusion: 通过协调记忆循环的正向推理与反向进化，MemMA 为构建更具鲁棒性的长程交互智能体提供了高效的即插即用方案。
---

## 摘要
记忆增强型大语言模型（LLM）智能体通过维护外部记忆库来支持长程交互，然而现有的大多数系统将记忆的构建、检索和利用视为孤立的子程序。这带来了两个耦合的挑战：一是记忆周期前向路径上的策略盲目性，即构建和检索是由局部启发式方法而非显式策略推理驱动的；二是后向路径上稀疏且延迟的监督，即下游任务的失败很少能直接转化为对记忆库的修复。为了应对这些挑战，我们提出了 MemMA，这是一个即插即用的多智能体框架，能够在前向和后向路径上协调记忆周期。在前向路径上，元思考器（Meta-Thinker）产生结构化指导，在构建过程中引导记忆管理器（Memory Manager），并在迭代检索过程中指导查询推理器（Query Reasoner）。在后向路径上，MemMA 引入了原位自我演化的记忆构建机制，通过合成探测问答对来验证当前记忆，并在记忆最终确定前将失败转化为修复动作。在 LoCoMo 上的广泛实验表明，MemMA 在多个 LLM 基座模型上始终优于现有基准，并能以即插即用的方式提升三种不同存储后端的性能。我们的代码已开源至 https://github.com/ventr1c/memma。

## Abstract
Memory-augmented LLM agents maintain external memory banks to support long-horizon interaction, yet most existing systems treat construction, retrieval, and utilization as isolated subroutines. This creates two coupled challenges: strategic blindness on the forward path of the memory cycle, where construction and retrieval are driven by local heuristics rather than explicit strategic reasoning, and sparse, delayed supervision on the backward path, where downstream failures rarely translate into direct repairs of the memory bank. To address these challenges, we propose MemMA, a plug-and-play multi-agent framework that coordinates the memory cycle along both the forward and backward paths. On the forward path, a Meta-Thinker produces structured guidance that steers a Memory Manager during construction and directs a Query Reasoner during iterative retrieval. On the backward path, MemMA introduces in-situ self-evolving memory construction, which synthesizes probe QA pairs, verifies the current memory, and converts failures into repair actions before the memory is finalized. Extensive experiments on LoCoMo show that MemMA consistently outperforms existing baselines across multiple LLM backbones and improves three different storage backends in a plug-and-play manner. Our code is publicly available at https://github.com/ventr1c/memma.

---

## 论文详细总结（自动生成）

以下是对论文《MemMA: Coordinating the Memory Cycle through Multi-Agent Reasoning and In-Situ Self-Evolution》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）从短期对话向长程（Long-horizon）智能体演进，外部记忆库成为必不可少的组件。
*   **核心问题**：现有的记忆增强系统通常将记忆的**构建（Construction）**、**检索（Retrieval）**和**利用（Utilization）**视为孤立的子任务。这导致了两个主要挑战：
    1.  **正向路径的策略盲目性（Strategic Blindness）**：记忆构建和检索缺乏全局策略指导，容易出现冗余、冲突或检索目标不明确的问题。
    2.  **反向路径的反馈稀疏且延迟（Sparse and Delayed Feedback）**：只有在下游任务失败时才能发现记忆错误，且难以追溯并修复早期的构建错误。
*   **研究动机**：将记忆视为一个闭环的“记忆周期（Memory Cycle）”，通过多智能体协同和实时自我修复来优化整个生命周期。

### 2. 论文提出的方法论：MemMA 框架
MemMA 是一个即插即用的多智能体框架，核心思想是**“解耦策略推理与底层执行”**。

*   **正向路径：推理感知协同（Reasoning-Aware Coordination）**
    *   **元思考器（Meta-Thinker, $\pi_p$）**：作为“大脑”负责高层规划。在构建阶段提供指导（存什么、合并什么）；在检索阶段诊断证据是否充分，并指出缺失的信息。
    *   **记忆管理器（Memory Manager, $\pi_s$）**：根据元思考器的指导执行原子操作（添加、更新、删除、忽略）。
    *   **查询推理器（Query Reasoner, $\pi_r$）**：执行迭代检索。它不是简单的重写查询，而是根据元思考器的诊断，针对性地缩小信息差。
*   **反向路径：原位自我演化（In-Situ Self-Evolution）**
    *   **探针生成（Probe Generation）**：在每个会话结束后，自动生成合成的问答对（探针），涵盖事实、跨会话推理和时间推理。
    *   **原位验证与修复**：使用探针测试当前记忆。如果回答错误，系统会生成修复建议（如插入缺失实体、细化模糊描述），并在记忆正式提交前进行语义整合（去重和冲突解决）。

### 3. 实验设计
*   **数据集**：**LoCoMo**（长程对话记忆基准），包含多跳推理、时间推理、开放域和单跳问答。
*   **评价指标**：Token 级别的 F1 分数、BLEU-1 (B1) 以及使用 LLM 作为裁判的准确率 (ACC)。
*   **对比方法**：
    *   **被动基线**：全文本（Full Text）、朴素 RAG。
    *   **主动记忆系统**：LangMem、Mem0、A-Mem、LightMem。
*   **实验设置**：使用 GPT-4o-mini 和 Claude-Haiku-4.5 作为主要基座模型，验证框架的通用性。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：MemMA 是一个基于推理的智能体框架，主要通过调用 LLM API（如 OpenAI 和 Anthropic 的接口）实现，不涉及大规模的参数微调或从头训练，因此算力消耗主要体现在 API 调用成本和推理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 LLM 基座上进行了全量对比。
    *   **即插即用测试**：将 MemMA 应用于三种不同的存储后端（Single-Agent, A-Mem, LightMem），均观察到显著提升。
    *   **消融实验**：分别移除了构建指导、迭代检索和自我演化模块，验证了各组件的必要性。
    *   **参数敏感性分析**：探讨了检索预算 $k$ 和迭代次数 $H$ 对性能的影响。
*   **充分性评价**：实验设计非常全面且客观。通过跨模型、跨后端的测试，证明了该框架的鲁棒性和通用性。

### 6. 论文的主要结论与发现
*   **性能领先**：MemMA 在所有指标上均优于现有最强基线（如 LightMem），在 GPT-4o-mini 上将准确率从 75.66% 提升至 81.58%。
*   **协同效应**：单纯增加检索迭代次数而不进行策略诊断会导致“检索漂移”，而 Meta-Thinker 的介入能快速收敛并锁定正确证据。
*   **修复价值**：原位自我演化能有效修复构建阶段的遗漏，特别是对于命名实体和复杂事件细节的补充至关重要。
*   **后端无关性**：MemMA 能够提升任何底层记忆存储系统的表现，证明了其作为“协调层”的价值。

### 7. 优点
*   **闭环设计**：首次系统性地解决了记忆周期中正向策略盲目和反向反馈延迟的问题。
*   **即插即用**：不依赖特定的存储架构，具有极强的工程实用性。
*   **高质量监督**：通过合成探针实现了“密集监督”，解决了长程任务中奖励稀疏的难题。
*   **逻辑清晰**：将复杂的记忆管理任务分解为规划与执行，符合当前 Agentic Workflow 的主流趋势。

### 8. 不足与局限
*   **成本与延迟**：多轮迭代检索和探针生成会显著增加 API 调用成本和系统响应时间。
*   **场景限制**：实验主要集中在对话场景，对于非结构化流数据或缺乏清晰会话边界的任务，其“原位修复”机制可能需要调整。
*   **探针质量依赖**：自我演化的效果高度依赖于生成探针的 LLM 的能力（实验中发现使用更强的模型如 Claude-Opus 生成探针效果更好）。

（完）
