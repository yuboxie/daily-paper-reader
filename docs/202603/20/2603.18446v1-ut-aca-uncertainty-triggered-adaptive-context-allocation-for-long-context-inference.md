---
title: "UT-ACA: Uncertainty-Triggered Adaptive Context Allocation for Long-Context Inference"
title_zh: UT-ACA：面向长上下文推理的不确定性触发自适应上下文分配
authors: "Lang Zhou, Shuxuan Li, Zhuohao Li, Shi Liu, Zhilin Zhao, Wei-Shi Zheng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18446v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 针对长上下文推理动态调整上下文窗口
tldr: 针对大语言模型长文本推理中注意力稀释和固定上下文预算的局限性，本文提出UT-ACA框架。该框架通过学习不确定性检测器，结合语义嵌入和Logit置信度，动态调整每个Token的上下文窗口。当检测到证据不足时，系统会自动回退并扩大窗口重新生成。实验证明，该方法在保持生成质量的同时显著降低了平均上下文开销。
selection_source: fresh_fetch
motivation: 现有的长文本推理方法通常分配固定的上下文预算，无法适应不同Token对上下文需求的非均匀性。
method: 提出一种基于不确定性触发的自适应上下文分配框架，通过检测Token生成的不确定性来动态触发上下文窗口的扩展与回退重生成。
result: 实验结果显示，UT-ACA在长文本场景下大幅减少了平均上下文使用量，且未牺牲生成质量。
conclusion: 动态调整上下文分配是提升长文本推理效率的有效途径，UT-ACA通过不确定性感知实现了性能与开销的平衡。
---

## 摘要
由于注意力稀释和分布外退化，长上下文推理对于大语言模型而言仍具挑战性。上下文选择通过关注键值缓存（KV cache）条目的子集来缓解这一限制，但尽管 token 级别的上下文需求高度不均匀，大多数方法在整个解码过程中仍分配固定的上下文预算。为了解决这一问题，我们提出了不确定性触发的自适应上下文分配（UT-ACA），这是一个根据逐 token 不确定性动态调整上下文窗口的推理时框架。UT-ACA 学习了一个不确定性检测器，该检测器结合了语义嵌入与基于 logit 的置信度，同时考虑了跨解码步骤的不确定性累积。当显示证据不足时，UT-ACA 会选择性地回退，扩大上下文窗口，并在额外的支持下重新生成 token。实验表明，在长上下文设置下，UT-ACA 在保持生成质量的同时，大幅减少了平均上下文使用量。

## Abstract
Long-context inference remains challenging for large language models due to attention dilution and out-of-distribution degradation. Context selection mitigates this limitation by attending to a subset of key-value cache entries, yet most methods allocate a fixed context budget throughout decoding despite highly non-uniform token-level contextual demands. To address this issue, we propose Uncertainty-Triggered Adaptive Context Allocation (UT-ACA), an inference-time framework that dynamically adjusts the context window based on token-wise uncertainty. UT-ACA learns an uncertainty detector that combines semantic embeddings with logit-based confidence while accounting for uncertainty accumulation across decoding steps. When insufficient evidence is indicated, UT-ACA selectively rolls back, expands the context window, and regenerates the token with additional support. Experiments show that UT-ACA substantially reduces average context usage while preserving generation quality in long-context settings.

---

## 论文详细总结（自动生成）

### 论文总结：UT-ACA：面向长上下文推理的不确定性触发自适应上下文分配

#### 1. 核心问题与研究动机
大语言模型（LLM）在处理长文本时面临**注意力稀释**（Attention Dilution）和**分布外（OOD）退化**的问题。现有的上下文管理方法（如 KV Cache 压缩或选择）通常在整个解码过程中分配**固定的上下文预算**。然而，不同 Token 对上下文的需求是非均匀的：有些 Token 仅依赖局部信息，而有些则需要检索长程证据。固定预算会导致简单步骤的资源浪费和困难步骤的证据不足。

#### 2. 方法论：核心思想与关键技术
论文提出了 **UT-ACA** 框架，其核心思想是：**在推理时根据 Token 级别的不确定性动态调整上下文窗口**。

*   **不确定性检测器（Uncertainty Detector）**：
    *   **输入信号**：结合了轻量级的 **Logit Margin**（最高与次高概率的差值）和从模型隐藏层提取的**语义嵌入**（Semantic Embedding）。
    *   **时序建模**：使用 **LSTM** 捕捉解码过程中的不确定性累积效应。
    *   **三分类输出**：将生成状态分为“正确（Grounded Correct）”、“未知（Unknown）”和“幻觉（Hallucination）”。当非正确类别的概率之和超过正确类别时，触发调整。
*   **自适应分配与回退机制（Rollback & Regeneration）**：
    *   解码初始使用较小的上下文窗口进行“试探性生成”。
    *   若检测器发出高不确定性信号，系统执行**回退（Rollback）**：丢弃当前 Token 及其 KV 缓存，**扩大上下文窗口**（检索更多相关的 KV 块），然后重新生成该 Token。
    *   若信号稳定，则接受该 Token 并可能缩小窗口以节省资源。

#### 3. 实验设计
*   **数据集**：
    *   **合成传记数据集**：用于训练和验证不确定性检测器，包含 10,000 条训练样本，测试集长度覆盖 171k 到 400k Token。
    *   **公开基准测试**：$\infty$-Bench（长文本摘要与问答）、LongBench（多任务长文本理解）、RULER（长文本压力测试）。
*   **对比方法（Baselines）**：包括 InfLLM、RetrievalAttn、SnapKV、OmniKV、LLMLingua-2、TokenSelect、H2O 等主流长文本推理优化方法。
*   **评估指标**：除了常规的 F1、准确率外，还提出了**概念准确率评分（CAS）**，利用强模型（如 GPT-OSS-120B）评估语义一致性。

#### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA H100 GPU** 上完成。
*   **训练时长**：不确定性检测器的训练每轮约耗时 **1.5 小时**，共进行 8 次独立运行以选择最佳模型。
*   **推理开销**：检测器本身非常轻量（LSTM 前向传播仅需约 15-21ms），主要的延迟增量来自于触发回退后的重新生成步骤。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **检测器性能实验**：对比了启发式方法（如 Logit 标准差）与学习型检测器的准确率。
*   **长文本生成实验**：在 Llama-3.1-8B 和 Qwen2-7B 两个主流模型上验证了泛化性。
*   **消融实验**：验证了语义嵌入、Logit Margin 和 LSTM 模块各自的贡献，以及“三分类”对比“二分类”的优越性。
*   **延迟分析**：详细拆解了生成、检测、回退各阶段的时间占比。
实验设计较为充分，涵盖了从合成数据到真实复杂任务的迁移，对比基准选择具有代表性。

#### 6. 主要结论与发现
*   **效率与质量的平衡**：UT-ACA 在显著减少平均上下文 Token 使用量（例如在某些任务中减少约 25%-50%）的同时，保持甚至提升了生成质量。
*   **动态分配的必要性**：实验证明，仅在困难 Token 上投入更多计算资源（扩大窗口）比全局分配固定预算更有效。
*   **不确定性作为信号**：Token 级别的不确定性是触发上下文扩展的可靠指标，能够有效缓解长文本下的幻觉问题。

#### 7. 优点
*   **自适应性强**：打破了固定窗口的限制，实现了按需分配。
*   **即插即用**：不需要对底座 LLM 进行微调，仅需训练一个极小的辅助检测器。
*   **鲁棒性**：通过回退机制提供了“自我纠错”的可能性，减少了错误累积。
*   **评估创新**：提出的 CAS 指标比传统的字符串匹配更能反映长文本生成的真实质量。

#### 8. 不足与局限
*   **推理延迟**：虽然节省了显存和计算量，但频繁的回退和重新生成会增加端到端的推理延迟（Latency）。
*   **检测器依赖数据**：检测器的性能依赖于合成数据的质量和覆盖面，对于极度偏离训练分布的任务，检测器可能失效。
*   **策略复杂性**：窗口缩放策略（如 Update: Sub.16）仍包含一些人工设定的超参数，可能需要针对不同模型进一步调优。

（完）
