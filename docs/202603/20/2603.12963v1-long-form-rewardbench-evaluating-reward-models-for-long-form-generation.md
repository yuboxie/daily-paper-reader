---
title: "Long-form RewardBench: Evaluating Reward Models for Long-form Generation"
title_zh: Long-form RewardBench：评估长文本生成的奖励模型
authors: "Hui Huang, Yancheng He, Wei Liu, Muyun Yang, Jiaheng Liu, Kehai Chen, Bing Xu, Conghui Zhu, Hailong Cao, Tiejun Zhao"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12963v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 评估长文本生成的奖励模型
tldr: 针对长文本生成中奖励模型评估的空白，本文推出了首个专门的基准测试集 Long-form RewardBench。该基准涵盖问答、RAG、聊天、写作和推理五大任务，通过多阶段流程收集偏好数据。对20多个主流模型的评估显示，现有模型在长文本奖励建模方面仍有不足。研究还揭示了模型性能与错误位置及文本长度的相关性，并发现分类器模型比生成式模型具有更好的泛化能力。
selection_source: fresh_fetch
motivation: 尽管长文本生成在实际应用中至关重要，但目前仍缺乏专门评估奖励模型在长文本场景下表现的基准工具。
method: 提出了包含五大子任务的 Long-form RewardBench，通过多阶段数据收集流程构建偏好数据集，并设计了针对奖励模型的“大海捞针”测试。
result: 实验表明现有奖励模型在长文本处理上能力不足，且模型表现受错误位置和响应长度影响，其中分类器模型的泛化性优于生成式模型。
conclusion: 本研究填补了长文本奖励模型评估的空白，为提升模型在复杂长文本场景下的对齐能力提供了重要的基准平台和改进方向。
---

## 摘要
基于强化学习的对齐技术的广泛采用，凸显了奖励模型日益增长的重要性。目前已建立了多种基准来评估不同领域和场景下的奖励模型。然而，尽管长文本生成在现实应用中起着至关重要的作用，但在评估长文本生成的奖励模型方面仍存在显著空白。为了填补这一空白，我们推出了 Long-form RewardBench，这是首个专门为长文本生成设计的奖励建模测试平台。我们的基准涵盖了五个关键子任务：问答（QA）、检索增强生成（RAG）、聊天（Chat）、写作（Writing）和推理（Reasoning）。我们通过精心设计的多阶段数据收集过程收集了指令和偏好数据，并对 20 多个主流奖励模型（包括分类器模型和生成式模型）进行了广泛的实验。研究结果表明，当前模型仍然缺乏长文本奖励建模能力。此外，我们设计了一种新颖的长文本“大海捞针”测试（Long-form Needle-in-a-Haystack Test），揭示了奖励建模性能与回复中错误位置以及回复总长度之间的相关性，并观察到分类模型和生成模型之间存在明显的特征差异。最后，我们证明了在相同数据上训练时，分类器比生成式模型表现出更好的泛化能力。作为长文本奖励建模的首个基准，本项工作旨在为可视化这一关键领域的进展提供一个稳健的平台。

## Abstract
The widespread adoption of reinforcement learning-based alignment highlights the growing importance of reward models. Various benchmarks have been built to evaluate reward models in various domains and scenarios. However, a significant gap remains in assessing reward models for long-form generation, despite its critical role in real-world applications. To bridge this, we introduce Long-form RewardBench, the first reward modeling testbed specifically designed for long-form generation. Our benchmark encompasses five key subtasks: QA, RAG, Chat, Writing, and Reasoning. We collected instruction and preference data through a meticulously designed multi-stage data collection process, and conducted extensive experiments on 20+ mainstream reward models, including both classifiers and generative models. Our findings reveal that current models still lack long-form reward modeling capabilities. Furthermore, we designed a novel Long-form Needle-in-a-Haystack Test, which revealed a correlation between reward modeling performance and the error's position within a response, as well as the overall response length, with distinct characteristics observed between classification and generative models. Finally, we demonstrate that classifiers exhibit better generalizability compared to generative models trained on the same data. As the first benchmark for long-form reward modeling, this work aims to offer a robust platform for visualizing progress in this crucial area.

---

## 论文详细总结（自动生成）

### Long-form RewardBench：长文本生成奖励模型评估基准论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在专业领域的广泛应用，长文本生成（如长篇报告、复杂推理、详细写作）的需求日益增长。然而，现有的奖励模型（Reward Models, RM）评估基准（如 RewardBench）主要集中在几十到几百个 token 的短文本上。长文本生成在逻辑连贯性、信息一致性和结构完整性方面面临独特挑战。
**研究动机**：填补长文本场景下奖励模型评估工具的空白，通过构建专门的基准测试集，分析现有 RM 在处理长回复时的优劣，并探讨文本长度和错误位置对模型判断的影响。

#### 2. 论文提出的方法论
论文构建了 **Long-form RewardBench**，其核心思想和技术细节包括：
*   **多阶段数据构建流程**：
    1.  **指令收集**：从 QuoraQA、RAGBench、WildChat 等数据集筛选高质量长文本指令，涵盖 QA、RAG、Chat、Writing、Reasoning 五大领域。
    2.  **回复生成**：利用 15+ 种主流开源（如 Qwen-2.5, Llama-3）和闭源（如 GPT-4o, Claude-3.5）模型生成长回复，确保回复长度在 200 到 30,000+ token 之间。
    3.  **偏好标注（LLM-as-a-Judge）**：采用多阶段自动化流程，包括准则生成、点分（Pointwise Scoring）、偏好选择和成对验证（Pairwise Validation），确保“选中（Chosen）”回复在质量上显著优于“拒绝（Rejected）”回复。
*   **评估方法**：采用 **Best-of-N (BoN)** 采样评估。即给定一个指令和 4 个回复（1 个 Chosen，3 个 Rejected），RM 需准确识别出 Chosen 回复。
*   **长文本“大海捞针”测试（Needle-in-a-Haystack Test）**：通过在长文本的不同位置插入事实性错误或安全违规内容，测试 RM 捕捉细微错误的能力。

#### 3. 实验设计
*   **数据集/场景**：涵盖五大子任务：问答（QA）、检索增强生成（RAG）、多轮对话（Chat）、创意写作（Writing）和数学推理（Reasoning）。
*   **对比方法**：评估了 20 多个主流奖励模型，分为两类：
    *   **序列分类器（Sequence Classifier）**：如 Skywork-Reward、Llama-3.1-RM 等。
    *   **生成式模型（Generative Model）**：如 GPT-4o、Claude-3.5、Gemini-2.5 以及经过微调的 RISE-Judge 等。
*   **Benchmark 对照**：同时提供了模型在 RewardBench 1 & 2 上的得分作为参考。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **实验细节**：论文主要侧重于基准测试的构建和对现有公开模型的评估。在“训练长度影响”实验中提到了对模型进行微调，但未披露具体的硬件环境参数。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   基准测试包含 1816 组偏好对。
    *   “大海捞针”测试构造了 7200 个样本对，涵盖 1K 到 8K 的不同长度。
    *   训练长度实验使用了 30k 偏好对进行微调分析。
*   **充分性与公平性**：实验覆盖了从 3B 到 70B+ 不同参数规模的模型，对比了分类式和生成式两种主流架构。通过多阶段验证（如 Gemini-2.5-pro 最终审核）来降低标注偏差，实验设计较为客观、严谨。

#### 6. 论文的主要结论与发现
*   **分类器整体占优**：尽管参数量较小，序列分类器在长文本偏好建模上的整体表现优于大多数强大的生成式模型。
*   **推理任务例外**：在数学推理（Reasoning）子项中，生成式模型表现远超分类器（>90% vs ~70%），这归功于生成式模型的思维链（CoT）能力。
*   **“迷失在中部”现象**：生成式模型在检测长文本中间位置的错误时准确率显著下降；而分类器对长度更鲁棒，但倾向于关注文本末尾的差异。
*   **泛化能力差异**：分类器在短文本上训练后，能较好地泛化到长文本；而生成式模型对训练数据的长度非常敏感，需要长文本数据才能在长场景下表现良好。
*   **评分模式缺陷**：生成式模型在“评分（Scoring）”模式下的表现远逊于“选择（Selection）”模式，容易给出相同分数导致判别失败。

#### 7. 优点
*   **填补空白**：首次针对长文本生成场景提出了专门的奖励模型评估基准。
*   **维度创新**：引入“大海捞针”测试，从错误位置和文本长度两个细粒度维度剖析 RM 的行为特征。
*   **标注严谨**：结合了多阶段 LLM 评分和规则验证（针对推理题），提高了 Benchmark 的金标准质量。

#### 8. 不足与局限
*   **推理任务评估不一致**：由于获取 3 个错误推理回复较难，Reasoning 子项采用了成对（Pairwise）评估，而其他子项采用 BoN 评估，这可能导致综合评分的权重解释存在偏差。
*   **模型偏见风险**：偏好标注高度依赖 GPT-4o 和 Gemini-2.5，可能引入“模型自我偏好”或特定的风格偏见。
*   **长度上限**：虽然涵盖了长文本，但受限于当前 LLM 的生成能力，测试集主要集中在 1K-8K token，对于超长文本（如 32K+）的覆盖仍有限。

（完）
