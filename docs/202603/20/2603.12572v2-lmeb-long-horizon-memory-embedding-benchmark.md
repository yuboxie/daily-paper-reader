---
title: "LMEB: Long-horizon Memory Embedding Benchmark"
title_zh: LMEB：长跨度记忆嵌入基准
authors: "Xinping Zhao, Xinshuo Hu, Jiaxin Xu, Danyu Tang, Xin Zhang, Mengjia Zhou, Yan Zhong, Yao Zhou, Zifei Shan, Meishan Zhang, Baotian Hu, Min Zhang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12572v2"
tags: ["query:lh-llm"]
score: 9.0
evidence: 涉及长程记忆检索任务和时间跨度较远信息的基准测试
tldr: 本研究针对现有文本嵌入基准在长程记忆检索评估方面的不足，提出了LMEB基准。LMEB涵盖22个数据集和193个零样本任务，分为情景、对话、语义和程序四类记忆类型。通过对15个主流模型的评估，发现现有模型在处理碎片化、上下文相关及跨时间信息的长程记忆任务上仍有提升空间，且该基准与传统检索基准具有正交性，为长程记忆增强系统提供了重要的评估框架。
selection_source: fresh_fetch
motivation: 现有文本嵌入基准局限于传统段落检索，难以评估模型在处理碎片化、上下文相关及长时依赖的长程记忆任务中的表现。
method: 构建了包含22个数据集和193个零样本任务的LMEB基准，涵盖四种记忆类型，并对15个不同规模的主流嵌入模型进行了系统评估。
result: 实验发现LMEB具有较高挑战性，模型性能并不随参数量增加而线性提升，且其评估维度与传统MTEB基准具有正交性。
conclusion: LMEB填补了长程记忆嵌入评估的空白，揭示了传统检索能力无法直接推广到长程记忆检索，为未来模型优化提供了新方向。
---

## 摘要
记忆嵌入对于 OpenClaw 等记忆增强系统至关重要，但在当前的文本嵌入基准测试中，对其评估尚不充分。这些基准测试狭隘地关注传统的段落检索，未能评估模型处理涉及碎片化、上下文相关且时间跨度较长的信息的长跨度记忆检索任务的能力。为此，我们推出了长跨度记忆嵌入基准（LMEB），这是一个评估嵌入模型处理复杂长跨度记忆检索任务能力的综合框架。LMEB 涵盖了 22 个数据集和 193 个零样本检索任务，横跨四种记忆类型：情景记忆、对话记忆、语义记忆和程序记忆，包含 AI 生成和人工标注的数据。这些记忆类型在抽象程度和时间依赖性方面各不相同，捕捉了记忆检索的不同方面，反映了现实世界中的多样化挑战。我们评估了 15 个广泛使用的嵌入模型，参数量从亿级到百亿级不等。结果显示：（1）LMEB 提供了合理的难度水平；（2）较大的模型并不总是表现更好；（3）LMEB 与 MTEB 表现出正交性。这表明该领域尚未收敛到一个能够在所有记忆检索任务中表现出色的通用模型，且传统段落检索的性能可能无法泛化到长跨度记忆检索。总之，通过提供标准化且可重复的评估框架，LMEB 填补了记忆嵌入评估中的关键空白，推动了文本嵌入在处理长期、上下文相关的记忆检索方面的进一步发展。LMEB 可在 https://github.com/KaLM-Embedding/LMEB 获取。

## Abstract
Memory embeddings are crucial for memory-augmented systems, such as OpenClaw, but their evaluation is underexplored in current text embedding benchmarks, which narrowly focus on traditional passage retrieval and fail to assess models' ability to handle long-horizon memory retrieval tasks involving fragmented, context-dependent, and temporally distant information. To address this, we introduce the Long-horizon Memory Embedding Benchmark (LMEB), a comprehensive framework that evaluates embedding models' capabilities in handling complex, long-horizon memory retrieval tasks. LMEB spans 22 datasets and 193 zero-shot retrieval tasks across 4 memory types: episodic, dialogue, semantic, and procedural, with both AI-generated and human-annotated data. These memory types differ in terms of level of abstraction and temporal dependency, capturing distinct aspects of memory retrieval that reflect the diverse challenges of the real world. We evaluate 15 widely used embedding models, ranging from hundreds of millions to ten billion parameters. The results reveal that (1) LMEB provides a reasonable level of difficulty; (2) Larger models do not always perform better; (3) LMEB and MTEB exhibit orthogonality. This suggests that the field has yet to converge on a universal model capable of excelling across all memory retrieval tasks, and that performance in traditional passage retrieval may not generalize to long-horizon memory retrieval. In summary, by providing a standardized and reproducible evaluation framework, LMEB fills a crucial gap in memory embedding evaluation, driving further advancements in text embedding for handling long-term, context-dependent memory retrieval. LMEB is available at https://github.com/KaLM-Embedding/LMEB.

---

## 论文详细总结（自动生成）

### 论文分析报告：LMEB (Long-horizon Memory Embedding Benchmark)

#### 1. 核心问题与整体含义
*   **研究动机**：随着智能体（Agent）和记忆增强系统（如 OpenClaw）的发展，模型对长期、碎片化且具有强上下文依赖信息的检索能力变得至关重要。
*   **核心痛点**：现有的文本嵌入基准（如 MTEB、BEIR）主要侧重于传统的段落检索（Passage Retrieval），即针对组织良好的静态信息进行匹配。这些基准无法有效评估模型在处理**长跨度（Long-horizon）**、**碎片化**、**时间相关**以及**高度依赖上下文**的记忆检索任务中的表现。
*   **整体含义**：论文提出了 LMEB 基准，旨在填补记忆嵌入评估领域的空白，为开发能够处理复杂长期记忆的嵌入模型提供标准化的“度量衡”。

#### 2. 方法论
*   **核心思想**：将记忆检索划分为四个维度，并基于“抽象程度”和“时间依赖性”两个关键特征进行分类。
*   **记忆分类体系**：
    1.  **情景记忆 (Episodic)**：低抽象、高时间依赖。关注特定时间、地点发生的事件序列。
    2.  **对话记忆 (Dialogue)**：高抽象、高时间依赖。关注多轮交互中的上下文、用户偏好和历史轮次。
    3.  **语义记忆 (Semantic)**：低抽象、低时间依赖。关注稳定的通用知识和事实。
    4.  **程序记忆 (Procedural)**：高抽象、低时间依赖。关注技能、动作序列和工具调用逻辑。
*   **关键技术细节**：
    *   **统一格式**：将 22 个来源各异的数据集转换为统一的 IR（信息检索）风格格式（Queries, Corpus, Qrels, Candidates）。
    *   **时间锚定**：针对含有相对时间描述（如“两天前”）的查询，在文本中附加显式的查询时间锚点以消除歧义。
    *   **元数据编码**：在 Corpus 的标题或文本中保留时间戳和会话层级元数据，支持时间敏感型检索。
    *   **检索约束**：引入 `candidates` 文件，模拟现实中受限的记忆池（如仅在特定对话历史中检索），而非全库检索。

#### 3. 实验设计
*   **数据集与场景**：涵盖 22 个数据集，共计 193 个零样本（Zero-shot）检索任务。
    *   **情景类**：EPBench, KnowMeBench。
    *   **对话类**：LoCoMo, LongMemEval, REALTALK, TMD, MemBench, ConvoMem。
    *   **语义类**：QASPER, NovelQA, PeerQA, SciFact 等。
    *   **程序类**：Gorilla, ToolBench, ReMe, MemGovern 等。
*   **对比方法**：评估了 15 个主流嵌入模型，参数量跨度从 300M 到 12B，包括：
    *   **大参数模型**：KaLM-Embedding-Gemma3 (12B), bge-multilingual-gemma2 (9B), Qwen3-Embedding (8B/4B), NV-Embed-v2 (7B), e5-mistral-7b。
    *   **小参数模型**：jina-v5 (Nano/Small), bge-m3, multilingual-e5-large, EmbeddingGemma-300M 等。
*   **实验设置**：分为“无指令 (w/o inst.)”和“有指令 (w/ inst.)”两种模式，主要评估指标为 NDCG@10 和 Recall@10。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**评估过程所使用的具体 GPU 型号、数量或总推理时长。
*   **模型来源**：所有评估模型均来自公开的 Checkpoints（如 Hugging Face），重点在于基准测试的构建与模型性能的横向对比，而非模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖 4 大记忆类型、22 个数据集、193 个任务。
    *   对 15 个模型在两种指令设置下进行了全面测试。
    *   进行了数据集多样性分析（加权 Jaccard 相似度）和与 MTEB 的相关性分析。
*   **充分性与公平性**：实验设计较为充分。通过统一输入长度（1024 tokens）、标准化的 IR 指标以及零样本设置，确保了不同架构模型之间的对比是客观且公平的。

#### 6. 主要结论与发现
1.  **难度合理**：顶尖模型的平均得分在 61 分左右，说明 LMEB 既有挑战性，又不是不可逾越，具有良好的区分度。
2.  **规模法则不绝对**：较大的模型（如 12B）在某些记忆任务上表现不如较小的模型（如 300M），表明模型架构、训练数据分布对记忆检索的影响大于参数量。
3.  **正交性发现**：LMEB 与传统基准 MTEB 的相关系数接近 0。这意味着在传统段落检索上表现优异的模型，在长跨度记忆检索上可能表现糟糕，证明了 LMEB 评估的是一种独立的能力维度。
4.  **指令敏感性**：不同模型对任务指令的反应迥异，部分模型（如 NV-Embed-v2）对指令不敏感，而另一些模型则高度依赖指令来提升性能。

#### 7. 优点
*   **填补空白**：首次系统性地定义并构建了针对“长跨度记忆”的嵌入评估基准。
*   **分类科学**：借鉴心理学记忆分类，将复杂的检索任务结构化，有助于诊断模型的具体短板。
*   **实用性强**：兼容 MTEB 框架，提供开源工具包，极大地方便了后续研究者的集成与复现。
*   **真实性**：结合了 AI 生成数据和人类真实标注数据（如 REALTALK 的 21 天真实对话），兼顾了规模与真实性。

#### 8. 不足与局限
*   **语言覆盖**：目前仅限于英文数据集，缺乏对多语言长跨度记忆能力的评估。
*   **长度限制**：实验中将输入截断为 1024 tokens，对于某些极长上下文的记忆任务，可能无法完全体现嵌入模型处理超长依赖的能力。
*   **合成数据偏差**：部分数据集（如 EPBench）完全由 AI 生成，可能存在与真实人类记忆分布不一致的风险。
*   **应用限制**：主要关注文本嵌入，尚未涉及多模态（如视觉记忆）的长跨度检索。

（完）
