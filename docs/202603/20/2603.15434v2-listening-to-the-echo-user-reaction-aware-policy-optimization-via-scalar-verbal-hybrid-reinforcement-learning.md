---
title: "Listening to the Echo: User-Reaction Aware Policy Optimization via Scalar-Verbal Hybrid Reinforcement Learning"
title_zh: 倾听回响：通过标量-语言混合强化学习实现用户反应感知的策略优化
authors: "Jing Ye, Xinpei Zhao, Lu Xiang, Yaping Zhang, Chengqing Zong"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15434v2"
tags: ["query:llm-ac"]
score: 9.0
evidence: 情感支持对话系统与积极情感转变
tldr: 针对情感支持对话系统中专家定义奖励信号稀疏且缺乏解释性的问题，本文提出RAPO框架。该框架通过模拟用户反应产生稠密的自然语言反馈，利用事后对话选择、生成式事后反馈以及标量-言语混合策略优化，将用户的情绪转变作为核心学习信号。实验证明，RAPO在提升交互质量和促进用户积极情绪转变方面显著优于现有强化学习基准。
selection_source: fresh_fetch
motivation: 现有的情感支持系统依赖稀疏的专家标量奖励，难以解释响应失败原因或适应动态用户状态。
method: 提出RAPO框架，通过事后对话选择关键轮次，并结合生成式反馈与标量-言语混合强化学习进行细粒度语义优化。
result: 在ESC和Sotopia数据集上的实验表明，RAPO在驱动积极交互结果方面显著超越了强强化学习基准。
conclusion: 通过利用用户反应作为直接反馈信号，标量与言语结合的混合优化能更有效地提升对话系统的交互质量和情感支持能力。
---

## 摘要
尽管目前的情感支持对话系统通常依赖专家定义的标量奖励进行对齐，但这些信号存在严重的信息稀疏性。它们无法解释回复失败的原因，也无法说明如何适应动态的用户状态，往往偏离了促进积极情感转变的实际目标。在实践中，最直接且可靠的学习信号源于用户在持续交互过程中的连续反应。因此，我们提出了反应感知策略优化（RAPO），这是一个针对交互结果而非评分标准进行优化的框架。RAPO 将对话视为一个反应驱动的过程，并利用模拟的用户回复通过三个核心组件生成密集自然语言反馈：事后对话选择（Hindsight Dialogue Selection），用于分离出能显著改变用户情感轨迹的关键轮次；生成式事后反馈（Generative Hindsight Feedback），将用户反应转化为对比排序信号和自然语言评论；以及标量-语言混合策略优化（Scalar-Verbal Hybrid Policy Optimization），它将用于全局对齐的标量奖励优化与用于细粒度语义细化的语言反馈蒸馏相结合。在 ESC 和 Sotopia 上的广泛实验表明，RAPO 在推动积极交互结果方面显著优于强大的强化学习基准模型。

## Abstract
While current emotional support dialogue systems typically rely on expert-defined scalar rewards for alignment, these signals suffer from severe information sparsity. They cannot explain why a response failed or how to adapt to dynamic user states, often diverging from the actual goal of facilitating positive emotional shifts. In practice, the most direct and reliable learning signal emerges from the user's continuous reactions during ongoing interaction. We therefore propose Reaction Aware Policy Optimization (RAPO), a framework that optimizes over interaction consequences rather than rubric scores. RAPO treats dialogue as a reaction-driven process and utilizes simulated user responses to generate dense natural-language feedback through three core components: Hindsight Dialogue Selection, which isolates pivotal turns that meaningfully alter user emotional trajectories; Generative Hindsight Feedback, which transforms user reactions into contrastive ranking signals and natural-language critiques; and Scalar-Verbal Hybrid Policy Optimization, which couples scalar reward optimization for global alignment with verbal feedback distillation for fine-grained semantic refinement. Extensive experiments on ESC and Sotopia demonstrate that RAPO significantly outperforms strong reinforcement learning baselines in driving positive interaction outcomes.

---

## 论文详细总结（自动生成）

以下是对论文《Listening to the Echo: User-Reaction Aware Policy Optimization via Scalar-Verbal Hybrid Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与整体含义
论文聚焦于**情感支持对话系统（ESC）**的优化。当前主流方法依赖专家定义的静态标量奖励（如根据准则打分）进行强化学习对齐，但这存在两大缺陷：
*   **优化错配（Hallucinated Empathy）**：模型可能为了迎合专家评分标准而生成看似完美但对用户而言空洞、机械的回复（如重复“我很抱歉”），无法真正引发用户的情感转变。
*   **信号稀疏与歧义**：单一的数字评分无法解释回复为何失败，也无法指导模型如何根据用户动态状态进行细粒度调整。
**核心动机**：作者主张将学习信号从“专家准则”转向“**用户反应**”。通过模拟用户在交互中的实时反馈，将对话视为一个反应驱动的过程，从而实现更精准的情感对齐。

### 2. 方法论：RAPO 框架
RAPO（反应感知策略优化）通过以下三个核心组件实现：
*   **事后对话选择（Hindsight Dialogue Selection, HDS）**：利用 GPT-4o 识别并筛选出对话中真正影响用户情感轨迹或对话走向的“关键轮次”，避免模型在大量低信息量的模板化回复上过度拟合。
*   **生成式事后反馈（Generative Hindsight Feedback, GHF）**：
    1.  **用户模拟**：针对模型生成的多个候选回复，利用用户模拟器生成相应的用户反应。
    2.  **对比评论生成**：生成式奖励模型（GRM）同时观察多个交互轨迹，生成归一化的排序分数和自然语言评论（Critique），解释优劣原因。
*   **标量-语言混合策略优化（Scalar-Verbal Hybrid Policy Optimization）**：
    1.  **标量强化学习**：采用 GRPO 算法，利用排序后的标量奖励进行全局策略对齐。
    2.  **语言强化学习（自蒸馏）**：将自然语言评论作为输入，通过**在线自蒸馏（On-Policy Self-Distillation）**，让模型学习如何根据反馈修正错误。
    3.  **统一目标函数**：结合了宏观的策略梯度（标量）和微观的令牌级修正（语言），使模型同时掌握“做什么（策略）”和“怎么说（表达）”。

### 3. 实验设计
*   **数据集与场景**：
    *   **ESConv**：标准情感支持对话数据集。
    *   **EmoHarbor**：评估个性化情感支持的交互式基准。
    *   **ESC-Eval**：评估情感支持技能的多维度基准。
    *   **SOTOPIA**：复杂的开放域多智能体社会智能交互环境（用于验证通用性）。
*   **对比方法（Baselines）**：
    *   **闭源模型**：GPT-4o, GPT-5, Doubao-Pro, Claude-3.5-Sonnet。
    *   **开源/领域模型**：SoulChat 2.0, PsyChat。
    *   **强化学习基准**：SFT, GRPO, CPO, PPO, RLOO 等。

### 4. 资源与算力
*   **硬件**：使用了 **8 × NVIDIA A100 GPU** 集群。
*   **训练时长**：
    *   ESC 任务训练约 **13 小时**（相比之下，不含模拟的 CPO 约 9 小时）。
    *   SOTOPIA 环境训练约 **27 小时**。
*   **模型底座**：主要基于 Qwen-2.5-7B-Instruct, Qwen-3-8B 和 Llama-3-8B。

### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **主实验**：在三个 ESC 基准和一个社会智能基准上验证了 RAPO 的领先地位。
*   **消融实验**：分别验证了用户反应建模（URM）和自蒸馏（SD）的必要性，结果显示两者具有协同效应。
*   **人类评估**：通过 50 组随机样本的双盲成对比较，验证了自动评分与人类偏好的一致性。
*   **稳定性分析**：展示了训练过程中策略熵、奖励分数和回复长度的变化曲线，证明了算法的收敛性和稳定性。
实验设计客观、公平，涵盖了从自动指标到交互式评估的完整链路。

### 6. 主要结论与发现
*   **用户反应是更优信号**：实时用户反应比预定义的专家准则包含更稠密、更具指导性的信息。
*   **混合优化效果显著**：结合标量奖励和语言反馈能有效解决奖励歧义，RAPO 在 EmoHarbor 上比 GRPO 提升了 12.4%。
*   **通用性强**：RAPO 不仅在情感支持任务中表现优异，在 SOTOPIA 这种复杂的社会博弈任务中也超越了 GPT-4o 和多种强 RL 基准。
*   **数据质量胜过数量**：通过 HDS 选择的关键轮次进行训练，效果优于全量数据训练。

### 7. 优点
*   **范式创新**：从“匹配准则”转向“驱动反应”，更符合对话交互的本质。
*   **闭环修正机制**：通过自蒸馏将自然语言反馈转化为令牌级的梯度修正，解决了标量奖励“知其然不知其所以然”的问题。
*   **评估全面**：不仅关注静态指标（BLEU/BERTScore），更强调在动态模拟环境中的交互表现。

### 8. 不足与局限
*   **计算开销较大**：由于需要在线调用 LLM 模拟用户反应并生成评论，训练时间显著长于传统 RL 方法。
*   **依赖模拟器质量**：系统的性能上限受限于用户模拟器和奖励模型的准确性，若模拟器存在偏差，可能导致策略漂移。
*   **领域局限**：目前主要在对话类任务中验证，在更广泛的顺序决策任务（如机器人控制）中的适用性尚待探索。

（完）
