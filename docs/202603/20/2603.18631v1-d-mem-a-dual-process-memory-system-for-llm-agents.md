---
title: "D-Mem: A Dual-Process Memory System for LLM Agents"
title_zh: D-Mem：大语言模型智能体的双过程记忆系统
authors: "Zhixing You, Jiachen Yuan, Jason Cai"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18631v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 用于长程推理的双过程记忆系统
tldr: 针对自主智能体在长程推理中对高保真内存的需求，本文提出D-Mem双过程内存系统。传统基于检索的内存框架因有损抽象常丢失关键细节，D-Mem通过保留轻量级向量检索处理常规查询，并引入详尽的“全审议”模块作为高保真回退方案。利用多维质量门控策略，系统能动态平衡效率与精度。实验证明，该方法在显著降低计算成本的同时，性能接近全量处理水平，有效解决了细粒度上下文理解难题。
selection_source: fresh_fetch
motivation: 现有的增量检索式内存框架由于采用有损抽象，往往会丢失关键的细粒度上下文信息，难以应对复杂查询。
method: 提出一种双过程架构，结合轻量级向量检索与详尽的“全审议”回退模块，并采用多维质量门控策略进行动态调度。
result: "在LoCoMo基准测试中，D-Mem以极低的计算成本实现了接近全审议模块96.7%的性能，显著优于传统的静态检索基准。"
conclusion: D-Mem通过协同快速检索与深度审议，在保证认知经济性的同时提升了智能体处理长程复杂任务的记忆可靠性。
---

## 摘要
随着持久化、自适应自主智能体的发展，为这些系统配备高保真记忆访问能力以进行长程推理已成为一项关键需求。然而，目前主流的基于检索的记忆框架通常遵循增量处理范式，即不断提取对话记忆并更新至向量数据库，在查询时依赖语义检索。虽然这种方法速度较快，但其本质上依赖于有损抽象，经常遗漏上下文中的关键信息，且难以处理依赖细粒度上下文理解的查询。为此，我们提出了 D-Mem，一种双过程记忆系统。它保留了用于常规查询的轻量级向量检索，同时建立了一个详尽的全量审议（Full Deliberation）模块作为高保真后备方案。为了在不牺牲准确性的情况下实现认知经济性，D-Mem 采用了一种多维质量门控策略来动态衔接这两个过程。在 LoCoMo 和 RealTalk 基准测试上使用 GPT-4o-mini 和 Qwen3-235B-Instruct 进行的实验证明了我们方法的有效性。值得注意的是，我们的多维质量门控策略在 LoCoMo 基准上配合 GPT-4o-mini 达到了 53.5 的 F1 分数。这优于我们的静态检索基准 Mem0$^* (51.2)，并恢复了全量审议模块 96.7% 的性能 (55.3)，同时计算成本显著降低。

## Abstract
Driven by the development of persistent, self-adapting autonomous agents, equipping these systems with high-fidelity memory access for long-horizon reasoning has emerged as a critical requirement. However, prevalent retrieval-based memory frameworks often follow an incremental processing paradigm that continuously extracts and updates conversational memories into vector databases, relying on semantic retrieval when queried. While this approach is fast, it inherently relies on lossy abstraction, frequently missing contextually critical information and struggling to resolve queries that rely on fine-grained contextual understanding. To address this, we introduce D-Mem, a dual-process memory system. It retains lightweight vector retrieval for routine queries while establishing an exhaustive Full Deliberation module as a high-fidelity fallback. To achieve cognitive economy without sacrificing accuracy, D-Mem employs a Multi-dimensional Quality Gating policy to dynamically bridge these two processes. Experiments on the LoCoMo and RealTalk benchmarks using GPT-4o-mini and Qwen3-235B-Instruct demonstrate the efficacy of our approach. Notably, our Multi-dimensional Quality Gating policy achieves an F1 score of 53.5 on LoCoMo with GPT-4o-mini. This outperforms our static retrieval baseline, Mem0$^\ast$ (51.2), and recovers 96.7\% of the Full Deliberation's performance (55.3), while incurring significantly lower computational costs.

---

## 论文详细总结（自动生成）

### D-Mem: 大语言模型智能体的双过程记忆系统论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）自主智能体在长程对话中面临的**记忆失效**问题展开研究。现有的记忆框架（如 Mem0、Zep）多采用“增量处理范式”，将对话压缩为语义片段存入向量数据库。
*   **核心痛点：** 这种“查询无关”的压缩会导致**有损抽象（Lossy Abstraction）**，丢失细粒度的上下文（如隐含的时间逻辑、多跳依赖关系）。
*   **研究动机：** 借鉴人类认知的“双过程理论”（系统 1 与系统 2），开发一种既能保持检索效率，又能通过深度审议恢复丢失细节的记忆系统。

#### 2. 论文提出的方法论：D-Mem 架构
D-Mem 由三个协同组件构成，模拟人类从快速直觉到深度思考的切换：
*   **系统 1 (Mem0*)：轻量级检索基础。** 在 Mem0 基础上改进，通过向量数据库进行 Top-K 语义检索。改进点包括在提取阶段引入 Top-10 相似记忆作为背景，并增加余弦相似度阈值（>0.8）过滤冗余信息。
*   **多维质量门控 (Quality Gating)：动态路由。** 这是一个元认知评估步骤，通过 LLM 评估系统 1 生成的初步答案。评估维度包括：**相关性 (Relevance)**、**忠实度与一致性 (Faithfulness & Consistency)**、**完整性 (Completeness)**。若未通过任一维度，则触发系统 2。
*   **系统 2 (Full Deliberation)：全量审议回退。** 绕过压缩记忆，直接处理原始对话历史。
    *   **分块提取：** 将历史分为每 60 条消息一组的块，由 LLM 提取与查询相关的细节并打分（0-10）。
    *   **多级过滤：** 仅保留高分事实（>6分），并由 LLM 进行二次精炼。
    *   **答案生成：** 基于高保真事实生成最终回答。

#### 3. 实验设计
*   **数据集：**
    *   **LoCoMo：** 包含长达 2.4 万 Token 的长程对话，涵盖单跳、多跳、时间推理和开放域问题。
    *   **RealTalk：** 真实的 21 天长程对话数据集，平均每篇对话超过 1.6 万词。
*   **对比方法（Baselines）：** 包括 Full Context（全上下文）、标准 RAG、LangMem、Mem0、Zep、Nemori、E-mem 等。
*   **评估指标：** F1 Score（词汇重叠）、BLEU（n-gram 忠实度）、LLM-as-a-Judge（语义等价性评分）。

#### 4. 资源与算力
*   **硬件环境：** 实验在一台联想笔记本电脑上完成，配置为 **NVIDIA GeForce RTX 4070 Laptop GPU (8GB VRAM)** 和 Intel Core i9-13900HX CPU。
*   **模型调用：** 使用了 GPT-4o-mini 和 Qwen3-235B-Instruct 的 API。
*   **计算规模：** 整个评估过程在 24 小时内通过并行 API 调用完成，总计消耗约 **4 亿个 Token**。向量数据库使用本地 Qdrant 实例。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流长对话基准上进行了详尽测试，涵盖了 1540 个（LoCoMo）和 728 个（RealTalk）问题。
*   **消融与对比：** 针对门控策略对比了“多数投票”、“共识机制”和“质量门控”三种策略。
*   **模型泛化：** 在 OpenAI 和 Qwen 两个不同架构、不同规模的模型上验证了有效性。
*   **客观性：** 实验对比了多种最新的记忆框架，并提供了详细的分类性能分析（如单跳 vs 多跳），实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **性能跨越：** D-Mem 的“全量审议”模块建立了新的性能上限，显著优于全上下文输入（解决了“Lost-in-the-Middle”问题）。
*   **效率平衡：** 质量门控策略在 LoCoMo 上仅消耗全量审议 **35.8%** 的 Token，却恢复了其 **96.7%** 的 F1 性能。
*   **难度相关性：** 门控机制在处理复杂问题（如时间推理、多跳推理）时增益最明显，因为这些场景受“有损抽象”影响最深。
*   **模型差异：** 发现 Qwen3-235B 在门控评估中比 GPT-4o-mini 更严格，导致更高的回退触发率。

#### 7. 优点
*   **认知经济性：** 成功实现了“按需分配算力”，常规问题快处理，复杂问题深处理。
*   **高保真度：** 通过查询引导的原始文本扫描，克服了向量检索中常见的相对时间计算错误（如“昨天”的逻辑解析）。
*   **模块化：** 质量门控作为一个独立组件，可以轻松集成到现有的 Agent 架构中。

#### 8. 不足与局限
*   **扩展性瓶颈：** 尽管有门控，但“全量审议”本质上仍需线性扫描原始历史，对于“无限长度”或超大规模历史的扩展性受限。
*   **缺乏全局状态跟踪：** 系统 2 采用分块处理，虽然有滑动窗口，但仍缺乏跨块的显式逻辑链合成，可能在极长程的因果推理中存在盲点。
*   **成本风险：** 在极端复杂的对话环境下，若门控频繁触发回退，API 调用成本依然较高。

（完）
