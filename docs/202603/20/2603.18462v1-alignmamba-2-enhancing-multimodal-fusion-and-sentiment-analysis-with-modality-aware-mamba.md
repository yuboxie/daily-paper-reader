---
title: "AlignMamba-2: Enhancing Multimodal Fusion and Sentiment Analysis with Modality-Aware Mamba"
title_zh: AlignMamba-2：利用模态感知 Mamba 增强多模态融合与情感分析
authors: "Yan Li, Yifei Xing, Xiangyuan Lan, Xin Li, Haifeng Chen, Dongmei Jiang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18462v1"
tags: ["query:llm-ac"]
score: 9.0
evidence: 用于情感计算和情感分析的高效框架
tldr: AlignMamba-2 针对多模态情感分析中 Transformer 计算复杂度高和 Mamba 难以捕捉全局非序列关系的问题，提出了一种高效融合框架。该框架通过双重对齐策略增强模态一致性，并设计了包含模态特定与共享专家的混合专家架构 Mamba 层以处理异构性。实验证明其在动态序列和静态图像任务上均达到了 SOTA 水平。
selection_source: fresh_fetch
motivation: 旨在解决 Transformer 处理长序列时的计算瓶颈，以及 Mamba 在多模态全局对齐和异构性处理上的不足。
method: 引入了基于最优传输和最大均值差异的双重对齐策略，并构建了模态感知的混合专家 Mamba 层。
result: 在 CMU-MOSI 和 CMU-MOSEI 等多个动态与静态多模态基准测试中，该模型在性能和效率上均优于现有方法。
conclusion: AlignMamba-2 成功将高效的 Mamba 架构扩展到多模态领域，为情感分析提供了一个兼顾性能与效率的新方案。
---

## 摘要
在大规模预训练模型时代，将通用知识有效地迁移到特定的情感计算任务中仍然是一个挑战，特别是在计算效率和多模态异构性方面。虽然基于 Transformer 的方法在建模模态间依赖关系方面表现出色，但其二次方计算复杂度限制了它们在长序列数据中的应用。基于 Mamba 的模型作为一种计算效率高的替代方案应运而生；然而，其固有的序列扫描机制难以捕捉对于有效跨模态对齐至关重要的全局、非序列关系。为了解决这些局限性，我们提出了 AlignMamba-2，这是一个用于多模态融合和情感分析的高效且有效的框架。我们的方法引入了一种双重对齐策略，利用最优传输（Optimal Transport）距离和最大均值差异（Maximum Mean Discrepancy）对模型进行正则化，在不增加推理时间开销的情况下，促进模态间的几何和统计一致性。更重要的是，我们设计了一个模态感知 Mamba 层，该层采用混合专家（Mixture-of-Experts）架构，包含模态特定专家和模态共享专家，以在融合过程中显式处理数据异构性。在四个具有挑战性的基准测试上进行的广泛实验，包括动态时间序列（CMU-MOSI 和 CMU-MOSEI 数据集）和静态图像相关任务（NYU-Depth V2 和 MVSA-Single 数据集），证明了 AlignMamba-2 在从动态时间序列分析到静态图文分类的各种模式识别任务中，在有效性和效率方面都达到了新的最先进水平（SOTA）。

## Abstract
In the era of large-scale pre-trained models, effectively adapting general knowledge to specific affective computing tasks remains a challenge, particularly regarding computational efficiency and multimodal heterogeneity. While Transformer-based methods have excelled at modeling inter-modal dependencies, their quadratic computational complexity limits their use with long-sequence data. Mamba-based models have emerged as a computationally efficient alternative; however, their inherent sequential scanning mechanism struggles to capture the global, non-sequential relationships that are crucial for effective cross-modal alignment. To address these limitations, we propose \textbf{AlignMamba-2}, an effective and efficient framework for multimodal fusion and sentiment analysis. Our approach introduces a dual alignment strategy that regularizes the model using both Optimal Transport distance and Maximum Mean Discrepancy, promoting geometric and statistical consistency between modalities without incurring any inference-time overhead. More importantly, we design a Modality-Aware Mamba layer, which employs a Mixture-of-Experts architecture with modality-specific and modality-shared experts to explicitly handle data heterogeneity during the fusion process. Extensive experiments on four challenging benchmarks, including dynamic time-series (on the CMU-MOSI and CMU-MOSEI datasets) and static image-related tasks (on the NYU-Depth V2 and MVSA-Single datasets), demonstrate that AlignMamba-2 establishes a new state-of-the-art in both effectiveness and efficiency across diverse pattern recognition tasks, ranging from dynamic time-series analysis to static image-text classification.

---

## 论文详细总结（自动生成）

### AlignMamba-2：利用模态感知 Mamba 增强多模态融合与情感分析

#### 1. 核心问题与整体含义（研究动机和背景）
在多模态情感计算领域，现有的 Transformer 架构虽然擅长建模模态间依赖，但其二次方计算复杂度限制了在长序列数据上的应用。新兴的 Mamba 架构（基于状态空间模型 SSM）虽具备线性复杂度，但存在两大缺陷：
*   **扫描机制局限性**：其单向序列扫描难以捕捉跨模态的全局、非序列化对应关系。
*   **模态不可知性**：现有 Mamba 融合方法通常将不同模态视为同质序列，忽略了数据间的异构性（如音频、视觉、文本的统计分布差异）。
**AlignMamba-2** 旨在通过预融合对齐和模态感知架构，在保持高效计算的同时解决上述对齐与异构性问题。

#### 2. 方法论：核心思想与关键技术
该框架主要由三个阶段组成：
*   **单模态编码与初步建模**：使用单模态编码器提取特征，并通过浅层 Mamba 块捕捉内部上下文。
*   **双重对齐策略（训练期正则化）**：
    *   **局部几何对齐（OT 距离）**：利用最优传输（Optimal Transport）距离，通过 Sinkhorn 算法计算 token 级的最小变换成本，强制模态间的几何一致性。
    *   **全局统计对齐（MMD）**：利用最大均值差异（Maximum Mean Discrepancy）匹配不同模态在高维再生核希尔伯特空间（RKHS）中的统计矩，确保全局分布一致。
    *   *注：对齐仅作为训练损失，推理时无额外计算开销。*
*   **模态感知 Mamba 融合层**：
    *   引入 **混合专家（MoE）** 架构替换 Mamba 的标准线性投影层。
    *   设计了**模态特定专家**（处理各模态独特属性）和**模态共享专家**（捕捉跨模态通用模式）。
    *   采用**确定性路由**：根据 token 的原始模态标签直接分发，避免了复杂且不稳定的学习化路由。

#### 3. 实验设计
*   **数据集**：
    *   **动态时间序列**：CMU-MOSI 和 CMU-MOSEI（多模态情感分析）。
    *   **静态图像任务**：NYU-Depth V2（RGB-D 场景识别）和 MVSA-Single（图文情感分类）。
*   **Benchmark 与对比方法**：
    *   对比了 10 余种 Transformer 基础模型（如 MulT, MISA, Self-MM, GeminiFusion 等）。
    *   对比了最新的 Mamba 基础模型（如 Cobra, Sigma, AlignMamba-1 等）。

#### 4. 资源与算力
*   **软件环境**：基于 PyTorch 实现，使用 Adam 优化器，对齐部分使用了 GeomLoss 库。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但在效率分析中展示了模型在处理 10k 到 100k 长度序列时的内存占用和推理延迟，证明其在长序列下的显存利用率远优于 Transformer。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同性质的基准数据集上进行了测试，涵盖了回归、二分类和多分类任务。
*   **消融实验**：系统验证了“双重对齐损失”、“MoE 机制”以及“确定性路由 vs 学习化路由”的贡献。
*   **扩展性分析**：包括跨数据集泛化实验（MOSI 与 MOSEI 互测）、超参数敏感性分析（对齐权重 $\lambda$）以及定性案例研究（Case Study）。
*   **评价**：实验设计全面，对比基准涵盖了从 2019 年到 2025 年的主流 SOTA 方法，通过长序列压力测试验证了效率优势，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：AlignMamba-2 在所有四个数据集上均达到了 SOTA 水平，尤其在处理复杂的动态序列时表现优异。
*   **效率卓越**：相比 Transformer，该模型在长序列（如 100k tokens）下保持了线性的显存增长和推理延迟，且解决了 AlignMamba-1 在推理时计算 OT 矩阵导致的 OOM 问题。
*   **对齐的重要性**：实验证明，预融合的对齐正则化是提升 Mamba 融合效果的关键，弥补了其扫描机制在全局建模上的不足。
*   **模态感知的有效性**：MoE 结构能有效分离模态特有信息与共享信息，提升了模型的泛化能力。

#### 7. 优点
*   **推理零成本对齐**：通过将 OT 和 MMD 转化为训练损失，在不增加部署负担的前提下提升了模型精度。
*   **架构创新**：将 MoE 引入 Mamba 投影层，并结合确定性路由，巧妙解决了多模态异构性问题。
*   **高扩展性**：模型不仅适用于时间序列，也能有效处理静态图像和文本的融合。

#### 8. 不足与局限
*   **训练开销**：虽然推理高效，但在训练阶段计算 OT 距离和 MMD 损失会引入额外的计算负担和内存消耗。
*   **超参数依赖**：双重对齐损失的权重（$\lambda_{OT}, \lambda_{MMD}$）对性能有一定影响，需要针对不同任务进行精细调优。
*   **模态数量限制**：实验主要集中在二模态或三模态融合，对于更多模态（如 5 种以上）的扩展效果及专家负载均衡尚待验证。

（完）
