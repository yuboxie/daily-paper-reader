---
title: "STRUCTUREDAGENT: Planning with AND/OR Trees for Long-Horizon Web Tasks"
title_zh: STRUCTUREDAGENT：基于与或树的长程网络任务规划
authors: "ELita Lobo, Xu Chen, Jingjing Meng, Nan Xi, Yang Jiao, Chirag Agarwal, Yair Zick, Yan Gao"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05294v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 针对长程任务的分层规划和结构化记忆
tldr: 针对大语言模型智能体在处理长程 Web 任务时存在的规划能力弱、记忆受限及贪婪行为等问题，本文提出 STRUCTUREDAGENT 框架。该框架结合动态 AND/OR 树的在线分层规划器与结构化记忆模块，实现了高效搜索与约束满足。实验证明，该方法在 WebVoyager 和 WebArena 等基准测试中显著提升了长程任务性能，并提供了可解释的规划过程。
selection_source: fresh_fetch
motivation: 现有 Web 智能体在复杂长程任务中因缺乏有效规划和历史追踪能力，容易陷入局部最优或提前终止。
method: 提出一种基于动态 AND/OR 树的分层规划框架，并结合结构化记忆模块来维护候选方案和约束条件。
result: 在 WebVoyager、WebArena 及购物基准测试中，STRUCTUREDAGENT 的表现均优于标准的基于 LLM 的智能体。
conclusion: 通过分层规划和结构化记忆，可以有效提升 LLM 智能体在复杂 Web 环境下的长程决策能力和可解释性。
---

## 摘要
大语言模型（LLMs）的最新进展使得用于顺序决策的智能体系统成为可能。此类智能体必须感知其环境，跨多个时间步进行推理，并采取优化长期目标的行动。然而，现有的网络智能体在处理复杂、长程任务时面临困难，原因在于用于追踪历史的上下文内存有限、规划能力薄弱，以及导致过早终止的贪婪行为。为了应对这些挑战，我们提出了 STRUCTUREDAGENT，这是一个包含两个核心组件的分层规划框架：(1) 一个使用动态与或树进行高效搜索的在线分层规划器；(2) 一个追踪并维护候选解的结构化记忆模块，用以提高信息寻求任务中的约束满足度。该框架还能生成可解释的分层计划，从而简化调试并在需要时促进人工干预。我们在 WebVoyager、WebArena 和自定义购物基准测试上的结果表明，与标准的基于 LLM 的智能体相比，STRUCTUREDAGENT 提升了在长程网络浏览任务中的性能。

## Abstract
Recent advances in large language models (LLMs) have enabled agentic systems for sequential decision-making. Such agents must perceive their environment, reason across multiple time steps, and take actions that optimize long-term objectives. However, existing web agents struggle on complex, long-horizon tasks due to limited in-context memory for tracking history, weak planning abilities, and greedy behaviors that lead to premature termination. To address these challenges, we propose STRUCTUREDAGENT, a hierarchical planning framework with two core components: (1) an online hierarchical planner that uses dynamic AND/OR trees for efficient search and (2) a structured memory module that tracks and maintains candidate solutions to improve constraint satisfaction in information-seeking tasks. The framework also produces interpretable hierarchical plans, enabling easier debugging and facilitating human intervention when needed. Our results on WebVoyager, WebArena, and custom shopping benchmarks show that STRUCTUREDAGENT improves performance on long-horizon web-browsing tasks compared to standard LLM-based agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **STRUCTUREDAGENT** 的分层规划框架，旨在解决大语言模型（LLM）智能体在处理复杂、长程（Long-Horizon）网络任务时的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 网络智能体在面对需要数十步操作的复杂任务时表现不佳。
*   **研究动机**：
    *   **上下文记忆受限**：网页内容庞大（常超过 20k tokens），智能体难以在有限的窗口内追踪复杂的历史和备选路径。
    *   **规划能力薄弱**：现有智能体多采用顺序或贪婪决策，缺乏对任务的深度分解。
    *   **容错机制缺失**：一旦某个步骤失败，智能体往往无法有效回溯或尝试替代策略。
    *   **不可解释性**：黑盒化的决策过程导致难以调试或进行人工干预。

### 2. 方法论：核心思想与技术细节
STRUCTUREDAGENT 的核心是将任务建模为动态构建的 **AND/OR 树（与或树）**，并结合**结构化记忆**。

*   **动态 AND/OR 树规划器**：
    *   **节点类型**：**AND 节点**（代表必须全部完成的子目标）、**OR 节点**（代表可选的替代策略）、**ACTION 节点**（原子级的浏览器操作）。
    *   **改进型贪婪深度优先搜索（DFS）**：不同于传统 DFS，该算法引入了三种执行状态（ENTERING, EXITING, FAILED），允许节点被多次访问、修复或剪枝。
    *   **核心操作**：由 LLM 充当高层控制器，执行节点扩展（Expansion）、节点修复（Repair）、全局树更新（Global Update）和完成度检查（Completion Check）。
*   **结构化记忆模块（Structured Memory）**：
    *   针对信息寻求类任务，建立动态表格追踪候选实体及其满足的约束条件。
    *   通过 LLM 提取任务约束，并在探索过程中不断更新表格，避免重复访问已排除的页面。
*   **人工干预支持**：由于规划过程是层级化的树状结构，人类操作员可以在任何层级介入，修正错误的子目标分解。

### 3. 实验设计
*   **基准测试（Benchmarks）**：
    1.  **Custom Complex Shopping (Amazon Easy/Hard)**：包含 60 个长程购物任务，要求满足极其复杂的自然语言约束（如特定分辨率、价格、认证等）。
    2.  **WebVoyager**：包含 129 个真实世界的网站任务。
    3.  **WebArena**：在 630 个任务上进行评估，涵盖地图、购物、Reddit 和 GitLab 等场景。
*   **对比方法**：
    *   **AgentOccam**：之前的强力基线，使用简单的动作树。
    *   **BasicClaudeAction**：带有完整历史记录的直接 LLM 调用。
    *   **SteP**：基于堆栈且包含人工编写策略的智能体。
    *   **WebarenaReplication**：官方基准的参考实现。
*   **骨干模型**：主要使用 Claude 3.5 Sonnet、Claude 3.7 和 Kimi-k2。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 训练算力（如型号或数量）。
*   由于该框架主要是一个**推理时（Inference-time）**规划框架，其核心开销在于调用 LLM API 的次数和 Token 消耗，而非模型训练。实验中使用了 Claude 和 Gemini 等商业 API。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流且具有挑战性的基准上进行了测试，总计处理了数百个复杂任务。
*   **评估维度**：采用了 LLM-as-a-Judge（GPT-4, Gemini, Kimi 等多模型交叉验证）和人工评估（针对 Amazon Hard 任务）相结合的方式。
*   **充分性**：实验涵盖了不同家族的骨干模型（Claude, Kimi），并进行了消融实验（如结构化记忆的影响），验证了框架的通用性和鲁棒性。实验设计客观，通过多模型裁判减少了单一模型评估的偏差。

### 6. 主要结论与发现
*   **性能提升**：在复杂购物任务（Amazon Hard）上，STRUCTUREDAGENT 显著优于 AgentOccam（人工评估领先约 7%-14%）。在 WebArena 综合评分中达到 52.6%，位居第一。
*   **长程任务优势**：任务越复杂、约束越多，该框架的优势越明显。
*   **模型通用性**：即使使用更强的骨干模型（如 Claude 3.7），分层规划依然能带来显著的增益，说明架构优化与模型能力提升是互补的。
*   **探索效率**：虽然 STRUCTUREDAGENT 的动作步数通常多于贪婪型智能体，但其成功率更高，因为它会系统性地探索替代方案而非轻易放弃。

### 7. 优点与亮点
*   **可解释性强**：AND/OR 树清晰地展示了智能体的意图分解，便于人类理解和纠偏。
*   **鲁棒的错误恢复**：通过“节点修复”和“错误回溯”机制，能够从执行失败中恢复，这是传统顺序智能体难以做到的。
*   **结构化记忆**：有效解决了长程任务中“信息遗忘”的问题，提高了约束满足的精确度。

### 8. 不足与局限
*   **推理成本**：由于涉及频繁的树操作和状态总结，API 调用次数较多，可能导致更高的延迟和金钱成本。
*   **简单任务的冗余**：在非常简单的短程任务中，分层规划可能引入不必要的复杂性，甚至因过度约束导致性能轻微下降。
*   **对骨干模型依赖**：如果底层 LLM 在节点扩展阶段产生严重的逻辑幻觉，整个规划树的质量仍会受限。

（完）
