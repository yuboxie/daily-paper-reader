---
title: "SarcasmMiner: A Dual-Track Post-Training Framework for Robust Audio-Visual Sarcasm Reasoning"
title_zh: SarcasmMiner：一种用于鲁棒视听讽刺推理的双轨后训练框架
authors: "Zhu Li, Yongjian Chen, Huiyuan Lai, Xiyuan Gao, Shekhar Nayak, Matt Coler"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05275v1"
tags: ["query:llm-hum"]
score: 9.0
evidence: 多模态讽刺检测与推理
tldr: 针对多模态讽刺检测中跨模态不一致性导致的推理难题，本文提出 SarcasmMiner 框架。该框架通过强化学习后训练，将讽刺检测重构为结构化推理任务。采用双轨蒸馏策略初始化模型并训练生成式奖励模型（GenRM），利用 GRPO 算法结合准确率与推理质量的双重奖励进行优化，有效减少了多模态推理中的幻觉，显著提升了检测性能。
selection_source: fresh_fetch
motivation: 旨在解决多模态讽刺检测中跨文本、音频和视觉线索的语用不一致性，以及基础模型在推理过程中容易产生幻觉的问题。
method: 提出一种基于强化学习的后训练框架，通过双轨蒸馏初始化模型并训练生成式奖励模型，采用 GRPO 算法对准确率和推理质量进行解耦优化。
result: "在 MUStARD++ 数据集上，该方法将 F1 分数从零样本的 59.83% 和有监督微调的 68.23% 提升至 70.22%。"
conclusion: 研究表明，关注推理过程的奖励建模能够显著增强多模态讽刺检测的性能及其在多模态数据上的接地能力。
---

## 摘要
多模态讽刺检测需要通过跨模态推理来解决文本、声学和视觉线索之间的语用不一致性。为了利用基础模型实现鲁棒的讽刺推理，我们提出了 SarcasmMiner，这是一个基于强化学习的后训练框架，能够抵御多模态推理中的幻觉。我们将讽刺检测重新表述为结构化推理，并采用双轨蒸馏策略：高质量的教师轨迹用于初始化学生模型，而全量轨迹则用于训练生成式奖励模型 (GenRM) 以评估推理质量。学生模型通过群体相对策略优化 (GRPO) 进行优化，并对准确性和推理质量使用解耦奖励。在 MUStARD++ 数据集上，SarcasmMiner 将 F1 分数从 59.83%（零样本）和 68.23%（有监督微调）提升至 70.22%。这些研究结果表明，推理感知的奖励建模增强了性能和多模态接地（grounding）。

## Abstract
Multimodal sarcasm detection requires resolving pragmatic incongruity across textual, acoustic, and visual cues through cross-modal reasoning. To enable robust sarcasm reasoning with foundation models, we propose SarcasmMiner, a reinforcement learning based post-training framework that resists hallucination in multimodal reasoning. We reformulate sarcasm detection as structured reasoning and adopt a dual-track distillation strategy: high-quality teacher trajectories initialize the student model, while the full set of trajectories trains a generative reward model (GenRM) to evaluate reasoning quality. The student is optimized with group relative policy optimization (GRPO) using decoupled rewards for accuracy and reasoning quality. On MUStARD++, SarcasmMiner increases F1 from 59.83% (zero-shot), 68.23% (supervised finetuning) to 70.22%. These findings suggest that reasoning-aware reward modeling enhances both performance and multimodal grounding.

---

## 论文详细总结（自动生成）

以下是对论文《SarcasmMiner: A Dual-Track Post-Training Framework for Robust Audio-Visual Sarcasm Reasoning》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态讽刺检测（Multimodal Sarcasm Detection）极具挑战性，因为它依赖于文本、音频（语调、重音）和视觉（表情、动作）之间的“语用不一致性”（如：用平淡的语气说出赞美的话）。
*   **核心问题**：现有的多模态大语言模型（MLLMs）在处理此类任务时，虽然能做出预测，但往往存在“推理幻觉”——即模型会编造不存在的声学或视觉证据来强行解释其预测结果。
*   **研究动机**：旨在通过强化学习（RL）和结构化推理，提升模型在复杂语境下的鲁棒性，并减少推理过程中的虚假证据。

### 2. 方法论
SarcasmMiner 提出了一个三阶段的后训练（Post-training）框架：
*   **阶段 1：推理流生成（Manifold Generation）**
    *   使用强大的教师模型（Qwen3-Omni-30B）对多模态输入进行随机采样，生成包含 `<think>`（推理链）和 `<answer>`（最终标签）的结构化轨迹。
*   **阶段 2：双轨数据构造与蒸馏（Dual-Track Distillation）**
    *   **Track A（高质量 SFT）**：筛选出预测正确且逻辑连贯的“黄金数据”，用于初始化学生模型（Qwen2.5-Omni-7B）。
    *   **Track B（生成式奖励模型 GenRM）**：利用包含错误预测和幻觉推理的全量轨迹，训练一个轻量级的判别模型（Qwen2.5-3B），使其能够识别推理过程是否合理。
*   **阶段 3：基于解耦奖励的 GRPO 优化**
    *   采用**群体相对策略优化（GRPO）**算法，无需额外的 Critic 网络，节省显存。
    *   **解耦奖励机制**：总奖励由三部分加权组成：
        1.  **准确率奖励 ($R_{acc}$)**：预测标签是否正确。
        2.  **格式奖励 ($R_{fmt}$)**：是否遵循指定的结构化输出格式。
        3.  **推理质量奖励 ($R_{GenRM}$)**：由阶段 2 训练的 GenRM 给出，专门惩罚推理中的幻觉。

### 3. 实验设计
*   **数据集**：使用 **MUStARD++**，这是一个包含 1,202 条语料的英语多模态讽刺检测基准，涵盖文本、语音和视频。
*   **评估指标**：
    *   **结果准确性**：Accuracy (Acc) 和 Macro-F1。
    *   **推理质量**：引入 **GAR (GenRM Acceptance Rate)**，即 GenRM 判定为有效的推理轨迹比例，作为衡量逻辑一致性和接地能力的代理指标。
*   **对比方法**：
    *   **Zero-shot 基础模型**：MiniCPM-o-2.6, Phi-4-Multimodal, Qwen2.5-Omni-7B, Qwen3-Omni-30B。
    *   **消融变体**：不同初始化策略（Greedy, Best-of-N, Diverse Sampling）以及不同 RL 配置（有无 SFT, 有无 GenRM）。

### 4. 资源与算力
*   **硬件环境**：使用了单节点、**4 张 NVIDIA H100 90GB GPU**。
*   **软件框架**：基于 `ms-swift` 训练框架和 `DeepSpeed ZeRO-3` 优化技术。
*   **训练细节**：
    *   SFT 阶段：LoRA 微调（r=8, α=32），训练 4 个 epoch。
    *   RL 阶段：GRPO 训练 2 个 epoch，学习率为 $1 \times 10^{-5}$。
    *   推理引擎：使用 `vLLM` 进行采样加速。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了主实验对比、详细的消融实验（针对三个阶段的每个关键变量）、混淆矩阵分析以及具体的案例研究（Case Study）。
*   **充分性评价**：
    *   **客观性**：通过对比 30B 教师模型和 7B 学生模型，证明了特定任务后训练的优越性。
    *   **公平性**：在消融实验中，通过调整 epoch 数量确保了不同初始化策略下的参数更新总数一致，保证了对比的公平。
    *   **局限性**：实验仅在 MUStARD++ 一个数据集上进行，虽然该数据集是领域标杆，但样本量（1,202）相对较小，可能存在过拟合特定场景的风险。

### 6. 主要结论与发现
*   **性能突破**：SarcasmMiner (7B) 的 F1 分数达到 **70.22%**，不仅远超其 7B 基座模型（59.83%），甚至超越了 30B 的教师模型（65.89%）。
*   **幻觉抑制**：引入 GenRM 奖励后，GAR 指标从 84.47% 提升至 **90.43%**，混淆矩阵显示假阳性（False Positives）显著降低，表明模型减少了对中性言论的过度解读。
*   **推理价值**：实验证明，单纯优化准确率会导致模型寻找统计捷径（Reward Hacking），而解耦的推理奖励能强制模型关注真实的视听线索。

### 7. 优点
*   **创新的奖励设计**：通过 GenRM 将“推理质量”量化为奖励信号，有效解决了 MLLM 在语用任务中的幻觉问题。
*   **高效的训练策略**：结合双轨蒸馏和 GRPO，既利用了失败案例的监督信号，又规避了 RL 训练中的显存瓶颈。
*   **可解释性强**：通过 CoT 强制模型输出推理过程，并证明了这种过程的改进直接提升了最终结果的可靠性。

### 8. 不足与局限
*   **数据集单一**：仅在 MUStARD++ 上验证，缺乏在其他语言或更大规模多模态数据集上的泛化性测试。
*   **教师模型依赖**：框架的第一步高度依赖于极高性能的教师模型（如 Qwen3-30B），若教师模型本身在某些极端语境下存在偏见，可能会传递给学生模型。
*   **实时性挑战**：虽然模型只有 7B，但由于引入了复杂的 CoT 推理步骤，在实际实时交互场景中的推理延迟可能会增加。

（完）
