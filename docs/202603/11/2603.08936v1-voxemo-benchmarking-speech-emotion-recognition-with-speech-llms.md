---
title: "VoxEmo: Benchmarking Speech Emotion Recognition with Speech LLMs"
title_zh: VoxEmo：利用语音大语言模型进行语音情感识别的基准测试
authors: "Hezhao Zhang, Huang-Cheng Chou, Shrikanth Narayanan, Thomas Hain"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08936v1"
tags: ["query:llm-ac"]
score: 9.0
evidence: 针对语音大语言模型的情感识别和副语言推理基准测试
tldr: 本研究针对语音大模型在语音情感识别中的评估难题，提出了VoxEmo基准。该基准涵盖15种语言的35个语料库，通过标准化工具包提供多复杂度提示词，并引入分布感知软标签协议和提示词集成策略，以模拟人类情感的模糊性。研究发现，尽管零样本语音大模型在硬标签准确率上略逊于监督基准，但在捕捉人类主观情感分布方面表现出独特优势，为情感智能评估提供了新视角。
selection_source: fresh_fetch
motivation: 现有语音大模型在情感识别中存在提示词敏感性及零样本随机性问题，且传统基准未能体现人类情感感知的模糊性。
method: 构建了包含35个语料库的VoxEmo基准，并设计了多级提示词工具包、分布感知软标签协议及提示词集成策略。
result: 零样本语音大模型在硬标签准确率上虽落后于监督模型，但在情感分布的对齐上展现出与人类主观感知的高度一致性。
conclusion: VoxEmo基准为语音大模型的情感理解提供了稳健的评估框架，强调了其在模拟人类复杂情感感知方面的独特潜力。
---

## 摘要
语音大语言模型（Speech LLMs）通过生成式接口在语音情感识别（SER）领域展现出巨大潜力。然而，从闭集分类向开放文本生成的转变引入了零样本随机性，使得评估对提示词（prompts）高度敏感。此外，传统的语音大语言模型基准测试忽略了人类情感固有的模糊性。因此，我们提出了 VoxEmo，这是一个为语音大语言模型设计的综合性语音情感识别基准，涵盖了 15 种语言的 35 个情感语料库。VoxEmo 提供了一个标准化的工具包，包含从直接分类到副语言推理等不同复杂程度的提示词。为了反映现实世界的感知与应用，我们引入了一种分布感知的软标签协议和一种模拟标注者分歧的提示词集成策略。实验表明，虽然零样本语音大语言模型在硬标签准确率上落后于有监督的基准模型，但它们在对齐人类主观分布方面表现出独特的优势。

## Abstract
Speech Large Language Models (LLMs) show great promise for speech emotion recognition (SER) via generative interfaces. However, shifting from closed-set classification to open text generation introduces zero-shot stochasticity, making evaluation highly sensitive to prompts. Additionally, conventional speech LLMs benchmarks overlook the inherent ambiguity of human emotion. Hence, we present VoxEmo, a comprehensive SER benchmark encompassing 35 emotion corpora across 15 languages for Speech LLMs. VoxEmo provides a standardized toolkit featuring varying prompt complexities, from direct classification to paralinguistic reasoning. To reflect real-world perception/application, we introduce a distribution-aware soft-label protocol and a prompt-ensemble strategy that emulates annotator disagreement. Experiments reveal that while zero-shot speech LLMs trail supervised baselines in hard-label accuracy, they uniquely align with human subjective distributions.

---

## 论文详细总结（自动生成）

这是一份关于论文《VoxEmo: Benchmarking Speech Emotion Recognition with Speech LLMs》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的语音情感识别（SER）通常被视为闭集分类任务，而语音大语言模型（Speech LLMs）将其转变为生成式任务。这种转变带来了**零样本随机性**（对提示词高度敏感）和**评估标准不统一**的问题。
*   **研究动机**：
    *   现有基准测试忽略了人类情感感知的**主观性与模糊性**（不同标注者对同一段语音可能有不同看法）。
    *   缺乏一个大规模、标准化的工具包来评估生成式语音模型在多语言、多场景下的情感理解能力。
    *   需要区分“表达的情感”（演讲者意图）与“感知的情感”（听者感受）之间的差异。

### 2. 方法论
*   **核心思想**：构建一个包含35个语料库的超大规模基准 VoxEmo，并开发一套标准化的提示词（Prompt）评估协议，通过“软标签”和“提示词集成”来捕捉情感的复杂性。
*   **关键技术细节**：
    *   **标准化提示词工具包**：设计了五种复杂度的提示词族，包括：直接决策（Direct）、加入ASR转录（+T）、加入声学描述（+A）、以及加入推理过程（+R）。
    *   **分布感知软标签协议**：不使用单一的硬标签，而是将多个标注者的投票转化为概率分布 $y = [n_1/N, ..., n_C/N]$，以此衡量模型输出与人类主观感知分布的对齐程度。
    *   **提示词集成策略（Prompt Ensemble）**：为了降低生成随机性，将5种不同提示词的预测结果进行投票集成，形成一个预测分布，模拟人类标注者的分歧。
    *   **元数据架构**：显式记录标签来源（感知 vs. 表达），并定义了跨域评估设置，以隔离标签集偏移和声学偏移。

### 3. 实验设计
*   **数据集与场景**：
    *   涵盖 **35个语料库**，涉及 **15种语言**（包括英语、中文、德语、波兰语、阿姆哈拉语等）。
    *   区分了“表演式（Acted）”和“自然发生（In-the-wild）”两种录制环境。
*   **基准模型（Benchmark）**：
    *   **Qwen2-Audio-7B-Instruct (Q2A)**
    *   **Audio Flamingo 3 (AF3)**
*   **对比方法**：
    *   **零样本（Zero-shot）**：使用不同提示词组合直接推理。
    *   **有监督微调（SFT）**：使用 LoRA 技术对模型进行轻量化微调。
    *   **传统 SOTA 基准**：对比 EmoBox 等传统有监督 SER 模型的性能。
*   **评估指标**：
    *   硬标签：加权准确率（WA）、未加权准确率（UA）、F1分数。
    *   软标签（分布对齐）：KL散度（KLD）、JS散度（JSD）、余弦相似度（Sim）、均方误差（MSE）。

### 4. 资源与算力
*   **微调设置**：使用了 LoRA 适配器（秩 $r=8$，$\alpha=16$），仅更新不到 0.15% 的参数。
*   **训练参数**：AdamW 优化器，学习率 $1 \times 10^{-5}$，10% 预热，bf16 精度，有效批大小为 16，训练 10 个 epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但考虑到 7B 参数规模的模型和 35 个数据集的规模，通常需要多块 A100 或 H100 级别的显卡支持。

### 5. 实验数量与充分性
*   **实验规模**：论文在 35 个数据集上进行了详尽的零样本测试，并在 5 个具有多标注者信息的数据集上进行了深入的软标签分析，同时还进行了跨语料库（Cross-corpus）的迁移实验。
*   **充分性与公平性**：
    *   实验涵盖了从 2006 年到 2025 年发布的各类语料库，样本量从几百条到几十万条不等，具有极强的代表性。
    *   通过固定解码设置（Greedy Decoding）和统一的解析规则，确保了不同模型间对比的公平性。
    *   **客观性**：作者诚实地披露了模型训练数据可能存在的重叠（Data Contamination）风险。

### 6. 主要结论与发现
*   **提示词敏感性**：提示词对性能影响巨大。Q2A 在加入声学描述（+A）时在表演类数据上表现更好，而 ASR 转录（+T）则有助于其在自然语音中的表现。
*   **硬指标 vs. 软分布**：零样本 Speech LLMs 在硬标签准确率上通常**落后于**专门的有监督模型，但在**对齐人类主观分布**方面表现出独特优势，能够捕捉到情感的模糊性。
*   **微调效果**：SFT 能显著缩小与传统基准的差距，Q2A 在 15/30 个可比数据集中达到了与传统模型相当或更好的水平。
*   **模型差异**：Q2A 在大多数任务上优于 AF3，且在模拟人类标注共识方面更有效。

### 7. 优点与亮点
*   **规模宏大**：是目前针对 Speech LLMs 最全面的情感识别基准，填补了多语言、多场景评估的空白。
*   **视角独特**：不再单纯追求“分类正确”，而是通过软标签评估模型是否具备像人类一样的“情感主观性”。
*   **工具化**：提供了标准化的评估工具包和披露清单，极大地提升了生成式 SER 研究的可重复性。

### 8. 不足与局限
*   **模型多样性有限**：仅评估了两个基于 Whisper-large-v3 编码器的 7B 模型，尚未覆盖更大规模或基于 Tokenizer 架构的其他模型。
*   **软标签数据受限**：由于许多公开数据集不提供原始标注者信息，软标签分析仅限于 5 个语料库。
*   **超参数敏感性**：SFT 仅使用了一套固定的 LoRA 配置，可能未充分挖掘某些模型（如 AF3）的潜力。
*   **细粒度分析缺失**：尚未深入探讨类别不平衡、说话人效应等底层因素对模型表现的具体影响。

（完）
