---
title: "Addressing Missing and Noisy Modalities in One Solution: Unified Modality-Quality Framework for Low-quality Multimodal Data"
title_zh: 一种方案解决缺失与噪声模态：面向低质量多模态数据的统一模态质量框架
authors: "Sijie Mai, Shiqin Han, Haifeng Hu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02695v1"
tags: ["query:llm-ac"]
score: 9.0
evidence: 多模态情感计算框架
tldr: 该研究针对现实场景中多模态数据存在的缺失和噪声等低质量问题，提出了统一模态质量（UMQ）框架。该框架将缺失和噪声视为统一的低质量问题，通过秩引导的质量评估器、跨模态质量增强器以及质量感知专家混合模块（MoE），有效提升了模型在各种低质量数据下的鲁棒性和性能，为多模态情感计算提供了更具鲁棒性的解决方案。
selection_source: fresh_fetch
motivation: 现有的多模态研究通常将模态缺失和模态噪声分开处理，缺乏一种能够同时应对多种低质量场景的统一解决方案。
method: 提出了包含秩引导质量评估器、跨模态质量增强器和质量感知专家混合模块（MoE）的UMQ框架，以统一处理和增强低质量模态表示。
result: 在多个数据集上的实验表明，UMQ在模态完整、缺失和噪声等多种设定下均一致优于现有的最先进基准模型。
conclusion: UMQ框架通过统一建模低质量模态，显著增强了多模态情感计算模型在复杂现实环境中的鲁棒性和泛化能力。
---

## 摘要
现实场景中遇到的多模态数据通常质量较低，其中噪声模态和缺失模态是严重阻碍模型性能和鲁棒性的典型形式。然而，以往的工作通常分别处理噪声模态和缺失模态。相比之下，我们共同处理缺失和噪声模态，以增强模型在低质量数据场景下的鲁棒性。我们将噪声和缺失模态视为一个统一的低质量模态问题，并提出了一个统一模态质量（UMQ）框架，以增强多模态情感计算中的低质量表示。首先，我们通过一种排序引导的训练策略，利用显式监督信号训练质量估计器，该策略通过添加排序约束来比较不同表示的相对质量，从而避免了由不准确的绝对质量标签引起的训练噪声。然后，为每个模态构建一个质量增强器，利用其他模态提供的样本特定信息以及定义的模态基准表示提供的模态特定信息来增强单模态表示的质量。最后，我们提出了一个具有特定路由机制的质量感知混合专家模块，以便更具针对性地解决多种模态质量问题。在完整、缺失和噪声模态的设置下，UMQ 在多个数据集上始终优于最先进的基准模型。

## Abstract
Multimodal data encountered in real-world scenarios are typically of low quality, with noisy modalities and missing modalities being typical forms that severely hinder model performance and robustness. However, prior works often handle noisy and missing modalities separately. In contrast, we jointly address missing and noisy modalities to enhance model robustness in low-quality data scenarios. We regard both noisy and missing modalities as a unified low-quality modality problem, and propose a unified modality-quality (UMQ) framework to enhance low-quality representations for multimodal affective computing. Firstly, we train a quality estimator with explicit supervised signals via a rank-guided training strategy that compares the relative quality of different representations by adding a ranking constraint, avoiding training noise caused by inaccurate absolute quality labels. Then, a quality enhancer for each modality is constructed, which uses the sample-specific information provided by other modalities and the modality-specific information provided by the defined modality baseline representation to enhance the quality of unimodal representations. Finally, we propose a quality-aware mixture-of-experts module with particular routing mechanism to enable multiple modality-quality problems to be addressed more specifically. UMQ consistently outperforms state-of-the-art baselines on multiple datasets under the settings of complete, missing, and noisy modalities.

---

## 论文详细总结（自动生成）

这是一份关于论文《Addressing Missing and Noisy Modalities in One Solution: Unified Modality-Quality Framework for Low-quality Multimodal Data》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的多模态情感计算（MAC）中，数据质量往往不理想，主要表现为**模态噪声**（如背景干扰、传感器误差）和**模态缺失**（如设备故障、传输丢失）。
*   **研究动机**：现有方法通常将“噪声”和“缺失”视为两个独立的问题分别处理，这限制了模型在复杂现实场景下的鲁棒性。
*   **核心思想**：论文提出将两者视为统一的“低质量模态”问题，并开发了**统一模态质量（UMQ）框架**。该框架旨在通过评估模态质量、增强低质量表示以及针对不同质量组合进行专门建模，来提升模型的整体性能。

### 2. 论文提出的方法论
UMQ 框架由三个核心组件构成：

*   **秩引导的质量评估器 (Quality Estimator)**：
    *   为每个模态训练一个评估器，输出质量得分 $\alpha_m$。
    *   **关键技术**：采用**秩引导（Rank-guided）训练策略**。由于绝对质量标签难以定义，研究者通过比较原始表示与添加噪声后的表示、以及不同预测损失下的表示，利用排序损失（Ranking Loss）来学习相对质量，避免了绝对标签带来的训练噪声。
*   **质量增强器 (Quality Enhancer)**：
    *   利用“样本特定信息”（来自其他模态）和“模态特定信息”（来自定义的模态基准表示）来修复低质量特征。
    *   **模态解耦操作**：通过正交损失、互信息约束和重构损失，将特征分解为样本特定（Sample-specific）和模态特定（Modality-specific）部分。
*   **质量感知专家混合模块 (MQ-MoE)**：
    *   针对 $|M|$ 个模态可能产生的 $2^{|M|}$ 种质量组合（高/低质量组合），设计了专门的路由机制。
    *   **约束机制**：引入平衡损失、样本方差损失和相同质量配置约束（Lsame），确保具有相同质量模式的样本被路由到相似的专家模块，从而实现针对性处理。

### 3. 实验设计
*   **数据集**：
    *   **情感分析 (MSA)**：CMU-MOSI, CMU-MOSEI, CH-SIMS。
    *   **幽默检测 (MHD)**：UR-FUNNY。
    *   **讽刺检测 (MSD)**：MUStARD。
*   **实验场景**：
    *   **完整模态**：评估基础性能。
    *   **缺失模态**：设置缺失率 (MR) 从 0.1 到 0.7。
    *   **噪声模态**：设置噪声率 (NR) 从 0.1 到 0.7（包括高斯噪声、拉普拉斯噪声和随机擦除）。
*   **对比方法**：包括 MFM, Self-MM, C-MIB, ITHP, Multimodal Boosting 等 SOTA 模型，以及最新的多模态大模型（MLLMs）如 Qwen2.5Omni、HumanOmni 等。

### 4. 资源与算力
*   **硬件环境**：论文明确指出使用了 **NVIDIA RTX 2080Ti GPU**。
*   **软件环境**：PyTorch 1.13.1, CUDA 11.6。
*   **训练细节**：使用 AdamW 优化器，学习率在 5e-6 到 1e-5 之间，Batch Size 为 32-64。论文未详细说明总训练时长，但提到了针对超参数进行了 50 次随机网格搜索。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽。涵盖了 5 个主流数据集，并在每个数据集上进行了完整、缺失（7 种比例）、噪声（7 种比例）三种场景的测试。
*   **消融实验**：对质量评估、秩引导训练、质量增强、模态解耦、MQ-MoE 等核心组件均做了消融分析，验证了各部分的必要性。
*   **客观性与公平性**：通过 5 次随机种子运行取平均值，并与多种类型的基准（包括生成式、对齐式、架构式方法及大模型）进行对比，实验结果具有较高的说服力和客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：UMQ 在所有测试数据集和所有质量场景下均达到了 SOTA 性能。
*   **鲁棒性强**：在极端缺失（MR=0.7）或高噪声（NR=0.7）情况下，UMQ 的性能下降幅度远小于对比方法。
*   **统一方案的有效性**：证明了将缺失视为噪声的一种特殊形式（已知噪声模式）并进行统一建模是可行的且高效的。
*   **质量评估的准确性**：可视化分析显示，质量评估器能准确识别被噪声污染的模态，并引导 MoE 模块选择正确的专家。

### 7. 优点（亮点）
*   **视角创新**：打破了噪声和缺失分类处理的传统，提出了统一的模态质量视角。
*   **训练策略巧妙**：引入秩引导训练，解决了模态质量缺乏绝对真值标签的难题。
*   **增强机制全面**：质量增强器同时考虑了全局模态分布（基准表示）和局部样本关联，比单纯的跨模态重构更有效。
*   **泛化性好**：不仅在情感分析上表现优异，在幽默和讽刺检测等任务上也展现了极强的通用性。

### 8. 不足与局限
*   **计算复杂度**：随着模态数量 $|M|$ 的增加，MQ-MoE 的质量组合数 $2^{|M|}$ 会呈指数级增长，虽然可以通过共享专家缓解，但在多模态（如 5 个以上模态）场景下的扩展性可能受限。
*   **噪声模拟单一**：虽然测试了多种噪声，但训练时主要依赖高斯噪声模拟，对于现实中更复杂的非线性失真或语义偏离的噪声处理能力尚待进一步验证。
*   **超参数敏感性**：框架包含多个辅助损失函数（如 $\beta_{de}, \beta_{est}, \beta_{EH}, \beta_{moe}$），超参数调优过程可能较为繁琐。

（完）
