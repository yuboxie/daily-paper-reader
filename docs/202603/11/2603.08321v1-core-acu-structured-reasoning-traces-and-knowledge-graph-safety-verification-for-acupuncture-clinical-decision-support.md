---
title: "CORE-Acu: Structured Reasoning Traces and Knowledge Graph Safety Verification for Acupuncture Clinical Decision Support"
title_zh: CORE-Acu：面向针灸临床决策支持的结构化推理轨迹与知识图谱安全验证
authors: "Liuyi Xu, Yun Guo, Ming Chen, Zihan Dun, Yining Qian, An-Yang Lu, Shuang Li, Lijun Liu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08321v1"
tags: ["query:ehr-tr"]
score: 9.0
evidence: 用于临床决策支持和大模型结构化推理追踪
tldr: 针对针灸临床决策支持中大模型黑盒化和幻觉问题，本文提出CORE-Acu框架。该框架结合结构化思维链（S-CoT）与知识图谱（KG）安全验证，通过构建首个针灸结构化推理轨迹数据集和符号否决机制，实现了从辨证到选穴的可解释推理与硬性安全约束，显著提升了决策的准确性与安全性。
selection_source: fresh_fetch
motivation: 解决大语言模型在针灸临床决策中存在的推理不可追溯、幻觉风险及缺乏严格安全保障的问题。
method: 提出结合结构化思维链、知识图谱安全验证及词表匹配实体重加权损失（LMERL）的神经符号框架。
result: "在1000个测试案例中实现了零安全违规，显著优于GPT-4o的8.5%违规率，并提升了实体忠实度。"
conclusion: CORE-Acu为针灸临床决策提供了一个兼具推理可审计性和严格安全合规性的鲁棒神经符号解决方案。
---

## 摘要
大语言模型（LLMs）在临床决策支持（CDS）方面展现出巨大潜力，但其黑盒特性（以不可追溯的推理和概率性幻觉为特征）在针灸领域带来了严峻挑战，而该领域对可解释性和安全性有着严格要求。为此，我们提出了 CORE-Acu，这是一个用于针灸临床决策支持的神经符号框架，它集成了结构化思维链（S-CoT）与知识图谱（KG）安全验证。首先，我们构建了首个针灸结构化推理轨迹数据集以及一个模式约束的微调框架。通过强制执行从辨证到治则、治疗方案及穴位选择的显式因果链，我们将隐性的中医（TCM）推理转化为可解释的生成约束，缓解了基于 LLM 的 CDS 的不透明性。此外，我们构建了一个中医安全知识图谱，并建立了一个基于符号否决机制（Symbolic Veto Mechanism）的“生成-验证-修正”闭环推理系统，利用确定性规则拦截幻觉并强制执行硬性安全边界。最后，我们引入了词表匹配实体重加权损失（LMERL），通过在微调过程中自适应地放大高风险实体的梯度贡献，纠正了通用优化中因频率与重要性不匹配而导致的术语漂移。在 1,000 个预留案例上的实验表明，CORE-Acu 具有卓越的实体保真度和推理质量。至关重要的是，CORE-Acu 实现了 0/1,000 的观测安全违规率（95% 置信区间：0-0.37%），而 GPT-4o 在相同规则下的违规率为 8.5%。这些结果确立了 CORE-Acu 作为一个稳健的针灸临床决策支持神经符号框架，同时保证了推理的可审计性和严格的安全合规性。

## Abstract
Large language models (LLMs) show significant potential for clinical decision support (CDS), yet their black-box nature -- characterized by untraceable reasoning and probabilistic hallucinations -- poses severe challenges in acupuncture, a field demanding rigorous interpretability and safety. To address this, we propose CORE-Acu, a neuro-symbolic framework for acupuncture clinical decision support that integrates Structured Chain-of-Thought (S-CoT) with knowledge graph (KG) safety verification. First, we construct the first acupuncture Structured Reasoning Trace dataset and a schema-constrained fine-tuning framework. By enforcing an explicit causal chain from pattern identification to treatment principles, treatment plans, and acupoint selection, we transform implicit Traditional Chinese Medicine (TCM) reasoning into interpretable generation constraints, mitigating the opacity of LLM-based CDS. Furthermore, we construct a TCM safety knowledge graph and establish a ``Generate--Verify--Revise'' closed-loop inference system based on a Symbolic Veto Mechanism, employing deterministic rules to intercept hallucinations and enforce hard safety boundaries. Finally, we introduce the Lexicon-Matched Entity-Reweighted Loss (LMERL), which corrects terminology drift caused by the frequency--importance mismatch in general optimization by adaptively amplifying gradient contributions of high-risk entities during fine-tuning. Experiments on 1,000 held-out cases demonstrate CORE-Acu's superior entity fidelity and reasoning quality. Crucially, CORE-Acu achieved 0/1,000 observed safety violations (95\% CI: 0--0.37\%), whereas GPT-4o exhibited an 8.5\% violation rate under identical rules. These results establish CORE-Acu as a robust neuro-symbolic framework for acupuncture clinical decision support, guaranteeing both reasoning auditability and strict safety compliance.

---

## 论文详细总结（自动生成）

这是一份关于论文《CORE-Acu: Structured Reasoning Traces and Knowledge Graph Safety Verification for Acupuncture Clinical Decision Support》的结构化深入总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在针灸临床决策支持（CDS）中存在“黑盒化”现象（推理不可追溯）和“概率性幻觉”（生成错误或危险的处方）。
*   **研究动机**：
    *   **针灸的特殊性**：针灸是一种侵入性干预，对安全性要求极高（如孕妇禁针穴位），且中医讲究“辨证论治”，需要严密的逻辑链条。
    *   **现有模型缺陷**：通用模型往往直接从症状映射到穴位，跳过了中间的辨证、病机分析和治则制定，导致推理与结果脱节。
    *   **频率-重要性错配**：标准训练目标（交叉熵损失）对高频虚词和低频但关键的医疗实体（如特定穴位名）一视同仁，导致术语生成精度不足。

### 2. 核心方法论
论文提出了 **CORE-Acu** 神经符号框架，包含三个关键技术：
*   **结构化思维链（S-CoT）与模式约束微调**：
    *   构建了包含 42,512 个样本的针灸推理数据集。
    *   强制模型遵循显式因果链：**主诉 → 辨证（诊断）→ 病机（病理）→ 治则（原则）→ 选穴（处方）**。
*   **中医安全知识图谱（TCM-KG）与符号否决机制**：
    *   构建包含 4,628 个节点和 12,500 条边的知识图谱，涵盖穴位属性和禁忌规则。
    *   采用**“生成-验证-修正”（GVR）闭环推理**：模型生成初步方案后，由符号模块根据 KG 规则进行拦截，若发现违规（如配伍禁忌），则反馈错误信息引导模型重新生成，直至通过验证或触发人工干预。
*   **词表匹配实体重加权损失（LMERL）**：
    *   在微调阶段，通过词表识别安全关键实体（穴位、证型等）。
    *   **公式思想**：自适应放大这些高风险实体的梯度贡献（权重因子 $\alpha=1.5$），使模型在优化时更关注术语的准确性而非仅仅是语言的流畅度。

### 3. 实验设计
*   **数据集**：
    *   **Acu-Reasoning**：首个大规模针灸 S-CoT 数据集（42,512 条）。
    *   **测试集**：1,000 个完全独立的临床案例。
*   **对比基准（Benchmarks）**：
    *   **通用模型**：GPT-4o, Qwen-Max, GLM-4.6。
    *   **医疗专用模型**：HuatuoGPT-o1-7B。
    *   **基础模型**：Qwen3-8B（作为 Base）。
*   **评估指标**：
    *   文本质量：BLEU-4, ROUGE-L。
    *   实体精度：Entity-F1。
    *   **安全性**：安全违规率（SVR）、实体幻觉率（EntHall）。
    *   推理质量：LLM-as-a-Judge（使用 GPT-5.2 评分）及人类专家盲评（n=4 复杂案例）。

### 4. 资源与算力
*   **硬件环境**：使用 1 台服务器，配置为 **NVIDIA GeForce RTX 4090 (24GB) GPU**，16 vCPU AMD EPYC 9654。
*   **训练细节**：采用 LoRA（低秩自适应）参数高效微调，秩 $r=16$，训练 3 个 Epoch，最大序列长度 2048。
*   **推理细节**：GVR 循环的最大重试次数 $T_{max}=5$。

### 5. 实验数量与充分性
*   **实验规模**：在 1,000 个预留案例上进行了大规模定量测试，样本量足以支撑统计显著性分析。
*   **消融实验**：专门针对 LMERL 的权重因子 $\alpha$ 进行了敏感性分析，并对“符号否决机制”进行了消融测试（证明了仅靠微调无法达到零违规，必须结合 KG 验证）。
*   **客观性**：引入了第三方模型（GPT-5.2）作为裁判，并结合了资深中医专家的定性评价，评估维度涵盖辨证准确性、病机逻辑等 5 个临床维度，实验设计较为全面、公平。

### 6. 主要结论与发现
*   **安全性突破**：CORE-Acu 在 1,000 个测试案例中实现了 **0% 的安全违规率**，而 GPT-4o 在相同规则下的违规率为 8.5%，HuatuoGPT 为 12.0%。
*   **推理质量提升**：通过 S-CoT，模型在病机分析和治则制定上的逻辑一致性显著增强，解决了“结果正确但理由荒谬”的问题。
*   **实体精度**：LMERL 有效降低了术语漂移，Entity-F1 分数显著优于所有基准模型。
*   **闭环修正有效性**：在 40 个初次生成存在违规的案例中，GVR 机制成功引导模型在重试预算内完成了 100% 的修正。

### 7. 优点与亮点
*   **神经符号结合**：将 LLM 的灵活性与知识图谱的确定性结合，为高风险医疗场景提供了“硬性安全边界”。
*   **首创数据集**：填补了针灸领域缺乏结构化推理轨迹数据集的空白。
*   **可解释性强**：输出不仅有穴位，还有完整的辨证逻辑，符合临床医生的思维习惯。
*   **优化目标创新**：LMERL 解决了医疗领域长尾实体识别难的痛点。

### 8. 不足与局限
*   **知识图谱覆盖范围**：目前的 KG 虽然包含 4,628 个节点，但仍无法穷尽所有罕见病症或复杂的临床变体。
*   **二元否决的局限**：目前的符号验证是基于“对/错”的二元逻辑，可能无法处理某些具有争议或需要权衡利弊的复杂临床决策。
*   **多模态缺失**：针灸诊断高度依赖望闻问切（如舌象、面色），目前模型仅处理文本信息，尚未整合图像等模态。
*   **应用限制**：目前仅在模拟案例上验证，尚未进行真实世界的临床随机对照试验（RCT）。

（完）
