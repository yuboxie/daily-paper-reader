---
title: "Memex(RL): Scaling Long-Horizon LLM Agents via Indexed Experience Memory"
title_zh: Memex(RL)：通过索引经验记忆扩展长程大语言模型智能体
authors: "Zhenting Wang, Huancheng Chen, Jiayun Wang, Wei Wei"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04257v1"
tags: ["query:lh-llm"]
score: 10.0
evidence: 通过索引记忆扩展长程大语言模型智能体
tldr: 针对大语言模型智能体在长程任务中受限于上下文窗口的问题，本文提出 Memex 机制。该机制通过索引化经验记忆，将简洁摘要与稳定索引保留在工作上下文中，而将完整交互存入外部数据库。配合 MemexRL 强化学习框架，智能体能自主学习总结、归档与检索策略。实验证明，Memex 在显著减少上下文占用的同时，提升了长程任务的成功率。
selection_source: fresh_fetch
motivation: 现有的上下文压缩方法（如截断或摘要）会导致原始证据丢失，限制了智能体在处理复杂长程任务时的表现。
method: 提出 Memex 索引记忆机制，结合外部数据库存储完整细节，并利用 MemexRL 强化学习框架优化智能体的读写与检索决策。
result: 在挑战性的长程任务中，Memex 智能体在显著降低工作上下文占用的同时，实现了更高的任务成功率。
conclusion: 索引化经验记忆是一种比纯摘要更高效、低损耗的长程记忆方案，能有效扩展智能体在有限资源下的决策能力。
---

## 摘要
大语言模型（LLM）智能体在处理长程任务时，从根本上受到有限上下文窗口的限制。随着轨迹的增长，在上下文中保留工具输出和中间推理过程很快变得不可行：工作上下文变得过长，最终超出上下文预算，并且即使远端证据仍然存在，也变得难以利用。现有的解决方案通常通过截断或滚动摘要来缩短上下文，但这些方法从根本上是有损的，因为它们压缩或丢弃了过去的证据本身。我们引入了 Memex，这是一种索引经验记忆机制，它在不丢弃证据的情况下压缩上下文。Memex 维持一个由简洁结构化摘要和稳定索引组成的紧凑工作上下文，同时将高保真的底层交互存储在这些索引下的外部经验数据库中。智能体随后可以决定何时解引用（dereference）索引，并恢复当前子目标所需的精确历史证据。我们利用强化学习框架 MemexRL 优化了读写行为，采用针对上下文预算下索引记忆使用而定制的奖励塑造（reward shaping），使智能体学会总结什么、归档什么、如何索引以及何时检索。与仅使用摘要的方法相比，这产生了一种损耗显著更低的长程记忆形式。我们进一步提供了理论分析，展示了 Memex 循环在保持决策质量的同时，通过有限的解引用次数，使有效上下文计算量在历史增长时保持有界。实验表明，在具有挑战性的长程任务中，使用 MemexRL 训练的 Memex 智能体在显著减少工作上下文使用的同时，提高了任务成功率。

## Abstract
Large language model (LLM) agents are fundamentally bottlenecked by finite context windows on long-horizon tasks. As trajectories grow, retaining tool outputs and intermediate reasoning in-context quickly becomes infeasible: the working context becomes prohibitively long, eventually exceeds the context budget, and makes distant evidence harder to use even when it is still present. Existing solutions typically shorten context through truncation or running summaries, but these methods are fundamentally lossy because they compress or discard past evidence itself. We introduce Memex, an indexed experience memory mechanism that instead compresses context without discarding evidence. Memex maintains a compact working context consisting of concise structured summaries and stable indices, while storing full-fidelity underlying interactions in an external experience database under those indices. The agent can then decide when to dereference an index and recover the exact past evidence needed for the current subgoal. We optimize both write and read behaviors with our reinforcement learning framework MemexRL, using reward shaping tailored to indexed memory usage under a context budget, so the agent learns what to summarize, what to archive, how to index it, and when to retrieve it. This yields a substantially less lossy form of long-horizon memory than summary-only approaches. We further provide a theoretical analysis showing the potential of the Memex loop to preserve decision quality with bounded dereferencing while keeping effective in-context computation bounded as history grows. Empirically, on challenging long-horizon tasks, Memex agent trained with MemexRL improves task success while using a significantly smaller working context.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Memex(RL)** 的机制，旨在通过索引化经验记忆（Indexed Experience Memory）和强化学习，解决大语言模型（LLM）智能体在处理长程（Long-Horizon）任务时面临的上下文窗口限制问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 智能体在执行长达数十甚至数百步的任务时，会产生大量的观察结果、工具输出和推理轨迹。现有的上下文管理方法（如截断或滚动摘要）是“有损”的，会丢失关键的原始证据（如具体的 ID、代码片段或日志），导致智能体在后续步骤中无法准确回溯。
*   **研究动机**：现有的相似度检索（RAG）在处理结构化工具调用时往往不够精确。作者希望模仿人类管理长期工作的方式：在脑中保留紧凑的进度状态（工作内存），同时将详细笔记和引用存储在外部（外部存储），并在需要时通过精确的索引进行调取。

### 2. 方法论：核心思想与关键技术
*   **Memex 机制**：
    *   **索引经验记忆（IEM）**：将上下文分为两部分。一是**工作上下文**，包含简洁的结构化摘要和稳定索引（Index Map）；二是**外部经验存储**，一个键值对数据库，存储高保真的原始交互内容。
    *   **核心操作**：
        *   `CompressExperience`：将当前冗长的轨迹压缩，生成摘要并为关键内容分配索引存入外部库。
        *   `ReadExperience(index)`：通过解引用（Dereference）操作，将外部库中的精确内容重新注入当前上下文。
    *   **双模式归档**：支持智能体自主编写内容归档，或通过“锚点提取”（指定开始、中间、结束文本）实现无损的原始文本抓取。
*   **MemexRL 强化学习框架**：
    *   **算法基础**：基于 GRPO（群体相对策略优化）算法。
    *   **奖励塑造（Reward Shaping）**：除了任务成功奖励外，引入了**上下文溢出惩罚**（鼓励主动压缩）、**冗余工具调用惩罚**（鼓励使用检索而非重复执行命令）和**格式错误惩罚**。
    *   **分段轨迹处理**：将包含多次压缩的轨迹切分为独立段落进行训练，但共享最终的任务奖励，以解决长程信用分配问题。
    *   **软触发机制**：系统实时向智能体反馈当前上下文占用情况，由智能体自主决定压缩时机。

### 3. 实验设计
*   **实验场景**：使用了修改后的 **ALFWorld**（一个家庭交互式环境）。
*   **Benchmark 改进**：为了增加难度，作者隐藏了环境的初始观察和可用命令列表，限制了“查看（look）”动作的次数，并强制对摘要进行截断。这迫使智能体必须依赖 `ReadExperience` 才能获取之前看到的物体 ID 和位置信息。
*   **对比基准**：主要对比了经过 MemexRL 训练的智能体与未经 RL 训练（仅靠 Prompt）的智能体在相同任务下的表现。

### 4. 资源与算力
*   **模型**：使用 Qwen3-30B-A3B-Thinking-2507（一个约 30B 参数的 MoE 模型，激活参数约 3B）。
*   **训练框架**：基于开源的 LLM RL 框架 **Slime**。
*   **算力细节**：文中提到使用了 **INT4 量化**进行推理采样，并使用**量化感知训练（QAT）**进行反向传播（权重为 INT4，梯度为 BF16）。虽然未明确给出具体的 GPU 数量和训练总时长，但提到 Batch Size 为 32，GRPO 组大小为 8。

### 5. 实验数量与充分性
*   **实验规模**：训练集包含 **3553 个任务**。
*   **充分性**：作者展示了训练过程中的动态变化（成功率、惩罚值、工具使用频率的变化趋势）。实验设计了针对性的消融观察（如压缩次数与检索次数的演变），证明了智能体确实学会了“减少重复劳动、增加精准检索”的行为模式。
*   **局限性**：实验主要集中在 ALFWorld 这一类具身智能体模拟环境上，尚未在更广泛的软件工程（如 SWE-bench）或复杂科研分析任务上进行大规模验证。

### 6. 主要结论与发现
*   **性能提升**：MemexRL 将任务成功率从 24.2% 提升至 **85.6%**。
*   **效率优化**：在成功率大幅提升的同时，峰值工作上下文长度从约 1.7 万 token 降至 **9600 token** 左右（降低了约 43%）。
*   **行为转变**：经过训练，智能体调用 `CompressExperience` 的次数减少（变得更高效），而调用 `ReadExperience` 的次数显著增加，表明其学会了利用外部记忆来辅助决策。

### 7. 优点
*   **低损耗压缩**：相比于传统的文本摘要，索引机制保证了原始证据（如复杂的 ID 字符串）的绝对精确性。
*   **理论支撑**：论文提供了理论分析，证明了在解引用次数有界的情况下，Memex 循环可以保持决策质量，同时使计算量不随历史增长而爆炸。
*   **自主性强**：将记忆管理视为一种“工具调用”，让智能体通过 RL 自主学习何时、如何管理自己的记忆，而非依赖硬编码的启发式规则。

### 8. 不足与局限
*   **环境单一**：目前仅在修改版的 ALFWorld 上验证，该环境虽然模拟了长程依赖，但任务类型相对固定。
*   **冷启动依赖**：智能体需要具备一定的初始工具使用能力（Warm-start），对于完全没有指令遵循能力的基座模型可能难以适用。
*   **复杂性**：引入了额外的外部存储管理和复杂的奖励函数设计，增加了系统实现的复杂度。

（完）
