---
title: "Bounded State in an Infinite Horizon: Proactive Hierarchical Memory for Ad-Hoc Recall over Streaming Dialogues"
title_zh: 无限时界中的有限状态：用于流式对话即时召回的主动式层级记忆
authors: "Bingbing Wang, Jing Li, Ruifeng Xu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04885v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 无限长程约束下的时序推理与记忆基准
tldr: 针对无限流式对话中的记忆召回挑战，本文提出了STEM-Bench基准测试，揭示了现有方法在保真度与效率间的矛盾。为此，作者开发了ProStream框架，通过主动式分层记忆和自适应时空优化，在保持有限状态的同时实现了高效、准确的即时记忆召回，显著提升了流式对话系统的性能与响应速度。
selection_source: fresh_fetch
motivation: 现有的记忆机制难以在无限流式对话中平衡即时召回的保真度与推理效率，导致长程对话处理能力受限。
method: 提出了ProStream框架，通过多粒度蒸馏的主动分层记忆和自适应时空优化技术，动态优化记忆保留并维持有界知识状态。
result: 实验结果显示，ProStream在STEM-Bench基准测试中的准确率和推理效率均显著优于现有的检索式和全上下文基准方法。
conclusion: 该研究通过主动式分层记忆管理，成功解决了流式对话中的保真度-效率困境，为无限时域下的智能对话系统提供了高效方案。
---

## 摘要
现实世界的对话通常以无限流的形式展开。因此，它需要有限状态记忆机制在无限时界内运行。然而，现有的“先读后思”记忆模式与这一设定根本不匹配，因为它无法在流展开时支持即时记忆召回。为了探索这一挑战，我们引入了 STEM-Bench，这是首个用于记忆流式评估（STreaming Evaluation of Memory）的基准测试。它包含对话流中的 1.4 万多个问答对，用于评估在无限时界约束下的感知保真度、时间推理和全局意识。对 STEM-Bench 的初步分析表明存在一个关键的“保真度-效率困境”：基于检索的方法使用片段上下文，而全上下文模型则会产生无限的延迟。为解决这一问题，我们提出了 ProStream，一种用于流式对话的主动式层级记忆框架。它通过多粒度蒸馏对连续流进行推理，从而实现按需的即时记忆召回。此外，它采用自适应时空优化，根据预期效用动态优化记忆保留。它在不牺牲推理保真度的前提下，实现了有限的知识状态，从而降低了推理延迟。实验表明，ProStream 在准确性和效率方面均优于基准模型。

## Abstract
Real-world dialogue usually unfolds as an infinite stream. It thus requires bounded-state memory mechanisms to operate within an infinite horizon. However, existing read-then-think memory is fundamentally misaligned with this setting, as it cannot support ad-hoc memory recall while streams unfold. To explore this challenge, we introduce \textbf{STEM-Bench}, the first benchmark for \textbf{ST}reaming \textbf{E}valuation of \textbf{M}emory. It comprises over 14K QA pairs in dialogue streams that assess perception fidelity, temporal reasoning, and global awareness under infinite-horizon constraints. The preliminary analysis on STEM-Bench indicates a critical \textit{fidelity-efficiency dilemma}: retrieval-based methods use fragment context, while full-context models incur unbounded latency. To resolve this, we propose \textbf{ProStream}, a proactive hierarchical memory framework for streaming dialogues. It enables ad-hoc memory recall on demand by reasoning over continuous streams with multi-granular distillation. Moreover, it employs Adaptive Spatiotemporal Optimization to dynamically optimize retention based on expected utility. It enables a bounded knowledge state for lower inference latency without sacrificing reasoning fidelity. Experiments show that ProStream outperforms baselines in both accuracy and efficiency.

---

## 论文详细总结（自动生成）

这篇论文针对流式对话中的长短期记忆挑战，提出了首个专门的基准测试和一种创新的主动式层级记忆框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：现实世界的对话通常是持续不断的“无限流”，而非静态文档。现有的长文本处理方法大多遵循“先读后思”（Read-then-think）范式，假设上下文是静态且完全可访问的。
*   **核心挑战**：在无限流式对话中，系统面临**保真度-效率困境（Fidelity-Efficiency Dilemma）**。
    *   **全上下文模型**：虽然推理保真度高，但随着对话增长，计算成本和延迟呈爆炸式增长（无界状态）。
    *   **检索式方法（如RAG）**：虽然延迟较低且稳定，但会将上下文碎片化，导致逻辑推理能力下降。
*   **研究动机**：探索如何在有限的计算预算（有界状态）下，在无限的对话流中实现高质量的即时记忆召回（Ad-Hoc Recall）。

### 2. 方法论：ProStream 框架
ProStream 将记忆维护重新表述为一个**有界状态演化过程**，包含四个关键阶段：

1.  **主动语义流感知（Proactive Semantic Stream Perception）**：
    *   利用 Whisper ASR 将音频转为文本，并通过语义漂移检测（余弦相似度阈值）将连续流切分为离散的语义块（Semantic Blocks），避免上下文碎片化。
2.  **层级多粒度蒸馏（Hierarchical Multi-Granular Distillation）**：
    *   将语义块蒸馏为三层拓扑结构：**场景（Scene）**、**事件（Event）**和**原子记忆单元（AMU，即事实三元组）**。这种结构化存储有助于捕捉多粒度关联。
3.  **自适应时空优化（Adaptive Spatiotemporal Optimization）**：
    *   **核心公式**：定义节点效用 $u_{v,t} = \alpha \cdot \log(f_{v,t} + 1) + \beta \cdot \exp(-\Delta t_v / \tau)$，综合考虑访问频率（$f$）和时间衰减（$\Delta t$）。
    *   **算法流程**：将记忆保留建模为“带衰减值的在线背包问题”。当记忆超出预算（$T_{max}$）时，采用贪婪边际效用策略进行剪枝、语义合并或级联抽象，确保状态有界。
4.  **概率证据增强推理（Probabilistic Evidence-Grounded Reasoning）**：
    *   采用自顶向下的检索策略，结合语义相关性和节点效用权重，为即时查询提供最相关的证据路径。

### 3. 实验设计
*   **基准测试 (STEM-Bench)**：
    *   **数据集**：基于 LongDialQA，涵盖《生活大爆炸》、《老友记》、《办公室》三个剧本域。
    *   **规模**：包含 14,938 个 QA 对、55,673 条话语、6,053 个语义主题。
    *   **评估维度**：高保真感知（HFP）、结构逻辑推理（SLR）、动态全局意识（DGA）。
*   **对比方法**：
    *   **传统基准**：标准 RAG、全上下文（Full Context）、滚动摘要。
    *   **先进检索/结构化方法**：GraphRAG、HippoRAG、RAPTOR、RQ-RAG。
    *   **智能体记忆系统**：MemoRAG、A-Mem、MemGAS。

### 4. 资源与算力
*   **硬件环境**：论文提到实验在 NVIDIA GPU 上运行，并使用了 PyTorch 内存优化技术。
*   **模型组件**：使用了 Whisper large-v3（语音转写）、ms-marco-MiniLM（嵌入）、LaMini-T5（蒸馏）、GLiNER（实体提取）以及 Qwen 系列（3B/7B/14B）作为生成底座。
*   **缺失信息**：论文**未明确说明**具体的 GPU 数量、具体的训练时长或推理总耗时，仅提供了平均推理延迟（秒）作为效率指标。

### 5. 实验充分性与客观性
*   **实验规模**：做了非常详尽的对比，涵盖了 10 种以上的基线方法。
*   **消融实验**：针对短期感知缓冲（B）、层级树（Mtree）和待处理缓冲区（Mpend）分别做了消融，验证了各模块的必要性。
*   **多维度分析**：包括不同模型规模（3B 到 14B）的扩展性分析、上下文复杂度的敏感性分析，以及不同嵌入模型的影响分析。
*   **客观性**：引入了 Gemini-2.5-Pro 作为专家评委进行评分，并计算了标注者间的一致性（Cohen’s κ = 0.74），确保了 Benchmark 的质量。

### 6. 主要结论与发现
*   **性能领先**：ProStream 在准确性和效率上均显著优于所有基线，甚至在某些推理指标上超过了“全上下文”Oracle 模型，证明了主动蒸馏能减少冗余噪声。
*   **效率优势**：ProStream 实现了常数级的单步时间复杂度 $O(T_{max})$，其推理延迟不随对话长度增加而增长，解决了无界延迟问题。
*   **扩展性**：随着 LLM 底座规模增大，ProStream 带来的性能提升更显著，说明更强的模型能更好地利用结构化记忆。

### 7. 论文亮点
*   **首创性**：提出了首个针对无限流式对话记忆召回的基准测试 STEM-Bench。
*   **范式转变**：从被动的“检索式扫描”转向主动的“层级状态维护”，更符合人类记忆的认知规律。
*   **理论支撑**：将记忆管理建模为在线背包问题，并给出了近似比保证和复杂度证明，具有较强的数学严谨性。

### 8. 不足与局限
*   **指令-保真度冲突**：错误分析显示，当用户提问包含错误前提时，模型倾向于顺从用户指令而非坚持历史事实（产生幻觉）。
*   **语义噪声干扰**：在极高密度的长篇独白中，模型有时会忽略细微但关键的上下文线索（如背景中的小细节）。
*   **应用限制**：目前主要基于剧本对话，对于专业领域（如医疗、法律）的流式对话处理能力尚待验证。
*   **实时性开销**：虽然推理延迟低，但后台的“感知-蒸馏-优化”循环仍需持续的计算资源支持。

（完）
