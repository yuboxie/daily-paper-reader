---
title: Chow-Liu Ordering for Long-Context Reasoning in Chain-of-Agents
title_zh: Chain-of-Agents 中用于长上下文推理的 Chow-Liu 排序
authors: "Naman Gupta, Vaibhav Singh, Arun Iyer, Kirankumar Shiragur, Pratham Grover, Ramakrishna B. Bairi, Ritabrata Maiti, Sankarshan Damle, Shachee Mishra Gupta, Rishikesh Maurya, Vageesh D. C"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09835v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 多智能体框架中的长上下文推理
tldr: 针对长文本推理中的 Chain-of-Agents (CoA) 框架，本文研究了分块处理顺序对信息损耗的影响。由于 CoA 采用有限共享内存的顺序处理模式，分块顺序决定了信息瓶颈的严重程度。作者提出利用 Chow-Liu 树学习分块间的依赖结构，并通过广度优先遍历确定处理顺序。实验证明，该方法在多个长文本基准测试中显著优于默认顺序和基于语义分数的排序，有效提升了回答准确性。
selection_source: fresh_fetch
motivation: 顺序多智能体推理框架在处理长文本时存在信息瓶颈，且最终推理结果高度依赖于文本分块的处理顺序。
method: 利用 Chow-Liu 树构建分块间的依赖结构，并采用广度优先遍历生成能减少信息损失的最优处理顺序。
result: 在三个长文本基准测试中，该方法在答案相关性和精确匹配准确率上均优于默认文档顺序和基于语义评分的排序。
conclusion: 优化分块处理顺序是提升长文本顺序推理性能的关键，Chow-Liu 排序提供了一种有效的结构化解决方案。
---

## 摘要
诸如 Chain-of-Agents (CoA) 之类的顺序多智能体推理框架通过将输入分解为块（chunks），并使用基于大语言模型（LLM）的工作智能体顺序处理这些块来处理长上下文查询，这些智能体读取并更新有界的共享内存。从概率角度来看，CoA 旨在逼近一个能够对整个长上下文进行联合推理的模型所对应的条件分布。CoA 通过隐状态分解实现这一点，其中只有先前处理过的证据的有界摘要在智能体之间传递。由此产生的有界内存近似引入了有损信息瓶颈，使得最终的证据状态本质上取决于块的处理顺序。在这项工作中，我们研究了长上下文推理中的块排序问题。我们利用著名的 Chow-Liu 树来学习一种依赖结构，该结构优先处理强相关的块。实验表明，对所得树进行广度优先遍历产生的块排序减少了智能体之间的信息损失，并且在三个长上下文基准测试中，在答案相关性和精确匹配准确度方面始终优于默认的文档块排序和基于语义评分的排序。

## Abstract
Sequential multi-agent reasoning frameworks such as Chain-of-Agents (CoA) handle long-context queries by decomposing inputs into chunks and processing them sequentially using LLM-based worker agents that read from and update a bounded shared memory. From a probabilistic perspective, CoA aims to approximate the conditional distribution corresponding to a model capable of jointly reasoning over the entire long context. CoA achieves this through a latent-state factorization in which only bounded summaries of previously processed evidence are passed between agents. The resulting bounded-memory approximation introduces a lossy information bottleneck, making the final evidence state inherently dependent on the order in which chunks are processed.   In this work, we study the problem of chunk ordering for long-context reasoning. We use the well-known Chow-Liu trees to learn a dependency structure that prioritizes strongly related chunks. Empirically, we show that a breadth-first traversal of the resulting tree yields chunk orderings that reduce information loss across agents and consistently outperform both default document-chunk ordering and semantic score-based ordering in answer relevance and exact-match accuracy across three long-context benchmarks.

---

## 论文详细总结（自动生成）

这篇论文针对长文本推理中的顺序多智能体框架（如 Chain-of-Agents, CoA）提出了基于概率图模型的优化策略。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在处理超长文本时，LLM 往往受限于上下文窗口。CoA 框架通过将文本分块并由多个智能体顺序处理（传递有界内存/摘要）来解决此问题。然而，这种“顺序处理+有损压缩”的模式导致最终推理结果高度依赖于分块的处理顺序。
*   **研究动机**：现有的排序方法（如默认文档顺序或简单的语义相似度排序）忽略了文本块之间的内在依赖关系。如果相关的证据块在序列中被分得太开，中间的压缩步骤会导致关键信息丢失。
*   **整体含义**：本文旨在寻找一种最优的块处理顺序，通过建模块间的统计依赖结构，最大限度地保留推理所需的关键信息。

### 2. 论文提出的方法论
*   **核心思想**：将文本分块排序问题转化为概率图模型中的结构学习问题。利用 **Chow-Liu 算法** 寻找能够最逼近全局联合分布的树状依赖结构。
*   **关键技术细节**：
    1.  **依赖建模**：将每个文本块视为随机变量，利用块与块之间的嵌入向量（Embedding）余弦相似度作为“互信息”的代理指标。
    2.  **树构建**：构建一个以块为节点、相似度为边权的完全图，通过最大权重生成树（MWST）算法提取 Chow-Liu 树。
    3.  **顺序生成（CL-Ordering）**：
        *   **根节点选择**：选择与用户查询（Query）语义最相似的块作为根节点。
        *   **遍历策略**：从根节点开始进行**广度优先遍历（BFS）**。这种方式能确保语义相关和统计依赖的块在处理序列中尽可能靠近。
*   **算法流程**：编码块向量 -> 计算两两相似度 -> 构建最大权重生成树 -> 确定查询相关的根节点 -> BFS 遍历生成序列 -> 交付 CoA 框架执行。

### 3. 实验设计
*   **数据集/场景**：
    *   **HELMET**：包含来自 ∞BENCH 和 NarrativeQA 的长文本问答任务。
    *   **NarrativeQA**：专门筛选了上下文长度超过 256K token 的查询。
*   **Benchmark 指标**：
    *   **Ragas Answer Relevance**：利用 LLM 作为裁判评估答案的相关性。
    *   **Exact Match (EM)**：针对多选题（LongQA-MC）的精确匹配准确率。
*   **对比方法**：
    *   **DEFAULT**：文档原始的自然顺序。
    *   **DENSE**：基于块与查询的相似度进行降序排列（类似 RAG 的重排序逻辑）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号、数量或训练时长）。
*   **模型使用**：实验主要基于 API 调用和本地推理，使用的模型包括 GPT-4.1、GPT-4.1-mini 和 Qwen-3-14B。嵌入模型使用了 OpenAI 的 `text-embedding-3-large` 和 Qwen-3-Embedding-8B。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三个不同参数规模和厂商的模型家族（GPT-4 系列和 Qwen 系列）。
    *   在三个主流长文本基准上进行了测试。
    *   **消融实验**：对比了不同的嵌入模型（OpenAI vs Qwen）、不同的评分函数（BM25 vs 向量相似度）以及不同的遍历策略（DFS vs BFS）。
*   **充分性评价**：实验设计较为充分，通过跨模型、跨数据集的对比验证了方法的鲁棒性。消融实验有效地证明了 Chow-Liu 树捕捉全局依赖的能力优于局部的贪婪搜索（DFS）。

### 6. 主要结论与发现
*   **性能提升**：CL-Ordering 在所有模型和数据集上均表现最佳。在 EM 指标上，相比默认顺序提升了 **10.68%**，相比语义排序提升了 **6.89%**。
*   **排序的重要性**：实验证明，即使不改变检索到的内容，仅仅改变处理顺序就能显著影响长文本推理的质量。
*   **全局 vs 局部**：基于树结构的全局依赖建模（BFS）优于仅关注当前块邻居的局部建模（DFS），说明长文本推理需要更宏观的上下文组织。

### 7. 优点
*   **理论严谨**：将直觉上的排序问题引入了经典的 Chow-Liu 概率框架，具有较强的数学支撑。
*   **即插即用**：该方法不需要对 LLM 进行微调，也不增加推理时的 Token 开销，仅作为预处理步骤即可提升 CoA 性能。
*   **泛化性强**：在不同规模的模型（从 mini 到大参数模型）上均有稳定收益。

### 8. 不足与局限
*   **依赖嵌入质量**：方法的核心前提是嵌入向量能准确代表块间的互信息。如果嵌入模型在特定领域表现不佳，排序效果可能受限。
*   **计算开销**：虽然不增加推理开销，但构建 $N \times N$ 的相似度矩阵在分块数量极多时（例如数千个块）会带来额外的预处理计算量。
*   **静态排序**：目前的排序是在处理前确定的，无法根据智能体在推理过程中的中间发现动态调整顺序。

（完）
