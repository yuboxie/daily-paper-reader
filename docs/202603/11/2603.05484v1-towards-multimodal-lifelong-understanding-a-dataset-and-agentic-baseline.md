---
title: "Towards Multimodal Lifelong Understanding: A Dataset and Agentic Baseline"
title_zh: 迈向多模态终身理解：一个数据集与智能体基线
authors: "Guo Chen, Lidong Lu, Yicheng Liu, Liangrui Dong, Lidong Zou, Jixin Lv, Zhenquan Li, Xinyi Mao, Baoqi Pei, Shihao Wang, Zhiqi Li, Karan Sapra, Fuxiao Liu, Yin-Dong Zheng, Yifei Huang, Limin Wang, Zhiding Yu, Andrew Tao, Guilin Liu, Tong Lu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05484v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 多模态终身理解与长程推理
tldr: 本研究针对现有视频理解数据集缺乏真实生活长时序特性的问题，推出了MM-Lifelong数据集，涵盖181.1小时跨越日、周、月尺度的视频。研究揭示了现有大模型在处理超长视频时的“工作记忆瓶颈”和“全局定位崩溃”现象。为此，作者提出了递归多模态智能体（ReMA），通过动态内存管理和递归信念状态更新，在长时序理解任务上显著优于现有方法，为多模态终身学习提供了新的基准和解决方案。
selection_source: fresh_fetch
motivation: 现有视频理解数据集多为密集剪辑，无法模拟真实生活中稀疏且跨度极长的多模态终身理解场景。
method: 构建了181.1小时的多尺度数据集MM-Lifelong，并提出一种基于动态内存管理和递归信念状态更新的智能体ReMA。
result: 实验表明ReMA有效解决了端到端模型在长上下文下的饱和问题及智能体在稀疏时间线上的定位失效问题。
conclusion: 该工作通过新数据集和智能体架构，为多模态终身理解研究提供了严谨的评估基础和高效的基准模型。
---

## 摘要
尽管视频理解数据集的规模已扩展至数小时，但它们通常由密集拼接的片段组成，与自然、无脚本的日常生活有所不同。为了弥补这一差距，我们推出了 MM-Lifelong，这是一个专为多模态终身理解设计的数据集。该数据集包含 181.1 小时的素材，按天、周和月三个尺度进行组织，以捕捉不同的时间密度。广泛的评估揭示了当前范式中的两种关键失效模式：端到端多模态大语言模型（MLLMs）因上下文饱和而面临工作记忆瓶颈（Working Memory Bottleneck），而代表性的智能体基线在处理稀疏的月度时间线时会出现全局定位崩溃（Global Localization Collapse）。为解决这些问题，我们提出了递归多模态智能体（ReMA），它采用动态记忆管理来迭代更新递归信念状态，性能显著优于现有方法。最后，我们建立了旨在隔离时间与领域偏差的数据集划分，为监督学习和分布外泛化的未来研究提供了严谨的基础。

## Abstract
While datasets for video understanding have scaled to hour-long durations, they typically consist of densely concatenated clips that differ from natural, unscripted daily life. To bridge this gap, we introduce MM-Lifelong, a dataset designed for Multimodal Lifelong Understanding. Comprising 181.1 hours of footage, it is structured across Day, Week, and Month scales to capture varying temporal densities. Extensive evaluations reveal two critical failure modes in current paradigms: end-to-end MLLMs suffer from a Working Memory Bottleneck due to context saturation, while representative agentic baselines experience Global Localization Collapse when navigating sparse, month-long timelines. To address this, we propose the Recursive Multimodal Agent (ReMA), which employs dynamic memory management to iteratively update a recursive belief state, significantly outperforming existing methods. Finally, we establish dataset splits designed to isolate temporal and domain biases, providing a rigorous foundation for future research in supervised learning and out-of-distribution generalization.

---

## 论文详细总结（自动生成）

### 论文总结：迈向多模态终身理解（MM-Lifelong）

#### 1. 核心问题与研究动机
*   **核心问题**：现有的长视频理解研究主要集中在“密集剪辑”的视频（如电影、短片），其观测时长（$T_{dur}$）几乎等于物理时间跨度（$T_{span}$）。然而，真实的“终身理解”场景具有**高度的时间稀疏性**（$T_{span} \gg T_{dur}$），即模型需要跨越数天甚至数月的未观测间隙（如睡眠、断续记录）来关联事件。
*   **研究动机**：
    *   硬件（如 NVIDIA Rubin 平台）已支持超长上下文，但模型在处理超长视频流时表现不佳。
    *   现有数据集缺乏对“终身地平线”（Lifelong Horizon）的严格定义和数据支持。
    *   旨在揭示并解决端到端模型在超长上下文下的“工作记忆瓶颈”问题。

#### 2. 方法论：递归多模态智能体（ReMA）
*   **核心思想**：不直接让模型处理原始海量视频帧，而是将多模态流转化为一个动态更新的、语言增强的**递归信念状态（Recursive Belief State）**。
*   **关键技术流程**：
    1.  **感知阶段（Perception Phase）**：
        *   **视频分段**：将长视频切分为固定长度（如5分钟）的片段。
        *   **被动感知（MMInspect）**：利用 MLLM 提取片段的摘要和关键信息。
        *   **记忆整合（MemManage）**：将新提取的信息增量式地存入全局记忆库（Memory Bank），并自动合并语义重叠或冗余的节点。
    2.  **控制阶段（Control Phase）**：
        *   **迭代推理**：LLM 控制器根据用户查询，在记忆库中搜索（MemSearch）或针对特定时间段进行精细化重检（MMInspect）。
        *   **递归更新**：每次工具调用的结果都会反馈并更新信念状态，直到生成最终答案。

#### 3. 实验设计
*   **数据集（MM-Lifelong）**：
    *   **规模**：181.1 小时视频，涵盖 1289 个问题和 1810 个线索区间。
    *   **多尺度场景**：
        *   **天级（Day）**：游戏通关视频（23.6h），侧重高密度状态追踪。
        *   **周级（Week）**：第一视角生活记录（51.9h），侧重日常例行公事。
        *   **月级（Month）**：无脚本直播流（105.6h，跨度51天），侧重极高稀疏度和概念漂移。
*   **Benchmark 与对比方法**：
    *   **端到端 MLLMs**：GPT-5、Qwen3-VL (235B/30B)、Video-XL-2、Eagle-2.5、Nemotron-v2。
    *   **智能体基线**：VideoMind、LongVT、DeepVideoDiscovery (DVD)。
*   **评估指标**：答案准确率（LLM 评分）和参考定位精度（Ref@N，衡量模型找寻证据区间的能力）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **模型调用**：实验主要基于 API 调用（如 GPT-5）和开源模型推理（如 Qwen3-VL-A22B）。由于 ReMA 涉及多次递归调用和视频分段处理，其推理过程对算力和 API 配额的需求显著高于端到端模型。

#### 5. 实验数量与充分性
*   **实验规模**：在三个不同时间尺度的子集上进行了全面测试，涵盖了从 10 分钟到 10 小时以上的不同时间跨度（Temporal Certificate）。
*   **消融实验**：非常充分。作者分析了递归深度（Round 0-8）、感知粒度（2min 到 Full Video）、不同控制器骨干（MLLM vs. 纯文本 LLM）的影响。
*   **客观性**：采用了严格的时间顺序划分（Train/Val/Test），避免了时间上的数据泄露；同时通过人类评估验证了“LLM-as-a-Judge”的可靠性（一致性达 99.39%）。

#### 6. 主要结论与发现
*   **工作记忆瓶颈**：端到端 MLLM 在处理超长视频时，性能随帧数增加会出现“饱和”甚至下降，且几乎丧失了在长流中精确定位证据的能力。
*   **全局定位崩溃**：现有的视频智能体在面对月度尺度的稀疏时间线时，往往无法有效检索到关键片段。
*   **ReMA 的优越性**：通过将视频转化为语言空间的记忆，ReMA 在所有尺度上均显著优于端到端模型和现有智能体，尤其是在定位精度（Ref@300）上提升巨大。

#### 7. 优点
*   **数据集定义严谨**：首次提出了 $T_{span} \gg T_{dur}$ 的终身理解准则，更符合真实 AI 助手的应用场景。
*   **线索接地（Clue-Grounded）**：标注不仅有答案，还有对应的视频证据区间，为模型的可解释性和定位能力提供了硬性约束。
*   **智能体架构高效**：ReMA 的递归机制有效克服了线性处理长视频的上下文压力。

#### 8. 不足与局限
*   **主体多样性有限**：每个尺度的数据集仅围绕单一主体（一个主播、一个游戏角色等），可能存在特定主体的行为偏差。
*   **未观测区间的复杂性**：虽然考虑了时间间隙，但对于间隙期间物理世界发生的不可见演化（如环境剧变）对后续理解的影响研究尚浅。
*   **成本与延迟**：递归推理虽然效果好，但多次调用大模型会导致推理成本高昂且响应速度慢，难以直接应用于实时交互场景。

（完）
