---
title: Reproducible Synthetic Clinical Letters for Seizure Frequency Information Extraction
title_zh: 用于癫痫发作频率信息提取的可重复合成临床信函
authors: "Yujian Gan, Stephen H. Barlow, Ben Holgate, Joe Davies, James T. Teo, Joel S. Winston, Mark P. Richardson"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11407v1"
tags: ["query:ehr-tr"]
score: 9.0
evidence: 使用LLM从临床信函中提取癫痫发作频率
tldr: 本研究针对癫痫临床信函中发作频率提取困难且隐私敏感的问题，开发了一个可重复的合成数据框架。通过教师模型生成包含结构化标签和证据的合成信函，并微调开源大模型。实验表明，仅在合成数据上训练的模型在真实临床数据上表现优异，F1分数最高达0.858，为临床信息提取提供了一种无需共享敏感数据的高效方案。
selection_source: fresh_fetch
motivation: 癫痫发作频率信息通常记录在难以标注和共享的非结构化临床信函中，限制了相关研究和临床护理。
method: 开发了一个利用教师模型生成带有结构化标签和证据链的合成临床信函框架，并据此微调了多个开源大语言模型。
result: 在真实临床信函测试中，仅使用合成数据训练的模型在细粒度分类上取得了0.788的F1分数，在实用分类上达到了0.847。
conclusion: 合成数据驱动的结构化监督学习能有效提取复杂的临床信息，且无需共享敏感患者文本，具有良好的泛化性。
---

## 摘要
癫痫发作频率信息对于癫痫研究和临床护理至关重要，但通常记录在多变的自由文本临床信函中，难以进行标注和共享。我们开发了一个可重复且保护隐私的框架，利用完全合成但符合任务真实性的癫痫信函来提取发作频率。我们定义了一个结构化标签方案，涵盖了癫痫负担的常见描述，包括明确的频率、范围、簇发、无发作间隔、未知频率以及明确的无发作声明。教师语言模型生成了 NHS 风格的合成信函，并配有标准化的标签、理由和证据跨度。我们在这些合成信函上微调了几个开源权重语言模型（4B-14B 参数），以从完整文档中提取发作频率，比较了直接数值预测与结构化标签预测，并测试了基于证据的输出。在由临床医生检查的真实临床信函留出集上，仅在合成数据上训练的模型表现出良好的泛化能力，且结构化标签的表现始终优于直接数值回归。使用 15,000 封合成训练信函，模型在细粒度类别上的 micro-F1 分数最高达到 0.788，在实用类别上达到 0.847；一个面向医学的 4B 模型分别达到了 0.787 和 0.858。基于证据的输出还支持快速的临床验证和错误分析。这些结果表明，合成的、结构化的、基于证据的监督可以在不共享敏感患者文本的情况下实现稳健的癫痫发作频率提取，并可能推广到其他具有时间复杂性的临床信息提取任务。

## Abstract
Seizure-frequency information is important for epilepsy research and clinical care, but it is usually recorded in variable free-text clinic letters that are hard to annotate and share. We developed a reproducible, privacy-preserving framework for extracting seizure frequency using fully synthetic yet task-faithful epilepsy letters. We defined a structured label scheme covering common descriptions of seizure burden, including explicit rates, ranges, clusters, seizure-free intervals, unknown frequency, and explicit no-seizure statements. A teacher language model generated NHS-style synthetic letters paired with normalized labels, rationales, and evidence spans. We fine-tuned several open-weight language models (4B-14B parameters) on these synthetic letters to extract seizure frequency from full documents, comparing direct numeric prediction with structured label prediction and testing evidence-grounded outputs. On a clinician-checked held-out set of real clinic letters, models trained only on synthetic data generalized well, and structured labels consistently outperformed direct numeric regression. With 15,000 synthetic training letters, models achieved micro-F1 scores up to 0.788 for fine-grained categories and 0.847 for pragmatic categories; a medically oriented 4B model achieved 0.787 and 0.858, respectively. Evidence-grounded outputs also supported rapid clinical verification and error analysis. These results show that synthetic, structured, evidence-grounded supervision can enable robust seizure-frequency extraction without sharing sensitive patient text and may generalize to other temporally complex clinical information extraction tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Reproducible Synthetic Clinical Letters for Seizure Frequency Information Extraction》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：癫痫发作频率是评估病情和治疗效果的关键指标，但该信息通常以非结构化文本形式记录在临床信函中。由于隐私保护（如英国 NHS 的严格规定）和去隐私化技术的局限性，真实的临床文本难以跨机构共享，导致模型训练缺乏高质量、可重复的大规模标注数据。
*   **研究背景**：现有的 NLP 方法在处理复杂的时间逻辑（如“每月发作 4-5 次”、“每两周一次簇发，每次 3 个”）时表现不佳。研究旨在开发一个**可重复且保护隐私**的框架，通过合成高质量的临床信函来训练模型，使其在不接触真实敏感数据的情况下，依然能在真实临床场景中达到高精度的提取效果。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用高能力的“教师模型”（GPT-5）生成任务忠实（Task-faithful）且标签准确的合成临床信函，并以此微调中等规模的开源模型。
*   **关键技术流程**：
    1.  **结构化标签方案设计**：定义了一套涵盖频率（Rate）、范围（Range）、簇发（Cluster）、无发作时长（Seizure-free duration）和未知（Unknown）的标准化标签格式。
    2.  **合成信函生成流水线**：
        *   基于 10 个脱敏的真实信函模板。
        *   使用 GPT-5 生成多样化的发作频率描述。
        *   将描述嵌入模板，并自动生成合成的患者人口统计信息（占位符替换）。
    3.  **思维链（CoT）与证据锚定**：教师模型不仅提供标签，还生成推理步骤（CoT）和原文证据跨度（Evidence Spans），用于监督微调。
    4.  **多阶段过滤**：通过一致性检查（Self-consistency），仅保留教师模型推理结果与预设标签完全一致的合成样本（最终保留 15,099 封）。
    5.  **模型微调**：使用 LLaMA-Factory 框架，对 4B 到 14B 参数的开源模型进行指令微调。

### 3. 实验设计
*   **数据集**：
    *   **真实数据**：来自伦敦金斯学院医院（KCH）的 1,781 封真实信函（其中 300 封作为经过临床医生双重审核的金标准测试集）。
    *   **合成数据**：15,099 封由 GPT-5 生成的合成信函。
*   **Benchmark 与对比方法**：
    *   **评估方案**：分为 Purist（细粒度 10 分类）和 Pragmatic（实用 4 分类）。
    *   **对比维度**：
        *   **输出格式对比**：数值回归（Format 1）vs 实用分类（Format 2）vs 结构化标签（Format 3）vs CoT+标签+证据（Format 4）。
        *   **训练数据对比**：纯真实数据（R）、纯合成数据（S）、真实+合成混合数据（R+S）。
        *   **模型对比**：Llama-3.1-8B, Qwen2.5 (7B/14B), Gemma-3-4B, MedGemma-4B 等 7 个模型。

### 4. 资源与算力
*   **算力配置**：使用了由 **8 张 NVIDIA V100 GPU (每张 32GB 显存)** 组成的集群。
*   **训练细节**：
    *   使用 DeepSpeed 进行分布式优化。
    *   每个模型训练 3 个 Epoch。
    *   合成数据生成成本约为 300 英镑（GPT-5 API 调用费）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多模型横向对比（7 种不同架构和规模的模型）。
    *   **数据缩放实验**：测试了合成训练集从 500、1,500、5,000 到 15,000 封信函的性能变化。
    *   **鲁棒性测试**：在分布平衡的真实子集（Real 150）和合成测试集上进行了交叉验证。
    *   **临床盲测**：由两名神经内科医生对 60 封信函（真实 vs 合成）进行盲测，评估合成文本的真实性。
*   **充分性评价**：实验设计非常充分且客观。通过引入多运行次数的均值和标准差（SD），确保了结果的统计可靠性；通过真实与合成数据的交叉评估，验证了模型的泛化能力。

### 6. 主要结论与发现
*   **合成数据有效性**：仅在合成数据上训练的模型在真实测试集上表现优异，Micro-F1 最高达 **0.788 (细粒度)** 和 **0.858 (实用分类)**，性能匹配甚至超过了仅用真实数据训练的模型。
*   **标签格式影响**：结构化标签（Format 3/4）的表现始终优于直接的数值回归，说明模拟临床语言逻辑比强制数值转换更易于模型学习。
*   **缩放效应**：随着合成数据量增加，模型性能持续提升，但在 5,000 封后增益趋于平缓。
*   **可解释性**：引入 CoT 和证据锚定不仅保持了高精度，还为临床审计提供了透明度，方便医生快速核实模型输出。

### 7. 优点与亮点
*   **隐私保护与可重复性**：通过全合成数据框架，解决了医疗 NLP 中最棘手的数据共享和隐私合规问题，使研究具有高度的可重复性。
*   **任务忠实度高**：不同于通用的合成文本，该框架专门针对癫痫发作频率的复杂表达（如簇发、范围）进行了深度定制。
*   **开源友好**：侧重于 4B-14B 的开源模型，证明了在有限算力下部署高性能医疗专用工具的可行性。

### 8. 不足与局限
*   **风格偏差**：临床医生盲测发现合成信函存在“美式英语风格”、“过于全面（像保险文档）”以及“用词过于结构化”等特征，与英国 NHS 真实的口语化/多变风格仍有差距。
*   **数据源单一**：真实数据仅来自一家医院，跨机构、跨地区的泛化能力尚需进一步验证。
*   **真实数据标注限制**：由于真实数据仅有数值标注，无法在真实数据上直接训练 Format 4（带证据的 CoT），限制了混合训练的深度。
*   **应用范围**：目前仅聚焦于癫痫发作频率，尚未扩展到其他复杂的临床指标（如药物副作用、共病等）。

（完）
