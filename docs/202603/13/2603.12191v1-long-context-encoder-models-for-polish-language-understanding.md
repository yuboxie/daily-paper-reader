---
title: Long-Context Encoder Models for Polish Language Understanding
title_zh: 波兰语理解的长上下文编码器模型
authors: "Sławomir Dadas, Rafał Poświata, Marek Kozłowski, Małgorzata Grębowiec, Michał Perełkiewicz, Paweł Klimiuk, Przemysław Boruta"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12191v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 波兰语的扩展上下文窗口
tldr: 针对波兰语自然语言处理中编码器模型上下文窗口受限的问题，本文提出了一种支持8192个token的长文本编码器模型。该模型通过位置嵌入适配和全参数持续预训练两阶段方法开发，并利用知识蒸馏推出了压缩版本。在包含KLEJ和FinBench在内的25项任务评估中，该模型在长文本理解上表现卓越，同时保持了短文本处理的高水准，成为目前波兰语及多语言模型中的领先方案。
selection_source: fresh_fetch
motivation: 传统的波兰语BERT类编码器模型受限于较短的上下文窗口，无法有效处理长文档任务。
method: 采用位置嵌入适配和全参数持续预训练的两阶段训练流程，并结合知识蒸馏技术生成轻量化模型。
result: 在25项波兰语任务评估中，该模型在长文本任务上显著优于竞争对手，且在短文本任务中保持了同等质量。
conclusion: 本研究成功开发了高性能的长文本波兰语编码器模型，为长文档理解提供了高效且参数经济的解决方案。
---

## 摘要
虽然仅解码器（decoder-only）的大语言模型（LLMs）近期在自然语言处理（NLP）领域占据主导地位，但仅编码器（encoder-only）架构在判别式任务中仍然是具有成本效益且参数高效的标准。然而，像 BERT 这样的经典编码器受限于较短的上下文窗口，不足以处理长文档。在本文中，我们针对波兰语解决了这一局限性，引入了一个能够处理长达 8192 个 token 序列的高质量波兰语模型。该模型是通过采用两阶段训练程序开发的，包括位置嵌入适配和全参数持续预训练。此外，我们还提出了通过知识蒸馏训练的压缩模型变体。这些模型在 25 个任务上进行了评估，包括 KLEJ 基准测试、新引入的金融任务套件（FinBench）以及其他分类和回归任务，特别是那些需要长文档理解的任务。结果表明，我们的模型在波兰语和多语言模型中实现了最佳的平均性能，在长上下文任务中显著优于竞争方案，同时在短文本上保持了相当的质量。

## Abstract
While decoder-only Large Language Models (LLMs) have recently dominated the NLP landscape, encoder-only architectures remain a cost-effective and parameter-efficient standard for discriminative tasks. However, classic encoders like BERT are limited by a short context window, which is insufficient for processing long documents. In this paper, we address this limitation for the Polish by introducing a high-quality Polish model capable of processing sequences of up to 8192 tokens. The model was developed by employing a two-stage training procedure that involves positional embedding adaptation and full parameter continuous pre-training. Furthermore, we propose compressed model variants trained via knowledge distillation. The models were evaluated on 25 tasks, including the KLEJ benchmark, a newly introduced financial task suite (FinBench), and other classification and regression tasks, specifically those requiring long-document understanding. The results demonstrate that our model achieves the best average performance among Polish and multilingual models, significantly outperforming competitive solutions in long-context tasks while maintaining comparable quality on short texts.

---

## 论文详细总结（自动生成）

以下是对论文《Long-Context Encoder Models for Polish Language Understanding》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管生成式大模型（Decoder-only）目前占据主流，但在文本分类、命名实体识别等判别式任务中，编码器模型（Encoder-only）因其参数效率和部署成本优势仍具不可替代性。
*   **核心问题**：传统的波兰语编码器（如 BERT、RoBERTa）受限于 512 个 token 的短上下文窗口，无法处理现代应用中的长文档。
*   **整体含义**：本文旨在填补波兰语长文本编码器模型的空白，通过改进架构和训练策略，发布了支持 8192 token 的高性能波兰语模型 `polish-roberta-8k` 及其压缩版本。

### 2. 论文提出的方法论
*   **基础架构**：基于现有的高质量模型 `polish-roberta-large-v2`，将其位置嵌入层扩展至 8192 位。
*   **两阶段训练流程**：
    1.  **位置嵌入适配**：冻结模型所有权重，仅训练新初始化的长位置嵌入层，以避免梯度剧烈波动破坏预训练权重。
    2.  **全参数持续预训练**：在 1500 亿 token 的波兰语语料库上进行 4 个 epoch 的全量微调。
*   **关键技术优化**：
    1.  **Flash Attention**：引入 Flash Attention 机制以降低长序列处理的内存消耗和计算开销。
    2.  **无污染打包（Contamination-free packing）**：在拼接多个短文档时，通过修改注意力掩码防止不同文档间的信息交叉干扰。
    3.  **模型压缩**：采用知识蒸馏（MSE 损失函数）将 24 层模型压缩为 12 层和 6 层版本，并发现“渐进式蒸馏”（24L -> 12L -> 6L）效果优于直接蒸馏。

### 3. 实验设计
*   **评估任务**：共包含 25 个任务，涵盖回归、单标签分类和多标签分类。
*   **数据集构成**：
    *   **KLEJ Benchmark**：波兰语标准基准（9 个任务）。
    *   **FinBench**：论文新提出的金融领域基准（7 个任务），包含银行意图分类、金融情感分析等。
    *   **其他任务**：包括长文本情感分析（IMDB 翻译版）、法律文档分类（EURLEX）等 9 个任务。
*   **对比方法**：
    *   **波兰语专用模型**：HerBERT-large/base, Polish-RoBERTa-v2。
    *   **多语言模型**：XLM-RoBERTa-large/base, EuroBERT, mmBERT。

### 4. 资源与算力
*   **语料规模**：1500 亿（150B）个波兰语 token。
*   **训练参数**：Batch Size 为 128（序列长度 8192），单次迭代处理超过 100 万个 token。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。仅提到使用了 AdamW 优化器和多项式学习率调度。

### 5. 实验数量与充分性
*   **实验规模**：在 25 个公开/半公开数据集上进行了广泛测试，并额外在银行内部私有数据（邮件分类、抵押贷款文档）上进行了验证。
*   **客观性与公平性**：
    *   每个任务均运行 5 次独立实验并取平均值。
    *   针对新架构（如 EuroBERT）尝试了不同的学习率以确保对比的公正性。
    *   实验覆盖了从短文本到长文本、从通用领域到金融/法律专业领域的多种场景，实验设计非常充分且客观。

### 6. 论文的主要结论与发现
*   **性能领先**：`polish-roberta-8k` 在 25 个任务的平均得分上超过了所有现有的波兰语和多语言编码器模型。
*   **长文本优势**：在长文本任务（如 Banking-Long, IMDB, EURLEX）中，该模型显著优于传统的 512-token 模型。
*   **短文本保持**：通过特定的检查点融合策略，模型在扩展上下文的同时，并未牺牲在短文本任务上的表现。
*   **领域适配价值**：银行内部实验证明，结合长上下文和领域数据微调，可将分类准确率提升多达 8 个百分点。

### 7. 优点
*   **实用性强**：直接解决了波兰语 NLP 在长文档处理上的痛点，且模型已开源。
*   **贡献了新基准**：FinBench 的引入为波兰语金融领域的研究提供了重要工具。
*   **压缩方案高效**：通过渐进式蒸馏得到的轻量化模型在性能与速度之间取得了极佳平衡，适合边缘设备部署。

### 8. 不足与局限
*   **硬件细节缺失**：未披露训练所需的具体算力资源，不便于其他研究者评估复现成本。
*   **多语言模型对比结果**：实验显示最新的多语言模型（如 EuroBERT）在波兰语上表现不佳，这可能反映了这些模型在小语种预训练上的不足，但也限制了本文结论在非波兰语环境下的普适性。
*   **任务类型局限**：主要集中在判别式任务，未探讨长上下文编码器在 RAG（检索增强生成）系统检索端等更广泛场景的应用。

（完）
