---
title: "IndexCache: Accelerating Sparse Attention via Cross-Layer Index Reuse"
title_zh: IndexCache：通过跨层索引复用加速稀疏注意力
authors: "Yushi Bai, Qian Dong, Ting Jiang, Xin Lv, Zhengxiao Du, Aohan Zeng, Jie Tang, Juanzi Li"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12201v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 加速长上下文大语言模型工作流中的稀疏注意力机制
tldr: 针对长文本大模型推理中稀疏注意力索引器计算开销大的问题，本文提出 IndexCache。该方法利用相邻层间注意力索引的冗余性，将层分为运行索引器的“全量层”和复用索引的“共享层”。通过免训练的贪婪搜索或基于蒸馏的训练优化，IndexCache 在保持模型性能的同时，显著减少了索引计算量，在 30B 模型上实现了高达 1.82 倍的预填充加速和 1.48 倍的解码加速。
selection_source: fresh_fetch
motivation: 现有的稀疏注意力机制中，索引器在每一层重复运行且具有平方复杂度，导致长文本推理效率受限。
method: 提出 IndexCache 架构，通过跨层复用索引并结合贪婪搜索选择策略或多层蒸馏训练来优化索引分配。
result: "在 30B 模型上减少了 75% 的索引计算，实现了显著的推理加速且几乎没有精度损失。"
conclusion: 跨层索引复用是提升长文本稀疏注意力模型推理效率的有效途径，具有极高的工程应用价值。
---

## 摘要
长上下文智能体工作流已成为大语言模型的一个典型用例，这使得注意力机制的效率对于推理速度和部署成本都至关重要。稀疏注意力有效地解决了这一挑战，DeepSeek 稀疏注意力（DSA）是一个具有代表性的生产级解决方案：一个轻量级的闪电索引器（lightning indexer）为每个查询选择前 k 个最相关的标记，将核心注意力复杂度从 $O(L^2)$ 降低到 $O(Lk)$。然而，索引器本身仍保持 $O(L^2)$ 的复杂度，并且必须在每一层独立运行，尽管连续层之间产生的 top-k 选择具有高度相似性。我们提出了 IndexCache，它通过将层划分为运行自身索引器的少量“全量层”（Full layers）和仅复用最近全量层 top-k 索引的大多数“共享层”（Shared layers），来利用这种跨层冗余。我们提出了两种互补的方法来确定和优化这种配置。无需训练的 IndexCache 采用贪心搜索算法，通过直接最小化校准集上的语言建模损失来选择保留索引器的层，无需更新权重。感知训练的 IndexCache 引入了多层蒸馏损失，针对其服务的所有层的平均注意力分布来训练每个保留的索引器，使得即使是简单的交错模式也能达到全索引器的精度。在 30B DSA 模型上的实验结果表明，IndexCache 可以在质量损失微乎其微的情况下减少 75% 的索引器计算量，与标准 DSA 相比，预填充（prefill）速度提升高达 1.82 倍，解码（decode）速度提升高达 1.48 倍。这些积极的结果在我们对生产级 GLM-5 模型的初步实验中得到了进一步证实（图 1）。

## Abstract
Long-context agentic workflows have emerged as a defining use case for large language models, making attention efficiency critical for both inference speed and serving cost. Sparse attention addresses this challenge effectively, and DeepSeek Sparse Attention (DSA) is a representative production-grade solution: a lightweight lightning indexer selects the top-k most relevant tokens per query, reducing core attention from $O(L^2)$ to $O(Lk)$. However, the indexer itself retains $O(L^2)$ complexity and must run independently at every layer, despite the fact that the resulting top-k selections are highly similar across consecutive layers. We present IndexCache, which exploits this cross-layer redundancy by partitioning layers into a small set of Full layers that run their own indexers and a majority of Shared layers that simply reuse the nearest Full layer's top-k indices. We propose two complementary approaches to determine and optimize this configuration. Training-free IndexCache applies a greedy search algorithm that selects which layers to retain indexers by directly minimizing language modeling loss on a calibration set, requiring no weight updates. Training-aware IndexCache introduces a multi-layer distillation loss that trains each retained indexer against the averaged attention distributions of all layers it serves, enabling even simple interleaved patterns to match full-indexer accuracy. Experimental results on a 30B DSA model show that IndexCache can remove 75% of indexer computations with negligible quality degradation, achieving up to 1.82$\times$ prefill speedup and 1.48$\times$ decode speedup compared to standard DSA. These positive results are further confirmed by our preliminary experiments on the production-scale GLM-5 model (Figure 1).

---

## 论文详细总结（自动生成）

### IndexCache：通过跨层索引复用加速稀疏注意力

#### 1. 核心问题与整体含义
随着长上下文（Long-context）应用（如智能体工作流、长链条推理）的普及，注意力机制的计算效率成为瓶颈。**DeepSeek 稀疏注意力（DSA）** 虽然通过轻量级“索引器”将核心注意力复杂度降至 $O(Lk)$，但索引器本身在每一层仍需进行 $O(L^2)$ 的全量计算。在超长文本（如 200K tokens）下，索引器的耗时在预填充阶段占比可达 80% 以上。
**论文的核心动机**是利用相邻 Transformer 层之间 Token 选择的高度相似性（冗余性），通过跨层复用索引来消除冗余计算，从而在不损失模型质量的前提下显著提升推理速度。

#### 2. 方法论：IndexCache 架构
IndexCache 将模型层分为两类：**全量层（Full, F）**和**共享层（Shared, S）**。F 层运行索引器并缓存结果，S 层直接复用最近一个 F 层的索引。
*   **免训练 IndexCache（Training-free）：** 针对现成模型，通过**贪心搜索算法**确定最优的 F/S 模式。算法在校准集上运行，每一步选择将一个 F 层改为 S 层，目标是使语言建模（LM）损失增加最小。
*   **感知训练 IndexCache（Training-aware）：** 在模型训练阶段，引入**多层蒸馏损失**。让 F 层的索引器不仅学习本层的注意力分布，还学习其服务的所有 S 层的平均注意力分布。论文在数学上证明了这种多层蒸馏等价于对目标层注意力分布“质心”的蒸馏。
*   **推理流程：** 仅需在推理循环中增加一个简单的条件分支，S 层跳过索引器计算，直接读取缓存的索引张量，不增加额外的显存负担。

#### 3. 实验设计
*   **模型对象：** 30B 参数的 DSA 模型（基于 GLM-4.7-Flash）以及生产级的 744B 参数 GLM-5 模型。
*   **Benchmark：**
    *   **长文本任务：** MRCR v2（指代消解）、GraphWalks（图路径）、LongBench v2、RULER、AA-LCR。
    *   **通用与推理任务：** AIME 2025（数学）、GPQA-Diamond（科学问答）、LiveCodeBench（编程）、IFBench（指令遵循）。
*   **对比方法：** 标准 DSA（全量索引）、均匀间隔复用（Uniform Interleaving）以及基于相似度搜索的复用模式。

#### 4. 资源与算力
*   **硬件环境：** 使用 NVIDIA H100 GPU 节点进行推理性能测试。
*   **软件框架：** 基于 SGLang 推理引擎，启用了数据并行（DP）注意力。
*   **训练细节：** 30B 模型的感知训练包含 1000 步密集预热（Warm-up）和 4000 步稀疏训练，使用 200K 上下文长度的 SFT 数据。具体的 GPU 总数和总训练时长未在文中详细列出，但提到该流程比完整 DSA 训练更短。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **加速测试：** 覆盖了 10K 到 200K 不同长度下的预填充延迟、单请求解码吞吐量和全缓存解码吞吐量。
*   **消融实验：** 对比了免训练 vs 感知训练、均匀模式 vs 搜索模式、有无跨层蒸馏损失的效果。
*   **规模扩展：** 在 744B 的超大规模模型上验证了方案的有效性。
**评价：** 实验设计非常充分且客观。作者不仅展示了正面结果，还详细记录了“基于相似度的搜索”这一直觉上可行但实际效果不佳的负面尝试，增强了论文的可信度。

#### 6. 主要结论与发现
*   **显著加速：** 在 30B 模型上，保留 1/4 索引器时，200K 长度下的预填充速度提升 **1.82x**，解码速度提升 **1.48x**。
*   **质量保持：** 通过贪心搜索或感知训练，IndexCache 在减少 75% 索引计算的情况下，长文本和推理任务的得分与全量索引模型几乎持平。
*   **层敏感性：** 发现早期层和过渡层对索引器移除更敏感；经过感知训练后，模型能适应简单的均匀复用模式。
*   **生产可行性：** 在 GLM-5 上的初步实验证明了该技术在大规模生产环境中的扩展性。

#### 7. 优点
*   **工程实现极简：** 无需复杂的算子重写，仅靠逻辑分支即可实现加速。
*   **零显存开销：** 复用已有的索引张量，不占用额外显存。
*   **理论与实践结合：** 既提供了无需重训的快速部署方案，也提供了通过微调进一步压榨性能的训练方案。
*   **透明度高：** 详细公开了搜索出的层模式（Pattern）和失败的尝试。

#### 8. 不足与局限
*   **极端稀疏性限制：** 当索引器保留率降至 1/8 时，即使经过搜索，长文本性能仍会出现较明显的下降。
*   **搜索成本：** 免训练方案的贪心搜索虽然比重训快，但对于超深模型（如数百层），搜索过程仍需一定的时间和算力。
*   **架构依赖：** 该方法目前主要针对类似 DSA 的“索引器+核心注意力”架构，对于其他类型的稀疏注意力（如固定模式稀疏）的适用性有待验证。

（完）
