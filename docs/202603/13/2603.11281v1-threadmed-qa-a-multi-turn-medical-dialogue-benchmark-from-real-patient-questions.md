---
title: "ThReadMed-QA: A Multi-Turn Medical Dialogue Benchmark from Real Patient Questions"
title_zh: ThReadMed-QA：一个源自真实患者提问的多轮医疗对话基准
authors: "Monica Munnangi, Saiph Savage"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11281v1"
tags: ["query:ehr-tr"]
score: 9.0
evidence: 基于真实患者问题的多轮医疗对话基准
tldr: 针对现有医疗问答基准多为单轮对话、无法模拟真实咨询过程的问题，本文推出了ThReadMed-QA。这是一个包含2437个真实医患对话线程、共8204个问答对的多轮对话基准。通过对GPT-5、Llama 3.3等主流大模型的评估，发现模型在多轮对话中表现显著下降，且存在严重的错误传播现象。该研究揭示了当前模型在处理复杂、持续性医疗咨询时的局限性，并提出了衡量对话一致性的新指标。
selection_source: fresh_fetch
motivation: 现有的医疗问答基准大多局限于单轮交互，无法反映真实医疗咨询中患者不断追问和澄清的迭代过程。
method: 从r/AskDocs提取了2437个真实的医患对话线程，构建了包含8204个问答对的ThReadMed-QA基准，并引入CCS和EPR指标评估模型的多轮对话能力。
result: "即使是最强的GPT-5在多轮对话中的完全正确率也仅为41.2%，且所有模型在进入第三轮对话时错误率均显著上升。"
conclusion: 研究揭示了模型在单轮能力与多轮可靠性之间存在矛盾，且单次错误会显著增加后续回答出错的概率，强调了提升医疗模型对话一致性的必要性。
---

## 摘要
医疗问答基准主要评估单轮交互，未能捕捉到真实患者咨询中迭代和寻求澄清的本质。我们推出了 ThReadMed-QA，这是一个包含 2,437 条从 r/AskDocs 提取的完整回答的医患对话线程基准，涵盖了多达 9 轮的 8,204 个问答对。与以往依赖模拟对话、对抗性提示或考试风格问题的研究不同，ThReadMed-QA 捕捉了真实的患者追问和经过验证的医生回答，反映了患者在网上寻求医疗信息的自然方式。我们使用基于医生标准答案校准的 LLM-as-a-judge 评分标准，在包含 238 个对话（948 个问答对）的分层测试集上评估了五种最先进的大语言模型（LLM）——GPT-5、GPT-4o、Claude Haiku、Gemini 2.5 Flash 和 Llama 3.3 70B。即使是表现最强的模型 GPT-5，其完全正确的回答率也仅为 41.2%。所有五种模型从第 0 轮到第 2 轮的表现均显著下降（p < 0.001），到第三轮时，错误回答率大约增加了两倍。我们发现了单轮能力与多轮可靠性之间存在根本性的矛盾：初始表现最强的模型（GPT-5：75.2 分；Claude Haiku：72.3 分，满分 100 分）在第 2 轮时下降幅度最大（分别下降了 16.2 分和 25.0 分），而表现较弱的模型则趋于平稳或略有提升。我们引入了两个指标来量化多轮失败模式：对话一致性得分（CCS）和错误传播率（EPR）。CCS 显示，在 Claude Haiku 的对话中，近三分之一的对话在同一线程内会在完全正确和完全错误的回答之间摆动。EPR 表明，在所有模型中，单次错误的轮次会使后续轮次出错的概率增加 1.9 到 6.1 倍。

## Abstract
Medical question-answering benchmarks predominantly evaluate single-turn exchanges, failing to capture the iterative, clarification-seeking nature of real patient consultations. We introduce ThReadMed-QA, a benchmark of 2,437 fully-answered patient-physician conversation threads extracted from r/AskDocs, comprising 8,204 question-answer pairs across up to 9 turns. Unlike prior work relying on simulated dialogues, adversarial prompts, or exam-style questions, ThReadMed-QA captures authentic patient follow-up questions and verified physician responses, reflecting how patients naturally seek medical information online. We evaluate five state-of-the-art LLMs -- GPT-5, GPT-4o, Claude Haiku, Gemini 2.5 Flash, and Llama 3.3 70B -- on a stratified test split of 238 conversations (948 QA pairs) using a calibrated LLM-as-a-judge rubric grounded in physician ground truth. Even the strongest model, GPT-5, achieves only 41.2% fully-correct responses. All five models degrade significantly from turn 0 to turn 2 (p < 0.001), with wrong-answer rates roughly tripling by the third turn. We identify a fundamental tension between single-turn capability and multi-turn reliability: models with the strongest initial performance (GPT-5: 75.2; Claude Haiku: 72.3 out of 100) exhibit the steepest declines by turn 2 (dropping 16.2 and 25.0 points respectively), while weaker models plateau or marginally improve. We introduce two metrics to quantify multi-turn failure modes: Conversational Consistency Score (CCS) and Error Propagation Rate (EPR). CCS reveals that nearly one in three Claude Haiku conversations swings between a fully correct and a completely wrong response within the same thread. EPR shows that a single wrong turn raises the probability of a subsequent wrong turn by 1.9-6.1x across all models.

---

## 论文详细总结（自动生成）

这是一份关于论文《ThReadMed-QA: A Multi-Turn Medical Dialogue Benchmark from Real Patient Questions》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的医疗问答（QA）基准测试（如 MedQA, PubMedQA）大多侧重于单轮交互和医学考试类问题，无法反映真实医疗咨询中患者**迭代追问、表达模糊、带有情感偏见及逐步披露信息**的特征。
*   **研究背景**：随着大语言模型（LLM）在医疗领域的应用增加，模型在理想化单轮测试中的高分掩盖了其在真实多轮对话中的脆弱性。研究旨在填补“真实患者视角”与“多轮对话鲁棒性”评估的空白。

### 2. 论文提出的方法论
*   **核心思想**：构建一个基于真实医患交互（Reddit 的 r/AskDocs 社区）的多轮对话数据集，并开发一套专门衡量多轮对话质量与稳定性的评估体系。
*   **关键技术细节**：
    *   **数据构建**：从 180 万个帖子中筛选出由认证医生回答、且包含至少 2 轮追问的对话。通过 Jaccard 重叠启发式算法选择最相关的对话分支，最终形成 2,437 个完整对话线程（8,204 个 QA 对）。
    *   **评估指标**：
        1.  **轮次水平衰减（Turn-Level Degradation）**：衡量准确率随对话轮次增加的变化。
        2.  **对话一致性得分（CCS）**：公式为 $1 - \frac{1}{|C|} \sum (max(s) - min(s))$，衡量同一对话内模型表现的波动程度。
        3.  **错误传播率（EPR）**：衡量前一轮回答错误导致后一轮继续出错的概率。
    *   **LLM-as-a-Judge**：使用 GPT-4o 作为裁判，基于医生提供的真值（Ground Truth），按照“正确(1.0)”、“部分正确(0.5)”、“错误(0.0)”三个等级进行校准评分。

### 3. 实验设计
*   **数据集/场景**：ThReadMed-QA 测试集（238 个对话，948 个 QA 对），涵盖短、中、长三种对话长度。
*   **对比方法（评估模型）**：
    *   **闭源前沿模型**：GPT-5 (gpt-5-2025-08-07)、GPT-4o、Claude Haiku 4.5、Gemini 2.5 Flash。
    *   **开源模型**：Llama 3.3 70B。
*   **Benchmark 设定**：所有模型均采用 Zero-shot（零样本）推理，模拟真实在线医疗助手的表现。
*   **消融实验（Oracle-Physician）**：将模型之前的回答替换为医生的标准回答，以区分性能下降是由于“上下文污染（模型被自己的错话带偏）”还是“问题本身变难”。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练算力（如型号、数量），因为该研究主要基于现成模型的 API 进行推理评估。
*   **成本记录**：详细列出了 API 调用成本。推理 5 个模型总花费约 20 美元，使用 GPT-4o 进行 4,740 次自动评分花费约 31 美元。

### 5. 实验数量与充分性
*   **实验规模**：评估了 5 种主流模型，涵盖 948 个 QA 对。
*   **充分性分析**：
    *   **统计显著性**：使用了 Mann-Whitney U 检验和 Bootstrap 95% 置信区间，证明了性能衰减的统计学意义（p < 0.001）。
    *   **多维度评估**：不仅看平均分，还通过 CCS 和 EPR 深入分析了对话动态。
    *   **消融实验**：通过 Oracle 实验深入探讨了衰减的根源，实验设计逻辑严密、客观公平。

### 6. 论文的主要结论与发现
*   **性能普遍不足**：即使是最强的 GPT-5，在真实多轮场景下的完全正确率也仅为 41.2%。
*   **轮次衰减效应**：所有模型表现随轮次增加显著下降，到第 3 轮时错误率通常翻倍。
*   **单轮与多轮的矛盾**：初始表现最好的模型（如 GPT-5, Claude Haiku）在进入多轮后跌幅最惨，而表现平庸的模型（如 Llama）反而更“稳定”。
*   **错误传播严重**：一旦模型在某一轮出错，下一轮出错的概率会增加 1.9 到 6.1 倍。
*   **衰减主因**：消融实验证明，性能下降的主要原因是**后续追问本身难度更高**，而非仅仅是上下文污染。

### 7. 优点
*   **生态效度高**：数据源于真实患者，而非人工合成或考试题，真实反映了医疗咨询的复杂性。
*   **指标创新**：提出的 CCS 和 EPR 指标为多轮对话系统的可靠性评估提供了新工具。
*   **评估严谨**：LLM-as-a-judge 评分标准细致（区分了安全红旗、紧迫性匹配等），并与医生逻辑对齐。

### 8. 不足与局限
*   **真值局限性**：Reddit 医生的回答虽然经过验证，但并非正式临床记录，可能存在个体差异或不完整性。
*   **语言与模态单一**：目前仅限于英文文本，未涉及多语言或患者常提供的医学影像（多模态）数据。
*   **长期行为缺失**：未能捕捉跨越数天的长期随访行为。
*   **裁判模型偏差**：虽然经过校准，但使用 GPT-4o 评价其他模型（尤其是 GPT 系列）可能存在潜在的偏好偏差。

（完）
