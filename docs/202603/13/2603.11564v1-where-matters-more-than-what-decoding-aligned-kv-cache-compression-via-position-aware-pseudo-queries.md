---
title: "Where Matters More Than What: Decoding-aligned KV Cache Compression via Position-aware Pseudo Queries"
title_zh: 位置比内容更重要：通过位置感知伪查询实现的解码对齐 KV 缓存压缩
authors: "Zhenxu Tian, Yi Su, Juntao Li, Min Zhang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11564v1"
tags: ["query:lh-llm"]
score: 10.0
evidence: 大语言模型长上下文的KV缓存压缩
tldr: 针对大语言模型推理中KV缓存占用过大的挑战，本文提出DapQ框架。现有压缩方法多依赖预填充阶段的注意力模式，难以准确预测解码阶段的Token重要性。DapQ基于“位置信息比语义内容更关键”的发现，通过位置感知伪查询模拟解码过程，建立有效的观察窗口进行精准的Token剔除。实验证明，DapQ在极低缓存预算下仍能保持优异性能，显著提升了长文本推理效率。
selection_source: fresh_fetch
motivation: 现有KV缓存压缩方法主要依赖预填充阶段的注意力权重，无法准确模拟和对齐解码阶段的实际Token需求。
method: 提出DapQ框架，利用位置感知伪查询模拟未来解码Token的注意力行为，从而建立更有效的Token重要性评估机制。
result: "在多个基准测试中表现卓越，特别是在仅保留3% KV缓存的情况下，在NIAH任务上达到了99.5%的近乎无损性能。"
conclusion: 通过位置感知伪查询对齐解码过程，可以更精准地识别关键Token，从而在极低内存占用下实现高效的长文本推理。
---

## 摘要
键值（KV）缓存对于大语言模型（LLMs）的高效推理至关重要，但过长的上下文会显著增加 KV 缓存的内存占用。现有的 KV 缓存压缩方法通常依赖于预填充阶段 Prompt 观察窗口内的输入侧注意力模式来估计 Token 的重要性。由于这些评估并非源自解码过程，它们无法为未来的生成保留关键 Token。直观上，一个有效的观察窗口应该镜像解码阶段的查询，以准确反映生成过程将关注哪些 Token。然而，在推理过程中，真实的解码查询本质上是不可用的。为了构建伪查询来近似它们，我们发现位置信息比语义内容起着更关键的作用。基于这一见解，我们提出了通过位置感知伪查询实现的解码对齐 KV 缓存压缩（DapQ），这是一种新颖且轻量级的逐出框架，它利用位置感知伪查询来模拟输出 Token，从而建立一个有效的观察窗口进行重要性评估。它与实际的生成上下文紧密对齐，并实现了精确的 Token 逐出。在多个基准测试和大语言模型上的广泛评估表明，DapQ 取得了卓越的性能，特别是在严格的内存限制下（例如，在仅 3% 的 KV 缓存预算下，在 NIAH 上实现了高达 99.5% 的近乎无损性能）。

## Abstract
The Key-Value (KV) cache is crucial for efficient Large Language Models (LLMs) inference, but excessively long contexts drastically increase KV cache memory footprint. Existing KV cache compression methods typically rely on input-side attention patterns within a prompt observation window to estimate token importance during the prefill stage. They fail to preserve critical tokens for future generation since these assessments are not derived from the decoding process. Intuitively, an effective observation window should mirror the decoding-stage queries to accurately reflect which tokens the generation process will attend to. However, ground-truth decoding queries are inherently unavailable during inference. For constructing pseudo queries to approximate them, we find that positional information plays a more critical role than semantic content. Motivated by this insight, we propose decoding-aligned KV cache compression via position-aware pseudo queries (DapQ), a novel and lightweight eviction framework that leverages position-aware pseudo queries to simulate the output tokens, thereby establishing an effective observation window for importance assessment. It aligns closely with the actual generation context and enables precise token eviction. Extensive evaluations across multiple benchmarks and LLMs demonstrate that DapQ achieves superior performance, particularly under strict memory constraints (e.g., up to nearly lossless performance 99.5% on NIAH with 3% KV cache budgets).

---

## 论文详细总结（自动生成）

以下是对论文《Where Matters More Than What: Decoding-aligned KV Cache Compression via Position-aware Pseudo Queries》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：大语言模型（LLM）在处理长文本时，KV Cache（键值缓存）的内存占用随上下文长度线性增长，成为推理效率的瓶颈。
*   **核心问题**：现有的 KV Cache 压缩方法（如 SnapKV、PyramidKV）主要依赖预填充阶段（Prefill）末尾的观察窗口来评估 Token 重要性。然而，预填充阶段的注意力模式与实际解码阶段（Decoding）的查询（Query）存在**不一致性**，导致模型可能剔除了对未来生成至关重要的 Token。
*   **整体含义**：论文发现，在构建模拟解码阶段的“伪查询”时，**位置信息（Position）的影响远大于语义内容（Content）**。基于此，提出了 DapQ 框架，通过赋予伪查询准确的未来位置编码，实现与解码过程对齐的精准缓存压缩。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：在预填充阶段构造一组带有“未来位置 ID”的伪 Token，利用它们产生的伪查询（Pseudo Queries）来模拟真实解码过程中的注意力分布，从而识别并保留真正重要的 KV 对。
*   **关键技术流程**：
    1.  **构造伪序列**：在长度为 $L_p$ 的 Prompt 后拼接 $N$ 个伪 Token（内容可以是 Prompt 的首尾拼接或随机采样）。
    2.  **位置对齐**：关键步骤是为这 $N$ 个伪 Token 分配解码阶段前 $N$ 步的真实位置 ID（即 $L_p$ 到 $L_p + N - 1$），而非紧随其后的序号。
    3.  **重要性评估**：模型处理扩展序列后，提取伪 Token 对应的 Query 向量 $Q_{pseudo}$。计算 $Q_{pseudo}$ 与 Prompt 中所有 Key 的注意力得分，并进行累加。
    4.  **精准剔除**：根据累加得分保留 Top-K 个最重要的 KV 对，随后立即丢弃所有伪 Token 及其产生的缓存。
    5.  **正式解码**：从位置 $L_p$ 开始正常的自回归生成。

### 3. 实验设计
*   **数据集/场景**：
    *   **综合长文本理解**：LongBench, LongBenchV2。
    *   **压力测试与检索**：Ruler, Needle-in-a-Haystack (NIAH)。
    *   **长上下文评估集**：HELMET。
*   **对比方法**：
    *   **全量缓存**：FullKV。
    *   **主流压缩方法**：SnapKV, PyramidKV, H2O, StreamingLLM (SLM), LaCache。
*   **评估模型**：LLaMA-3-8B-Instruct, LLaMA-3.1-8B-Instruct, Qwen2.5-7B-Instruct, Qwen3-8B。

### 4. 资源与算力
*   **算力设备**：论文明确指出效率评估（吞吐量、首字延迟 TTFT）是在单张 **NVIDIA H20 96G GPU** 上完成的。
*   **软件环境**：Python 3.10, PyTorch 2.6.0, Transformers 4.53.0, Flash-Attention 2.7.4。
*   **训练时长**：由于该方法是**无需微调（Training-free）**的即插即用方案，因此不涉及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流模型上进行了测试。
    *   覆盖了 5 个大型 Benchmark，包含数十个子任务（如 QA、摘要、代码、多文档推理等）。
    *   设置了多种 KV Cache 预算（64, 128, 256, 512 等）以验证极端压缩下的鲁棒性。
*   **消融实验**：针对伪查询的**语义内容、窗口长度、插入位置**进行了详细的消融分析。
*   **充分性评价**：实验设计非常充分且客观。通过对比不同内容（DC）与不同位置（DP）的相似度，有力支撑了“位置主导”的核心论点。同时，在不同架构（LLaMA vs Qwen）上的表现一致，证明了方法的普适性。

### 6. 主要结论与发现
*   **位置主导论**：Query 向量的表示受位置编码（如 RoPE）的影响极大，语义内容次之。只要位置 ID 正确，即使内容无关，其注意力模式也能高度接近真实解码 Query。
*   **性能卓越**：DapQ 在极低预算下表现惊人。例如在 NIAH 任务中，仅保留 3% 的 KV 缓存（Size=256），在 LLaMA-3-8B 上达到了 **99.5%** 的准确率。
*   **效率平衡**：DapQ 的首字延迟（TTFT）和吞吐量与 SnapKV 等高效基准持平，额外计算开销几乎可以忽略不计。

### 7. 优点（亮点）
*   **理论深刻**：深入探讨了位置编码在 Query-Key 交互中的决定性作用，为 KV 压缩提供了新的视角。
*   **即插即用**：无需任何模型微调或参数更新，兼容现有 Transformer 架构。
*   **极端压缩下的高可靠性**：在其他方法性能大幅崩溃的极小缓存设置下，DapQ 依然能保持较好的上下文召回率。

### 8. 不足与局限
*   **语义内容仍有残余影响**：虽然位置是主导，但实验显示语义内容完全无关时相似度仍有下降（从 1.0 降至 0.72），说明伪查询的内容构造仍有优化空间。
*   **层间差异未细化**：目前对所有层采用统一的伪查询策略，未考虑 LLM 不同层对位置和语义敏感度的差异。
*   **应用限制**：主要针对自回归解码模型，对于非因果掩码（Non-causal mask）或特定架构（如 SSM）的适用性尚未讨论。

（完）
