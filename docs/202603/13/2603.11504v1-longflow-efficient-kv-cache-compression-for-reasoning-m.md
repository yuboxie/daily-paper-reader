---
title: "LongFlow: Efficient KV Cache Compression for Reasoning M"
title_zh: LongFlow：面向推理模型的高效 KV 缓存压缩
authors: "Yi Su, Zhenxu Tian, Dan Qiao, Yuechi Zhou, Juntao Li, Min Zhang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11504v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 针对长输出推理模型的高效KV缓存压缩
tldr: "针对OpenAI-o1和DeepSeek-R1等推理模型在长输出场景下KV缓存占用过高、部署成本巨大的问题，本文提出了LongFlow。该方法通过利用注意力计算的中间结果，仅凭当前查询即可实现高效的KV缓存重要性评估，并开发了融合FlashAttention的定制化算子。LongFlow在保持模型精度的同时，实现了高达80%的压缩率和11.8倍的吞吐量提升，显著优化了长序列生成的效率。"
selection_source: fresh_fetch
motivation: 推理模型生成的长输出导致KV缓存内存消耗巨大且带宽压力沉重，而现有优化方法在长输出场景下效率低下且计算开销大。
method: 提出一种基于当前查询中间结果的高效重要性评估指标，并开发了集成FlashAttention、评估与令牌剔除的定制化融合算子。
result: "在80%的KV缓存压缩率下，LongFlow实现了最高11.8倍的吞吐量提升，且对模型推理准确率的影响微乎其微。"
conclusion: LongFlow为长输出推理模型提供了一种低开销、高效率的KV缓存压缩方案，显著提升了大规模部署的系统性能。
---

## 摘要
最近的推理模型（如 OpenAI-o1 和 DeepSeek-R1）在数学推理和代码生成等复杂任务上表现出了强大的性能。然而，这种性能提升伴随着显著增长的输出序列，导致部署成本大幅增加。特别是，长输出需要庞大的 KV 缓存，导致高内存消耗以及注意力计算期间严重的带宽压力。大多数现有的 KV 缓存优化方法是为长输入、短输出场景设计的，对于推理模型的长输出设置效果不佳。此外，先前工作中的重要性评估计算开销巨大，且在长生成过程中需要持续重新评估时变得难以承受。为了应对这些挑战，我们提出了 LongFlow，这是一种 KV 缓存压缩方法，它采用了一种高效的重要性评估指标，该指标仅使用当前查询从注意力计算的中间结果中推导得出。这种设计引入的计算开销微乎其微，且不需要辅助存储。我们进一步开发了一个自定义算子，将 FlashAttention、重要性评估和令牌剔除（token eviction）融合进单个优化算子中，从而提高了系统级效率。实验表明，LongFlow 在 80% 的 KV 缓存压缩率下实现了高达 11.8 倍的吞吐量提升，且对模型准确性的影响极小。

## Abstract
Recent reasoning models such as OpenAI-o1 and DeepSeek-R1 have shown strong performance on complex tasks including mathematical reasoning and code generation. However, this performance gain comes with substantially longer output sequences, leading to significantly increased deployment costs. In particular, long outputs require large KV caches, resulting in high memory consumption and severe bandwidth pressure during attention computation. Most existing KV cache optimization methods are designed for long-input, short-output scenarios and are ineffective for the long-output setting of reasoning models. Moreover, importance estimation in prior work is computationally expensive and becomes prohibitive when continuous re-evaluation is required during long generation. To address these challenges, we propose LongFlow, a KV cache compression method with an efficient importance estimation metric derived from an intermediate result of attention computation using only the current query. This design introduces negligible computational overhead and requires no auxiliary storage. We further develop a custom kernel that fuses FlashAttention, importance estimation, and token eviction into a single optimized operator, improving system-level efficiency. Experiments show that LongFlow achieves up to an 11.8 times throughput improvement with 80% KV cache compression with minimal impact on model accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：LongFlow —— 面向推理模型的高效 KV 缓存压缩

#### 1. 核心问题与整体含义（研究动机和背景）
随着 OpenAI-o1 和 DeepSeek-R1 等推理模型的兴起，模型通过长思维链（CoT）生成大量中间步骤来解决复杂问题。这种“长输出”范式带来了严峻的挑战：
*   **内存与带宽瓶颈**：极长的输出序列导致 KV 缓存（KV Cache）迅速膨胀，消耗海量显存并产生巨大的计算带宽压力。
*   **现有方法失效**：传统的 KV 缓存压缩方法（如 H2O、SnapKV）多针对“长输入、短输出”场景设计。在长生成过程中，这些方法往往需要昂贵的历史信息重新评估或额外的辅助存储，导致推理延迟增加，难以适配现代融合算子（如 FlashAttention）。
*   **研究目标**：开发一种专门针对长输出推理场景、计算开销极低且能显著提升吞吐量的 KV 缓存压缩方案。

#### 2. 方法论：核心思想与技术细节
LongFlow 的核心在于**“轻量化设计哲学”**，即实现“零历史依赖”和“零成本评估”。
*   **核心思想**：利用当前查询（Query）作为评估历史令牌重要性的唯一依据。研究发现，相邻生成步骤的查询具有极高的相似性，因此当前步骤的注意力分布可以作为下一帧重要性的可靠代理。
*   **重要性指标（LongFlowScore）**：
    *   该指标直接从注意力计算的中间结果——**贡献向量（Contribution Vector）**中推导。
    *   公式定义：$LongFlowScore(t_i) = \alpha_{it} \sum_{l=1}^d |(v_i)_l|$。其中 $\alpha_{it}$ 是注意力权重，$v_i$ 是 Value 向量。
    *   物理含义：它衡量了特定令牌对当前注意力输出的实际贡献大小。
*   **系统级优化（定制内核）**：
    *   开发了一个基于 Triton 的**融合算子**，将 FlashAttention 计算、重要性评估和令牌剔除（Token Eviction）整合进同一个 GPU Kernel 中。
    *   采用**静态内存管理**：预先分配 KV 缓存空间，通过覆盖旧令牌的方式避免内存碎片和动态分配开销。

#### 3. 实验设计
*   **模型选择**：DeepSeek-R1-Distill-Llama-8B 以及 Qwen3 系列（涵盖 0.6B、1.7B、4B、8B 四个尺寸）。
*   **数据集/场景**：
    *   **数学推理**：MATH-500, AMC-23, AIME-24, AIME-25, GSM8K。
    *   **科学与竞赛**：GPQA（研究生水平）、Minerva（大学水平）、Olympiad-Bench（奥赛水平）。
*   **Benchmark 与对比方法**：
    *   **Vanilla**：无压缩的标准注意力。
    *   **H2O**：基于累积注意力得分的经典方法。
    *   **VATP**：结合注意力得分与 Value 向量信息的进阶方法。
    *   **R-KV**：专门针对推理模型冗余模式设计的最新方法。
*   **设置**：生成长度设为 16,000 令牌，KV 缓存预算设为 2,400 或 3,200（约 80% 压缩率）。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在 **NVIDIA A100 40GB GPU** 上进行。
*   **算力细节**：文中未明确给出总训练时长（因为该方法主要是推理侧的训练后/免训练优化），但详细对比了推理延迟。例如，在 Qwen3-8B 上，定制内核将注意力模块延迟从 47ms 降低到了 8ms。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 2 个模型家族、5 个模型尺寸、8 个极具挑战性的推理数据集上进行了测试。
*   **充分性**：
    *   涵盖了从 0.6B 到 8B 的模型缩放实验，验证了方法的泛化性。
    *   进行了吞吐量、内存占用和内存碎片的系统级对比。
    *   提供了详细的数学证明（附录 A）来支撑其重要性指标的合理性。
*   **客观性**：对比了多种 SOTA 压缩算法，并设置了不同的 KV 预算以观察性能退化曲线，实验设计较为严谨。

#### 6. 主要结论与发现
*   **性能飞跃**：在 80% 的 KV 缓存压缩率下，LongFlow 实现了高达 **11.8 倍** 的吞吐量提升。
*   **精度保持**：在大幅压缩的情况下，模型在 AIME、MATH 等硬核推理任务上的准确率损失极小（例如 DeepSeek-R1-8B 的平均性能下降仅约 0.08%）。
*   **内存管理**：相比 H2O 等方法，LongFlow 的静态内存方案显著减少了内存碎片，支持更大的 Batch Size。

#### 7. 优点：亮点总结
*   **极低开销**：重要性评估完全复用注意力计算的中间值，几乎不产生额外计算负担。
*   **无需辅助存储**：不需要像其他方法那样存储历史注意力累加值或过去的查询向量。
*   **软硬件协同**：通过定制 Triton 内核实现了算法与底层硬件的高效适配，真正将算法优势转化为端到端的推理加速。

#### 8. 不足与局限
*   **查询稳定性依赖**：该方法假设相邻查询具有高相似性。如果遇到任务剧烈切换或高度随机的解码场景，重要性评估的准确性可能会下降。
*   **场景局限**：目前主要针对长输出的自回归解码阶段进行优化，对于长输入预填充（Prefill）阶段的优化涉及较少。
*   **剔除不可逆**：作为一种剔除式压缩方法，一旦令牌被移除便无法找回，在极极端长文本下可能存在潜在的信息丢失风险。

（完）
