---
title: "ZeroSense:How Vision matters in Long Context Compression"
title_zh: ZeroSense：视觉在长上下文压缩中的重要性
authors: "Yonghan Gao, Zehong Chen, Lijian Xu, Jingzhi Chen, Jingwei Guan, Xingyu Zeng"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11846v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 用于评估多模态大模型长上下文压缩的ZeroSense基准
tldr: 针对视觉文本压缩（VTC）在长文本建模中的应用，本文指出传统基于下游任务的评估方法受多模态大模型（MLLM）语言先验影响，无法准确衡量文本还原度。为此，作者提出了一个解耦评估框架及ZeroSense基准测试，通过消除语义关联性来纯粹评估VTC质量。实验表明，VTC质量与下游任务表现存在显著差异，证明了解耦评估的必要性。
selection_source: fresh_fetch
motivation: 现有的视觉文本压缩评估过度依赖下游任务，容易受到大模型语言先验的干扰，导致无法真实反映文本压缩的质量。
method: 提出了一种解耦评估框架和ZeroSense基准测试，通过使用低语义关联的样本来消除上下文依赖，从而纯粹衡量VTC的还原能力。
result: 实验证明VTC的实际压缩质量与下游任务的准确率之间存在显著背离，揭示了传统评估指标的局限性。
conclusion: 该研究强调了在长文本压缩中解耦模型推理能力与视觉压缩质量的重要性，为未来VTC技术的优化提供了更准确的评价标准。
---

## 摘要
最近，以 DeepSeek-OCR 为代表的视觉文本压缩（VTC）方法通过利用文本到图像渲染，在长上下文建模任务中实现了令人瞩目的高 Token 压缩率。然而，现有的评估协议严重依赖于下游任务的性能。由于多模态大语言模型（MLLM）具有强大的固有语言先验，此类评估指标无法准确衡量文本的保留情况。在这项工作中，我们引入了一个新的评估框架，通过解耦 MLLM 的能力来忠实地评估 VTC 质量。在该框架内，我们进一步引入了 ZeroSense 基准测试，以确保测试样本具有较低的语义相关性。通过消除上下文依赖，我们的基准测试保证了评估结果纯粹反映 VTC 质量，而不受下游模型语义推理能力的影响。在多个数据集上进行的广泛实验表明，VTC 质量与下游任务准确率存在显著差异，这凸显了我们解耦评估框架的必要性。

## Abstract
Recent visual-text compression (VTC) methods, typified by DeepSeek-OCR, report impressive high token compression ratios for long-context modeling tasks by leveraging text-to-image rendering. However, existing evaluation protocols heavily rely on downstream task performance. Such evaluation metrics fail to accurately measure text preservation due to the strong inherent linguistic priors of Multimodal Large Language Models (MLLMs). In this work, we introduce a new evaluation framework that decouples MLLMs' capabilities to faithfully assess VTC quality. Within this framework, we further introduce the ZeroSense Benchmark to ensure low semantic correlation of testing samples. By eliminating contextual dependencies, our benchmark guarantees that the evaluation results are purely reflective of VTC quality, unaffected by the semantic inference capabilities of downstream models. Extensive experiments across multiple datasets demonstrate that VTC quality and downstream task accuracy diverge significantly, highlighting the necessity of our decoupled evaluation framework.

---

## 论文详细总结（自动生成）

### 论文总结：ZeroSense - 视觉在长上下文压缩中的重要性

#### 1. 核心问题与研究动机
随着长上下文处理需求的增加，自注意力机制的平方复杂度成为瓶颈。**视觉文本压缩（Visual-Text Compression, VTC）**（如 DeepSeek-OCR）通过将长文本渲染为紧凑的文档图像来替代数千个文本 Token，从而实现推理加速。
然而，作者发现现有的评估协议存在严重缺陷：**多模态大模型（MLLM）具有强大的语言先验（Linguistic Priors）**。即使压缩导致图像模糊或丢失信息，模型也能根据上下文“猜出”文本。这掩盖了 VTC 策略真实的文本保留质量。论文的核心目标是**解耦模型推理能力与视觉感知质量**，真实衡量 VTC 的有效性。

#### 2. 方法论
论文提出了一个解耦评估框架和 **ZeroSense 基准测试**：
*   **解耦框架公式**：将端到端 OCR 概率 $F$ 分解为：
    $F \approx F_{prior} + OCR_{raw} \cdot K_{quality}$
    其中，$F_{prior}$ 是语义先验驱动的猜测概率，$OCR_{raw}$ 是模型原始的字符提取能力，$K_{quality}$ 是 VTC 策略的文本保留率。
*   **ZeroSense 基准测试构建**：
    1.  **特征提取**：从原始数据集（如 Fox, Omni）中逆向工程提取布局特征（字体大小、边界框、文本容量）。
    2.  **语义无关文本生成**：利用因果语言模型（如 Qwen2.5-7B），强制采样低概率 Token（$P < 10^{-5}$），生成完全没有语义关联的字符序列，创造“语义真空”。
    3.  **渲染与合成**：将生成的无意义文本填入提取的布局中，并保留原始背景纹理和噪声。
*   **核心思想**：通过在“语义真空”环境下测试，迫使模型只能依赖视觉信号，从而准确测量 $K_{quality}$。

#### 3. 实验设计
*   **数据集**：使用了 **Fox**、**Omni** 和 **DDI-100 v1.3** 作为原始数据源，并基于此构建了对应的 **ZeroSense** 版本。
*   **对比方法**：主要针对 **DeepSeek-OCR** 架构及其渲染策略进行评估。
*   **评估指标**：字符级准确率（Character-level Precision）和归一化编辑距离（Normalized Edit Distance）。
*   **实验场景**：测试了从 7.5× 到 17.5× 的不同 Token 压缩比例。

#### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA RTX A6000 GPU** 上运行。
*   **软件框架**：使用 **vLLM** 进行模型部署。
*   **模型**：使用 Qwen2.5-7B 作为生成 ZeroSense 文本的基础模型。
*   文中未明确提及具体的训练时长，因为该研究侧重于评估框架的构建和推理测试，而非从头训练大型模型。

#### 5. 实验数量与充分性
*   **实验规模**：在多个主流文档理解数据集上进行了广泛测试，涵盖了不同的文本密度和布局。
*   **消融与对比**：进行了初步的扰动实验（如单词随机打乱），证明简单的打乱无法完全消除语义关联，从而凸显了 ZeroSense 这种基于低概率采样方法的必要性。
*   **客观性**：通过数学公式定义了评估指标，并使用了人类标注的高保真样本作为校准基准，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **性能背离**：VTC 的实际质量与下游任务准确率显著背离。在 Fox 数据集上，传统评估显示 17.5× 压缩下仍有 81.3% 的准确率，但 ZeroSense 揭示其真实的视觉保留率（$K_{quality}$）仅为 27.4%。
*   **语义补偿效应**：随着压缩率增加，模型对语义先验（$F_{prior}$）的依赖程度显著提高（在 Fox 上从 23.8% 升至 67%）。
*   **数据集差异**：Omni 数据集由于包含大量极端高密度样本，其语义补偿效应弱于 Fox，这解释了不同数据集在 VTC 评估中的表现差异。

#### 7. 优点
*   **视角独特**：首次指出了 MLLM 语言先验在 VTC 评估中的“遮羞布”作用。
*   **方法科学**：通过解耦框架将复杂的端到端表现拆解为可量化的变量。
*   **基准实用**：ZeroSense 能够生成保持原始布局但消除语义的样本，为未来的 OCR 和 VTC 研究提供了更纯粹的测试工具。

#### 8. 不足与局限
*   **覆盖范围**：主要针对文本到图像的渲染压缩（如 DeepSeek-OCR），对于其他形式的视觉编码压缩（如特征图下采样）的适用性讨论较少。
*   **模型偏差**：评估结果可能受限于所选用的基础 MLLM 的原始 OCR 能力（$OCR_{raw}$），虽然框架尝试解耦，但不同模型的感知边界可能影响 $K_{quality}$ 的绝对值计算。
*   **应用限制**：目前主要关注文档类长文本，对于包含大量图表、复杂公式的非结构化视觉压缩评估尚待进一步扩展。

（完）
