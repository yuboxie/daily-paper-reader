---
title: Multimodal Emotion Recognition via Bi-directional Cross-Attention and Temporal Modeling
title_zh: 基于双向交叉注意力与时序建模的多模态情感识别
authors: "Junhyeong Byeon, Jeongyeol Kim, Sejoon Lim"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11971v1"
tags: ["query:llm-ac"]
score: 9.0
evidence: 视频数据中结合时序建模的多模态情感识别
tldr: 针对野外环境下单模态情感识别不足的问题，本文提出一种多模态框架。该方法利用预训练的CLIP和Wav2Vec 2.0提取视听特征，通过时间卷积网络（TCN）建模面部动态，并引入双向交叉注意力机制实现模态间的深度融合。此外，结合文本引导的对比学习增强语义对齐。在第10届ABAW挑战赛EXPR任务中，该框架显著提升了识别性能，为复杂环境下的情感分析提供了强有力的基准。
selection_source: fresh_fetch
motivation: 针对野外视频中光照、姿态及背景噪声等干扰，单模态特征难以全面捕捉复杂且动态的人类情感信息。
method: 采用CLIP和Wav2Vec 2.0作为骨干网络，结合TCN建模时序特征，并通过双向交叉注意力机制与文本引导对比学习进行多模态融合。
result: 在第10届ABAW挑战赛的EXPR基准测试中，该多模态框架的表现显著优于单模态模型，证明了其在复杂环境下的有效性。
conclusion: 实验证明，结合时序视觉建模、音频表征学习及跨模态融合技术能有效提升真实场景下情感识别的鲁棒性。
---

## 摘要
野外（in-the-wild）视频数据中的情感识别仍然是一个具有挑战性的问题，原因在于面部外观、头部姿态、光照、背景噪声的巨大变化，以及人类情感固有的动态特性。仅依赖单一模态（如面部表情或语音）通常不足以捕捉这些复杂的情感线索。为了解决这一问题，我们为第10届野外情感行为分析（ABAW）挑战赛中的表情（EXPR）识别任务提出了一种多模态情感识别框架。我们的方法利用大规模预训练模型，即用于视觉编码的 CLIP 和用于音频表示学习的 Wav2Vec 2.0，作为冻结的主干网络。为了建模面部表情序列中的时序依赖关系，我们在固定长度的视频窗口上采用了时序卷积网络（TCN）。此外，我们引入了一个双向交叉注意力融合模块，其中视觉和音频特征进行对称交互，以增强跨模态上下文关联并捕捉互补的情感信息。随后使用一个轻量级分类头进行最终的情感预测。我们进一步结合了基于 CLIP 文本特征的文本引导对比目标，以促进语义对齐的视觉表示。在 ABAW 第10届 EXPR 基准测试上的实验结果表明，所提出的框架提供了一个强大的多模态基准，并取得了优于单模态建模的性能。这些结果证明了结合时序视觉建模、音频表示学习和跨模态融合在不受限的真实世界环境中进行鲁棒情感识别的有效性。

## Abstract
Emotion recognition in in-the-wild video data remains a challenging problem due to large variations in facial appearance, head pose, illumination, background noise, and the inherently dynamic nature of human affect. Relying on a single modality, such as facial expressions or speech, is often insufficient to capture these complex emotional cues. To address this issue, we propose a multimodal emotion recognition framework for the Expression (EXPR) Recognition task in the 10th Affective Behavior Analysis in-the-wild (ABAW) Challenge.   Our approach leverages large-scale pre-trained models, namely CLIP for visual encoding and Wav2Vec 2.0 for audio representation learning, as frozen backbone networks. To model temporal dependencies in facial expression sequences, we employ a Temporal Convolutional Network (TCN) over fixed-length video windows. In addition, we introduce a bi-directional cross-attention fusion module, in which visual and audio features interact symmetrically to enhance cross-modal contextualization and capture complementary emotional information. A lightweight classification head is then used for final emotion prediction. We further incorporate a text-guided contrastive objective based on CLIP text features to encourage semantically aligned visual representations.   Experimental results on the ABAW 10th EXPR benchmark show that the proposed framework provides a strong multimodal baseline and achieves improved performance over unimodal modeling. These results demonstrate the effectiveness of combining temporal visual modeling, audio representation learning, and cross-modal fusion for robust emotion recognition in unconstrained real-world environments.

---

## 论文详细总结（自动生成）

以下是对论文《Multimodal Emotion Recognition via Bi-directional Cross-Attention and Temporal Modeling》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在“野外”（in-the-wild）环境下，面部表情识别面临光照变化、头部姿态不一、遮挡及背景噪声等严峻挑战。
*   **核心问题**：单模态（仅视觉或仅音频）难以捕捉复杂且动态的人类情感。现有的多模态融合方法往往缺乏对视听双向交互的深度建模，且对表情随时间演变的动态特征利用不足。
*   **研究目标**：提出一个整合了大规模预训练模型、时序卷积网络（TCN）和双向交叉注意力机制的框架，以提升在复杂真实场景下的情感识别鲁棒性。

### 2. 方法论
该框架由五个核心组件构成：
*   **特征提取（冻结骨干网）**：
    *   **视觉流**：使用 **CLIP (ViT-B/32)** 提取帧级视觉特征。
    *   **音频流**：使用 **Wav2Vec 2.0** 提取原始音频的上下文表征。
    *   **文本引导**：利用 CLIP 文本编码器生成“A face expressing [Emotion]”的语义嵌入。
*   **时序建模与适配**：
    *   **视觉 TCN**：采用 6 层**时序卷积网络（TCN）**，通过扩张因果卷积（Dilated Causal Convolutions）扩大感受野，捕捉面部表情的动态演变。
    *   **音频适配器**：通过线性层和归一化将音频特征维度对齐至视觉特征维度。
*   **双向交叉注意力融合（Bi-directional Cross-Attention）**：
    *   设计对称的注意力机制：视觉特征作为 Query 去查询音频特征（V-to-A），同时音频特征作为 Query 去查询视觉特征（A-to-V）。这种设计允许两种模态在信息缺失时互补。
*   **分类头**：经过时间平均池化后，将视听融合特征拼接，输入多层感知机（MLP）进行 8 类情感分类。
*   **文本引导对比学习（Text-Guided Contrastive Learning）**：
    *   在训练中引入对比损失（Contrastive Loss），强制视觉特征在嵌入空间中与对应的情感文本描述对齐，增强表征的语义判别力。

### 3. 实验设计
*   **数据集**：第 10 届 ABAW（Affective Behavior Analysis in-the-wild）挑战赛的 **EXPR（表情识别）任务**数据集。
*   **Benchmark（基准）**：官方提供的基于预训练 VGGFace 的基准模型。
*   **对比设置**：
    *   对比了不同时间窗口长度（30 帧 vs 60 帧）对识别性能的影响。
    *   对比了所提多模态框架与官方单模态/基础基准的性能差异。
*   **评价指标**：准确率（Accuracy）和 **Macro F1-score**（官方主要指标）。

### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA RTX A5000 GPU**。
*   **训练细节**：
    *   优化器：AdamW，学习率 $1 \times 10^{-5}$，采用余弦退火调度。
    *   训练轮数：30 Epochs。
    *   批大小（Batch Size）：64，并使用了梯度累积技术。
    *   随机种子：固定为 42 以保证可重复性。

### 5. 实验数量与充分性
*   **实验规模**：论文主要展示了在验证集上的性能对比，重点在于验证 30 帧和 60 帧两种配置。
*   **充分性评价**：
    *   **客观性**：通过与官方 Baseline 对比，证明了多模态及 TCN 建模的有效性。
    *   **局限性**：实验组数相对较少，缺乏详细的**消融实验**（例如：分别去掉 TCN、双向交叉注意力或对比学习损失，以量化各模块的具体贡献）。此外，未提供在测试集上的最终排名表现。

### 6. 主要结论与发现
*   **多模态优越性**：所提框架在 Macro F1 指标上显著超过官方基准（从 0.2500 提升至 0.3334）。
*   **时序上下文的重要性**：60 帧窗口的表现优于 30 帧窗口，说明更长的时序上下文有助于模型理解表情的形成与转换过程。
*   **语义对齐有效**：引入 CLIP 文本先验有助于在噪声较大的野外数据中维持特征的语义稳定性。

### 7. 优点
*   **强大的特征底座**：结合了视觉语言模型（CLIP）和自监督音频模型（Wav2Vec 2.0）的优势。
*   **融合机制科学**：双向交叉注意力比简单的特征拼接（Concatenation）更能捕捉模态间的深层关联。
*   **时序处理高效**：使用 TCN 代替传统的 RNN/LSTM，在保持并行计算效率的同时有效扩大了感受野。

### 8. 不足与局限
*   **消融分析缺失**：没有拆解各创新组件（TCN, Cross-Attention, Contrastive Loss）对最终性能的贡献占比。
*   **模态单一性**：虽然是多模态，但仅限于视听，未考虑文本（如语音转文字 ASR）或其他元数据。
*   **应用限制**：模型依赖于预训练的大模型特征，虽然 Backbone 冻结，但在实时推理场景下，提取 CLIP 和 Wav2Vec 2.0 特征的计算开销可能较高。

（完）
