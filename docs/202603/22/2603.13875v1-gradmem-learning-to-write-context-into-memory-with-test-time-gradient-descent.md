---
title: "GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent"
title_zh: GradMem：通过测试时梯度下降学习将上下文写入内存
authors: "Yuri Kuratov, Matvey Kairov, Aydar Bulatov, Ivan Rodkin, Mikhail Burtsev"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13875v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 长上下文的压缩内存和KV缓存替代方案
tldr: 针对大语言模型处理长上下文时KV缓存内存开销大的问题，本文提出GradMem。该方法通过在推理阶段对少量前缀记忆令牌进行梯度下降优化，将上下文信息压缩进固定大小的记忆状态中。GradMem利用自监督重构损失进行迭代式写入，相比传统的单向前向写入方法，在键值检索和自然语言任务（如bAbI、SQuAD）上表现出更强的记忆容量和泛化能力。
selection_source: fresh_fetch
motivation: 传统的KV缓存机制在处理长上下文时内存开销巨大，需要一种更高效的压缩记忆机制来替代。
method: 提出GradMem方法，在模型权重冻结的情况下，通过测试时梯度下降优化前缀记忆令牌，以最小化上下文重构损失。
result: 在关联键值检索任务中优于前向写入方法，并在bAbI和SQuAD等自然语言任务中展现出竞争力的性能。
conclusion: GradMem证明了通过测试时优化进行损失驱动的记忆写入，能比传统前向方法更有效地扩展模型处理长上下文的能力。
---

## 摘要
许多大语言模型应用需要基于长上下文进行条件生成。Transformer 通常通过存储每层过去激活的大型 KV 缓存（KV-cache）来支持这一点，但这会产生巨大的内存开销。一个理想的替代方案是压缩内存：一次性读取上下文，将其存储在紧凑状态中，并根据该状态回答多个查询。我们在上下文移除设置下对此进行研究，即模型在推理时必须在无法访问原始上下文的情况下生成答案。我们引入了 GradMem，它通过逐样本的测试时优化将上下文写入内存。给定一个上下文，GradMem 在保持模型权重冻结的情况下，对一小组前缀内存标记（prefix memory tokens）执行几步梯度下降。GradMem 显式地优化模型级的自监督上下文重建损失，从而实现具有迭代误差校正的损失驱动写入操作，这与仅前向（forward-only）的方法不同。在关联键值检索任务上，GradMem 在相同内存大小的情况下优于仅前向的内存写入器，且额外的梯度步骤比重复的前向写入更有效地扩展了容量。我们进一步证明 GradMem 可以迁移到合成基准之外：配合预训练语言模型，它在包括 bAbI 和 SQuAD 变体在内的自然语言任务上取得了具有竞争力的结果，且仅依赖于编码在内存中的信息。

## Abstract
Many large language model applications require conditioning on long contexts. Transformers typically support this by storing a large per-layer KV-cache of past activations, which incurs substantial memory overhead. A desirable alternative is ompressive memory: read a context once, store it in a compact state, and answer many queries from that state. We study this in a context removal setting, where the model must generate an answer without access to the original context at inference time. We introduce GradMem, which writes context into memory via per-sample test-time optimization. Given a context, GradMem performs a few steps of gradient descent on a small set of prefix memory tokens while keeping model weights frozen. GradMem explicitly optimizes a model-level self-supervised context reconstruction loss, resulting in a loss-driven write operation with iterative error correction, unlike forward-only methods. On associative key--value retrieval, GradMem outperforms forward-only memory writers with the same memory size, and additional gradient steps scale capacity much more effectively than repeated forward writes. We further show that GradMem transfers beyond synthetic benchmarks: with pretrained language models, it attains competitive results on natural language tasks including bAbI and SQuAD variants, relying only on information encoded in memory.

---

## 论文详细总结（自动生成）

以下是对论文《GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在处理长上下文时，通常依赖 KV 缓存（KV-cache）存储中间激活值。然而，KV 缓存随序列长度线性增长，带来了巨大的内存开销和计算负担。
*   **核心问题**：如何将长上下文压缩进一个固定大小、可重用的紧凑记忆状态（Memory State），且无需对模型进行全量微调？
*   **研究动机**：现有的“前向写入”（Forward-only）方法（如 RMT）在压缩时缺乏反馈，无法纠正写入过程中的信息丢失。本文受“测试时训练”（TTT）启发，探索利用梯度下降在推理阶段动态优化记忆表征。

### 2. 方法论
*   **核心思想**：将内存表征视为一组可优化的“前缀标记”（Memory Tokens）。在推理的写入阶段，通过梯度下降调整这些标记的数值，使其能够“重建”输入的上下文。
*   **关键技术细节**：
    *   **WRITE 阶段（写入）**：冻结模型参数 $\theta$，针对当前上下文 $C$，通过 $K$ 步梯度下降优化内存标记 $M$。优化目标是最小化自监督重构损失 $L_{write}$（即模型在给定 $M$ 的情况下预测上下文 $C$ 的交叉熵损失）。
    *   **READ 阶段（读取）**：丢弃原始上下文 $C$，模型仅根据优化后的内存 $\hat{M}$ 和新的查询 $Q$ 生成答案。
    *   **元学习（Meta-learning）**：为了让模型在极少数步数（如 $K \le 5$）内完成有效写入，研究者采用 MAML（模型无关元学习）框架，在训练阶段同时优化模型参数 $\theta$ 和内存的初始值 $M_0$。
*   **算法流程**：初始化 $M_0 \rightarrow$ 计算上下文重构损失 $\rightarrow$ 执行 $K$ 步梯度更新得到 $\hat{M} \rightarrow$ 基于 $\hat{M}$ 和 $Q$ 进行下游任务预测 $\rightarrow$ 通过二阶导数回传更新 $\theta$ 和 $M_0$。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成任务**：关联键值（KV）检索（Associative KV-retrieval），用于精确测量固定内存大小下的信息存储容量。
    *   **自然语言推理**：bAbI 任务（QA1-QA5），测试多事实推理能力。
    *   **自然语言问答**：Short SQuAD 变体，测试对真实文本的理解。
    *   **语言建模**：WikiText-103，评估长程依赖的建模能力。
*   **对比方法（Baselines）**：
    *   **Full-Attention Transformer**：保留完整 KV 缓存（性能上限）。
    *   **Mamba-2**：先进的状态空间模型（SSM）。
    *   **RMT (Recurrent Memory Transformer)**：代表前向写入的循环内存机制。
    *   **ARMT (Associative RMT)**：使用关联矩阵存储信息的改进版 RMT。

### 4. 资源与算力
*   **硬件支持**：实验明确提到使用了 **NVIDIA A100 GPU**。
*   **算力优化**：由于 GradMem 涉及二阶导数（Double-backward），计算开销巨大。作者实现了一套高效的注意力机制双向回传方案（如 Manual HVP 和 Flash HVP），将 1024 长度下的显存占用从 60GB 优化至 30GB。
*   **训练时长**：文中未明确给出总训练小时数，但强调了元学习训练比标准训练更耗时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成任务到自然语言任务的 4 大类数据集，并针对梯度步数 $K$、内存向量数量 $m$、上下文长度等变量进行了详尽的消融实验。
*   **客观性与公平性**：在对比 RMT 和 GradMem 时，严格控制了两者使用相同的模型架构和内存向量数量，确保了“梯度写入”与“前向写入”对比的有效性。实验结果显示，即使 $K=1$，梯度写入也优于前向写入。

### 6. 主要结论与发现
*   **梯度写入更高效**：在相同内存容量下，GradMem 存储的 KV 对数量远超前向写入方法。
*   **计算换容量**：增加测试时的梯度步数 $K$ 可以显著提升记忆容量，表现出良好的扩展性（Scaling），而重复前向写入（多次阅读上下文）的收益很快会饱和。
*   **任务无关性**：仅靠自监督的“重构损失”优化出的内存，就能在无需针对特定任务微调的情况下，解决复杂的下游问答任务。
*   **选择性记忆**：分析发现，GradMem 在优化过程中会优先保留对回答问题更有用的“值”（Values），而非机械地存储所有标记。

### 7. 优点
*   **迭代纠错能力**：通过损失驱动的优化，模型能识别哪些信息尚未被存入内存并进行迭代修正。
*   **推理灵活性**：用户可以在推理时根据算力预算灵活调整 $K$ 值，以获得更高的准确率。
*   **解耦设计**：内存作为输入前缀存在，不改变模型核心权重，具有良好的模块化特性。

### 8. 不足与局限
*   **训练成本高昂**：二阶导数的计算使得在大规模模型（如 70B+）上进行元学习训练变得极其困难。
*   **写入延迟**：在 WRITE 阶段，执行梯度下降比单次前向传递慢得多。虽然可以通过多查询（Multi-query）分摊成本，但在单次交互场景下实时性较差。
*   **重构目标的局限**：简单的标记重构可能无法捕捉到深层的语义逻辑，对于需要极高抽象能力的复杂推理任务，可能需要更高级的写入目标。

（完）
