---
title: Advancing Multimodal Agent Reasoning with Long-Term Neuro-Symbolic Memory
title_zh: 利用长期神经符号记忆提升多模态智能体推理能力
authors: "Rongjie Jiang, Jianwei Wang, Gengda Zhao, Chengyang Luo, Kai Wang, Wenjie Zhang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15280v1"
tags: ["query:lh-llm"]
score: 8.0
evidence: 用于多模态智能体推理的长程神经符号记忆
tldr: 针对多模态智能体在长期推理中缺乏演绎推理能力的问题，本文提出NS-Mem框架。该框架通过整合神经记忆与显式符号结构，构建了包含情节、语义和逻辑规则的三层架构，并利用SK-Gen机制从多模态经验中自动巩固知识。实验证明，NS-Mem在多模态推理任务中显著优于纯神经记忆系统，尤其在受限推理查询方面表现突出。
selection_source: fresh_fetch
motivation: 现有的多模态智能体记忆系统主要依赖神经表示，难以支持现实决策中关键的分析性演绎推理。
method: 提出NS-Mem框架，通过三层记忆架构（情节、语义、逻辑规则）和SK-Gen知识巩固机制，结合混合检索实现神经与符号的协同推理。
result: "在多模态推理基准测试中，NS-Mem比纯神经记忆系统平均提升了4.35%的准确率，在受限推理任务中提升高达12.5%。"
conclusion: 整合神经表示与符号规则的神经符号记忆系统能有效增强多模态智能体的长期推理和决策能力。
---

## 摘要
大语言模型的最新进展推动了在开放世界、多模态环境中运行的智能体的出现。为了支持长期推理，此类智能体通常配备有外部记忆系统。然而，大多数现有的多模态智能体记忆主要依赖于神经表示和基于向量的检索，这虽然适用于归纳式、直觉式推理，但在支持对现实世界决策至关重要的分析式、演绎式推理方面存在根本局限。为了解决这一局限，我们提出了 NS-Mem，这是一种长期神经符号记忆框架，旨在通过将神经记忆与显式符号结构和规则相结合，来提升多模态智能体的推理能力。具体而言，NS-Mem 围绕记忆系统的三个核心组件运行：(1) 由情境层、语义层和逻辑规则层组成的三层记忆架构；(2) 由 SK-Gen 实现的记忆构建与维护机制，该机制能从积累的多模态经验中自动整合结构化知识，并增量更新神经表示和符号规则；(3) 一种混合记忆检索机制，结合了基于相似性的搜索与确定性符号查询函数，以支持结构化推理。在现实世界多模态推理基准测试上的实验表明，神经符号记忆在整体推理准确率上比纯神经记忆系统平均提高了 4.35%，在受限推理查询上的提升高达 12.5%，验证了 NS-Mem 的有效性。

## Abstract
Recent advances in large language models have driven the emergence of intelligent agents operating in open-world, multimodal environments. To support long-term reasoning, such agents are typically equipped with external memory systems. However, most existing multimodal agent memories rely primarily on neural representations and vector-based retrieval, which are well-suited for inductive, intuitive reasoning but fundamentally limited in supporting analytical, deductive reasoning critical for real-world decision making. To address this limitation, we propose NS-Mem, a long-term neuro-symbolic memory framework designed to advance multimodal agent reasoning by integrating neural memory with explicit symbolic structures and rules. Specifically, NS-Mem is operated around three core components of a memory system: (1) a three-layer memory architecture that consists episodic layer, semantic layer and logic rule layer, (2) a memory construction and maintenance mechanism implemented by SK-Gen that automatically consolidates structured knowledge from accumulated multimodal experiences and incrementally updates both neural representations and symbolic rules, and (3) a hybrid memory retrieval mechanism that combines similarity-based search with deterministic symbolic query functions to support structured reasoning. Experiments on real-world multimodal reasoning benchmarks demonstrate that Neural-Symbolic Memory achieves an average 4.35% improvement in overall reasoning accuracy over pure neural memory systems, with gains of up to 12.5% on constrained reasoning queries, validating the effectiveness of NS-Mem.

---

## 论文详细总结（自动生成）

### 论文总结：利用长期神经符号记忆提升多模态智能体推理能力

#### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态智能体记忆系统主要依赖**神经表示（向量嵌入）**和**向量检索**。这种方式擅长“系统 1”式的直觉、归纳推理，但在处理需要逻辑演绎、前置条件判断及约束满足的“系统 2”式**分析性推理**时存在根本局限。
*   **研究动机**：在现实场景中（如机器人辅助），智能体不仅需要识别物体，还需要理解复杂的步骤依赖（如：碗碎了需要先去买碗才能做沙拉）。作者旨在通过结合神经记忆的灵活性与符号逻辑的确定性，构建一种能够支持长期、复杂逻辑推理的记忆框架。

#### 2. 方法论：NS-Mem 框架
NS-Mem 整合了神经表示与显式符号规则，核心包含三个部分：
*   **三层记忆架构**：
    1.  **情节层 (Episodic Layer)**：存储带时间戳的细粒度多模态观察（事件描述）。
    2.  **语义层 (Semantic Layer)**：维护实体属性和抽象知识，通过强化/削弱机制实现知识一致性。
    3.  **逻辑层 (Logic Layer)**：核心创新点。每个逻辑节点包含**神经索引**（用于向量检索发现）和**程序化有向无环图 (DAG)**（用于符号化确定性查询）。
*   **SK-Gen 自动构建与维护机制**：
    *   **构建**：从视频流中提取动作序列，利用 **PrefixSpan 算法**挖掘频繁模式，经 LLM 验证后转化为符号 DAG。
    *   **增量更新**：使用**指数移动平均 (EMA)** 更新神经索引以应对语义漂移；通过转移概率统计更新 DAG 结构，并提供了后验一致性的理论证明。
*   **混合检索与推理**：
    *   **查询分类**：将查询分为事实型、约束型和角色型。
    *   **多粒度检索**：结合目标级和步骤级索引进行重排序。
    *   **符号增强**：在检索到的 DAG 上直接执行确定性函数（如路径枚举、约束过滤），确保推理结果的精确性和可重复性。

#### 3. 实验设计
*   **数据集/场景**：使用 **M3-Bench** 基准，包含两个子集：
    *   **M3-Bench-robot**：100 个机器人视角拍摄的真实世界视频。
    *   **M3-Bench-web**：920 个网络来源的视频。
*   **对比方法**：
    *   **苏格拉底模型**：GPT-4o, Gemini-1.5-Pro, Qwen2.5-VL 等。
    *   **在线视频理解模型**：MovieChat, MA-LMM, Flash-VStream。
    *   **智能体方法**：**M3-Agent**（目前最先进的纯向量记忆智能体，作为主要 Baseline）。
*   **评估指标**：推理准确率（GPT-4o 作为裁判）、对话轮数、响应时间。

#### 4. 资源与算力
*   **硬件环境**：服务器配备 Intel Xeon Silver 4314 CPU，512GB 内存。
*   **GPU**：使用 **NVIDIA RTX A5000** GPU。
*   **细节**：文中未明确给出具体的训练时长或 GPU 数量，但提到实验是在该硬件配置下完成的。由于该方法侧重于推理和增量维护而非大规模从头训练，其算力需求主要集中在 VLM 推理和索引维护上。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 **9 组主要实验**（Exp-1 到 Exp-9），涵盖了总体性能对比、不同推理类型分析、查询类型分析、效率评估、消融实验、增量更新效果以及三个关键超参数（验证阈值、门控阈值、检索权重）的敏感性分析。
*   **充分性评价**：实验设计较为**充分且客观**。不仅在两个不同来源的数据集上验证了有效性，还通过消融实验证明了符号层与神经层的协同效应。引入理论证明（定理 1-3）进一步增强了算法设计的严谨性。

#### 6. 主要结论与发现
*   **准确率提升**：NS-Mem 在 M3-Bench-web 上达到 53.6% 的准确率，比 M3-Agent 提升了 **4.7%**；在机器人数据集上提升了 **4.0%**。
*   **约束推理优势**：在**程序化 (Procedural)** 和**受限 (Constrained)** 查询类型下，准确率相对提升高达 **50%**（绝对值提升约 12%）。
*   **效率优化**：由于符号函数可以一次性返回完整步骤，减少了智能体与环境/记忆的交互轮数（机器人场景下减少了 **15.8%** 的对话轮数）。
*   **增量维护有效性**：动态更新的 DAG 比静态结构在准确率和效率上均有提升，证明了 EMA 和统计更新的必要性。

#### 7. 优点与亮点
*   **架构创新**：成功将“系统 1”（神经）和“系统 2”（符号）结合在多模态记忆中，解决了纯向量检索在逻辑链条上的断裂问题。
*   **确定性保障**：通过符号 DAG 确保了推理过程的可解释性和结果的确定性，避免了 LLM 幻觉导致的逻辑错误。
*   **增量学习**：SK-Gen 机制支持在线更新，无需重新构建整个记忆库，适合开放世界的长期部署。
*   **理论支撑**：为转移概率的收敛性和符号查询的确定性提供了数学证明。

#### 8. 不足与局限
*   **LLM 依赖性**：在知识验证（SK-Gen Step 3）和查询分类中仍依赖 LLM，若底层 LLM 判断失误，可能导致错误的符号结构生成。
*   **冷启动问题**：PrefixSpan 算法需要一定的观察积累（支持度阈值）才能挖掘出逻辑规则，在初始阶段系统仍主要依赖神经记忆。
*   **复杂场景扩展**：虽然 DAG 能够处理分支，但对于极其复杂、非线性的因果逻辑，手动定义的符号函数可能难以完全覆盖。
*   **实验覆盖**：虽然在 M3-Bench 表现优异，但尚未在更广泛的具身智能任务（如实际机器人抓取控制）中验证其闭环执行能力。

（完）
