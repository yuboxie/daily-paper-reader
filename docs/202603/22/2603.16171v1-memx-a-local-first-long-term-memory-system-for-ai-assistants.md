---
title: "MemX: A Local-First Long-Term Memory System for AI Assistants"
title_zh: MemX：一种面向 AI 助手的本地优先长期记忆系统
authors: Lizheng Sun
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16171v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: AI助手的长期记忆系统
tldr: MemX 是一个为 AI 助手设计的本地优先长期记忆系统，旨在提供稳定、可解释且高效的记忆检索。该系统基于 Rust 和 libSQL 构建，采用多路召回（向量与关键词）、RRF 融合、四因子重排序及低置信度拒绝机制。实验表明，MemX 在中文基准测试中表现优异，并在大规模数据下保持低延迟，为本地化 AI 记忆管理提供了可靠的基准方案。
selection_source: fresh_fetch
motivation: 旨在解决现有 AI 助手记忆系统在本地化部署、检索稳定性及可解释性方面的不足。
method: 采用向量与关键词双路召回，结合 RRF 融合、四因子重排序及低置信度拒绝规则来优化检索流程。
result: "在中文基准测试中实现 91.3% 的 Hit@1，且在 10 万条记录规模下端到端搜索延迟保持在 90 毫秒以内。"
conclusion: MemX 证明了通过本地优先的架构和稳定性导向的检索设计，可以为 AI 助手提供高性能且可靠的长期记忆支持。
---

## 摘要
我们提出了 MemX，这是一种面向 AI 助手的本地优先长期记忆系统，采用了面向稳定性的检索设计。MemX 基于 Rust 语言实现，构建于 libSQL 和兼容 OpenAI 的嵌入（embedding）API 之上，为对话智能体提供持久化、可搜索且可解释的记忆。其检索流水线应用了向量召回、关键词召回、倒数排名融合（RRF）、四因子重排序，以及一种低置信度拒绝规则，用于在记忆库中不存在答案时抑制伪召回。我们从两个维度对 MemX 进行了评估。首先，通过两个自定义中文基准测试集（43 个查询，不超过 1,014 条记录）验证了流水线设计：在默认场景下 Hit@1 达到 91.3%，在高混淆场景下达到 100%，并具有保守的未命中查询抑制能力。其次，利用 LongMemEval 基准测试（500 个查询，最高 220,349 条记录）量化了系统在四种能力类型和三种存储粒度下的边界。在事实级粒度下，系统达到了 Hit@5=51.6% 和 MRR=0.380，是会话级性能的两倍，而时间维度和多会话推理仍具挑战性（Hit@5 <= 43.6%）。FTS5 全文索引在 10 万条记录规模下将关键词搜索延迟降低了 1,100 倍，使端到端搜索保持在 90 毫秒以内。与针对端到端智能体基准测试的 Mem0 及相关工作不同，MemX 专注于一个更窄、可复现的基准：本地优先部署、结构简单性、可解释检索以及面向稳定性的设计。

## Abstract
We present MemX, a local-first long-term memory system for AI assistants with stability-oriented retrieval design. MemX is implemented in Rust on top of libSQL and an OpenAI-compatible embedding API, providing persistent, searchable, and explainable memory for conversational agents. Its retrieval pipeline applies vector recall, keyword recall, Reciprocal Rank Fusion (RRF), four-factor re-ranking, and a low-confidence rejection rule that suppresses spurious recalls when no answer exists in the memory store.   We evaluate MemX on two axes. First, two custom Chinese-language benchmark suites (43 queries, <=1,014 records) validate pipeline design: Hit@1=91.3% on a default scenario and 100% under high confusion, with conservative miss-query suppression. Second, the LongMemEval benchmark (500 queries, up to 220,349 records) quantifies system boundaries across four ability types and three storage granularities. At fact-level granularity the system reaches Hit@5=51.6% and MRR=0.380, doubling session-level performance, while temporal and multi-session reasoning remain challenging (<=43.6% Hit@5). FTS5 full-text indexing reduces keyword search latency by 1,100x at 100k-record scale, keeping end-to-end search under 90 ms.   Unlike Mem0 and related work that targets end-to-end agent benchmarks, MemX focuses on a narrower, reproducible baseline: local-first deployment, structural simplicity, explainable retrieval, and stability-oriented design.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemX: A Local-First Long-Term Memory System for AI Assistants》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在多轮对话和跨会话中具有“无状态”性，缺乏持久记忆。现有的记忆系统（如 Mem0, MemGPT）多侧重于云端部署或端到端智能体任务，而在**本地优先（Local-first）**场景下，如何平衡检索召回率与抑制“无中生有”的伪召回（Spurious Recalls）是一个关键挑战。
*   **研究动机**：用户期望本地 AI 助手能积累知识、保护隐私、低延迟运行，并能在记忆库中无相关信息时保持“沉默”而非误报。MemX 旨在构建一个结构简单、可解释且面向稳定性的本地长期记忆系统。

### 2. 方法论
MemX 采用 Rust 编写，基于 libSQL（SQLite 的分支）构建，核心思想是**混合检索与多级过滤**：
*   **混合检索流水线**：
    1.  **双路召回**：并行执行向量搜索（基于 DiskANN/暴力搜索）和关键词匹配（基于 FTS5 全文索引）。
    2.  **RRF 融合**：使用倒数排名融合（Reciprocal Rank Fusion, k=60）合并两路结果。
    3.  **四因子重排序**：综合考虑语义相似度（权重 0.45）、时效性（0.25，基于半衰期衰减）、频率（0.05，区分显式访问与检索命中）和重要性（0.10）。
    4.  **归一化**：应用 Z-score 标准化和 Sigmoid 变换，使不同查询间的得分具有可比性。
*   **关键技术细节**：
    *   **低置信度拒绝规则**：若关键词召回为空且最大向量相似度低于阈值 $\tau$（推荐 0.50），则返回空结果，以减少误报。
    *   **去重机制**：包含内容去重和基于“标签签名”的去重，防止同类模板生成的记忆占据所有 Top-k 位次。
    *   **访问与检索分离**：独立记录 `access_count`（用户查看）和 `retrieval_count`（搜索命中），避免管理性操作污染排序信号。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **自定义中文基准（43 个查询）**：分为“默认场景”（1,014 条记录）和“高混淆场景”（600 条记录），涵盖用户偏好、操作规程、代码规范等。
    *   **LongMemEval（500 个查询）**：使用外部公开基准，包含 1.9 万个会话，扩展至最高 22 万条记录规模，评估信息提取、知识更新、多会话推理和时间推理四种能力。
*   **对比维度**：
    *   **存储粒度对比**：会话级（Session）、轮次级（Round）、事实级（Fact）。
    *   **消融实验**：逐一验证向量搜索、关键词搜索、拒绝规则和去重的贡献。
    *   **检索算法对比**：对比了 LIKE 子句与 FTS5 索引的性能差异。

### 4. 资源与算力
*   **模型使用**：使用了 **Qwen3-Embedding-0.6B**（1024 维）作为嵌入模型。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号或训练时长。由于该系统侧重于检索架构而非模型训练，实验主要关注推理延迟。
*   **性能表现**：在 10 万条记录规模下，端到端搜索延迟低于 90ms；FTS5 索引相比 LIKE 搜索实现了 1,100 倍的加速。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 543 个查询测试，涵盖了从千级到十万级的不同数据规模。
*   **充分性**：实验设计较为全面，包含了阈值扫描（Threshold Sweep）、存储粒度分析、消融实验以及针对拒绝规则的设计空间模拟。
*   **客观性**：使用了 95% Wilson 得分置信区间来报告二元指标，并对比了外部基准（LongMemEval），增强了结果的可信度。

### 6. 主要结论与发现
*   **粒度决定性能**：在 LongMemEval 中，**事实级（Fact-level）**存储的检索质量（Hit@5=51.6%）是会话级的两倍。
*   **稳定性提升**：通过拒绝规则，系统在保证高召回的同时，能有效识别并拒绝无答案的查询（Miss-Strict-Rate 达 75-100%）。
*   **工程优化至关重要**：在大规模本地存储中，全文索引（FTS5）是维持低延迟的关键，其性能远超传统的子字符串匹配。
*   **能力短板**：系统在时间推理和多会话推理方面表现较弱（Hit@5 $\le$ 43.6%），表明单次向量检索难以处理复杂的逻辑合成任务。

### 7. 优点
*   **本地优先与隐私**：完全运行在本地单文件数据库上，符合数据所有权原则。
*   **稳定性导向**：引入拒绝机制和四因子重排序，显著降低了 AI 助手胡言乱语的风险。
*   **高性能实现**：使用 Rust 和高效索引技术，在普通硬件上即可实现大规模记忆的秒级检索。
*   **可解释性**：通过显式的重排序因子和去重规则，使检索结果的来源和排序逻辑清晰可见。

### 8. 不足与局限
*   **复杂推理能力不足**：目前尚未整合图结构遍历或多跳推理，无法处理跨会话的复杂逻辑关联。
*   **去重机制的副作用**：在无标签的原子事实数据上，基于标签签名的去重可能会误删相关结果（导致 Hit@5 下降）。
*   **多主题覆盖限制**：对于包含多个子问题的复合查询，系统往往只能检索到最显著的一个主题。
*   **语言偏差**：自定义基准为中文，而 LongMemEval 为英文，跨语言的通用性仍需进一步验证。

（完）
