---
title: "Swarm: Co-Activation Aware KVCache Offloading Across Multiple SSDs"
title_zh: Swarm：跨多 SSD 的协同激活感知 KVCache 卸载
authors: "Tuowei Wang, Liyun Chu, Ruwen Fan, Ju Ren"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17803v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: LLM 推理中的 KV 缓存卸载与优化
tldr: 针对大语言模型推理中KV缓存占用内存过高的问题，本文提出Swarm系统。Swarm利用KV缓存访问中的“协同激活”现象，即特定KV条目常被同步访问的特性，将受限的单设备SSD访问转化为多SSD并行I/O。通过离线聚类、图基放置及选择性副本策略，并在运行时动态调整，Swarm显著提升了SSD卸载的带宽利用率，有效缓解了内存压力并降低了成本。
selection_source: fresh_fetch
motivation: 传统的KV缓存卸载方案受限于DRAM容量成本或单SSD的带宽瓶颈，难以高效处理大规模长文本推理任务。
method: 通过识别KV缓存的协同激活模式进行离线聚类，并利用图基放置和选择性副本技术将数据分布于多个SSD以实现并行访问。
result: 实验结果显示，Swarm成功将I/O耗时降低了2.41倍，并将有效带宽利用率提升了2.72倍。
conclusion: Swarm通过感知访问模式并利用多设备并行性，有效克服了SSD带宽限制，为低成本、高性能的KV缓存管理提供了新方案。
---

## 摘要
键值（KV）缓存已成为大语言模型（LLM）推理中内存消耗的主要来源。尽管将 KVCache 从 GPU 高带宽内存（HBM）卸载到 CPU DRAM 缓解了设备内存压力，但对于大规模、持久性的工作负载，DRAM 的容量仍然有限且成本高昂。固态硬盘（SSD）提供了一种具有成本效益的替代方案，但由于 PCIe 吞吐量受限以及单设备带宽限制，朴素的基于 SSD 的分页机制在根本上受限于带宽。在本文中，我们观察到真实工作负载中的 KVCache 激活表现出强且稳定的相关性。我们将这种现象称为 KVCache 协同激活（KVCache Co-Activation），即访问一个 KV 条目通常伴随着一组稳定且重复出现的其他 KV 条目。利用这一特性，我们提出了 Swarm，一个基于 SSD 的 KVCache 卸载系统，它将受带宽限制的单设备访问转换为跨多个 SSD 的并行 I/O。具体而言，Swarm 离线聚类协同激活的 KV 条目，并使用基于图的放置策略和选择性副本机制将生成的聚类分布在多个 SSD 上，以最大化并行 I/O 带宽。在运行时，Swarm 执行负载均衡的聚类检索，并动态调整聚类和缓存决策，以在不断变化的访问模式下维持高带宽利用率。评估结果表明，Swarm 将 I/O 时间减少了 2.41 倍，并将有效带宽利用率提高了 2.72 倍。

## Abstract
The key-value (KV) cache has become the dominant contributor to memory consumption in large language model (LLM) inference. Although offloading KVCache from GPU high-bandwidth memory (HBM) to CPU DRAM alleviates device memory pressure, DRAM remains capacity-limited and costly for large, persistent workloads. Solid-state drives (SSDs) provide a cost-effective alternative, but naive SSD-based paging is fundamentally bandwidth-bound due to limited PCIe throughput and per-device bandwidth constraints.   In this paper, we observe that KVCache activations in real-world workloads exhibit strong and stable correlations. We term this phenomenon KVCache Co-Activation, where accessing a KV entry is often accompanied by a stable and recurring set of other KV entries. Leveraging this property, we present Swarm, an SSD-based KVCache offloading system that converts bandwidth-bound single-device access into parallel I/O across multiple SSDs. Specifically, Swarm clusters co-activated KV entries offline and distributes the resulting clusters across SSDs using graph-based placement with selective replication to maximize parallel I/O bandwidth. At runtime, Swarm performs load-balanced cluster retrieval and dynamically adapts clustering and caching decisions to sustain high bandwidth utilization under evolving access patterns. Evaluations show that Swarm reduces I/O time by 2.41x and improves effective bandwidth utilization by 2.72x.

---

## 论文详细总结（自动生成）

以下是对论文《Swarm: Co-Activation Aware KVCache Offloading Across Multiple SSDs》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）推理中，KV Cache（键值缓存）随序列长度和模型深度增长，已成为内存消耗的主导因素。
*   **研究背景**：
    *   **内存瓶颈**：GPU HBM 容量有限且昂贵；CPU DRAM 虽然容量稍大，但面对超长上下文或持久化工作负载时仍显不足且成本高。
    *   **存储权衡**：SSD 提供了极高的容量和低廉的成本，但其 PCIe 接口较窄，单设备带宽有限，导致基于 SSD 的 KVCache 卸载方案受限于 I/O 带宽，严重拖慢推理速度。
*   **核心观察**：论文发现 KVCache 的激活并非随机，而是存在**协同激活（Co-Activation）**现象——即访问某个 KV 条目时，通常会伴随一组稳定且重复出现的其他条目。

### 2. 方法论：SWARM 系统设计
SWARM 的核心思想是将带宽受限的单设备访问转化为跨多个 SSD 的并行 I/O。
*   **离线阶段（建模与放置）**：
    *   **协同激活聚类**：通过分析历史激活模式构建邻接矩阵，计算 KV 条目间的距离。采用启发式算法选择“中心点（Medoid）”并贪婪扩展成聚类，允许高度相关的条目在不同聚类中存在副本。
    *   **分层分区放置**：
        *   **DRAM 层**：存放聚类中心点（作为索引）、局部窗口条目（保持上下文连续性）以及基于成本效益评分选出的“热聚类”。
        *   **SSD 层**：将聚类内的条目通过 Round-robin（轮询）策略均匀分布在多个 SSD 上，确保读取单个聚类时能触发多盘并行。
*   **在线阶段（检索与更新）**：
    *   **负载均衡调度**：合并待检索的聚类，去除重复项，并根据条目副本所在的 SSD 负载情况（Bucket 大小）进行动态路由，平衡各盘 I/O 压力。
    *   **动态适配**：在推理过程中，根据局部窗口内的激活模式动态将新生成的 KV 条目归类到现有聚类中，并实时更新 DRAM 缓存的替换策略。
    *   **异步 I/O 管道**：利用 `io_uring` 实现非阻塞读取，并通过预取技术使 SSD 传输与 GPU 计算重叠。

### 3. 实验设计
*   **数据集**：WikiText（语言建模）、LongBench（长文本理解）、MMLU（多任务知识）、GSM8K（数学推理）。
*   **模型（Benchmark）**：涵盖 Qwen3 (14B, 32B, 235B MoE)、Llama3.1 (70B)、GPT-OSS (120B)。
*   **对比方法**：
    *   **No Cluster**：无聚类的朴素 SSD 存储。
    *   **InfLLM**：基于固定大小块的上下文管理。
    *   **PQCache**：基于乘积量化和 K-means 聚类的检索方案。

### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**：Intel Xeon Gold 6530 (64核)。
    *   **GPU**：8 张 NVIDIA H20 (每张 96GB HBM)。
    *   **内存**：1TB DDR5 DRAM。
    *   **存储**：最多 8 块 NVMe SSD（包括三星 PM9A3 和英特尔 Optane 900P）。
*   **软件实现**：基于 Python 和 C++ 开发，代码量超过 2000 行，包含自定义的 I/O 后端。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的测试，包括端到端吞吐量、I/O 带宽利用率、模型准确度损失测试。
*   **消融实验**：针对离线聚类策略、SSD 放置策略、DRAM 缓存策略、在线调度算法分别进行了对比验证。
*   **敏感性分析**：测试了不同前缀长度（最高 1M token）、不同 SSD 数量（1-8块）、不同 SSD 型号以及不同稀疏率下的表现。
*   **评价**：实验设计全面，覆盖了从微观 I/O 性能到宏观推理吞吐量的各个维度，对比基准具有代表性，实验结果较为客观公平。

### 6. 主要结论与发现
*   **性能提升**：相比现有最优方案，SWARM 平均减少了 **2.41倍** 的 I/O 时间，提升了 **2.72倍** 的有效带宽利用率。
*   **带宽突破**：在 8 块 SSD 配置下，SWARM 达到了 **37.67 GB/s** 的聚合带宽，接近 GPU HBM 与 CPU DRAM 之间的传输速度。
*   **准确性**：在仅激活 10% KV 条目的情况下，SWARM 的模型准确度最接近全量激活的基准（Oracle）。
*   **可扩展性**：吞吐量随 SSD 数量增加呈线性增长趋势，证明了多盘并行策略的有效性。

### 7. 优点
*   **创新视角**：首次深入挖掘并利用了 KVCache 的“协同激活”这一统计特性。
*   **软硬协同**：不仅在算法层优化聚类，还针对现代存储接口（io_uring）和多盘拓扑进行了系统级优化。
*   **高性价比**：通过聚合廉价 SSD 的带宽，实现了媲美昂贵 DRAM 方案的性能，为长文本推理提供了低成本扩展路径。

### 8. 不足与局限
*   **离线开销**：聚类过程依赖于离线分析（Profiling），虽然是一次性的，但对于完全未知分布的新任务，可能需要重新采样。
*   **硬件依赖**：性能提升高度依赖于服务器是否插有多块 SSD，在单盘设备（如个人电脑）上优势不明显。
*   **动态性挑战**：虽然有在线更新机制，但在极端多变、无规律的对话场景下，协同激活模式的稳定性可能下降，导致 I/O 效率波动。

（完）
