---
title: "EviAgent: Evidence-Driven Agent for Radiology Report Generation"
title_zh: EviAgent：证据驱动的放射学报告生成智能体
authors: "Tuoshi Qi, Shenshen Bu, Yingfei Xiang, Zhiming Dai"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13956v1"
tags: ["query:ehr-tr"]
score: 8.0
evidence: 用于临床放射报告生成的证据驱动智能体
tldr: 针对放射科报告自动生成中多模态大模型存在的“黑盒”决策和缺乏外部知识等问题，本文提出了一种证据驱动的智能体 EviAgent。该方法通过将复杂的生成过程分解为细粒度的操作单元，并整合多维视觉专家与检索机制，为诊断提供显式视觉证据和高质量临床先验。实验证明，EviAgent 在多个主流数据集上均优于现有通用及专业模型，提升了报告生成的透明度与准确性。
selection_source: fresh_fetch
motivation: 现有多模态大模型在生成放射报告时缺乏决策透明度和显式视觉证据，且难以有效利用外部领域知识。
method: 提出 EviAgent 智能体，通过分解生成流程并集成视觉专家与检索模块，构建透明的推理轨迹以提供视觉证据和临床先验。
result: 在 MIMIC-CXR、CheXpert Plus 和 IU-Xray 等数据集上的实验表明，EviAgent 的性能显著超越了现有的通用和专业医疗模型。
conclusion: EviAgent 为自动化放射报告生成提供了一种鲁棒且可信的解决方案，有效提升了诊断过程的可解释性与准确性。
---

## 摘要
自动放射学报告生成在减轻放射科医生沉重工作量方面具有巨大潜力。尽管近期的多模态大语言模型（MLLMs）具备强大的视觉语言能力，但其临床部署受到固有局限性的严重制约：其“黑盒”决策机制由于缺乏支持诊断的显式视觉证据，导致生成的报告不可追溯，且难以获取外部领域知识。为了应对这些挑战，我们提出了证据驱动的放射学报告生成智能体（EviAgent）。与不透明的端到端范式不同，EviAgent 通过将复杂的生成过程分解为细粒度的操作单元，协调出透明的推理轨迹。我们集成了多维视觉专家和检索机制作为外部支持模块，赋予系统显式的视觉证据和高质量的临床先验。在 MIMIC-CXR、CheXpert Plus 和 IU-Xray 数据集上的广泛实验表明，EviAgent 的性能优于大规模通用模型和专业医学模型，为自动放射学报告生成提供了一种稳健且值得信赖的解决方案。

## Abstract
Automated radiology report generation holds immense potential to alleviate the heavy workload of radiologists. Despite the formidable vision-language capabilities of recent Multimodal Large Language Models (MLLMs), their clinical deployment is severely constrained by inherent limitations: their "black-box" decision-making renders the generated reports untraceable due to the lack of explicit visual evidence to support the diagnosis, and they struggle to access external domain knowledge. To address these challenges, we propose the Evidence-driven Radiology Report Generation Agent (EviAgent). Unlike opaque end-to-end paradigms, EviAgent coordinates a transparent reasoning trajectory by breaking down the complex generation process into granular operational units. We integrate multi-dimensional visual experts and retrieval mechanisms as external support modules, endowing the system with explicit visual evidence and high-quality clinical priors. Extensive experiments on MIMIC-CXR, CheXpert Plus, and IU-Xray datasets demonstrate that EviAgent outperforms both large-scale generalist models and specialized medical models, providing a robust and trustworthy solution for automated radiology report generation.

---

## 论文详细总结（自动生成）

# EviAgent：证据驱动的放射学报告生成智能体论文总结

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLMs）在自动生成放射学报告时存在两个致命缺陷：
    1.  **“黑盒”决策**：生成过程不透明，缺乏支持诊断的显式视觉证据，导致报告不可追溯，难以在临床部署。
    2.  **知识局限**：模型主要依赖内部参数化知识，难以有效获取和利用外部的专业领域知识和临床先验。
*   **研究背景**：放射科医生工作负荷极重，自动化报告生成需求迫切。虽然通用模型和医疗专用模型在语言指标上表现尚可，但常出现事实性错误（幻觉），且无法像人类医生一样通过观察、检索、推理的闭环来得出结论。

### 2. 论文提出的方法论
EviAgent 采用了一种**证据驱动的智能体框架**，将“端到端”的黑盒生成转变为“计划-行动-报告”的透明轨迹。
*   **核心思想**：通过协调多个视觉专家工具和检索机制，构建一条可验证的证据链，最后基于证据生成报告。
*   **关键技术流程**：
    1.  **规划与分解 (Planning)**：Planner（由 Qwen3-VL-8B 担任）将复杂的诊断任务分解为细粒度的操作单元（如：先检测病变，再定位病变）。
    2.  **工具增强的 ReAct 循环 (Tool Use)**：智能体动态调用工具箱。工具箱包含：
        *   **感知专家**：Swin-Transformer 分类器（初步筛查）、CheXagent（姿态识别）、MAIRA-2（视觉定位/Grounding）、MedSAM（解剖分割）。
        *   **检索增强知识 (RAG)**：基于 14 种病理构建的知识库，通过多模态嵌入检索相似的历史病例报告。
        *   **自定义扩展**：通过 Model Context Protocol (MCP) 协议支持“即插即用”的本地医疗工具。
    3.  **证据提取 (Evidence Extraction)**：从冗长的交互历史中过滤噪声，提取分类概率、坐标、检索到的临床事实，形成结构化证据链。
    4.  **输出生成 (Output)**：严格基于提取的证据链合成最终报告，确保临床准确性。

### 3. 实验设计
*   **数据集**：
    *   **MIMIC-CXR**：大规模胸部 X 光数据集（官方测试集 3,858 张图像）。
    *   **IU-Xray**：印第安纳大学公开数据集（测试集 1,180 张图像）。
    *   **CheXpert Plus**：包含文本报告和人口统计信息的增强数据集。
*   **Benchmark（对比方法）**：
    *   **闭源通用巨头**：GPT-5.1、Claude 4.5 Sonnet、Gemini-2.5-Flash。
    *   **医疗专用模型**：LLaVA-Med、HuatuoGPT-V、MedGemma、Lingshu（灵枢）。
    *   **开源通用模型**：InternVL 系列、Qwen-VL 系列。
*   **评估指标**：RaTEScore（实体感知指标）、SembScore（临床内容对齐）、RadCliQ^-1（综合词法、语义和临床关系的复合指标）。

### 4. 资源与算力
*   **硬件环境**：实验运行在 **NVIDIA H100 GPU** 上。
*   **推理引擎**：使用 **vLLM** 高吞吐推理框架，以确保本地部署的隐私合规性和推理速度。
*   **训练时长**：论文强调 EviAgent 是一个 **无需额外训练 (Training-free)** 的智能体框架，主要算力消耗在推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了全量测试，涵盖了从 8B 到闭源巨型规模的 11 种基准模型。
*   **消融实验**：
    *   针对**工具模块**：分别移除了分类、定位、检索模块，验证了分类器在防止错误传播中的核心作用。
    *   针对**推理机制**：验证了“规划”和“证据提取”步骤对最终性能的贡献。
*   **主观评估**：引入了 **LLM-as-a-Judge**（使用 DeepSeek-V3.2），从准确性、定位、专业性、临床可接受度四个维度进行评分。
*   **充分性评价**：实验设计较为全面，不仅有自动指标，还有定性案例分析和代理能力（VR、Tool Call 等）分析，客观地展示了模型在复杂 ICU 场景下的优势。

### 6. 论文的主要结论与发现
*   **性能超越**：EviAgent 在几乎所有指标上均优于现有的医疗专用模型和闭源通用模型。例如在 MIMIC-CXR 上，其 SembScore 相比基座模型 Qwen3-VL 提升了近一倍（26.1 -> 43.6）。
*   **证据的重要性**：实验证明，将诊断结论锚定在显式的视觉坐标和检索到的临床先验上，能显著减少幻觉。
*   **错误可追溯性**：通过证据日志，可以清晰地定位错误来源（是分类器看错了，还是定位工具偏了），而非模型随机生成的幻觉。

### 7. 优点（亮点）
*   **透明与可信**：解决了医疗 AI 最核心的“信任”问题，提供了可追溯的推理路径。
*   **隐私保护**：系统完全基于本地开源模型和工具构建，无需将敏感医疗数据上传云端。
*   **高度可扩展**：引入 MCP 协议，使医院能够轻松集成现有的专有诊断工具。
*   **无需训练**：作为一种 Agent 框架，它不需要昂贵的微调过程，具有极强的通用性。

### 8. 不足与局限
*   **推理延迟**：由于涉及多轮对话和多个外部工具的串行/并行调用，其生成报告的时间显著长于端到端的单次推理模型。
*   **工具依赖性**：系统的上限受限于集成工具的准确性。如果分类器或定位工具出现偏差，虽然错误可追溯，但仍会导致最终报告错误。
*   **计算开销**：运行多个专家模型（如 MedSAM, Swin-T 等）对显存和计算资源有一定要求。

（完）
