---
title: "VERI-DPO: Evidence-Aware Alignment for Clinical Summarization via Claim Verification and Direct Preference Optimization"
title_zh: VERI-DPO：通过声明验证与直接偏好优化实现临床摘要的证据感知对齐
authors: "Weixin Liu, Congning Ni, Qingyuan Song, Susannah L. Rose, Christopher Symons, Murat Kantarcioglu, Bradley A. Malin, Zhijun Yin"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10494v1"
tags: ["query:ehr-tr"]
score: 9.0
evidence: 基于 LLM 的 EHR 证据临床摘要
tldr: 针对临床总结中大模型易产生虚假信息或过度简化的问题，本文提出 VERI-DPO 框架。该方法通过检索增强的验证器对生成的声明进行证据核查，自动构建偏好对，并利用直接偏好优化（DPO）进行对齐。实验表明，该方法在显著降低临床总结中不支持声明比例的同时，保持了信息的完整性与准确性，有效解决了临床文本生成的忠实度难题。
selection_source: fresh_fetch
motivation: 现有的临床总结模型常出现不支持的陈述或因对齐导致的过度简化，亟需一种既能保证忠实度又能维持信息量的对齐方法。
method: 提出 VERI-DPO 框架，利用检索增强验证器对候选总结进行句子级声明核查，并基于核查结果构建偏好对，通过 DPO 优化模型。
result: "在 MIMIC-III 数据集上，该方法将不支持声明的比例从 10.7% 降至 1.9%，同时将有效性从 76.7% 提升至 82.5%。"
conclusion: VERI-DPO 证明了结合声明验证与偏好优化可以有效提升临床总结的忠实度，并避免模型出现“少说少错”的退化现象。
---

## 摘要
简要住院病程 (BHC) 叙述必须具备临床实用性，同时忠实于碎片化的电子健康记录 (EHR) 证据。基于大语言模型 (LLM) 的临床摘要生成器仍会引入未经证实的陈述，而对齐过程可能会诱发遗漏（即“少说为妙”式退化）。我们提出了 VERI-DPO，它利用声明验证来挖掘偏好，并通过直接偏好优化 (DPO) 将其蒸馏至摘要生成器中。在 MIMIC-III-Ext-VeriFact-BHC 数据集（包含 100 名 ICU 患者；按患者级别划分）上，我们训练了一个检索增强验证器，通过单标记格式将声明-证据对标注为“支持”、“不支持”或“未提及”。该验证器对采样的 BHC 候选文本中的句子级声明进行评分，并将分值差异聚合为覆盖感知效用，从而挖掘长度受控且以矛盾为锚点的偏好对。在留出集患者上的实验表明，验证器挖掘的偏好能根据矛盾密度区分候选文本；VERI-DPO 将“不支持”声明的比例从 10.7% 降低至 1.9%（本地验证器评判）以及从 11.6% 降低至 6.4%（GPT-4o 评判），同时将有效性从 76.7% 提升至 82.5%，并保持了信息丰富的长度。

## Abstract
Brief Hospital Course (BHC) narratives must be clinically useful yet faithful to fragmented EHR evidence. LLM-based clinical summarizers still introduce unsupported statements, and alignment can encourage omissions ("say-less" degeneration). We introduce VERI-DPO, which uses claim verification to mine preferences and distill them into the summarizer with Direct Preference Optimization (DPO). On MIMIC-III-Ext-VeriFact-BHC (100 ICU patients; patient-level splits), we train a retrieval-augmented verifier to label claim-evidence pairs as Supported, Not Supported, or Not Addressed via a single-token format. The verifier scores sentence-level claims from sampled BHC candidates and aggregates margins into a coverage-aware utility to mine length-controlled, contradiction-anchored preference pairs. On held-out patients, verifier-mined preferences separate candidates by contradiction density, and VERI-DPO reduces Not Supported claim rates from 10.7% to 1.9% (local verifier judge) and from 11.6% to 6.4% (GPT-4o judge), while improving validity from 76.7% to 82.5% and maintaining informative length.

---

## 论文详细总结（自动生成）

这是一份关于论文《VERI-DPO: Evidence-Aware Alignment for Clinical Summarization via Claim Verification and Direct Preference Optimization》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：简要住院病程（BHC）是临床出院小结的核心，要求既能准确反映碎片化的电子健康记录（EHR）证据，又要具备临床实用性。
*   **核心问题**：
    1.  **幻觉风险**：大语言模型（LLM）在生成临床摘要时常引入未经证实的陈述（Unsupported claims）。
    2.  **退化问题**：传统的对齐方法若缺乏细粒度信号，容易导致模型产生“少说少错”的退化行为（Degeneration-by-omission），即通过缩短篇幅或模糊表述来规避错误。
    3.  **标注成本**：临床事实核查需要将摘要分解为原子声明并逐一匹配证据，人工标注成本极高且难以规模化。

### 2. 方法论：VERI-DPO 框架
该方法通过三个阶段将验证器的反馈转化为模型的生成偏好：
*   **阶段 A：检索增强验证器训练**
    *   训练一个轻量级（8B级）验证器，输入为“（检索到的证据，声明）”对。
    *   采用单标记（Single-token）分类格式，输出 A（支持）、B（不支持/矛盾）、C（未提及）。
    *   引入**Logit Bias（逻辑偏置）**调节机制，在验证器决策时通过调整“不支持”类别的阈值，实现精确率与召回率的权衡。
*   **阶段 B：验证器驱动的偏好挖掘**
    *   对同一 EHR 提示词采样多个 BHC 候选文本，将其分解为句子级声明。
    *   利用验证器对声明打分，并计算**摘要级效用函数 $U$**：
        $$U = \lambda_A n_A - \lambda_B n_B - \lambda_C n_C + \lambda_{cov} \text{Coverage} - \dots$$
    *   **核心策略**：通过“高置信度矛盾锚定（HCNS）”筛选出包含明确错误的候选作为负例，同时施加长度和覆盖度约束，确保正例（Chosen）在减少错误的同时不损失信息量。
*   **阶段 C：DPO 对齐**
    *   使用挖掘出的偏好对 $(x, y^+, y^-)$，通过直接偏好优化（DPO）算法对摘要生成器进行微调，将验证器的知识蒸馏到生成策略中。

### 3. 实验设计
*   **数据集**：使用 **MIMIC-III-Ext-VeriFact-BHC**，包含 100 名 ICU 患者的 125 次住院记录，涉及约 4,787 份临床笔记。
*   **对比基准（Benchmarks）**：
    1.  **Base Model**：Llama-3.1-8B-Instruct。
    2.  **SFT Baseline**：仅在偏好对的正例上进行监督微调，验证改进是否仅来自格式学习。
    3.  **Best-of-8 Reranking**：采样 8 个结果并用验证器重排序，代表推理时的高计算量基准。
*   **评价指标**：不支持声明率（NS-rate）、支持声明数（#Supp）、有效性（Valid %）、输出长度（Chars）。
*   **评判员**：采用双重评判——本地验证器（Med42-8B）和外部强模型（GPT-4o）。

### 4. 资源与算力
*   **模型规格**：主要使用 8B 参数规模的骨干网络（Llama-3.1-8B, Med42-8B）。
*   **训练技术**：采用了 **4-bit 量化 (NF4)** 和 **LoRA 适配器**（秩 $r=16$）以降低显存占用。
*   **算力细节**：文中未明确提及具体的 GPU 型号和总训练时长，但提到了训练参数：有效 Batch Size 为 64（验证器）或 8（DPO），最大序列长度为 3072 标记。

### 5. 实验数量与充分性
*   **实验规模**：在 72/8/20 的患者级划分上进行训练和测试。DPO 训练集包含 1513 个挖掘出的偏好对。
*   **消融实验**：针对偏好挖掘策略进行了详细消融，对比了“随机配对”、“无 HCNS 锚定”、“无长度约束”等设置，证明了各组件对防止模型退化的必要性。
*   **客观性**：通过 GPT-4o 作为第三方独立法官进行评测，避免了“自卖自夸”的偏差，实验设计较为严谨。

### 6. 主要结论与发现
*   **显著降低幻觉**：在本地验证器下，不支持声明率从 **10.7% 降至 1.9%**；在 GPT-4o 评测下从 **11.6% 降至 6.4%**。
*   **克服退化**：相比 Base 模型，VERI-DPO 生成的摘要长度更长（2159 vs 1855 字符），支持的声明数量更多，有效解决了“少说为妙”的问题。
*   **优于 SFT**：单纯的 SFT 无法减少幻觉，证明了 DPO 提供的对比信号对于事实对齐至关重要。
*   **单样本高效性**：DPO 后的单次生成效果优于需要多次采样的 Best-of-8 重排序方法。

### 7. 优点
*   **可审计的中间信号**：验证器不仅提供训练信号，还生成可供临床医生查阅的证据 ID 和置信度，增强了系统的透明度。
*   **自动化的偏好构建**：提出了一套成熟的临床偏好挖掘流水线，解决了临床数据标注难的痛点。
*   **多约束优化**：通过效用函数平衡了事实性、覆盖度和长度，展示了对模型行为的精细控制。

### 8. 不足与局限
*   **检索依赖性**：系统的表现高度依赖于检索组件的质量。如果证据未被检索到，验证器可能误判为“未提及”。
*   **样本量限制**：实验仅基于 100 名 ICU 患者的单一数据集，跨机构、跨科室的泛化能力有待验证。
*   **自动化评测偏差**：虽然使用了双法官，但最终仍缺乏临床医生对生成摘要的端到端盲测评估。
*   **超参数敏感性**：效用函数中的权重（如 $\lambda_A, \lambda_B$）和 Logit Bias 需要根据开发集进行精细调优。

（完）
