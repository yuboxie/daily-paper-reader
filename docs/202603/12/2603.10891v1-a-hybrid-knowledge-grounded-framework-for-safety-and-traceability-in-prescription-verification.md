---
title: A Hybrid Knowledge-Grounded Framework for Safety and Traceability in Prescription Verification
title_zh: 一种用于处方审核安全性和可追溯性的混合知识增强框架
authors: "Yichi Zhu, Kan Ling, Xu Liu, Hengrun Zhang, Huiqun Yu, Guisheng Fan"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10891v1"
tags: ["query:llm-mr"]
score: 10.0
evidence: 使用LLM和知识图谱进行安全且有证据支持的处方审核
tldr: 针对处方审核中大语言模型事实不可靠和缺乏可追溯性的问题，本文提出PharmGraph-Auditor系统。该系统构建了结合关系型与图结构的混合医药知识库（HPKB），并利用迭代模式细化算法（ISR）从医学文本中提取知识。通过基于知识库的验证链（CoV）推理范式，将LLM转化为透明的推理引擎，实现安全且有据可查的处方审计，显著提升了药师的工作效率与安全性。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在处方审核等零容忍医疗领域中存在的事实不可靠、缺乏可追溯性及复杂推理能力不足的问题。
method: 构建了结合关系型与图结构的混合医药知识库，并提出基于知识库的验证链（CoV）推理范式，将LLM转化为可验证的推理引擎。
result: 实验结果显示该系统具备稳健的知识提取能力，并能显著提高药师进行处方验证的安全性和效率。
conclusion: 该框架通过混合知识驱动的透明推理机制，为实现安全、可追溯的自动化处方审计提供了一种有效的技术路径。
---

## 摘要
用药错误对患者安全构成重大威胁，使得药师审核（PV）成为至关重要但负担沉重的最后一道防线。由于大语言模型（LLMs）固有的事实不可靠性、缺乏可追溯性以及在复杂推理方面的弱点，将其直接应用于这一零容忍领域是不可行的。为了应对这些挑战，我们推出了 PharmGraph-Auditor，这是一个专为安全且基于证据的处方审计而设计的新型系统。该系统的核心是一个值得信赖的混合药物知识库（HPKB），它是在虚拟知识图谱（VKG）范式下实现的。该架构通过严格的映射层，战略性地统一了用于集合约束满足的关系组件和用于拓扑推理的图组件。为了构建该 HPKB，我们提出了迭代模式细化（ISR）算法，这是一个能够使图模式和关系模式从医学文本中共同演化的框架。在审计方面，我们引入了基于知识库的验证链（CoV），这是一种新的推理范式，将 LLM 从不可靠的生成器转变为透明的推理引擎。CoV 将审计任务分解为针对 HPKB 的一系列可验证查询，生成混合查询计划以从最合适的数据存储中检索证据。实验结果证明了强大的知识提取能力，并展示了使用 PharmGraph-Auditor 帮助药师实现更安全、更快速处方审核的前景。

## Abstract
Medication errors pose a significant threat to patient safety, making pharmacist verification (PV) a critical, yet heavily burdened, final safeguard. The direct application of Large Language Models (LLMs) to this zero-tolerance domain is untenable due to their inherent factual unreliability, lack of traceability, and weakness in complex reasoning. To address these challenges, we introduce PharmGraph-Auditor, a novel system designed for safe and evidence-grounded prescription auditing. The core of our system is a trustworthy Hybrid Pharmaceutical Knowledge Base (HPKB), implemented under the Virtual Knowledge Graph (VKG) paradigm. This architecture strategically unifies a relational component for set constraint satisfaction and a graph component for topological reasoning via a rigorous mapping layer. To construct this HPKB, we propose the Iterative Schema Refinement (ISR) algorithm, a framework that enables the co-evolution of both graph and relational schemas from medical texts. For auditing, we introduce the KB-grounded Chain of Verification (CoV), a new reasoning paradigm that transforms the LLM from an unreliable generator into a transparent reasoning engine. CoV decomposes the audit task into a sequence of verifiable queries against the HPKB, generating hybrid query plans to retrieve evidence from the most appropriate data store. Experimental results demonstrate robust knowledge extraction capabilities and show promises of using PharmGraph-Auditor to enable pharmacists to achieve safer and faster prescription verification.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Hybrid Knowledge-Grounded Framework for Safety and Traceability in Prescription Verification》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：用药错误是全球医疗安全的重大威胁，药师审核（PV）是最后一道防线，但面临数据过载和高压力的挑战。
*   **核心问题**：直接应用大语言模型（LLM）进行处方审核存在三大致命缺陷：**事实不可靠（幻觉）**、**缺乏可追溯性（黑盒决策）**以及**复杂推理能力弱**。在医疗这种“零容忍”领域，这些缺陷限制了 AI 的应用。
*   **整体含义**：本文提出了 **PharmGraph-Auditor** 系统，旨在通过构建一个结合关系型数据库（处理数值约束）和图数据库（处理语义拓扑）的混合知识库，将 LLM 从“生成器”转变为“基于证据的推理引擎”，实现安全、透明且可追溯的处方审计。

### 2. 论文提出的方法论
核心思想是基于**虚拟知识图谱（VKG）**范式的混合架构，分为构建和应用两个阶段：
*   **混合医药知识库（HPKB）**：
    *   **关系组件 (R)**：存储原子化、数值化的事实（如剂量阈值），利用 SQL 进行高效的集合约束检查。
    *   **图组件 (G)**：存储实体间的语义拓扑（如药物相互作用、过敏层级），利用 Cypher 进行多跳推理。
*   **迭代模式细化算法（ISR）**：一种人机协作算法。LLM 作为“缺口检测器”识别文本中现有模式无法表达的信息，人类专家作为“架构师”进行语义抽象，从而动态演化出覆盖面广且紧凑的知识库模式。
*   **基于知识库的验证链（CoV）推理范式**：
    1.  **任务分解**：将审计目标分解为剂量、禁忌、相互作用等可验证子任务。
    2.  **混合查询生成**：根据任务类型自动生成 SQL 或 Cypher 查询。
    3.  **证据筛选（P-EST）**：利用“患者画像驱动的证据选择树”剔除无关规则，仅保留与当前患者最相关的证据。
    4.  **证据增强合成**：LLM 结合筛选出的证据生成审计报告，并显式标注“信息缺失（Information Gaps）”。

### 3. 实验设计
*   **数据集与场景**：
    *   **知识提取实验**：使用 100 份多样化的药品说明书，由拥有 10 年经验的临床药师标注作为金标准。
    *   **处方审计实验**：使用 100 组真实的住院病历和处方，涵盖 500 个审计点（适应症、剂量、禁忌、特殊人群、相互作用）。
    *   **消融实验**：使用“红队测试”方法，利用 Gemini 2.5 Pro 生成了 1000 多个带有故意错误的合成处方。
*   **对比方法（Benchmark）**：
    *   **知识提取**：对比了 Zero-shot OpenIE（类似 GraphRAG）和 One-shot Schema-guided Agent（类似 AutoKG）。
    *   **处方审计**：对比了人类药师经验审核、传统规则型临床决策支持系统（CDSS）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量或训练时长）。
*   **模型使用**：实验主要基于 API 调用或推理，使用了 **GPT-4o**、**Deepseek-V3** 和 **Qwen3-32B** 等模型进行性能评估。由于该系统侧重于检索增强生成（RAG）和知识库构建，而非从头训练大模型，因此算力消耗主要集中在推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：
    *   完成了知识提取质量评估（P/R/F1 指标）。
    *   进行了真实临床场景的对比测试（100 份真实病历）。
    *   进行了 1000+ 样本的消融实验。
*   **充分性与客观性**：实验设计较为充分。通过引入人类专家金标准、真实住院数据以及合成的红队测试数据，从不同维度验证了系统的鲁棒性。对比了传统 CDSS 和人类药师，体现了实验的公平性和实际应用价值。

### 6. 论文的主要结论与发现
*   **性能提升**：PharmGraph-Auditor 在处方审计中的 F1 分数比传统规则型 CDSS 提升了 **13.4%**。
*   **安全性与效率**：系统在召回率（70.3%）上显著超过人类专家（45.9%），同时保持了较高的精确度（74.3%），有效缓解了药师的“警报疲劳”。
*   **混合架构的必要性**：消融实验证明，仅靠 LLM 内部知识或纯文本 RAG 无法达到医疗级的准确度，结构化的混合知识库是实现复杂数值约束和语义推理的关键。

### 7. 优点
*   **架构创新**：提出 R+G 的混合知识模型，解决了图数据库处理数值区间难、关系数据库处理多跳推理难的痛点。
*   **高可追溯性**：每一条审计结论都直接链接到原始说明书文本，符合循证医学要求。
*   **处理不确定性**：系统能够识别“信息缺失”，在患者数据不足时提醒药师，而非盲目给出结论，这在医疗 AI 中至关重要。

### 8. 不足与局限
*   **临床语境缺失**：在适应症审核中精确度较低（0.46），原因是系统无法识别某些“临床惯例”（如生理盐水作为溶剂被误报为无适应症用药）。
*   **实时性挑战**：虽然论文提到架构高效，但在超大规模知识库下的多 Agent 协作延迟尚未详细讨论。
*   **依赖高质量输入**：系统性能高度依赖于 PDF 解析（MinerU）的准确性和初始模式的质量。

（完）
