---
title: Acoustic and Semantic Modeling of Emotion in Spoken Language
title_zh: 口语中情感的声学与语义建模
authors: Soumya Dutta
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09212v1"
tags: ["query:llm-ac"]
score: 9.0
evidence: 大语言模型中情感的声学和语义建模
tldr: 本研究探讨了口语中情感的声学与语义联合建模，旨在提升AI对人类情感的理解与生成能力。论文首先提出了结合声学和语义监督的预训练策略，以学习更强的情感表征；其次，针对对话场景开发了融合跨模态注意力和专家混合机制的层次化识别架构；最后，引入了一种无文本、非平行的语音到语音情感风格迁移框架。研究证明了该方法在情感识别和风格迁移方面的优越性，并展示了风格迁移数据对识别性能的增强作用。
selection_source: fresh_fetch
motivation: 随着大语言模型的普及，如何让AI系统可靠地理解和生成口语中蕴含的复杂人类情感成为一个关键挑战。
method: 论文采用了结合声学与语义监督的预训练、基于跨模态注意力的层次化对话情感识别以及非平行的语音情感风格迁移技术。
result: 实验结果显示，所提方法显著提升了情感表征学习的质量、对话情感识别的准确率以及情感风格迁移的自然度。
conclusion: 通过联合建模声学与语义信息，可以有效增强语音情感的理解与合成，且风格迁移生成的语音可作为数据增强手段进一步优化识别模型。
---

## 摘要
情感在人类交流中起着核心作用，塑造了信任、参与度和社会互动。随着由大语言模型驱动的人工智能系统日益融入日常生活，使它们能够可靠地理解和生成人类情感仍然是一个重要的挑战。虽然情感表达本质上是多模态的，但本论文专注于通过口语传达的情感，并研究如何对声学和语义信息进行联合建模，以推进语音情感理解和情感合成。论文的第一部分研究了通过预训练进行情感感知表征学习。我们提出了结合声学和语义监督的策略，以学习能够更好捕捉语音中情感线索的表征。此外，还引入了一个语音驱动的有监督预训练框架，以便在不需要人工标注文本语料库的情况下实现大规模情感感知文本建模。第二部分探讨了对话场景下的情感识别。开发了结合跨模态注意力和混合专家融合的分层架构，以整合对话轮次中的声学和语义信息。最后，论文介绍了一个无文本且非平行的语音到语音情感风格迁移框架，该框架在保留说话人身份和语言内容的同时，实现了可控的情感转换。结果表明，情感迁移效果得到了提升，并证明风格迁移后的语音可用于数据增强，从而提高情感识别性能。

## Abstract
Emotions play a central role in human communication, shaping trust, engagement, and social interaction. As artificial intelligence systems powered by large language models become increasingly integrated into everyday life, enabling them to reliably understand and generate human emotions remains an important challenge. While emotional expression is inherently multimodal, this thesis focuses on emotions conveyed through spoken language and investigates how acoustic and semantic information can be jointly modeled to advance both emotion understanding and emotion synthesis from speech. The first part of the thesis studies emotion-aware representation learning through pre-training. We propose strategies that incorporate acoustic and semantic supervision to learn representations that better capture affective cues in speech. A speech-driven supervised pre-training framework is also introduced to enable large-scale emotion-aware text modeling without requiring manually annotated text corpora. The second part addresses emotion recognition in conversational settings. Hierarchical architectures combining cross-modal attention and mixture-of-experts fusion are developed to integrate acoustic and semantic information across conversational turns. Finally, the thesis introduces a textless and non-parallel speech-to-speech framework for emotion style transfer that enables controllable emotional transformations while preserving speaker identity and linguistic content. The results demonstrate improved emotion transfer and show that style-transferred speech can be used for data augmentation to improve emotion recognition.

---

## 论文详细总结（自动生成）

这是一份关于论文《Acoustic and Semantic Modeling of Emotion in Spoken Language》（口语中情感的声学与语义建模）的深度结构化总结：

### 1. 论文的核心问题与整体含义
本论文探讨了在口语交流中如何通过联合建模**声学（Acoustic）**和**语义（Semantic）**信息来提升人工智能对情感的理解与生成能力。
*   **研究动机**：尽管大语言模型（LLM）在文本处理上表现优异，但在处理语音情感时，往往依赖浅层的语义启发式，忽略了细微的声学变化（如音调、能量、节奏）。
*   **核心挑战**：现有的自监督学习（SSL）模型多以语言重建为目标，缺乏情感感知；对话中的情感识别（ERC）面临模态不平衡和上下文依赖问题；情感风格迁移则难以在无文本、非平行语料下实现情感与说话人身份的解耦。

### 2. 论文提出的方法论
论文分为三个主要部分，提出了多个创新模型：

*   **情感感知预训练（CARE 模型）**：
    *   **核心思想**：采用双编码器架构，分别引入声学和语义监督。
    *   **技术细节**：语义编码器通过卷积适配器将语音对齐到预训练 RoBERTa 的文本表征（使用 ASR 生成转录）；声学编码器以 PASE+ 特征为目标进行帧级预测。两者共享一个基于 WavLM 的通用编码器。
*   **对话情感识别（MiSTER-E 框架）**：
    *   **核心思想**：使用专家混合机制（MoE）在决策层融合模态。
    *   **技术细节**：包含语音专家（SALMONN）、文本专家（LLaMA）和多模态专家（跨模态注意力）。通过门控网络动态分配权重，并引入焦点损失（Focal Loss）解决类别不平衡，利用 KL 散度保持专家间的一致性。
*   **情感风格迁移（S2S-ZEST 框架）**：
    *   **核心思想**：基于解耦表征学习的语音到语音迁移。
    *   **技术细节**：将语音分解为内容（Soft-HuBERT 离散标记）、说话人（ECAPA-TDNN 嵌入）和情感（帧级/篇章级嵌入）。通过预测目标情感的基频（F0）和时长，利用 BigVGAN 合成保留原说话人身份但具有新情感的语音。

### 3. 实验设计
*   **数据集**：
    *   **预训练与自然场景**：MSP-PODCAST（约 409 小时）。
    *   **对话识别**：IEMOCAP、MELD、CMU-MOSI。
    *   **风格迁移**：ESD（英语部分）、CREMA-D、TIMIT。
    *   **其他**：DAIC-WOZ（抑郁检测）、RAVDESS-Song（歌唱情感）、Emo-DB（德语）、CaFE（法语）。
*   **Benchmark 与对比方法**：
    *   对比了 WavLM、HuBERT、data2vec 等主流 SSL 模型。
    *   对比了超大规模模型如 SONAR、SALMONN（7B/13B）。
    *   风格迁移对比了 StarGANv2-EST 和 VEVO。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA RTX A6000** GPU。
*   **训练时长**：
    *   CARE 预训练：200,000 步。
    *   MiSTER-E：LLM 微调约 10 分钟/epoch，SLLM 微调约 20 分钟/epoch，后续模块 100 epoch 约 10 分钟。
    *   S2S-ZEST：联合训练 200 epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从预训练、识别到合成的全流程，实验非常详尽。
*   **充分性**：
    *   在 **8 个不同数据集**上验证了 CARE 的泛化性。
    *   进行了大量的**消融实验**（如不同初始化方式、不同监督目标、不同损失函数的影响）。
    *   针对风格迁移，不仅有客观指标（WER、情感相似度、说话人相似度），还进行了 **30 人规模的主观听感测试（MOS）**。
*   **公平性**：所有对比模型在下游任务中均使用相同的分类后端，确保了表征能力的客观评价。

### 6. 主要结论与发现
*   **联合建模的优越性**：同时利用声学和语义特征的模型（如 CARE）在情感任务上显著优于单一模态或通用 SSL 模型。
*   **MoE 的有效性**：在模态贡献不均（如 MELD 数据集文本强、语音弱）时，决策层的 MoE 融合比特征层融合更稳健。
*   **解耦迁移可行性**：S2S-ZEST 证明了在无文本、非平行条件下，可以实现高质量的情感风格迁移，且生成的样本可作为数据增强手段提升识别模型性能（在低资源下提升约 5.6%）。
*   **模型规模并非唯一**：较小的专用模型（如 160M 的 CARE）在情感任务上可以超越参数量大几十倍的通用大模型（如 13B 的 SALMONN）。

### 7. 优点与亮点
*   **模块化设计**：MiSTER-E 的专家解耦设计有效解决了多模态任务中的“模态竞争”问题。
*   **零射迁移能力**：S2S-ZEST 实现了对未见说话人和未见情感类别的零样本迁移。
*   **实用性强**：提出了利用 LLM 为无标注语音生成“伪标签”的预训练策略，降低了对人工标注的依赖。

### 8. 不足与局限
*   **计算开销**：尽管提出了高效方案，但引入 LLM/SLLM 作为特征提取器仍带来较大的内存和计算负担。
*   **语言局限**：大部分实验集中在英语，虽然在德语和法语上做了简要验证，但跨语言情感建模的深度仍有待加强。
*   **黑盒问题**：论文缺乏对模型预测情感的解释性分析（即为什么模型认为这段语音是某种情感）。
*   **ASR 依赖**：语义建模高度依赖 ASR 质量，在噪声环境或极端情感导致 ASR 失效时，模型性能会受损。

（完）
