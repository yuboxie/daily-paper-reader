---
title: "AMB-DSGDN: Adaptive Modality-Balanced Dynamic Semantic Graph Differential Network for Multimodal Emotion Recognition"
title_zh: AMB-DSGDN：用于多模态情感识别的自适应模态平衡动态语义图微分网络
authors: "Yunsheng Wang, Yuntao Shou, Yilong Tan, Wei Ai, Tao Meng, Keqin Li"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.10043v1"
tags: ["query:llm-ac"]
score: 9.0
evidence: 多模态对话情感识别
tldr: 针对多模态对话情感识别中冗余噪声干扰和模态不平衡问题，本文提出AMB-DSGDN模型。该模型通过构建模态特定的动态语义子图捕捉说话人内外的情感依赖，利用差分图注意力机制过滤噪声并提取判别性特征，并设计自适应模态平衡机制协调各模态贡献，有效提升了识别性能。
selection_source: fresh_fetch
motivation: 现有方法难以过滤多模态特征中的冗余噪声，且主导模态容易抑制非主导模态的贡献，限制了整体识别性能。
method: 提出一种结合差分图注意力机制和自适应模态平衡机制的动态语义图差分网络，通过对比注意力分布去除噪声并动态调整模态权重。
result: 该方法成功提取了更纯净、更具判别性的情感表示，并有效缓解了模态间的不平衡问题。
conclusion: AMB-DSGDN通过差分去噪和自适应平衡策略，为多模态对话情感识别提供了一种更稳健且高效的解决方案。
---

## 摘要
多模态对话情感识别通过融合文本、视觉和音频模态来捕捉情感线索。然而，现有方法在建模情感依赖关系和学习多模态表示方面仍存在显著局限性。一方面，它们无法有效过滤多模态特征中的冗余或噪声信号，这阻碍了对说话人内部及说话人之间情感状态动态演变的准确捕捉。另一方面，在多模态特征学习过程中，主导模态往往会压倒融合过程，从而抑制语音和视觉等非主导模态的互补贡献，最终限制了整体识别性能。为了应对这些挑战，我们提出了一种自适应模态平衡动态语义图微分网络（AMB-DSGDN）。具体而言，我们首先为文本、语音和视觉构建特定模态的子图，其中每个模态包含说话人内部和说话人之间的图，以捕捉自我连续性和跨说话人的情感依赖。在这些子图之上，我们引入了一种微分图注意力机制，该机制计算两组注意力图之间的差异。通过显式对比这些注意力分布，该机制抵消了共享的噪声模式，同时保留了特定模态和上下文相关的信号，从而产生更纯净、更具辨别力的情感表示。此外，我们设计了一种自适应模态平衡机制，根据各模态在情感建模中的相对贡献来估计其丢弃概率。

## Abstract
Multimodal dialogue emotion recognition captures emotional cues by fusing text, visual, and audio modalities. However, existing approaches still suffer from notable limitations in modeling emotional dependencies and learning multimodal representations. On the one hand, they are unable to effectively filter out redundant or noisy signals within multimodal features, which hinders the accurate capture of the dynamic evolution of emotional states across and within speakers. On the other hand, during multimodal feature learning, dominant modalities tend to overwhelm the fusion process, thereby suppressing the complementary contributions of non-dominant modalities such as speech and vision, ultimately constraining the overall recognition performance. To address these challenges, we propose an Adaptive Modality-Balanced Dynamic Semantic Graph Differential Network (AMB-DSGDN). Concretely, we first construct modality-specific subgraphs for text, speech, and vision, where each modality contains intra-speaker and inter-speaker graphs to capture both self-continuity and cross-speaker emotional dependencies. On top of these subgraphs, we introduce a differential graph attention mechanism, which computes the discrepancy between two sets of attention maps. By explicitly contrasting these attention distributions, the mechanism cancels out shared noise patterns while retaining modality-specific and context-relevant signals, thereby yielding purer and more discriminative emotional representations. In addition, we design an adaptive modality balancing mechanism, which estimates a dropout probability for each modality according to its relative contribution in emotion modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《AMB-DSGDN: Adaptive Modality-Balanced Dynamic Semantic Graph Differential Network for Multimodal Emotion Recognition》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
多模态对话情感识别（MERC）旨在通过融合文本、音频和视觉信息来识别说话者的情感状态。论文指出当前研究面临两大核心挑战：
*   **情感依赖建模的噪声干扰：** 现有方法难以过滤多模态特征中的冗余或噪声信号，导致无法准确捕捉说话人内部（自我连续性）和说话人间（情感传染）情感状态的动态演变。
*   **模态不平衡问题：** 在融合过程中，主导模态（通常是文本）往往会压倒非主导模态（语音、视觉），抑制了后者的互补贡献，限制了模型的整体性能。

### 2. 论文提出的方法论
论文提出了一种名为 **AMB-DSGDN** 的框架，其核心思想是通过“差分去噪”和“动态平衡”来优化多模态表示。关键技术包括：
*   **多模态关系子图构建：** 为文本、语音、视觉分别构建独立的子图，每个子图包含“说话人内”和“说话人间”两种边，捕捉时间演化和交互关系。
*   **微分图注意力网络（DiffRGCN）：** 
    *   设计了正向和负向两个注意力分支，分别模拟情感增强和抑制信号。
    *   通过计算两组注意力图的差异（差分操作），抵消跨模态的共享噪声，保留特定模态且与上下文相关的判别性信号。
*   **自适应模态平衡机制（Adaptive Modality Dropout）：**
    *   根据每个模态在当前 Batch 中的相对性能（F1 分数）动态计算丢弃概率。
    *   对表现过强的“主导模态”进行随机特征丢弃，并对保留特征进行期望补偿缩放，从而强制模型学习弱势模态的互补信息。

### 3. 实验设计
*   **数据集：** 使用了两个基准数据集：**IEMOCAP**（双人对话，6类情感）和 **MELD**（多方对话，7类情感）。
*   **对比方法（Benchmarks）：** 对比了 12 种 SOTA 模型，包括基于 RNN 的（DialogueRNN）、基于图网络的（DialogueGCN, MMGCN, COGMEN, DER-GCN）以及最新的多模态融合模型（DEDNet, MERC-PLTAF）。
*   **评估指标：** 加权准确率（wa-ACC）和加权 F1 分数（wa-F1）。

### 4. 资源与算力
*   **软件框架：** 基于 PyTorch 实现。
*   **超参数：** 学习率 0.000068，Batch size 16，Adam 优化器，训练 100 轮。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号和数量。但在复杂度分析章节中对比了推理时间（ms）、吞吐量（samples/s）和 FLOPs，显示其计算开销较标准 GAT 有所增加（约 11%-24% 的推理延迟增加）。

### 5. 实验数量与充分性
论文进行了极其详尽的实验验证，具有很高的客观性和公平性：
*   **主实验：** 在两个数据集上验证了性能领先性。
*   **消融实验：** 验证了模态组合（单/双/全模态）、窗口大小、DiffRGCN 模块、模态平衡机制的有效性。
*   **敏感性分析：** 探讨了注意力头数、预热轮数（Warm-up）、丢弃概率参数的影响。
*   **鲁棒性测试：** 引入了 0.1 到 0.7 强度的全模态高斯噪声，验证模型在极端环境下的稳定性。
*   **长序列分析：** 专门针对 20-50 轮的话语序列进行了性能评估。
*   **极端不平衡测试：** 模拟文本模态权重占 80% 的极端情况，验证自适应调节能力。

### 6. 主要结论与发现
*   **性能提升：** 在 IEMOCAP 上 wa-F1 达到 75.64%（提升 1.85%），在 MELD 上达到 66.18%。
*   **差分机制有效性：** 差分注意力能显著过滤多模态冗余，使情感表示更“纯净”。
*   **模态平衡价值：** 动态丢弃主导模态特征能有效激活弱势模态的潜力，尤其在文本信息模糊时，音视频的补充作用被放大。
*   **长序列优势：** 相比 RNN 类方法，该模型在长对话中表现更稳健，不易出现上下文遗忘。

### 7. 优点（亮点）
*   **创新的去噪思路：** 引入“差分”概念处理图注意力，为解决多模态特征冗余提供了新视角。
*   **动态调节能力：** 不同于固定权重的融合，自适应丢弃机制能随训练进程实时调整模态贡献。
*   **精细化建模：** 区分说话人内外子图，并结合关系嵌入（Relation Embedding），使图卷积更具语义感知力。

### 8. 不足与局限
*   **计算开销：** 差分操作和多子图构建增加了计算复杂度，推理速度慢于简单的图网络模型。
*   **数据集偏差：** 在 MELD 数据集上的提升幅度有限，论文归因于该数据集类别极度不平衡及音视频质量较低。
*   **边缘部署挑战：** 论文提到由于图结构的复杂性，该模型在资源受限的边缘设备上实时运行仍面临挑战。
*   **超参数依赖：** 模态平衡机制依赖于预热轮数和基础丢弃概率的设置，可能需要针对不同领域数据进行微调。

（完）
