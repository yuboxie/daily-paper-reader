---
title: How Long Can Unified Multimodal Models Generate Images Reliably? Taming Long-Horizon Interleaved Image Generation via Context Curation
title_zh: 统一多模态模型能可靠地生成图像多久？通过上下文策展优化长程交错图像生成
authors: "Haoyu Chen, Qing Liu, Yuqian Zhou, He Zhang, Zhaowen Wang, Mengwei Ren, Jingjing Ren, Xiang Wang, Zhe Lin, Lei Zhu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07540v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 研究多模态模型中的长程交错图像生成和上下文策划
tldr: 本研究探讨了统一多模态模型在长序列交错图文生成中的可靠性问题。研究发现，随着生成长度增加，累积的视觉历史会产生“主动污染”，导致生成质量崩溃。为此，作者提出了UniLongGen，这是一种无需训练的推理策略，通过动态筛选和舍弃干扰性的视觉信号，实现上下文管理。实验证明，该方法在提升长程生成保真度和一致性的同时，显著降低了显存占用和推理时间。
selection_source: fresh_fetch
motivation: 现有多模态模型在长序列生成时，累积的视觉历史会干扰注意力机制，导致生成质量随图像事件增加而迅速下降。
method: 提出UniLongGen推理策略，利用模型内部相关性排名动态筛选上下文，主动遗忘干扰性的视觉信号。
result: 实验表明UniLongGen在长程生成的保真度与一致性上优于基准模型，同时降低了显存占用和推理耗时。
conclusion: 通过主动筛选上下文而非盲目保留全部历史，可以有效解决多模态模型在长序列生成中的质量崩溃问题。
---

## 摘要
统一多模态模型有望生成广泛的交错叙事，将文本和图像编织成连贯的长篇故事。然而，目前的系统面临着严重的可靠性差距：随着序列长度的增加，生成质量会迅速崩溃。在这项工作中，我们研究了这种失效背后的机制，并认为它与标准的长上下文挑战不同。我们揭示了在生成过程中，累积的视觉历史充当了主动污染源，这种衰减具体受图像事件数量而非原始 token 数量的支配。我们发现了一个结构性漏洞，即密集的视觉 token 会淹没注意力机制，产生扭曲未来合成的噪声。在这些机制见解的指导下，我们提出了 UniLongGen，这是一种无需训练的推理策略，它优先考虑安全条件约束而非全量召回。UniLongGen 不保留所有历史记录，而是动态策展模型的记忆，根据模型自身的内部相关性排名识别并丢弃干扰视觉信号。大量实验表明，这种主动遗忘方法对于稳定性至关重要：UniLongGen 在长程保真度和一致性方面显著优于基准模型，同时减少了内存占用和推理时间。

## Abstract
Unified multimodal models hold the promise of generating extensive, interleaved narratives, weaving text and imagery into coherent long-form stories. However, current systems suffer from a critical reliability gap: as sequences grow, generation quality rapidly collapses. In this work, we investigate the mechanism behind this failure and argue that it is distinct from standard long-context challenges. We reveal that in generation, accumulated visual history acts as a source of active pollution, a decay governed specifically by the number of image events rather than raw token count. We identify a structural vulnerability where dense visual tokens overwhelm the attention mechanism, creating noise that distorts future synthesis. Guided by these mechanistic insights, we propose UniLongGen, a training-free inference strategy that prioritizes safe conditioning over total recall. Instead of retaining all history, UniLongGen dynamically curates the model's memory, identifying and discarding interfering visual signals based on the model's own internal relevance rankings. Extensive experiments demonstrate that this active forgetting approach is essential for stability: UniLongGen significantly outperforms baselines in long-horizon fidelity and consistency, while simultaneously reducing memory footprint and inference time.

---

## 论文详细总结（自动生成）

这是一份关于论文《How Long Can Unified Multimodal Models Generate Images Reliably? Taming Long-Horizon Interleaved Image Generation via Context Curation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：统一多模态模型（UMMs）在生成长篇交错图文故事时存在“可靠性鸿沟”。虽然短序列表现优异，但随着生成长度增加（通常在20张图像后），图像质量会迅速崩溃，出现结构扭曲、身份（Identity）漂移和风格失真。
*   **研究动机**：作者发现这种失效并非单纯的“长上下文”Token溢出问题，而是一种**“事件瓶颈”（Event Bottleneck）**。
*   **关键发现**：
    *   **主动污染 vs. 被动稀释**：在纯文本中，历史过长会导致信息被“稀释”（模糊）；但在视觉生成中，密集的历史图像Token会产生“主动污染”。
    *   **注意力劫持**：由于Softmax的指数放大效应，历史图像中无关的视觉Token可能产生虚假的高相似度匹配，从而“劫持”注意力，将错误的细节注入当前合成步骤。

### 2. 方法论（UniLongGen）
UniLongGen 是一种**无需训练（Training-free）**的推理期上下文策展策略，核心思想是“主动遗忘”以确保“安全条件约束”。

*   **核心流程**：
    1.  **单次上下文剖析（One-Pass Profiling）**：在生成每张新图前，先用全量历史KV进行一次前向传播，探测模型内部的注意力信号。
    2.  **双深度评分（Dual-Depth Scoring）**：利用模型层的专业化分工进行筛选：
        *   **早期层（Early Layers）**：负责文本依赖的“接地”（Grounding），用于筛选相关的文本块。
        *   **后期层（Late Layers）**：负责VAE主导的“合成”（Synthesis），用于筛选相关的图像块。
    3.  **层拆分生成（Layer-Split Generation）**：在实际生成时，强制执行可见性策略：早期层只关注筛选出的文本，后期层只关注筛选出的图像，直接从KV Cache中剔除（Evict）非选中的Token。
*   **关键公式/逻辑**：通过计算当前查询（Query）均值与历史键（Key）的预Softmax点积相似度来定义相关性评分，从而避免Softmax带来的噪声干扰。

### 3. 实验设计
*   **数据集/场景**：构建了一个包含 **50个长程叙事模板** 的基准测试。每个模板包含40个图像生成位点，涉及重复出现的角色、持久的风格以及复杂的场景转换。
*   **评估指标**：
    *   **图像质量**：HPS v3、PickScore、GPT-5.2（作为多模态大模型裁判）。
    *   **一致性**：DINOv2（嵌入相似度）、GPT-5.2（角色身份和风格一致性评分）。
*   **对比方法**：
    *   **Dense KV**（全量历史）。
    *   **滑动窗口**（保留最近N个图像）。
    *   **Oracle（神谕）筛选**：基于人工标注的最相关图像。
    *   **Token级剪枝**：而非图像块级剪枝。

### 4. 资源与算力
*   **模型基础**：主要基于 **BAGEL**（一种混合自回归-扩散模型）进行实验，并在 **Lumina-mGPT**（纯自回归模型）上验证了泛化性。
*   **算力说明**：论文**未明确列出**具体的GPU型号、数量或总训练时长。但由于该方法是“无需训练”的推理策略，其核心优势在于降低了显存占用，并实现了最高 **11倍** 的推理加速（在1024x1024分辨率、350k Token背景下）。

### 5. 实验数量与充分性
*   **实验规模**：针对50个故事模板，每个生成40张图，总计生成并评估了数千张图像。
*   **消融实验**：非常详尽，涵盖了：
    *   探测信号的选择（文本 vs. ViT vs. VAE）。
    *   保留单元的粒度（图像级 vs. Token级）。
    *   丢弃处理方式（直接剔除 vs. 压缩/池化）。
    *   不同层的专业化分析。
*   **客观性**：通过对比“人工神谕筛选”发现，模型自身选择的上下文比人类认为相关的上下文效果更好，证明了“模型对齐”的重要性。实验设计考虑了随机种子影响，并使用了多种自动化和人类对齐的指标，较为客观公平。

### 6. 主要结论与发现
*   **图像计数比Token计数更重要**：失效点取决于图像事件的数量（约20张），而非Token的总数。
*   **剔除优于压缩**：直接从KV Cache中删除无关图像比使用池化等压缩手段更有效，因为压缩可能保留并放大“干扰噪声”。
*   **层专业化**：模型在不同深度对模态的关注点不同，UniLongGen 的层拆分策略精准匹配了这一特性。
*   **性能提升**：UniLongGen 在保持长程一致性的同时，显著解决了质量崩溃问题，且大幅提升了长序列下的推理效率。

### 7. 优点
*   **机制洞察深**：不仅提出了方法，还深入解释了“视觉污染”和“注意力劫持”的物理机制。
*   **即插即用**：无需重新训练模型，适用于现有的统一多模态架构。
*   **效率与质量双赢**：在提升生成质量的同时，通过KV Cache优化降低了计算开销。
*   **模型对齐**：利用模型自身的注意力偏好进行策展，优于外部检索器或人工启发式规则。

### 8. 不足与局限
*   **超参数依赖**：保留的图像数量 $K$ 和探测层的选择仍需手动设定，虽然实验证明其相对稳定，但在不同架构间可能需要微调。
*   **单次探测风险**：如果初始的单次探测（One-shot Probing）因为提示词模糊而产生偏差，可能会导致后续生成的上下文选择错误。
*   **应用限制**：主要针对交错图文生成，对于纯视频生成或极短序列的提升可能不明显。
*   **评估依赖**：高度依赖 GPT-5.2 等高级 LMM 作为裁判，可能存在模型偏见。

（完）
