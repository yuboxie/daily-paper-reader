---
title: "AdaMem: Adaptive User-Centric Memory for Long-Horizon Dialogue Agents"
title_zh: AdaMem：面向长程对话智能体的自适应以用户为中心的记忆系统
authors: "Shannan Yan, Jingchen Ni, Leqi Zheng, Jiajun Zhang, Peixi Wu, Dacheng Yin, Jing Lyu, Chun Yuan, Fengyun Rao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16496v1"
tags: ["query:lh-llm"]
score: 10.0
evidence: 长程对话智能体的自适应用户中心记忆
tldr: 针对长程对话智能体在记忆检索中存在的语义依赖过强、缺乏连贯性及粒度固定等问题，本文提出 AdaMem 框架。该框架通过整合工作记忆、情景记忆、人格记忆和图记忆，构建了多维度的用户中心记忆系统。在推理阶段，AdaMem 采用问题驱动的自适应检索路径和角色专业化流水线，实现了对复杂长程交互的高效建模。实验证明，AdaMem 在 LoCoMo 和 PERSONAMEM 基准测试中均达到 SOTA 水平。
selection_source: fresh_fetch
motivation: 现有记忆系统过度依赖语义相似度且缺乏时序因果连贯性，难以适应不同问题的动态粒度需求。
method: 提出一种自适应记忆框架，通过多维记忆组织（工作、情景、人格、图）和问题驱动的动态检索路径来增强长程对话能力。
result: 在 LoCoMo 和 PERSONAMEM 两个长程推理与用户建模基准测试中，AdaMem 均取得了当前最先进的性能表现。
conclusion: AdaMem 通过多层次记忆结构与自适应检索机制，有效提升了对话智能体在长周期交互中的个性化理解与推理能力。
---

## 摘要
大语言模型（LLM）智能体日益依赖外部记忆来支持长程交互、个性化辅助和多步推理。然而，现有的记忆系统仍面临三个核心挑战：它们往往过度依赖语义相似性，这可能会遗漏对以用户为中心的理解至关重要的证据；它们经常将相关的经历存储为孤立的片段，削弱了时间与因果的连贯性；且它们通常使用静态的记忆粒度，无法很好地适应不同问题的需求。我们提出了 AdaMem，一种面向长程对话智能体的自适应以用户为中心的记忆框架。AdaMem 将对话历史组织为工作记忆、情景记忆、人格记忆和图记忆，使系统能够在统一框架内保留近期上下文、结构化的长期经历、稳定的用户特征以及关系感知的连接。在推理时，AdaMem 首先解析目标参与者，然后构建一个以问题为条件的检索路径，该路径仅在需要时将语义检索与关系感知图扩展相结合，最后通过角色专门化的流水线进行证据合成和响应生成，从而产生答案。我们在 LoCoMo 和 PERSONAMEM 基准测试上评估了 AdaMem，涵盖了长程推理和用户建模。实验结果表明，AdaMem 在这两个基准测试上均达到了最先进的性能。代码将在论文被接收后发布。

## Abstract
Large language model (LLM) agents increasingly rely on external memory to support long-horizon interaction, personalized assistance, and multi-step reasoning. However, existing memory systems still face three core challenges: they often rely too heavily on semantic similarity, which can miss evidence crucial for user-centric understanding; they frequently store related experiences as isolated fragments, weakening temporal and causal coherence; and they typically use static memory granularities that do not adapt well to the requirements of different questions. We propose AdaMem, an adaptive user-centric memory framework for long-horizon dialogue agents. AdaMem organizes dialogue history into working, episodic, persona, and graph memories, enabling the system to preserve recent context, structured long-term experiences, stable user traits, and relation-aware connections within a unified framework. At inference time, AdaMem first resolves the target participant, then builds a question-conditioned retrieval route that combines semantic retrieval with relation-aware graph expansion only when needed, and finally produces the answer through a role-specialized pipeline for evidence synthesis and response generation. We evaluate AdaMem on the LoCoMo and PERSONAMEM benchmarks for long-horizon reasoning and user modeling. Experimental results show that AdaMem achieves state-of-the-art performance on both benchmarks. The code will be released upon acceptance.

---

## 论文详细总结（自动生成）

### AdaMem: 面向长程对话智能体的自适应以用户为中心的记忆系统

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体在长周期交互中的应用增加，外部记忆系统变得至关重要。然而，现有记忆系统存在三个核心局限：
*   **过度依赖语义相似性**：仅靠向量检索容易忽略与查询语义不直接相关但对理解用户至关重要的稳定偏好或行为模式。
*   **记忆碎片化**：将经历存储为孤立片段，削弱了事件间的时间和因果连贯性。
*   **静态记忆粒度**：固定长度的分块或粗粒度摘要无法灵活适应不同问题的需求（如事实性问题 vs. 跨度大的时间性问题）。
**AdaMem** 的提出旨在构建一个自适应、以用户为中心的记忆框架，通过多层次结构化存储和动态检索路径解决上述问题。

#### 2. 论文提出的方法论
AdaMem 采用了一种“四位一体”的记忆组织方式和基于角色的多智能体协作流水线：
*   **核心思想**：将对话历史按参与者（用户/助手）分别组织，并根据问题类型动态选择检索路径。
*   **四种记忆结构**：
    1.  **工作记忆 (Working Memory)**：有限容量的 FIFO 缓冲区，保留近期上下文。
    2.  **情景记忆 (Episodic Memory)**：长期结构化记录，包括事件、事实、属性和主题摘要。
    3.  **人格记忆 (Persona Memory)**：从情景记忆中提炼的稳定用户特征和偏好。
    4.  **图记忆 (Graph Memory)**：连接消息、主题、事实和属性的异构图，支持关系感知检索。
*   **关键技术流程**：
    *   **记忆构建**：Memory Agent 将原始对话归一化为包含主题、态度、事实等字段的记录，并定期将工作记忆固化为情景记忆，同步更新图结构。
    *   **自适应检索**：首先解析目标参与者，然后由路由规划器决定是否触发图扩展（如针对时间或因果问题）。
    *   **多智能体协作**：**Research Agent** 负责迭代式证据搜集与反思；**Working Agent** 负责将搜集的证据合成为最终答案。

#### 3. 实验设计
*   **数据集/基准测试**：
    *   **LoCoMo**：评估长程推理能力（单步、多步、时间推理及开放域问答），平均包含 35 个会话，约 9000 个 token。
    *   **PERSONAMEM**：评估用户建模和个性化响应能力，涵盖用户属性演变等 7 个类别。
*   **对比方法**：对比了 5 种代表性记忆框架：MemGPT、A-Mem、Mem0、LangMem 和 Zep。
*   **骨干模型**：使用了 GPT-4.1-mini、GPT-4o-mini 以及开源的 Qwen3-4B 和 Qwen3-30B。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA RTX A800 GPU** 上进行。
*   **参数设置**：检索 Top-K 设为 10，Research Agent 最大迭代次数为 2。
*   **训练时长**：文中未明确提及训练时长，因为该框架主要基于 RAG（检索增强生成）和 Prompt Engineering，侧重于推理阶段的记忆管理而非模型微调。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流长程对话基准上进行了全面测试，并针对不同规模的骨干模型（从 4B 到 30B 到闭源 API）验证了泛化性。
*   **消融实验**：详细分析了图记忆、融合模块、多智能体协作对性能的影响，并探讨了检索 Top-K 和迭代次数的敏感性。
*   **客观性**：实验设置了固定的超参数和随机种子（Temperature=0），对比了多种 SOTA 方法，实验设计较为客观、公平。

#### 6. 论文的主要结论与发现
*   **性能领先**：AdaMem 在 LoCoMo 和 PERSONAMEM 上均取得了 SOTA 结果。在 GPT-4.1-mini 下，LoCoMo 的 F1 分数提升了 4.4%；在时间推理类别中，提升幅度高达 **23.4%**。
*   **结构化优势**：图记忆对于恢复跨轮次依赖和时间关联证据至关重要。
*   **自适应性**：问题驱动的路由规划能有效平衡检索精度与计算成本，避免了简单问题过度检索带来的噪声。

#### 7. 优点
*   **多层次记忆融合**：成功将短期上下文、长期事实和抽象人格特征整合在统一框架内。
*   **动态路由机制**：不再使用单一的检索策略，而是根据问题类型（如“为什么”或“何时”）自适应调整检索深度。
*   **角色专业化**：通过 Memory/Research/Working Agent 的分工，减少了记忆维护与答案生成之间的干扰。

#### 8. 不足与局限
*   **系统复杂性与延迟**：多智能体协作和图扩展增加了推理延迟（平均 4.7 秒，高于 Mem0 的 3.7 秒）和 Token 消耗。
*   **依赖上游解析**：如果初始的消息理解（归一化）出现错误，后续的记忆更新和检索将受到级联影响。
*   **实体对齐挑战**：在处理隐式实体链接（如将“这本书”对应到具体书名）和绝对时间转换方面仍有提升空间。

（完）
