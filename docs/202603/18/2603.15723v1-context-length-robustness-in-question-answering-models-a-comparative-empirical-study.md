---
title: "Context-Length Robustness in Question Answering Models: A Comparative Empirical Study"
title_zh: 问答模型中的上下文长度鲁棒性：一项比较实证研究
authors: "Trishita Dhara, Siddhesh Sheth"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15723v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 长且嘈杂上下文中的上下文长度鲁棒性
tldr: 本研究针对大语言模型在长且多噪声上下文中的鲁棒性进行了实证研究。通过在SQuAD和HotpotQA基准上系统性增加无关上下文，研究发现模型性能随长度增加而下降，且多跳推理任务（HotpotQA）的准确率下降幅度几乎是单跨度提取任务（SQuAD）的两倍。该研究揭示了任务类型对上下文鲁棒性的显著影响，强调了在长文档应用中评估此类鲁棒性的重要性。
selection_source: fresh_fetch
motivation: 旨在探究大语言模型在处理包含大量无关信息的长上下文时，其问答性能的鲁棒性及任务依赖性。
method: 通过在保持答案相关信号不变的情况下，系统性地增加无关上下文长度，对比评估模型在SQuAD和HotpotQA上的表现。
result: 随着上下文长度增加，模型性能普遍下降，且多跳推理任务的准确率降幅约为单跨度提取任务的两倍。
conclusion: 多跳推理对上下文稀释更为敏感，建议在评估长文档或检索增强生成应用的模型可靠性时，应明确考察上下文长度鲁棒性。
---

## 摘要
大语言模型越来越多地被部署在相关信息嵌入长且多噪声上下文的场景中。尽管如此，在不同的问答任务中，模型对不断增长的上下文长度的鲁棒性仍未得到充分理解。在本研究中，我们利用 SQuAD 和 HotpotQA 这两个广泛使用的基准测试，对大语言模型的上下文长度鲁棒性进行了一项受控实证研究。我们通过在保留包含答案信号的同时系统地增加无关上下文的数量，评估了模型准确率随总上下文长度变化的情况。这使我们能够将上下文长度的影响与任务难度的变化隔离开来。我们的结果显示，随着上下文长度的增加，性能呈现一致的下降，且与单片段抽取任务相比，多跳推理任务的下降幅度明显更大。特别是，在同等的上下文扩展下，HotpotQA 的准确率下降几乎是 SQuAD 的两倍。这些发现强调了鲁棒性中与任务相关的差异，并表明多跳推理特别容易受到上下文稀释的影响。我们认为，在评估模型可靠性时，应明确评估上下文长度鲁棒性，特别是对于涉及长文档或检索增强生成的应用。

## Abstract
Large language models are increasingly deployed in settings where relevant information is embedded within long and noisy contexts. Despite this, robustness to growing context length remains poorly understood across different question answering tasks. In this work, we present a controlled empirical study of context-length robustness in large language models using two widely used benchmarks: SQuAD and HotpotQA.   We evaluate model accuracy as a function of total context length by systematically increasing the amount of irrelevant context while preserving the answer-bearing signal. This allows us to isolate the effect of context length from changes in task difficulty. Our results show a consistent degradation in performance as context length increases, with substantially larger drops observed on multi-hop reasoning tasks compared to single-span extraction tasks. In particular, HotpotQA exhibits nearly twice the accuracy degradation of SQuAD under equivalent context expansions.   These findings highlight task-dependent differences in robustness and suggest that multi-hop reasoning is especially vulnerable to context dilution. We argue that context-length robustness should be evaluated explicitly when assessing model reliability, especially for applications involving long documents or retrieval-augmented generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Context-Length Robustness in Question Answering Models: A Comparative Empirical Study》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究大语言模型（LLM）在处理包含大量无关信息的长上下文时，其问答性能的“上下文长度鲁棒性”（Context-Length Robustness）。
*   **研究动机**：在检索增强生成（RAG）和长文档分析等实际应用中，模型需从长且嘈杂的背景中提取关键信息。现有研究虽关注长文本处理能力，但往往混淆了任务难度与上下文长度的影响。本文旨在通过受控实验，探究在保持答案信号不变的情况下，单纯增加无关上下文长度如何影响不同类型问答任务（单步提取 vs. 多步推理）的性能。

### 2. 方法论
*   **核心思想**：采用“信号+噪声”的构造方式。将包含答案的关键文本（信号段）固定，通过填充无关的文本块（干扰段）来系统性地扩展总上下文长度。
*   **关键技术细节**：
    *   **信号提取**：从原始上下文中提取包含正确答案的 256 标记（tokens）窗口。
    *   **干扰项注入**：从同数据集的其他实例中采样 128 标记的文本块作为干扰项，排除包含答案字符串的片段。
    *   **长度设置**：构建 256、512、1024、2048 四种目标长度。
*   **数学建模**：
    *   定义准确率函数 $Acc_M(L)$ 为长度 $L$ 的函数。
    *   提出**信噪比（SNR）视角**：假设 Transformer 的注意力均匀分布，信号段获得的注意力质量与 $|s_i|/L$ 成正比。对于需要 $k$ 步推理的任务，成功概率按 $(|s_i|/L)^k$ 衰减，解释了为何多跳任务对长度更敏感。

### 3. 实验设计
*   **数据集**：
    *   **SQuAD**：代表单跨度答案提取任务（局部证据提取）。
    *   **HotpotQA**：代表多跳推理任务（需整合多个文档的信息）。
*   **评估模型**：对比了不同规模的指令微调模型：`gpt-4.1` 和 `gpt-4.1-mini`。
*   **Benchmark 与指标**：以 256 长度下的表现为基准，使用**精确匹配（Exact Match, EM）**作为核心评估指标。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：由于实验基于 OpenAI 的 API 模型进行推理评估，主要涉及的是推理调用成本。作者提到使用了确定性解码（Temperature=0），并实施了指数退避算法以处理 API 速率限制。

### 5. 实验数量与充分性
*   **实验规模**：每个数据集随机抽取 200 个验证实例，涵盖 2 个模型、2 个数据集和 4 种长度，总计进行了约 3200 次模型查询。
*   **充分性与客观性**：
    *   **充分性**：虽然 200 个样本量不算巨大，但作者通过 95% 自举置信区间（Bootstrap Confidence Intervals）证明了结果的统计显著性。
    *   **公平性**：实验严格控制了变量，确保不同长度下的答案信号完全一致，且干扰项在不同模型间保持统一，实验设计非常客观。

### 6. 主要结论与发现
*   **性能普遍下降**：随着无关上下文增加，所有模型的准确率均呈下降趋势，证明长上下文本身即构成干扰。
*   **任务依赖性显著**：多跳推理（HotpotQA）的鲁棒性远低于单步提取（SQuAD）。在同等长度扩展下，HotpotQA 的准确率降幅（约 25-27%）几乎是 SQuAD（约 2-6%）的数倍。
*   **模型规模的影响**：增大模型规模（从 mini 到标准版）能提升绝对准确率，但**无法消除**对长度的敏感性，两者的性能下降曲线趋势相似。

### 7. 优点与亮点
*   **受控变量法**：通过固定信号段，成功隔离了“上下文长度”这一单一变量，避免了任务难度变化对实验的干扰。
*   **理论解释力**：引入了简单的数学框架（信噪比衰减），为观察到的实验现象（多跳任务更易崩溃）提供了直观的机械论解释。
*   **实用参考价值**：为 RAG 系统的开发者提供了警示，即简单的增加上下文窗口并不等同于推理能力的提升。

### 8. 不足与局限
*   **长度覆盖有限**：实验最高仅测试到 2048 标记，未涉及目前 LLM 宣称的 100k 甚至更长的极端上下文场景。
*   **评估指标单一**：仅使用 Exact Match，可能无法捕捉到模型在长文本中产生的“部分正确”或“幻觉”细节。
*   **噪声类型单一**：干扰项仅来自同数据集的随机片段，未考虑更具挑战性的对抗性噪声或矛盾信息。
*   **样本量较小**：200 个样本虽然在统计上可行，但在覆盖复杂推理模式方面可能存在局限。

（完）
