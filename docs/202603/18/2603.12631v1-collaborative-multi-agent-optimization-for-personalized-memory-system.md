---
title: Collaborative Multi-Agent Optimization for Personalized Memory System
title_zh: 面向个性化记忆系统的协同多智能体优化
authors: "Wenyu Mao, Haoyang Liu, Zhao Liu, Haosong Tan, Yaorui Shi, Jiancan Wu, An Zhang, Xiang Wang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12631v1"
tags: ["query:lh-llm"]
score: 9.0
evidence: 用于长期对话的多智能体记忆系统
tldr: 针对个性化大模型在长对话记忆管理中的局限，本文提出CoMAM框架。传统方法往往独立优化多智能体系统中的单个智能体，忽略了跨智能体协作对全局性能的影响。CoMAM通过强化学习将智能体执行建模为顺序马尔可夫决策过程，利用排名一致性动态整合局部任务奖励与全局准确性奖励，实现多智能体的协同优化。实验证明，该方法在记忆构建与检索任务中显著优于现有主流系统。
selection_source: fresh_fetch
motivation: 现有的多智能体记忆系统通常独立优化单个智能体，忽略了智能体间的协作依赖，导致局部优化难以保证全局系统性能。
method: 提出CoMAM框架，将智能体协作建模为顺序MDP，并利用基于排名一致性的自适应权重整合局部与全局奖励，实现联合强化学习优化。
result: 实验结果表明，CoMAM在个性化记忆管理任务上超越了领先的记忆系统，验证了协同优化策略的有效性。
conclusion: 通过协同强化学习对多智能体进行联合优化，能有效对齐局部任务改进与全局系统目标，显著提升个性化记忆系统的整体表现。
---

## 摘要
记忆系统通过缓解捕获长期用户-大语言模型（LLM）对话时的上下文窗口限制，对个性化 LLM 至关重要。通常，此类系统利用多个智能体来处理多粒度记忆构建和个性化记忆检索任务。为了优化系统，现有方法侧重于通过提示工程或微调，独立地使智能体专门化于其局部任务。然而，它们忽视了跨智能体协作，而局部智能体的独立优化很难保证全局系统性能。为解决这一问题，我们提出了一种用于多智能体记忆系统的协同强化学习框架（CoMAM），通过联合优化局部智能体来促进协作。具体而言，我们将智能体的执行正则化为序列马尔可夫决策过程（MDP），以将智能体间的依赖关系嵌入到状态转移中，从而产生局部任务奖励（例如记忆构建的信息覆盖率）和全局奖励（即问答准确率）。然后，我们通过局部奖励与全局奖励之间的群体级排名一致性来量化每个智能体的贡献，并将其作为自适应权重来分配全局信用并整合局部-全局奖励。每个智能体都通过这些整合后的奖励进行优化，使局部改进与全局性能保持一致。实验表明，CoMAM 优于领先的记忆系统，验证了我们提出的用于联合优化的协同强化学习的有效性。

## Abstract
Memory systems are crucial to personalized LLMs by mitigating the context window limitation in capturing long-term user-LLM conversations. Typically, such systems leverage multiple agents to handle multi-granular memory construction and personalized memory retrieval tasks. To optimize the system, existing methods focus on specializing agents on their local tasks independently via prompt engineering or fine-tuning. However, they overlook cross-agent collaboration, where independent optimization on local agents hardly guarantees the global system performance. To address this issue, we propose a Collaborative Reinforcement Learning Framework for Multi-Agent Memory Systems (CoMAM), jointly optimizing local agents to facilitate collaboration. Specifically, we regularize agents' execution as a sequential Markov decision process (MDP) to embed inter-agent dependencies into the state transition, yielding both local task rewards (e.g., information coverage for memory construction) and global rewards (i.e., query-answer accuracy). Then, we quantify each agent's contribution via group-level ranking consistency between local and global rewards, treating them as adaptive weights to assign global credit and integrate local-global rewards. Each agent is optimized by these integrated rewards, aligning local improvements with the global performance. Experiments show CoMAM outperforms leading memory systems, validating the efficacy of our proposed collaborative reinforcement learning for joint optimization.

---

## 论文详细总结（自动生成）

以下是对论文《Collaborative Multi-Agent Optimization for Personalized Memory System》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何优化个性化大语言模型（LLM）中的多智能体记忆系统，以解决长期对话超长上下文的管理问题。
*   **研究背景**：现有的记忆系统通常由多个专门智能体组成（如负责构建记忆的智能体和负责检索的智能体）。
*   **研究动机**：现有方法（如提示工程或独立强化学习）倾向于**独立优化**单个智能体。这种“局部专家”的集合忽略了智能体间的协作依赖。例如，构建智能体可能存储了冗余信息，虽然其局部指标（覆盖率）很高，但却干扰了检索智能体的准确性。论文指出，局部最优并不等于全局最优，因此需要一种**联合优化**框架。

### 2. 方法论：CoMAM 框架
CoMAM（Collaborative Multi-Agent Memory system）的核心思想是将多智能体协作建模为一个整体，通过强化学习实现端到端的对齐。
*   **核心组件**：
    1.  **提取智能体（Extraction Agent）**：从原始对话中提取细粒度事实（Fine-grained memory）。
    2.  **画像智能体（Profile Agent）**：从细粒度记忆中抽象出粗粒度用户偏好（Coarse-grained memory）。
    3.  **检索智能体（Retrieval Agent）**：根据查询（Query）从多粒度记忆中检索信息并生成答案。
*   **关键技术细节**：
    *   **执行轨迹正则化（MDP Regularization）**：将异步、异构的智能体执行过程建模为一个顺序马尔可夫决策过程（MDP）。将前一个智能体的输出作为后一个智能体的状态输入，从而在状态转移中嵌入智能体间的依赖关系。
    *   **自适应信用分配（Adaptive Credit Assignment）**：为了解决全局目标（问答准确率）与局部任务（如信息覆盖率）之间的对齐问题，引入了基于 **NDCG（归一化折损累计增益）** 的排名一致性评分。
    *   **奖励整合公式**：计算局部奖励与全局奖励在群体采样中的排名一致性，将其作为权重 $w_n$，最终奖励为：$r_{final} = r_{local} + w_n \cdot r_{global}$。
    *   **优化算法**：采用 **GRPO（群体相对策略优化）** 算法，通过群体内的相对奖励来更新策略，无需额外的 Critic 网络，提高了训练效率。

### 3. 实验设计
*   **数据集**：使用 **PersonaMem** 基准测试，包含 180 多个长期对话历史和约 6,000 个多项选择题。
*   **场景设置**：涵盖了三种不同的上下文长度：**32K、128K 和 1M tokens**。
*   **查询类型**：细分为 7 类，包括事实召回、偏好追踪、建议生成等。
*   **对比方法（Baselines）**：
    *   **无记忆系统**：Base（直接输入）、RAG（原始检索）。
    *   **基于提示的记忆系统**：CAM、MemoryBank、A-Mem。
    *   **基于强化学习的记忆系统**：Mem1、Memory-R1（独立优化策略）。
*   **模型骨干**：采用了 Qwen2.5 (3B/7B) 和 Llama-3.1/3.2 (3B/8B) 系列模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **配置细节**：提到了训练时的 Batch Size 为 128，GRPO 的群体大小（Group Size）为 8，上下文窗口设置为 32K，模型参数量在 3B 到 8B 之间。

### 5. 实验数量与充分性
*   **实验规模**：在 2 种模型家族、3 种上下文长度、7 种查询类型下进行了全面测试。
*   **消融实验**：
    *   验证了三个智能体（提取、画像、检索）各自的必要性。
    *   对比了“独立 RL”与“MDP 联合 RL”的效果。
    *   测试了不同信用分配策略（仅局部、仅全局、固定权重 vs. 自适应权重）的影响。
*   **充分性评价**：实验设计较为充分，通过多维度的消融实验证明了联合优化和自适应信用分配的有效性。对比基准涵盖了从传统 RAG 到最新的 RL 优化方法，具有较强的说服力。

### 6. 主要结论与发现
*   **联合优化优于独立优化**：即使单个智能体在局部任务上表现出色，若不进行联合优化，系统全局性能仍受限。
*   **CoMAM 性能领先**：在所有上下文长度（尤其是 128K 和 1M）下，CoMAM 的准确率显著超过了 Memory-R1 等现有最强基准，提升幅度达 8.5% - 16.7%。
*   **自适应权重的作用**：通过 NDCG 衡量局部与全局的一致性，能有效减少信用分配的歧义，使智能体在保持局部专业性的同时服务于全局目标。
*   **效率优势**：联合训练虽然单步更复杂，但由于多个智能体并行进化，达到收敛所需的总步数往往少于依次独立训练的总和。

### 7. 优点
*   **视角创新**：首次系统性地探讨了多智能体记忆系统中的“跨智能体协作”优化问题。
*   **数学建模严谨**：将复杂的异步系统正则化为顺序 MDP，并引入 NDCG 进行信用分配，逻辑自洽。
*   **泛化性强**：在 Qwen 和 Llama 两个主流模型系列上均表现出一致的性能提升。

### 8. 不足与局限
*   **静态 MDP 限制**：目前的 MDP 建模是静态顺序的，未考虑现实中智能体可能存在的动态调用频率或实时记忆更新。
*   **智能体任务范围有限**：目前仅关注提取、画像和检索，未涉及记忆的冲突检测、冗余删除或长期遗忘机制。
*   **局部-局部对齐缺失**：虽然实现了局部与全局的对齐，但智能体之间（局部与局部）的显式直接对齐尚未建模。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
