---
title: Exclusive Self Attention
title_zh: 互斥自注意力
authors: Shuangfei Zhai
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09078v1"
tags: ["query:lh-llm"]
score: 7.0
evidence: 排他性自注意力机制在序列长度增加时改善上下文建模
tldr: 本研究提出排他性自注意力（XSA），通过修改传统自注意力机制，强制模型仅捕捉与当前Token自身值向量正交的信息，从而排除自身位置干扰。在高达27亿参数规模的语言模型实验中，XSA在建模性能上持续优于标准自注意力，且在长序列任务中表现出更显著的优势，有效提升了上下文建模能力。
selection_source: fresh_fetch
motivation: 传统的自注意力机制可能过度关注Token自身信息，限制了对上下文背景的有效建模。
method: 通过约束注意力机制仅捕获与Token自身值向量正交的信息，从而在计算中排除自身位置的影响。
result: 在不同规模（最高2.7B参数）的语言模型上，XSA均优于标准自注意力，且增益随序列长度增加而扩大。
conclusion: XSA是一种简单有效的改进方案，能显著增强Transformer在处理长序列时的上下文建模性能。
---

## 摘要
我们引入了互斥自注意力（Exclusive Self Attention, XSA），这是对自注意力（SA）的一种简单改进，旨在提升 Transformer 的序列建模性能。其核心思想是约束注意力机制，使其仅捕捉与标记（token）自身数值向量正交的信息（从而排除自身位置的信息），以此促进更好的上下文建模。在标准语言建模任务上的评估结果显示，在高达 27 亿参数的模型规模下，XSA 的表现始终优于 SA，且随着序列长度的增加，其性能提升愈发显著。

## Abstract
We introduce exclusive self attention (XSA), a simple modification of self attention (SA) that improves Transformer's sequence modeling performance. The key idea is to constrain attention to capture only information orthogonal to the token's own value vector (thus excluding information of self position), encouraging better context modeling. Evaluated on the standard language modeling task, XSA consistently outperforms SA across model sizes up to 2.7B parameters and shows increasingly larger gains as sequence length grows.